# Trilha E — Manutenção — Índice e dependências

Título, dependências e status de cada task da trilha. Quando o status diz "ver
**Resultado** na task", o histórico completo está no final do arquivo da task. Ver
[../README.md](../README.md) para protocolo de execução e invariantes globais.

| Task | Título | Depende de | Status |
|------|--------|-----------|--------|
| [E1](e1-javadoc-portugues.md) | Traduzir javadocs/comentários em inglês para português (violações da regra G7; ~15 arquivos main + ~15 test no censo de 2026-07-16) — 4 lotes, **1 sessão por lote**, checklist de progresso no próprio arquivo; só diff de comentário, zero código | — | ✅ FECHADA 2026-07-16 (Lote 1: `core/src/main` `decoder/`+`ir/`+`ir/opt/`, 9 arquivos; Lote 2: `codegen/`+`jit/`+`debug/`+`core/`+`memory/`+`arch/`+`coprocessor/`+`swi/`, 11 arquivos; Lote 3: `core/src/test` inteiro, 20 arquivos; Lote 4: `truffle/` main+test, 15 arquivos revisados — já 100% português, zero diff — + varredura final do grep limpa. `mvn -o test` verde 614 core + 13 truffle em todos os lotes) |
| [E2](e2-armtracelistener-memory-abort-hook.md) | `ArmTraceListener#onMemoryAbort` — fecha a lacuna de observabilidade sob `runBlocks`/JIT (achado da sessão extra de debug da F3, `virtual-arm-box`) | — | ✅ (2026-08-16) — gancho novo aditivo em `ArmCore#enterMemoryAbort`, único ponto de convergência de `step()`/bloco interpretado/bloco compilado(JIT); PC exato da instrução faltosa observável nos 3 caminhos pela primeira vez (antes só `step()` tinha eventos por-instrução). 3 testes novos em `ArmCoreMemoryAbortTest` provando paridade do PC exato nos 3 caminhos. `mvn -o test` verde (core) + `mvn -o install`; G5 revalidado (gbaemu verde, ndsemu verde, armbox 40/41 — a 1 falha é a mesma pré-existente de `Armv7TortureTest`/VFP, não-regressão) |
| [E5](e5-cobertura-isa.md) | Tabela de cobertura de ISA gerada por medição (`docs/COBERTURA-ISA.md`, `./gerar-cobertura-isa.sh`) | — | ✅ FECHADA 2026-08-21 — ver **Resultado** na task; achado real: espaço incondicional mal decodificado, virou `E6` |
| [E6](e6-espaco-incondicional-undefined.md) | `ArmDecoder`: espaço incondicional (`cond==0b1111`) tem que virar `UNIMPLEMENTED`, não cair no dispatch condicional (fecha o invariante G8) | — | ✅ FECHADA 2026-08-21 — ver **Resultado** na task; `decodeUnconditional` novo, 8 carve-outs movidos sem mudar bits, `CoprocessorDecoder`/`VfpDecoder` preservados, teste de regressão do `VHADD` (E5). `mvn -o test` verde + G5 revalidado nos 4 consumidores (armbox com a falha pré-existente já documentada, não-regressão) |
| [E3](e3-strex-asm-interp-divergence.md) | `StoreExclusive` (STREX): divergência real ASM×interpretador, revelada pela G4.1/n3dsemu (não introduzida por ela) | — | ✅ (2026-08-19) — causa raiz achada, NÃO era bug de semântica de LDREX/STREX em nenhum backend (`AsmRuntimeHelpers`/`IrMemoryExecutor` já espelhavam a mesma ordem de operações — checagem do monitor ANTES da escrita, `notifyOrdinaryWrite` idêntico). Era um bug do PRÓPRIO harness de diagnóstico: `DivergenceCheckingCodeEmitter` isola o candidato num `scratchCore` restaurado por bloco via `ArmCore#loadState`, que limpa o monitor de exclusividade DE PROPÓSITO (falha espúria de STREX é permitida pela arquitetura num save-state real) — mas esse mesmo restore roda a CADA bloco IR, não só em save-states de verdade. Padrão de spinlock/retry (`LDREX` num bloco, `STREX` no bloco seguinte após o branch de retry) faz o candidato SEMPRE ver o monitor aberto e falhar, enquanto o oráculo — rodando no core real, nunca passando por esse restore — vê a reserva de verdade e sucede; exatamente a divergência observada (`reference=0`/sucesso, `candidate=1`/falha). `ArmV6ExclusiveNativeEquivalenceTest` (B1.6) não pegava porque testa LDREX+STREX no MESMO bloco via `BlockEquivalenceHarness` (sem o restore por bloco do `DivergenceCheckingCodeEmitter`). Fix: após o restore, transfere a reserva do monitor do core real pro `scratchCore` (`DivergenceCheckingCodeEmitter.java`, aditivo — `ArmCore#loadState` continua limpando o monitor para save-states reais, sem mudança). Teste de regressão novo (`DivergenceCheckingCodeEmitterExclusiveMonitorTest`, reproduz cross-block antes do fix, verde depois). `mvn -o test` verde (core+truffle) + `mvn -o install`; G5 revalidado: gbaemu verde, ndsemu verde, n3dsemu verde (os 2 testes que a task cita — `Application3dsxTest` — voltam a passar), armbox 40/41 (a 1 falha é a mesma pré-existente `Armv7TortureTest`/`VfpRegisters`, não-regressão, já documentada pela E2/B1.8) |

| [E7](e7-a64-jit-guest-exceptions-escaping-to-host.md) | A64 JIT: 2 bugs reais faziam exceções de guest (`MemoryTranslationException64`/`Aarch64*Exception`) escaparem para o host em vez de entrar no handler do guest — achados retomando a F11 | — | ✅ (2026-08-26) — (1) `Ir64BlockCompiler` nunca cercava o bloco nativo com `try/catch` (precedente 32-bit `AsmBlockCompiler` tem desde B4.1.3) — bug latente em TODO bloco A64 quente; (2) `JitRuntime64#execute` nunca protegia o `lift()` de um bloco QUENTE (precedente 32-bit `JitRuntime#LIFT_FAULT_CYCLES` desde B4.1.5) — este era a causa real do `TRANSLATION_FAULT_L3 em 0x200` que travava o boot JIT da F11. 2 testes de regressão novos (`Ir64BlockCompilerMemoryAbortTest`, `JitRuntime64LiftFaultTest`), confirmados falhando sem o fix. `mvn -o test` verde + `install`; G5 nos 5 consumidores ✅ (armbox 40/41 pré-existente). Sem release publicado nesta sessão. Ver **Resultado** na task |
| [E8](e8-advsimd-integer-rm-vs-bit11-dispatch-bug.md) | `decodeAdvancedSimdInteger`: bug de dispatch achado (não corrigido) pela B8.11 — usava o VALOR de `Rm` para separar "three different" das formas que reaproveitam `Rm` como opcode disfarçado, mas "three different" aceita QUALQUER `Rm` e por isso colide | — | ✅ (2026-08-26) — priorizada pelo usuário entre 4 candidatas. Achado mais amplo que o documentado pela B8.11 (só citava `Rm>=16`): a colisão também acontece com `Rm=0`/`Rm=1`. Discriminador real é bit11 do word (fixo em `0` só em "three different", `1` nas demais) — corrigido reordenando o dispatch para checar bit11 antes de `Rm`; `ADVSIMD_INT_RM_ACROSS_LANES_BIT` (checagem de bit solto) trocada por máscara/padrão exatos (`Rm=16`/`17` apenas, não `16..31`). `mvn -o test` verde + `install`; G5 completo nos 5 consumidores + armbox ✅. Ver **Resultado** na task |
| [E9](e9-cobertura-isa-mrs-bank-eret-regressao.md) | Investigar a "regressão" de `MRS_bank`/`MSR_bank`/`ERET`/`HVC`/`SMC` em `docs/COBERTURA-ISA.md` achada (não investigada) pela `B10.6b`/`B10.6c` | — | ✅ (2026-08-27) — não era regressão nem instabilidade do medidor: falso positivo antigo (G8, misdecode silencioso) corrigido por `B9.8.2`-`B9.8.5`, que deram decode dedicado + gate real por `ArmFeature` a essas 5 instruções — nenhum preset declara `VIRTUALIZATION_EXTENSIONS` ainda (decisão explícita da B9.8.4), então `❌` é o valor correto. Tabela regenerada e COMMITADA (a sessão anterior tinha descartado). Ver **Resultado** na task |
| [E10](e10-advsimd-aliasing-largura-mista.md) | Aliasing `Rd`==`Rn`/`Rm` nas famílias AdvSIMD de **largura mista** do A64 — o achado que a B19.4 registrou (Armadilha 4) em `executeShiftWidenImmediate` e deixou para task própria | — | ✅ (2026-09-02) — bug confirmado em **6 das 7** funções auditadas de `Ir64VectorArithmeticExecutor` (`executeShiftWidenImmediate`, `executeWidening`, `executeWideningByElement`, `executeWide`, `executeNarrow`, `executeNarrowUnary`, `executeShiftNarrowImmediate`), todas corrigidas com o buffer `long[]` de `executeConvertPrecision` (B19.4). 8 testes novos, **7 confirmados falhando antes do fix**. Achado que corrigiu a própria spec: **alargar só é inseguro com `q == 0`** (na forma `2` a fonte é a metade alta e a escrita nunca a alcança) e **estreitar só com `q == 1`** — o inverso exato. Zero mudança de decode ⇒ `COBERTURA-ISA.md` intocada. `mvn -o test` 2914+18 verdes, G5 verde nos 5 consumidores. Ver **Resultado** na task |
| [E11](e11-inventario-qemu-fixado.md) | **Fixar a revisão do inventário do QEMU** — `gerar-cobertura-isa.sh` baixa de `master` e só baixa se o arquivo não existir (`target/` é gitignored), então o resultado de `./gerar-cobertura-isa.sh` depende de QUANDO a máquina baixou os `.decode` pela primeira vez, e nada registra contra qual revisão a tabela vigente foi medida. **Medido 2026-09-03 com download fresco, sem tocar em `core/`**: `t16.decode` 86→87 (linha nova `MAYBE_UNDEF_T1_HINT`, do commit QEMU `2931a675e9` "Make Thumb T1 hint space UNDEF before v6T2"), `sve.decode` 929→947, `sme.decode` 623→651; v4T e v5TE caem de **100% para 99%** — ⚠️ **invalida a manchete da B22.6** ("A32/T16/T32/VFP com 0 ❌"), que segue repetida na `FILA-EXECUCAO.md` e no plano B22. Inclui investigação escopada (com regra de decisão para não estourar escopo): os hints T16 (`YIELD`/`WFE`/`WFI`/`SEV`/`NOP`) medem `✅` em v6K/MPCore, mas o espaço de hint T1 é **v6T2+** — possível gap de GATING, mesma classe de B9.11/B9.16 | — | ✅ (2026-09-03) — `QEMU_REV` fixado em `2931a675e9d3…` (SHA, não `master`) + invalidação de cache por `target/isa-decode/.rev`; `IsaCoverageReport` escreve a revisão no cabeçalho da tabela; rito de bump documentado no script. Contra a revisão FIXADA o delta é só `t16` 86→87 (`MAYBE_UNDEF_T1_HINT`) — `sve`/`sme` **inalterados** (o +18/+28 que a spec viu era de commits posteriores ao SHA fixado; B17/B18 seguem corretos em 929/623). `MAYBE_UNDEF_T1_HINT` curado só p/ v4T,v5TE (commit como fonte) → v4T/v5TE **ficam 100%** (a queda p/ 99% da spec era sem curadoria). Item 4: veredito = **abrir task ([E13](e13-t16-hint-space-undef-antes-v6t2.md))** — o commit reverte a B9.14 (espaço de hint T1 INTEIRO UNDEF antes de v6T2 em perfil A, 14 células v6K/MPCore + código + risco de regressão no n3dsemu/ARM11). Afirmações "0 ❌ em 32 bits" qualificadas com a revisão. Ver **Resultado** na task |
| [E12](e12-tsv-a64-esconde-trabalho.md) | A curadoria `A64` de `docs/isa-nao-aplicavel.tsv` escondia trabalho pendente nas versões em que a feature EXISTE — 111 linhas migradas para o mapa de features de `IsaCoverageReport` | B19.5.2 | ✅ (2026-09-04) — **spec re-medida e corrigida antes de executar**: a v1 errava em 3 pontos (123 vs **111** linhas; "2 features sem constante" vs **8**; e não mencionava o 2º mecanismo de esconderijo, linhas TSV com arquitetura `*`). 9 constantes novas em `Aarch64Feature` (`CRC32`/`LRCPC2`/`LSE128`/família FP8/`FPRCVT`+2 de matriz), 2 erros de FATO na TSV corrigidos contra a fonte (`FEAT_LSE128` era Armv9.4-A, não v8.9; 4 nomes `FEAT_*` eram inventados). `isAarch64VersionColumn` removido + `IsaCoverageReportA64CurationGuardTest` (8 testes) fecha as DUAS portas. **Achado que mudou o desenho**: 66 das 82 células que passariam a `✅` eram **misdecode (G8)** — `CPY*`/`SETG*`→`FpLoadLiteral64`, `LDRA`→`NOP_HINT`, `FAMAX`/`FAMIN`/`FSCALE`→`VectorInsert*`; viraram `⚠️` via `AARCH64_MISDECODED` em vez de `✅` falso, e geraram task nova. Delta: 1158 `·→❌`, 66 `·→⚠️`, 16 `·→✅` (`SEVL`, cobertura real que a TSV apagava), 0 vazamento, 32 bits intocado. **Global 89%→84%** (denominador +1240) — medição honesta, precedentes B9.11/B19.5.2. `mvn -o test` 2971+21 verdes, G5 ✅. Ver **Resultado** na task |
| [E13](e13-t16-hint-space-undef-antes-v6t2.md) | Espaço de hint T1 (Thumb 16 bits, `1011 1111 ---- 0000`) deve UNDEF antes de v6T2 em perfil A — **reverte a decisão da B9.14** para `v6K`/`MPCore` à luz do commit QEMU `2931a675e9d3…` ("Make Thumb T1 hint space UNDEF before v6T2", `trans_MAYBE_UNDEF_T1_HINT`: UNDEF sem `ARM_FEATURE_M` nem `ARM_FEATURE_THUMB2`). Gate novo `THUMB2 \|\| M_PROFILE` na sub-forma hint do `ThumbDecoder`; 14 células `✅`→`·` (colunas v6K/MPCore); inverte os testes v6K/MPCore da B9.14 + regressão negativa; curadoria TSV. **Risco real de regressão no n3dsemu (ARM11 MPCore = ARMv6K)** — G5 dele é o teste da mudança | E11 | ⬜ |

Legenda: ⬜ pendente · 🟡 em andamento · ✅ concluída
