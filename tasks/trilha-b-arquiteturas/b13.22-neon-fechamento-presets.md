# B13.22 — Fechamento do épico B13: presets com NEON e o fim do `NOT_IN_ANY_PRESET`

**Trilha:** B · **Repo:** arm-jitter (+ revalidação G5) · **Depende de:** B13.16, B13.21 (na prática, todo o épico)
**Épico:** `b13-plano-neon-a32.md` · **Status:** ⬜

## Contexto

Última task do épico B13. **É ela que faz "não se aplica a nenhum preset atual" sumir da tabela** para
os três grupos NEON — e, com isso, transforma ~325 linhas de trabalho invisível em cobertura medida.

Durante todo o épico (B13.1-B13.21) a disciplina foi: implementar tudo gateado por
`ArmFeature.ADVANCED_SIMD` (e as features irmãs), **sem nenhum preset declará-las**, para que cada
task fosse **zero-diff de runtime** e a tabela ficasse byte a byte idêntica. Isso permitiu 20 tasks
seguidas sem risco de regressão nos 5 consumidores. **O preço é que nada disso conta na tabela** —
e é esta task que cobra o preço de volta.

### O que muda na medição

`IsaCoverageReport.GROUPS` marca hoje três grupos como `NOT_IN_ANY_PRESET`:

```java
new Group("neon-dp.decode", "NEON — processamento de dados", 32, Probe.ARM32, true,
        NOT_IN_ANY_PRESET, "Advanced SIMD: extensão OPCIONAL do ARMv7-A; nenhum preset a declara hoje."),
new Group("neon-ls.decode", "NEON — load/store", 32, Probe.ARM32, true, NOT_IN_ANY_PRESET, ""),
new Group("neon-shared.decode", "NEON — formas compartilhadas VFP/NEON", 32, Probe.ARM32, true,
        NOT_IN_ANY_PRESET, ""),
```

Os três passam a ter uma `Applicability` real (`arch -> arch.has(ArmFeature.ADVANCED_SIMD)`), e as
297 + 5 + 23 = **325 linhas** entram no denominador das colunas que declararem a feature.

⚠️ **A cobertura GLOBAL provavelmente vai CAIR no momento em que o preset novo entrar**, porque o
denominador cresce mais rápido que o numerador nas colunas afetadas (nem tudo do NEON estará ✅ — as
formas F16 do A32, por exemplo, ficam para a task irmã). **Isso é o resultado certo**; precedente:
B9.11 ("v6-M 52%→47%") e B19.5.2. Não maquiar.

**Leitura obrigatória**: o `b13-plano-neon-a32.md` INTEIRO, os `## Resultado` de **B13.1** (o banco
D0-D31 e a feature `VFPV3_D32`) e **B13.22**-adjacentes (B13.16 e B13.21, as duas últimas de
conteúdo), a **B12** (`b12-catalogo-processadores-arm.md` — o catálogo de processadores, que ganha
entradas aqui) e a **B9.11**/**B19.5.2** (o precedente de medição honesta que baixa o número). No
código: `arch/ArmArchitecture` (os presets e `withDecoderExtensions`/`withThumb32DecoderExtensions`),
`arch/ArmProcessor` (o catálogo), `core/ArmFeature`,
`core/src/test/.../tools/IsaCoverageReport` (`GROUPS`, `Applicability`).

## Objetivo

Existem presets públicos com NEON; os três grupos deixam de ser `NOT_IN_ANY_PRESET`; e o catálogo de
processadores ganha os núcleos que hoje não existem por falta de NEON.

## Inclui

1. **Preset(s) públicos com NEON** — no mínimo um `ARMV7A_NEON` (ARMv7-A + `ADVANCED_SIMD` +
   `VFPV3_D32`), plugando **todos** os `DecoderExtension` do épico (A32) e o `Thumb2NeonDecoder`
   (B13.16). **Conferir quais features irmãs cada preset deve declarar**: `ADVANCED_SIMD_RDM`
   (B13.5), `CRYPTO` (B13.15), `COMPLEX_NUMBER_ARITHMETIC` (B13.17), `DOT_PRODUCT`/
   `INT8_MATRIX_MULTIPLY` (B13.18/B13.19), `FP16_FUSED_MULTIPLY_ADD_LONG` (B13.20), `BFLOAT16`
   (B13.21) — **cada uma só nos presets cuja arquitetura real a tem**. Um `Cortex-A8` não tem
   `FEAT_BF16`.
2. **`Applicability` real** nos três grupos de `IsaCoverageReport` (substituindo `NOT_IN_ANY_PRESET`).
3. **Curadoria de versão** para as features que não são do ARMv7-A base: as linhas de
   `FEAT_FCMA`/`FEAT_DotProd`/`FEAT_I8MM`/`FEAT_FHM`/`FEAT_BF16`/`FEAT_CRYPTO` precisam medir `·` nas
   colunas cujos presets não as têm — **usando o mecanismo certo** (a lição da **E12**: não a
   exclusão grossa da TSV).
4. **Entradas novas em `ArmProcessor`** — os núcleos que a **B12** deixou de fora por falta de NEON:
   `Cortex-A8`, `Cortex-A9`, `Cortex-A5`, `Cortex-A7`, `Cortex-A15`, `Cortex-A17`… **Conferir um a
   um qual extensão cada um realmente tem** (NEON é OPCIONAL no ARMv7-A: há Cortex-A9 sem NEON) —
   entrada factualmente errada é pior que ausência, princípio que a B12.4/B12.6 estabeleceram.
5. **Medição honesta** — a tabela nova, com o delta explicado nas duas direções.
6. Testes (ver Aceite).

## Não inclui

- **Implementar qualquer instrução que a medição revele faltando** — cada gap vira degrau novo
  (provável: as formas **F16 do NEON A32**, que B13.6/B13.8/B13.11/B13.13 adiaram para a task irmã
  "NEON FP16 AArch32"). **Enumerar com destino nomeado**, nunca excluir.
- **ARMv8-A de 32 bits** ⇒ épico **B14** (VFP incondicional), que tem preset próprio.
- **Emissão nativa / Truffle** dos `Kind` NEON ⇒ C12.8 / A10.7.

## Especificação

### NEON é OPCIONAL no ARMv7-A

É o ponto que decide o desenho dos presets. `ADVANCED_SIMD` não pode entrar no `ARMV7A` existente —
isso mudaria o comportamento de um preset público (violação de **G3**) e passaria a decodificar NEON
para consumidores que hoje não o esperam. **Preset NOVO**, aditivo, como a B12.5 fez com
`ARMV6`/`ARMV6T2`/`ARMV6Z`.

### A conta do denominador

Antes de mexer, registrar a tabela atual; depois, a nova. Esperado:

- colunas sem NEON (`v4T`, `v5TE`, `v6K`, `MPCore`, `v6-M`, `v7-M`, e o `ARMV7A` sem NEON):
  **inalteradas** — os grupos continuam `·` lá;
- coluna(s) do preset novo: **+325 no denominador**, com o numerador sendo o que o épico de fato
  implementou;
- **global**: cai, sobe ou fica — medir e explicar.

## Passos

1. Ler "Leitura obrigatória". Guardar a tabela ANTES.
2. Levantar, núcleo a núcleo, quais extensões cada processador candidato tem de verdade (fonte: ARM
   TRM / a lista de processadores usada pela B12).
3. Preset(s) novos + plug de todas as extensões do épico.
4. `Applicability` real nos 3 grupos + curadoria de versão pelo mecanismo certo.
5. `ArmProcessor` + entradas novas.
6. Remedir; enumerar o que faltou **com destino nomeado**.
7. `mvn -o test` (JBR 25) + `install` + **G5 nos 5 consumidores** — atenção redobrada: é a primeira
   task do épico que **não** é zero-diff.
8. `INDICE.md` da trilha B (linha B13), `## Resultado`, `tasks/FILA-EXECUCAO.md`, commit
   `B13.22: …`, `git push`.

## Aceite

- **`ARMV7A` (e todos os presets existentes) inalterados** — G3. Teste provando que uma palavra NEON
  continua `UNIMPLEMENTED` em `ARMV7A`, `ARM11_MPCORE`, `ARMV6K`, etc.
- **Preset novo decodifica NEON** — uma amostra que cubra as seções do épico (three-same,
  2-reg-and-shift, `Vimm_1r`, 3-reg-different, 2-reg-and-scalar, two-reg-misc, conversões, permuta,
  cripto, `neon-ls`, `neon-shared`), em **A32 e T32**.
- **Features irmãs corretas por preset**: um preset sem `CRYPTO` recusa `AESE`; um sem `BFLOAT16`
  recusa `VDOT_b16`; etc.
- **`ArmProcessor`**: cada entrada nova conferida contra a extensão real do núcleo, com a fonte
  citada no Javadoc. **Nenhuma entrada "aproximada"** (princípio da B12.4/B12.6).
- **Os três grupos deixam de ser `NOT_IN_ANY_PRESET`** em `docs/COBERTURA-ISA.md` — a frase "não se
  aplica a nenhum preset atual" some deles.
- **Delta medido e explicado nas duas direções**, incluindo a queda global se houver.
- **Lista do que faltou, com destino nomeado** — em especial as formas F16 do NEON A32.
- `mvn -o test` verde + **G5 verde nos 5 consumidores** (não é zero-diff; suítes verdes é o gate).

## Armadilhas

1. **Não acrescentar `ADVANCED_SIMD` ao `ARMV7A` existente** — G3. Preset novo.
2. **NEON é opcional no ARMv7-A**, e as features irmãs (cripto, dot-product, BF16…) são opcionais e
   de versões diferentes. Declarar tudo em todo preset é factualmente errado.
3. **A cobertura pode CAIR** — é o resultado certo. Precedentes: B9.11, B19.5.2.
4. **Não usar a exclusão grossa da TSV** para a curadoria de versão — é exatamente o problema que a
   **E12** conserta do lado A64. Usar o mecanismo por feature.
5. **Primeira task não-zero-diff do épico** — o G5 aqui é de verdade, não formalidade.
6. **Entrada de catálogo errada é pior que ausência** (B12.4/B12.6).
7. **O que faltar vira degrau, não exclusão** — regra máxima.

## Não fazer

- Não mexer nos presets públicos existentes.
- Não declarar features que o núcleo real não tem.
- Não maquiar a queda de cobertura.
- Não implementar o que a medição revelar faltando (vira degrau).
- Não usar a TSV para curadoria de versão.

## Resultado (2026-09-19)

**Preset novo**: `ArmArchitecture.ARMV7A_NEON` (`extending(ARMV7A, ...)`, aditivo, `ARMV7A`
intocado) — só `ADVANCED_SIMD` + `VFPV3_D32`. **Decisão de escopo**: as 7 features irmãs do épico
(`ADVANCED_SIMD_RDM`/`CRYPTO`/`COMPLEX_NUMBER_ARITHMETIC`/`DOT_PRODUCT`/`INT8_MATRIX_MULTIPLY`/
`FP16_FUSED_MULTIPLY_ADD_LONG`/`BFLOAT16`) são todas extensões ARMv8.x (v8.0-cripto até v8.6) que
NENHUM núcleo ARMv7-A real tem — declará-las no preset seria entrada factualmente errada (mesmo
princípio de B12.4/B12.6), então ficaram de fora; a curadoria abaixo garante que elas medem `·`
(não `❌`) nesta coluna. Decoders plugados: os 7 A32 do épico B13 + `CoprocessorDecoder`, com
`NeonSharedDecoder` por ÚLTIMO na lista; no Thumb-2, `Thumb2NeonDecoder` (B13.16) + ... +
`Thumb2CoprocessorDecoder`, com `Thumb2NeonSharedDecoder` (NOVO nesta task — wrapper fino que reusa
`NeonSharedDecoder` sem transformação, já que o cabeçalho de `neon-shared.decode` confirma encoding
idêntico A32/T32, mesmo padrão do `Thumb2VfpDecoder`) também por último. **Por que por último**:
`NeonSharedDecoder` nunca devolve `null` (fecha o "null debt" do arquivo desde a B13.21 com
`unimplemented(...)` explícito para qualquer `raw` que não bata em nenhuma das 23 linhas) — um
decoder assim só pode vir depois de TODOS os outros, senão nada registrado depois dele na lista é
alcançável. **Achado real, não hipotético**: a primeira versão desta task registrou os dois ANTES de
`CoprocessorDecoder`/`Thumb2CoprocessorDecoder` e isso quebrou `MCR`/`MRC`/`DMB`/`MRS`/branches
largos sob `ARMV7A_NEON` — ver a seção "Achado real via JaCoCo" mais abaixo para o relato completo.

**`IsaCoverageReport`**: os 3 grupos saem de `NOT_IN_ANY_PRESET` para `Applicability` real
(`arch -> arch.has(ADVANCED_SIMD)`); coluna nova `v7-A+NEON` entre `v7-A` e `v6-M`. Curadoria de
versão ARM32 nova (`ARM32_VERSION_REQUIREMENTS`, espelho por nome de `AARCH64_VERSION_REQUIREMENTS`
da E12) cobre as 35 linhas das 7 features irmãs — todas medem `·` na coluna nova (nenhum preset as
declara), nunca `❌`.

**Medição** (`docs/COBERTURA-ISA.md`, `./gerar-cobertura-isa.sh`; números FINAIS, já com a correção
de ordem de decoder documentada mais abaixo — a primeira medição, com o bug, subestimava em 37
células): global **99%→98%** (20544/20675 → 21484/21726), coluna nova **`v7-A+NEON` 89%
(940/1051)** — queda esperada e documentada (Armadilha 3, precedente B9.11/B19.5.2/B14.7). Dentro só
dos 325 encodings dos 3 grupos NEON (inalterados pela correção de ordem, ver abaixo): **279 ✅ / 35 ·
(features irmãs, corretas) / 11 ❌ real**. Os 11 gaps reais, com destino nomeado:
- 7 formas `SHA1C_3s`/`SHA1P_3s`/`SHA1M_3s`/`SHA1SU0_3s`/`SHA256H_3s`/`SHA256H2_3s`/`SHA256SU1_3s`
  (`neon-dp.decode`, seção "3same_crypto") — **nenhum decoder A32/T32 as implementa hoje** (só as 7
  de "2-reg-misc" da B13.15 — `AESE`/`AESD`/`AESMC`/`AESIMC`/`SHA1H`/`SHA1SU1`/`SHA256SU0` —
  existem); candidata a task própria "NEON SHA de 3 registradores A32" (a semântica já existe no
  núcleo A64 via `Ir64CryptoShaThreeRegisterOp`, seria migração D1 da RFC B13.2, não algoritmo novo).
- 4 formas `VCVT_HS_2sh`/`VCVT_HU_2sh`/`VCVT_SH_2sh`/`VCVT_UH_2sh` (`neon-dp.decode`,
  "2-reg-and-shift", ponto fixo↔F16) — já eram a lacuna F16 conhecida e nomeada desde B13.8 ("task
  irmã NEON FP16 AArch32", depende de B19.5.1); não nova.

**`ArmProcessor`**: `Cortex-A5`/`A7`/`A8`/`A9`/`A12`/`A15`/`A17` ganharam entrada IRMÃ `_NEON`
resolvendo para `ARMV7A_NEON` (aditivo — a entrada sem sufixo continua `ARMV7A`, a aproximação
conservadora default; NEON é opção de síntese, não atributo fixo do nome comercial, confirmado nos
TRMs de cada núcleo). Nenhuma das 7 features irmãs entrou em nenhuma entrada — nenhum destes núcleos
ARMv7-A as tem.

**Testes**: `ArmV7aNeonPresetTest` novo — G3 (`ARMV7A`/`ARMV6K`/`ARM11_MPCORE`/`ARMV5TE`/`ARMV4T`
continuam `UNIMPLEMENTED` para NEON, A32 e T32), `ARMV7A_NEON` decodifica 6 das seções do épico em
A32 (three-same, 2-reg-and-shift, `Vimm_1r`, 3-reg-different, two-reg-misc, permuta) + `neon-ls`, e
recusa cripto/`neon-shared` sozinho (features irmãs ausentes); uma arquitetura só-de-teste com as 7
features irmãs (construída com decoders próprios, NUNCA via `extending(ARMV7A_NEON, ...)` — achado
de processo: `extending` herda os OBJETOS decoder já fechados sobre a `ArmArchitecture` antiga, que
continuam consultando `ArmFeature` na instância errada; o mesmo quebra-cabeça ovo-e-galinha
documentado em todo preset de `ArmArchitecture.java`, mas que eu só percebi ao ver 2 testes falharem
depois de "adicionar" as features via `extending`) prova que a fiação decodifica cripto/`neon-shared`
quando as features estão presentes, em A32 e T32. As 2 seções não amostradas aqui
(2-reg-and-scalar, conversões) são cobertas pela medição exaustiva acima (todas as 325 linhas, não
uma amostra). `mvn -o test`: **4709 testes verdes** (nesta rodada; sobe para 4717 depois da
correção de ordem documentada abaixo, que acrescentou testes de regressão novos). Dois guards
pré-existentes
(`IsaCoverageReport32BitCurationGuardTest`/`IsaCoverageReportV8A32ColumnTest`) tinham a lista de
colunas hardcoded sem a nova — corrigidos para incluir `v7-A+NEON` na posição certa (achado de
manutenção, não bug de produção).

**Achado real (bug de correção, achado com JaCoCo — a pedido do usuário, memória
`feedback-testes-falhando-prioridade-cobertura`)**: a PRIMEIRA versão desta task registrava
`NeonSharedDecoder`/`Thumb2NeonSharedDecoder` ANTES de `CoprocessorDecoder`/`Thumb2CoprocessorDecoder`
nas duas listas. `mvn -o verify` (JaCoCo) mostrou o branch `if (decoded == null)` de
`Thumb2NeonSharedDecoder` **nunca coberto** — investigando o porquê, achei que `NeonSharedDecoder`
NUNCA devolve `null` desde a B13.21 (ela fechou o "null debt" do arquivo fazendo qualquer `raw` fora
das 23 linhas cair em `unimplemented(...)` explícito, G8, sem checar primeiro se o `raw` está dentro
do frame `neon-shared`). Como nenhuma task anterior jamais registrou `NeonSharedDecoder` numa
`ArmArchitecture` de verdade (o próprio B13.17 documenta "nenhum preset a declara" até esta task), o
efeito colateral nunca tinha sido exercitado: com `NeonSharedDecoder` ANTES de `CoprocessorDecoder`,
QUALQUER encoding do espaço incondicional que sobrasse (ex. `MCR`/`MRC`/`CDP`/`STC`/`LDC`) virava
`UNIMPLEMENTED` sem nunca alcançar `CoprocessorDecoder` — confirmado por probe direto contra
`0xEE010F10` (`MCR p15,0,r0,c1,c0,0`, instrução real de boot que habilita a MMU): `ARMV7A` decodifica
`COPROCESSOR`, a primeira versão de `ARMV7A_NEON` decodificava `UNIMPLEMENTED`. O mesmo valia do lado
Thumb-2 para `Thumb2BranchDecoder`/`Thumb2MiscDecoder`/`Thumb2CoprocessorDecoder` (branches largos,
barreiras `DMB`/`DSB`/`ISB`, `MRS`/`MSR`, coprocessador Thumb — todos ficariam inalcançáveis).
**Corrigido** movendo os dois decoders para o FIM de cada lista — `ArmDecoder`/`ThumbDecoder`
consultam as extensões em ordem e usam a primeira resposta não-nula, então um decoder que nunca
devolve `null` só pode vir por último. `Thumb2NeonSharedDecoder` também simplificado (removida a
checagem `if (decoded == null)`, código morto provado pelo achado acima — o JaCoCo confirma 0
instruções/linhas/branches perdidos depois). Testes de regressão novos em `ArmV7aNeonPresetTest`
(`armv7aNeonStillDecodesOrdinaryCoprocessorInstructionsInA32/InThumb32`,
`armv7aNeonStillDecodesThumb2MiscInstructions`) + `Thumb2NeonSharedDecoderTest`
(`neverReturnsNullEvenForAWordOutsideTheNeonSharedFrame`, documenta o contrato real). `mvn -o
verify`: JaCoCo confirma `ArmArchitecture`/`ArmProcessor`/`Thumb2NeonSharedDecoder` 100% de linhas e
branches cobertos (0 `MISSED` nas três classes no `jacoco.csv`). `docs/COBERTURA-ISA.md` MUDA
bastante com esta correção — não é um ajuste cosmético: `MCR`/`MRC`/`MCRR`/`MRRC` (`a32.decode`) e
`YIELD`/`WFE`/`WFI`/`SEV`/`SEVL`/`ESB`/`NOP`/`CPS`/`CLREX`/`DSB`/`DMB`/`ISB`/`MRS_bank`/`MRS_reg`/
`MRS_v7m`/`MSR_bank`/`MSR_reg`/`MSR_v7m`/`BXJ`/`ERET`/`SUB_rri`/`SMC`/`HVC`/`UDF`/`B_cond_thumb`/`TT`
(`t32.decode`) — nenhum deles NEON, todos vítimas reais do bug de ordenação — viram `✅` na coluna
`v7-A+NEON` depois da correção (eram `❌` com a ordem errada). **Números corrigidos** (a versão FINAL,
depois do fix): global **98%** (21484/21726, não 21447 — a versão com o bug subestimava em 37
células), coluna `v7-A+NEON` **89% (940/1051)**, não 85% — `a32.decode` sob esta coluna sobe de 92%
para 94% (251/266) e `t32.decode` de 76% para 86% (269/310). **Os 3 grupos NEON (`neon-dp`/`neon-ls`/
`neon-shared`) não mudam nada** com este fix — `MCR`/`DMB`/etc pertencem a `a32.decode`/`t32.decode`,
grupos diferentes; os números de 279 ✅/35 ·/11 ❌ acima já eram os corretos, medidos tanto antes
quanto depois da correção de ordem.

**G5**: `mvn install` local + suíte de testes automatizada verde nos 5 consumidores
(`gbaemu`/`ndsemu`/`armbox`/`virtual-arm-box`/`n3dsemu`) contra o `arm-jitter` atualizado — rodado
DUAS vezes (antes e depois da correção de ordem). Nenhum consumidor referencia `ARMV7A_NEON`
diretamente, mas o achado do JaCoCo mostrou que o risco NÃO era só teórico — um decoder mal ordenado
num preset novo pode, em tese, mascarar um problema em qualquer preset que reutilize a mesma lista
de extensões por acidente de copy-paste; as 5 suítes automatizadas continuam sendo o gate real.

**Épico B13 (NEON de 32 bits) FECHADO.**
