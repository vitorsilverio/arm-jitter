# B16.10 — MVE: deslocamentos por imediato, shift-and-insert e `VSHLL` T1 (`VMOVL`) — 38 encodings

**Trilha:** B · **Repo:** arm-jitter (+ revalidação G5) · **Depende de:** B16.6 (⬜)
**Épico:** `b16-plano-mve-helium.md` · **Status:** ⬜ (spec escrita 2026-09-13, ainda não executada)

## Contexto

Décimo degrau da escada B16 (partição normativa na spec da **B16.1**). É uma das três famílias em
que a contagem do épico bate exatamente com a medição: **38** (seção `# Shifts by immediate`,
linhas 599-655, mais a seção `# Logical immediate operations`? **Não** — ver abaixo: `Vimm_1r`
pertence à B16.13; as 38 são exatamente as linhas 601-655).

### O encoding real (`target/isa-decode/mve.decode` — medido)

Formatos (linhas 95-117):

```
@2_shl_b .... .... .. 001 shift:3 .... .... .... .... &2shift qd=%qd qm=%qm size=0
@2_shl_h .... .... .. 01  shift:4 .... .... .... .... &2shift qd=%qd qm=%qm size=1
@2_shl_w .... .... .. 1   shift:5 .... .... .... .... &2shift qd=%qd qm=%qm size=2

@2_shll_b .... .... ... 01 shift:3 .... .... .... .... &2shift qd=%qd qm=%qm size=0
@2_shll_h .... .... ... 1  shift:4 .... .... .... .... &2shift qd=%qd qm=%qm size=1

# Right shifts are encoded as N - shift, where N is the element size in bits.
%rshift_i5  16:5 !function=rsub_32
%rshift_i4  16:4 !function=rsub_16
%rshift_i3  16:3 !function=rsub_8

@2_shr_b .... .... .. 001 ... .... .... .... .... &2shift qd=%qd qm=%qm size=0 shift=%rshift_i3
@2_shr_h .... .... .. 01 .... .... .... .... .... &2shift qd=%qd qm=%qm size=1 shift=%rshift_i4
@2_shr_w .... .... .. 1 ..... .... .... .... .... &2shift qd=%qd qm=%qm size=2 shift=%rshift_i5
```

As 38 linhas (cada mnemônico aparece 2 ou 3 vezes, uma por largura `b`/`h`/`w`):

```
# Shifts by immediate                                        linhas 601-631, 24 linhas
VSHLI     (b/h/w)   111 0 1111 1 . ... ... ... 0 0101 0 1 . 1 ... 0
VQSHLI_S  (b/h/w)   111 0 1111 1 . ... ... ... 0 0111 0 1 . 1 ... 0
VQSHLI_U  (b/h/w)   111 1 1111 1 . ... ... ... 0 0111 0 1 . 1 ... 0
VQSHLUI   (b/h/w)   111 1 1111 1 . ... ... ... 0 0110 0 1 . 1 ... 0
VSHRI_S   (b/h/w)   111 0 1111 1 . ... ... ... 0 0000 0 1 . 1 ... 0
VSHRI_U   (b/h/w)   111 1 1111 1 . ... ... ... 0 0000 0 1 . 1 ... 0
VRSHRI_S  (b/h/w)   111 0 1111 1 . ... ... ... 0 0010 0 1 . 1 ... 0
VRSHRI_U  (b/h/w)   111 1 1111 1 . ... ... ... 0 0010 0 1 . 1 ... 0

# VSHLL T1 encoding; the T2 VSHLL encoding is elsewhere in this file
# Note that VMOVL is encoded as "VSHLL with a zero shift count"; we
# implement it that way rather than special-casing it in the decode.
VSHLL_BS  (b/h)     111 0 1110 1 . 1 .. ... ... 0 1111 0 1 . 0 ... 0   linhas 636-646, 8 linhas
VSHLL_BU  (b/h)     111 1 1110 1 . 1 .. ... ... 0 1111 0 1 . 0 ... 0
VSHLL_TS  (b/h)     111 0 1110 1 . 1 .. ... ... 1 1111 0 1 . 0 ... 0
VSHLL_TU  (b/h)     111 1 1110 1 . 1 .. ... ... 1 1111 0 1 . 0 ... 0

# Shift-and-insert                                            linhas 649-655, 6 linhas
VSRI      (b/h/w)   111 1 1111 1 . ... ... ... 0 0100 0 1 . 1 ... 0   @2_shr_*
VSLI      (b/h/w)   111 1 1111 1 . ... ... ... 0 0101 0 1 . 1 ... 0   @2_shl_*
```

24 + 8 + 6 = **38**.

### Os dois achados que o arquivo documenta em comentário

1. **Deslocamentos à DIREITA são codificados como `N - shift`** (`%rshift_i3`/`_i4`/`_i5` com
   `rsub_8`/`rsub_16`/`rsub_32`). Um decoder que leia `bits[18:16]`/`[19:16]`/`[20:16]` como a
   contagem crua produz o deslocamento errado em TODA instrução desta família — bug silencioso
   clássico (mesmo padrão que NEON A32 usa, e que a B13.x/`NeonShiftImmediateDecoder` já deve
   tratar: **auditar e reusar**).
2. **`VMOVL` não tem encoding próprio**: é `VSHLL` T1 com contagem zero. O comentário é explícito
   ("we implement it that way rather than special-casing it in the decode"). Portanto **não** criar
   um `InstructionKind.VMOVL` nem um caso especial — se um encoding de `VSHLL` T1 com `shift == 0`
   aparecer, ele É o `VMOVL` e o executor faz a coisa certa naturalmente (alargar sem deslocar).
   Registrar isso no Javadoc.
3. **A largura vem do PADRÃO DE BITS, não de um campo `size`.** `@2_shl_b` exige `bits[20:18]=001`,
   `@2_shl_h` exige `bits[20:19]=01`, `@2_shl_w` exige `bit20=1` — e é justamente por isso que cada
   mnemônico tem 3 linhas. A distinção é por prefixo do campo de imediato, e cada prefixo deixa um
   número diferente de bits para o `shift` (3/4/5). Implementar como "leia `size` de um campo" está
   errado.

## Objetivo

As 38 linhas decodificam sob `ArmFeature.MVE_INTEGER` e executam deslocamento por imediato
(esquerda, direita aritmético/lógico, com/sem arredondamento, com/sem saturação), inserção
deslocada (`VSRI`/`VSLI`) e alargamento com deslocamento (`VSHLL` T1, incluindo o caso `VMOVL`),
predicados por elemento. 38 células `·`/`❌` → `✅`.

## Inclui

1. **Decode das 38 linhas** com a decodificação correta de largura por prefixo e de contagem
   (`N - shift` nas formas `_shr`), `%qd`/`%qm`, o bit `U` (bit 28), e as recusas:
   - `Q > 7` → `null`;
   - `Qd == Qm` nas formas alargantes `VSHLL_*` (o destino tem o dobro da largura; **confirmar** no
     QEMU se é UNDEF);
   - os prefixos que não batem com nenhuma das 3 larguras (`bits[20:18] == 000`, por exemplo) →
     `null` (G8: sobra tem que cair em `UNIMPLEMENTED`).
2. **Auditoria de reuso**: `AdvSimdLanes#shiftImmediate` (`AdvSimdShiftImmediateOp`) e
   `shiftWidenImmediate` (`AdvSimdShiftWidenOp`) já existem e já são consumidos pelo A64 e pelo
   NEON A32. Auditar as 38 contra esses catálogos ANTES de escrever (mesma disciplina da B16.6);
   `VQSHLUI` (saturação signed→unsigned) e `VSRI`/`VSLI` (inserção, que preserva bits do destino)
   são os candidatos a "talvez não exista".
3. **Reuso do decoder NEON**: `core/.../decoder/NeonShiftImmediateDecoder.java` já decodifica
   deslocamentos por imediato NEON com a MESMA codificação `N - shift`. Auditar se a extração pode
   ser compartilhada (os campos de registrador diferem: NEON usa `D`/`Q` de 4 bits, MVE usa `%qd`/
   `%qm` limitados a `Q0`-`Q7`).
4. **Execução** predicada + `FPSCR.QC` só para lanes ativas nas formas saturantes
   (`VQSHLI_S`/`VQSHLI_U`/`VQSHLUI`).
5. Testes (ver Aceite).

## Não inclui

- **`VSHLL` T2** (linhas 228-273, 8 linhas) — **B16.7**. Mesmo mnemônico, encoding totalmente
  diferente (`shift == esize` fixo, dentro dos blocos `{}` que sobrepõem `VMULH`).
- **Deslocamentos estreitantes** (`VSHRNB`/`VSHRNT`/`VQSHRN*`/`VQRSHRN*`/`VQSHRUN*`/`VQRSHRUN*`) e
  `VSHLC` — **B16.11**.
- **Deslocamento por vetor** (`VSHL_S`/`VSHL_U`/`VRSHL_*`/`VQSHL_*`/`VQRSHL_*`, `@2op_rev`) —
  **B16.6**; **por escalar de GPR** (`VSHL_S_scalar` etc.) — **B16.9**.
- **`Vimm_1r`** (imediato modificado, linha 597) — **B16.13**.
- **`VCVT_*_fixed`** (linhas 798-808), que também usam `%rshift_i5`/`%rshift_i4` — **B16.12**.
- Emissão nativa/Truffle.

## Especificação

### Leitura obrigatória antes de codar

- `target/isa-decode/mve.decode`, linhas 95-117 (todos os formatos `@2_shl_*`/`@2_shll_*`/`@2_shr_*`
  e os `%rshift_*`) e 599-655.
- `tasks/trilha-b-arquiteturas/b16.1-...md` (partição + Armadilha 6), `b16.2-...md`
  (`## Resultado`: predicação), `b16.6-...md` (inventário de reuso) e `b16.7-...md`
  (`## Resultado`: onde `VSHLL` T2 ficou — para garantir que esta task não o toca).
- `core/src/main/java/dev/vitorsilverio/armjitter/decoder/NeonShiftImmediateDecoder.java` **inteiro**
  — o precedente mais próximo (mesma codificação `N - shift`).
- `core/src/main/java/dev/vitorsilverio/armjitter/advsimd/AdvSimdLanes.java`
  (`shiftImmediate`, `shiftWidenImmediate`, `shiftNarrowImmediate`), `AdvSimdShiftImmediateOp.java`,
  `AdvSimdShiftWidenOp.java`.
- QEMU real via `WebFetch`: `target/arm/tcg/translate-mve.c` (`do_2shift`, `do_2shift_vec`,
  `trans_VSHLL_BS` e cia., `trans_VSRI`/`VSLI`) e `target/arm/tcg/mve_helper.c`
  (`DO_2SHIFT`, `DO_2SHIFT_SAT`, `DO_VSHLL`, `DO_VSRI`/`DO_VSLI`).

## Passos

1. Ler "Leitura obrigatória"; auditar `AdvSimdLanes`/`NeonShiftImmediateDecoder` contra as 38 e
   escrever o inventário de reuso.
2. Implementar o decode com largura-por-prefixo e contagem `N - shift`, recusando os prefixos
   inválidos e o espaço de `VSHLL` T2.
3. Implementar/reusar os executores (inclusive o caso `shift == 0` = `VMOVL`).
4. Testes (ver Aceite).
5. `mvn -o test` + `install` + G5 + `./gerar-cobertura-isa.sh` com delta medido (exatamente 38
   células; **nenhuma célula de B16.6/B16.7/B16.11/B16.12/B16.13 pode mudar**).
6. `INDICE.md`, `## Resultado`, commit `B16.10: ...`, `git push`.

## Aceite

- As 38 linhas decodificam sob `MVE_INTEGER`; prefixos inválidos, `Q > 7` e `Qd == Qm` (formas
  alargantes, se UNDEF) são `null` (G8).
- **Contagem `N - shift`**: `VSHRI_S` de halfword com campo `0b0001` desloca **15**, não 1 —
  teste explícito por largura (8/16/32), é o bug mais provável desta task.
- **Largura por prefixo**: um encoding com `bits[20:18] == 001` é `b`, com `bits[20:19] == 01` é
  `h`, com `bit20 == 1` é `w`; `bits[20:18] == 000` é recusado.
- **`VMOVL`**: `VSHLL_BS` com contagem zero alarga sem deslocar (teste que documenta que não há
  `Kind` próprio).
- `VSRI`/`VSLI` preservam os bits do destino que a inserção não cobre (teste com `Qd`
  pré-carregado com padrão conhecido).
- `QC` só para lanes ativas nas 3 formas saturantes; predicação verificada em pelo menos uma forma
  de cada sub-família.
- As asserções pré-existentes de `AdvSimdLanes#shiftImmediate` (A64/NEON) continuam passando (G1).
- `docs/COBERTURA-ISA.md`: exatamente 38 células; `mvn -o test` verde; G5 verde.

## Armadilhas

1. **`N - shift` é a armadilha número um desta família** e o arquivo avisa em comentário
   ("Right shifts are encoded as N - shift, where N is the element size in bits"). O `NEON` A32 tem
   o mesmo esquema — se `NeonShiftImmediateDecoder` já o trata, reusar; se não, é achado a reportar.
2. **`VSHLL` T1 (aqui) × T2 (B16.7)** — dois comentários do arquivo apontam um para o outro. O
   espaço T1 é `111x 1110 1 . 1 ..` (`bits[27:23]`), o T2 é `111x 1110 0 . 11 ..`. Máscara larga
   aqui rouba as 8 células da B16.7 (ou vice-versa). Medir o delta.
3. **`bits[20:16]` são compartilhados entre "largura" e "contagem"** — não há campo `size`. Um
   decoder que declare `size` como campo e `shift` como campo separado vai ler bits sobrepostos.
4. **`VQSHLUI` só existe na forma `U=1`** (`111 1 1111`), e é saturação de signed para unsigned — não
   é `VQSHLI_U`. Confundir as duas é misdecode silencioso.
5. **`Thumb2NocpDecoder`** cobre `bits[27:24] ∈ {1110,1111}` — todas as 38. Ordem de registro.
6. **`VCVT_*_fixed` (B16.12) usa os MESMOS `%rshift_i5`/`%rshift_i4`** e vive em `1110 1111 1 .` /
   `1111 1111 1 .`, o mesmo prefixo de `VSHLI`/`VSHRI`. Se o decoder desta task tratar o espaço
   `bits[27:23] = 11111` genericamente, engole as 8 células de `VCVT_*_fixed`. Excluir
   explicitamente.
7. **G4** e `QC` só para lanes ativas.

## Não fazer

- Não implementar `VSHLL` T2 (B16.7), deslocamentos estreitantes/`VSHLC` (B16.11), `Vimm_1r`
  (B16.13) nem `VCVT_*_fixed` (B16.12).
- Não criar `InstructionKind`/`IrOp` próprio para `VMOVL`.
- Não ler a contagem de deslocamento à direita como valor cru.
- Não modelar `size` como campo independente do imediato.
- Não duplicar semântica já presente em `AdvSimdLanes`.

## Resultado

**Fechada em 2026-09-18.** As 38 linhas decodificam sob `MVE_INTEGER` em `Thumb2MveShiftImmediateDecoder`
(novo), registrado ANTES de `Thumb2NocpDecoder` em `ARMV8_1M_MVE`. Dois `IrOp` novos:
`MveVectorShiftImmediate` (as 10 formas não-alargantes, reusa `AdvSimdShiftImmediateOp`/
`AdvSimdLanes#shiftImmediateMasked`, nova) e `MveVectorShiftWidenImmediateInterleaved` (`VSHLL` T1,
reusa `AdvSimdLanes#shiftWidenInterleavedMasked` — a MESMA função que a B16.7 já usa para o T2, só
com `shift` do encoding em vez de fixo em `esize`).

**Bits verificados diretamente contra `target/isa-decode/mve.decode` vendorizado (não contra a
transcrição da spec)**: a largura vem do prefixo `bits[21:16]` — `bits[21:19]=001`→byte (shift em
`bits[18:16]`), `bits[21:20]=01`→halfword (`bits[19:16]`), `bit21=1`→word (`bits[20:16]`);
`bits[21:19]=000` é recusado (mesmo frame de `Vimm_1r`/B16.13, `null` não `unimplemented`, G8). A
transcrição do corpo da task tinha essa mesma regra deslocada 1 bit (`bits[20:18]` etc.) — a
implementação segue o arquivo real, não a spec.

**Achado que corrige a suposição do Inclui #1**: `Qd == Qm` NÃO é `UNDEF` em `VSHLL` — confirmado via
`WebFetch` de `target/arm/tcg/translate-mve.c` (`do_2shift_vec` só chama
`mve_check_qreg_bank(s, a->qd | a->qm)`, nunca compara os dois índices). Nenhuma recusa extra além de
`Q > 7`.

**`VSHLL` T1 reusa o núcleo intercalado da T2**: confirmado verbatim contra `mve_helper.c`
(`DO_VSHLL`: `m[H_ESIZE(le*2+TOP)] << shift`) que T1 e T2 têm a MESMA indexação `le*2+top` — a única
diferença real é que T1 tem `shift` no encoding (`0..esize-1`, `0`=`VMOVL`) em vez de fixo em
`esize`. `VMOVL` não ganhou `Kind` próprio (comentário literal do arquivo: "we implement it that way
rather than special-casing it in the decode") — o núcleo compartilhado já produz o resultado certo
com `shift=0`.

**Reuso confirmado por medição**: o mapeamento `(nibble, U) → AdvSimdShiftImmediateOp` desta família
MVE é EXATAMENTE a tabela `opc`/`U` de `NeonShiftImmediateDecoder#shiftOperation` (NEON A32) — método
promovido de `private` para pacote-visível, sem duplicar a tabela.

**`N - shift`**: `AdvSimdLanes` ganhou `computeShiftImmediateChecked` (extraído de `shiftImmediate`,
mesma disciplina de `computeThreeSameChecked`) e `shiftImmediateMasked` (predicado por byte, devolve
`QC`) — `shiftImmediate` (A64/NEON) passou a delegar ao núcleo extraído, comportamento idêntico
(G3). O decoder resolve `shift = (8 << esz) - raw` nos 3 nibbles `_shr`/`VSRI` e usa `raw` direto nos
3 `_shl`/`VSQSHLUI`/`VQSHLI`.

**Exclusões por construção (G8)**: `VSHRNB`/`VSHRNT`/`VQSHRN*` (B16.11, ainda não implementada) vivem
no MESMO `bits[27:24]=1110`/`bit23=1` de `VSHLL`, mas com `bit7=1`/`bit0=1` (`VSHLL` exige
`bit7=0`/`bit0=0`) — checado explicitamente para não engolir aquele espaço. `VCVT_*_fixed` (B16.12)
vive no MESMO prefixo de `VSHLI`/`VSHRI`, mas o nibble nunca bate nos 6 valores conhecidos — excluído
por CONSTRUÇÃO do `switch`, sem checagem extra.

**Validação**: `mvn -o test` 100% verde (4212 + truffle 73, após ajustar `TruffleCodeEmitterSupportsCoherenceTest`
para os 2 `Kind` novos, 149→151, e a lista de descobertos, 83→85). `docs/COBERTURA-ISA.md` **zero-diff**
(mesma Armadilha 7 da B16.2/B16.8/B16.9: `mve.decode` só sai de `NOT_IN_ANY_PRESET` na B16.14).
`docs/COBERTURA-JIT.md` +2 `Kind` (`MveVectorShiftImmediate`/`MveVectorShiftWidenImmediateInterleaved`,
ambos `❌`/`❌`, sem emissão nativa/Truffle nesta task). G5 verde (`gbaemu` e `ndsemu` `mvn -o test`
completos, sem instalar arm-jitter localmente antes — `mvn -o install` feito nesta sessão).

**Achado real, pego numa auditoria de cobertura pós-fechamento (mesmo dia)**: o reuso de
`NeonShiftImmediateDecoder#shiftOperation` era amplo demais — a tabela do NEON A32 também mapeia
`opc=0001`→`SSRA`/`USRA` e `opc=0011`→`SRSRA`/`URSRA` (formas RMW-acumuladas que o NEON tem e o MVE
não). Nenhuma linha real do `mve.decode` usa esses dois nibbles com `bit4=1` neste frame (confirmado
por grep no arquivo vendorizado); o decoder ORIGINAL da task fabricava `MveVectorShiftImmediate(SSRA/
USRA/SRSRA/URSRA, ...)` para encodings genuinamente reservados — violação de G8 (deveria devolver
`null`, não inventar uma instrução plausível). Corrigido com um filtro explícito `isKnownNibble`
(só os 6 nibbles reais desta família) ANTES de consultar a tabela reusada, mais 4 testes de
regressão (nibble `0001`/`0011` × `U=0/1`) e mais 8 testes cobrindo ramos do `VSHLL` que não tinham
teste próprio (bit21/bit7/bit0/bit6/bit4/nibble/prefixo-reservado/`Q>7`). `mvn -o test` + G5
revalidados verdes após o fix; `docs/COBERTURA-ISA.md`/`docs/COBERTURA-JIT.md` sem diff adicional
(o gap nunca apareceu como célula "coberta" — era decode incorreto sob o mesmo `Kind`, não uma
lacuna de contagem).
