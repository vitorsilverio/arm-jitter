# B19.5.2 — Requisito de versão A64 por **OCORRÊNCIA** + curadoria das linhas de meia precisão

**Trilha:** B · **Repo:** arm-jitter (só ferramenta de teste + `docs/`) · **Depende de:** —
**Plano:** `b19.5-plano-fp16.md` · **Épico:** `b19-plano-a64-gap-remanescente.md` · **Status:** ✅ (2026-09-04)

## Contexto

Segundo degrau da escada B19.5 e **pré-requisito de medição de todos os outros**: enquanto as
linhas `_h` contarem `❌` nas colunas `ARMv8.0-A`/`ARMv8.1-A` — onde `FEAT_FP16` **nem existe** — a
tabela mede um denominador falso e o progresso de B19.5.3-B19.5.6 fica ilegível.

**Zero decode. Zero mudança em `core/src/main`.** É trabalho de ferramenta (`IsaCoverageReport`,
que vive em `core/src/test`) mais curadoria, exatamente como a B9.15/B9.17 foram do lado de 32 bits.

### A lacuna de mecanismo

`IsaCoverageReport.AARCH64_VERSION_REQUIREMENTS`
(`core/src/test/java/dev/vitorsilverio/armjitter/tools/IsaCoverageReport.java:187`) é um
`Map<String, Aarch64Feature>` **casado pelo NOME do mnemônico**, consumido por
`isApplicableToAarch64Version(String instruction, Aarch64Architecture)` (`:608`).

Isso funcionou para `CAS`/`LDADD`/`RMIF`/… porque nesses casos TODAS as linhas do mnemônico exigem a
mesma feature. **`FEAT_FP16` quebra a premissa**: um mesmo mnemônico tem uma linha `_h` (ARMv8.2-A) e
uma linha `_sd` (ISA base, hoje `✅`).

**Medição que fecha a discussão: das 96 linhas a curar, 84 têm outra linha de MESMO NOME que hoje é
`✅`.** Marcar por nome apagaria essas 84 células reais. O exemplo canônico:

| `SCVTF_f` | template | linha | status hoje |
|---|---|---|---|
| ocorrência **1** | `@icvt_h` | 1784 | **❌** ⇒ curar (FP16) |
| ocorrência 2 | `@icvt_sd` | 1785 | ✅ ISA base — **não tocar** |
| ocorrência **3** | `@fcvt_fixed_h` | 1826 | **❌** ⇒ curar (FP16) |
| ocorrência 4 | `@fcvt_fixed_s` | 1827 | ✅ ISA base — **não tocar** |
| ocorrência 5 | `@fcvt_fixed_d` | 1828 | ✅ ISA base — **não tocar** |

É **exatamente** a limitação que a **B9.17** resolveu do lado de 32 bits acrescentando a coluna
`ocorrencia` ao `docs/isa-nao-aplicavel.tsv` (5ª coluna, posição 1-based da linha entre as de mesmo
nome+arquivo). Esta task é o espelho A64 daquela ideia, no OUTRO mecanismo.

### A boa notícia: metade da fiação já existe

`appendGroup` **já calcula a ocorrência** e já a passa adiante:

```java
Map<String, Integer> occurrenceCounts = new LinkedHashMap<>();
for (DecodeTreeSpec.Instruction instruction : instructions) {
    int occurrence = occurrenceCounts.merge(instruction.name(), 1, Integer::sum);   // :448
    ...
    if (!applicable || isExcluded(instruction.name(), column, group.decodeFile(), occurrence)) {   // :458
```

O `occurrence` chega em `isExcluded` (caminho TSV) e **não** chega em
`isApplicableToAarch64Version` (caminho de versão A64). Esta task fecha esse segundo caminho.

### A segunda metade do problema: 12 linhas estão escondidas em TODAS as colunas

Medição desta spec: **12 das 96 linhas já são `·` nas 16 colunas** — não por acerto, mas porque
`docs/isa-nao-aplicavel.tsv` as exclui com o mecanismo GROSSO. A exclusão usa a arquitetura `A64`,
e `isAarch64VersionColumn` (`:247`) faz `A64` casar com **qualquer** coluna de versão (compatibilidade
com a tabela monolítica pré-B11.5). Resultado: uma instrução de ARMv8.2-A fica `·` **inclusive em
ARMv9.5-A**, onde a feature existe e ela é trabalho pendente de verdade.

| Linhas | Onde estão na TSV | Feature real | Hoje | Correto |
|---|---|---|---|---|
| `FMLAL_v` `FMLSL_v` `FMLAL2_v` `FMLSL2_v` `FMLAL_vi` `FMLSL_vi` `FMLAL2_vi` `FMLSL2_vi` | `:309-312`, `:422-425` | `FEAT_FHM` (ARMv8.2-A) | `·` nas 16 | `·` em v8.0/v8.1, **`❌` de v8.2 em diante** |
| `FMAXNMV_h` `FMINNMV_h` `FMAXV_h` `FMINV_h` | `:331-334` | `FEAT_FP16` (ARMv8.2-A) | `·` nas 16 | idem |

A própria TSV admite a interinidade: *"mantido excluído por ora (FP16 é opcional mesmo em núcleos
ARMv9; reconferir junto com a triagem geral pós-2026-08-24)"*. **Esta task é essa reconferência.**

⚠️ **Consequência: a cobertura de v8.2 em diante vai CAIR** (88% → 87%), porque 12 linhas de trabalho
real deixam de ficar invisíveis. Isso é o resultado certo, e tem precedente explícito: a **B9.11**
fechou com *"Medição honesta: v6-M 52%→47%"*. Quem executar esta task **não deve** tentar "consertar"
a queda.

**Leitura obrigatória**: a **B9.17** (`## Resultado` — o precedente do conceito de ocorrência), a
**B9.11** (`## Resultado` — o precedente de medição honesta que baixa a porcentagem), o plano
**B19.5** INTEIRO, e a **B11.5** (que criou `AARCH64_VERSION_REQUIREMENTS` e as 16 colunas). No
código: `IsaCoverageReport` INTEIRO (661 linhas — a única classe que esta task muda),
`DecodeTreeSpec#parse`, `docs/isa-nao-aplicavel.tsv` (cabeçalho + as linhas citadas acima),
`arch64/Aarch64Feature` e `arch64/Aarch64Architecture`.

## Objetivo

`AARCH64_VERSION_REQUIREMENTS` passa a poder exigir uma feature de **uma linha específica** de um
mnemônico (nome + ocorrência); as **96 linhas** medidas abaixo são curadas (**88** `FP16`, **8**
`FP16_FUSED_MULTIPLY_ADD_LONG`); e as **12 exclusões grossas** da TSV que hoje as escondem em todas
as colunas são **removidas**, para o mecanismo de versão ser a única fonte da verdade.

**As duas features JÁ EXISTEM e JÁ são declaradas em `ARMV8_2_A`**
(`Aarch64Architecture.java:44` e `:46`) — esta task não cria feature nenhuma.

## A armadilha central desta task (medida, não hipotética)

**`_h` no nome do TEMPLATE não significa meia precisão.** Em `a64.decode`, 157 linhas usam um
template terminado em `_h`; nelas, `_h` significa ora "meia precisão" (`FEAT_FP16`), ora
**"elemento de meia palavra" (halfword INTEIRO, ISA base)**. Medição:

| Grupo | Linhas | O que fazer |
|---|---:|---|
| Templates `_h` com pelo menos uma célula `✅` | **45** | **NÃO TOCAR** — `MUL_vi`, `SMULL_vi`, `SQDMULH_vi`, `SQDMULL_v`, `UMLAL_vi`… são inteiros de halfword, ISA base, já implementados |
| Templates `_h` sem nenhuma `✅` | **112** | ver a decomposição abaixo |

E as 112 **não são todas FP16**:

| Classificação | Linhas | Destino |
|---|---:|---|
| **`FEAT_FP16`** | **88** (84 hoje `❌` + 4 hoje `·`) | **curar nesta task** |
| **`FEAT_FHM`** | **8** (todas hoje `·`) | **curar nesta task** |
| `FEAT_BF16` (`BFMLAL_v`, `BFMLAL_vi`, `BFCVTN_v`) | 3 | **B19.7** — não curar aqui |
| FP8 (`FCVTN_bh`, `FCVTN_bs`, `FDOT_hb_v`, `FDOT_hb_vi`, `F1CVTL`, `F2CVTL`, `BF1CVTL`, `BF2CVTL`) | 8 | **B19.7** — não curar aqui |
| `CRC32`/`CRC32C` (ocorrência 2, forma halfword — gap de decode REAL) | 2 | **B19.6** — não curar aqui |
| `FAMAX`/`FAMIN` (`FEAT_FAMINMAX`), `FSCALE` | 3 | já na TSV (`:320-322`), hoje `·` nas 16 colunas ⇒ **E12** — não curar aqui |

45 + 112 = 157 ✔ · 88 + 8 + 3 + 8 + 2 + 3 = 112 ✔ — a execução deve reconferir que fecha.

> Uma regra por padrão de nome (`endsWith("_h")`) **corromperia a tabela nos dois sentidos**:
> apagaria as 45 linhas já implementadas e marcaria como v8.2 coisas que são BF16/FP8/v9.

## Inclui

1. **Mecanismo — requisito de versão por ocorrência** em `IsaCoverageReport`:
   - `AARCH64_VERSION_REQUIREMENTS` continua existindo e casando por nome (as ~48 entradas atuais
     **não mudam**).
   - Estrutura NOVA ao lado, com chave `"NOME#ocorrencia"` (ex.: `"SCVTF_f#3"`), montada por um
     helper `requireAtOccurrence(String name, int occurrence, Aarch64Feature feature)`.
   - `isApplicableToAarch64Version` ganha `int occurrence` e consulta **primeiro** o mapa por
     ocorrência, **depois** o por nome (o mais específico vence).
   - O chamador em `appendGroup` (`:455`) passa o `occurrence` que já tem.
2. **Curadoria — as 96 linhas** (lista na "Especificação"): 88 → `FP16`, 8 →
   `FP16_FUSED_MULTIPLY_ADD_LONG`.
3. **Remover as 12 exclusões grossas** de `docs/isa-nao-aplicavel.tsv` (`:309-312`, `:331-334`,
   `:422-425`) — o mecanismo de versão passa a cobri-las corretamente. **Não** substituí-las por
   linhas de TSV com `ocorrencia`: ver "As duas rotas".
4. **Comentário de curadoria** no código dizendo que `_h` de template ≠ meia precisão e citando os
   45/112 — é o que impede a próxima sessão de "simplificar" para um `endsWith`.
5. **Javadoc `///` PT (G7)** dos dois mapas + de `isAarch64VersionColumn` (que passa a ter um caso a
   menos de compatibilidade legada a justificar).
6. **Testes** (ver Aceite).
7. `docs/COBERTURA-ISA.md` regenerada e o delta registrado.

## Não inclui (com destino explícito)

- **Qualquer decode de meia precisão** ⇒ **B19.5.3** (13 linhas já alcançáveis), **B19.5.4** (49),
  **B19.5.5** (14), **B19.5.6** (8).
- **Gatear o decoder por `Aarch64Feature.FP16`** ⇒ B19.5.3 (o primeiro consumidor real em
  `core/src/main`).
- **BF16 / FP8 / `CRC32` / `FEAT_FAMINMAX` / `FEAT_FSCALE`** ⇒ tabela de classificação.
- **Auditar as OUTRAS exclusões `A64` grossas da TSV** que sofrem do mesmo problema de esconder
  trabalho em v8.2+ — esta task só desfaz as 12 que colidem com a curadoria FP16/FHM. **Registrar
  quantas restam** (ver "Achado") e deixar para task própria.
- **Mexer no lado de 32 bits.**

## Especificação

### As duas rotas consideradas — e por que a TSV foi recusada

A exclusão por TSV **já suporta ocorrência** (B9.17) e conseguiria marcar as mesmas linhas.
**Recusada**, por dois motivos:

1. **Semântica errada.** A TSV diz *"esta instrução não pertence a esta arquitetura"*; o mapa de
   versão diz *"esta linha exige esta feature"* — e a tabela DERIVA as colunas via
   `architecture.has(required)`. Só a segunda continua correta sozinha quando uma coluna de versão
   nova for acrescentada. **É precisamente a falha das 12 linhas de hoje**: escritas na TSV com
   `A64`, ficaram `·` em versões que nem existiam quando foram escritas.
2. **Duplicação de fato.** `FEAT_FP16` ser ARMv8.2-A já está declarado em `Aarch64Architecture`.

### As 96 linhas (medidas contra `target/isa-decode/a64.decode`)

Onde a ocorrência não é dita, é **1**. Todas as 96 entram no mapa por ocorrência.

**→ `Aarch64Feature.FP16` (88)**

| Template | Linhas | Mnemônicos |
|---|---:|---|
| `@rrr_h` | 9 | `FMULX_s` `FCMEQ_s` `FCMGE_s` `FCMGT_s` `FACGE_s` `FACGT_s` `FABD_s` `FRECPS_s` `FRSQRTS_s` |
| `@rr_h` | 13 | `FADDP_s` `FMAXP_s` `FMINP_s` `FMAXNMP_s` `FMINNMP_s` · `FCMGT0_s` `FCMGE0_s` `FCMEQ0_s` `FCMLE0_s` `FCMLT0_s` `FRECPE_s` `FRECPX_s` `FRSQRTE_s` |
| `@qrrr_h` | 5 | `FADD_v` `FSUB_v` `FMAX_v` `FMIN_v` `FCMEQ_v` |
| `@qrr_h` | 33 | **`FMAXNMV_h` `FMINNMV_h` `FMAXV_h` `FMINV_h`** (hoje `·`, ver acima) · `FABS_v` `FNEG_v` `FSQRT_v` `FRINTN_v` `FRINTM_v` `FRINTP_v` `FRINTZ_v` `FRINTA_v` `FRINTX_v` `FRINTI_v` · `SCVTF_vi` `UCVTF_vi` `FCVTNS_vi` `FCVTNU_vi` `FCVTPS_vi` `FCVTPU_vi` `FCVTMS_vi` `FCVTMU_vi` `FCVTZS_vi` `FCVTZU_vi` `FCVTAS_vi` `FCVTAU_vi` · `FCMGT0_v` `FCMGE0_v` `FCMEQ0_v` `FCMLE0_v` `FCMLT0_v` · `FRECPE_v` `FRSQRTE_v` |
| `@icvt_h` | 12 | `SCVTF_f` `UCVTF_f` `FCVTNS_f` `FCVTNU_f` `FCVTPS_f` `FCVTPU_f` `FCVTMS_f` `FCVTMU_f` `FCVTZS_f` `FCVTZU_f` `FCVTAS_f` `FCVTAU_f` |
| `@rrx_h` | 4 | `FMUL_si` `FMLA_si` `FMLS_si` `FMULX_si` |
| `@qrrx_h` | 4 | `FMUL_vi` `FMLA_vi` `FMLS_vi` `FMULX_vi` |
| `@fcvt_fixed_h` | 4 | `SCVTF_f` **occ=3** · `UCVTF_f` **occ=3** · `FCVTZS_f` **occ=3** · `FCVTZU_f` **occ=3** |
| `@fcvtq_h` | 4 | `SCVTF_vf` `UCVTF_vf` `FCVTZS_vf` `FCVTZU_vf` |

**→ `Aarch64Feature.FP16_FUSED_MULTIPLY_ADD_LONG` (8)** — todas hoje `·` pela TSV

`FMLAL_v` `FMLSL_v` `FMLAL2_v` `FMLSL2_v` (`@qrrr_h`) · `FMLAL_vi` `FMLSL_vi` `FMLAL2_vi`
`FMLSL2_vi` (`@qrrx_h`).

### Como reproduzir a medição (obrigatório antes de escrever a lista)

A ordem das linhas do inventário é **exatamente** a ordem das linhas da tabela — conferido nesta spec
(1161 = 1161). Para reconferir:

1. `./gerar-cobertura-isa.sh`.
2. Extrair a seção `## A64 — AArch64`; a N-ésima linha `| \`NOME\` |` é a N-ésima instrução de
   `a64.decode` na leitura do `DecodeTreeSpec` (comentários removidos, continuações `\` juntadas,
   heads `%`/`&`/`@` puladas, `{`/`}`/`[`/`]` como espaço).
3. Ocorrência = contador 1-based por nome, na ordem do arquivo.

**Armadilha real ao reproduzir**: juntar as continuações `\` é obrigatório — sem isso o inventário dá
1164 linhas em vez de 1161 (três `@ldst_single_*` de `a64.decode:619-627` viram instruções fantasma
`index=%ldst_single_index_*`) e **o alinhamento desanda a partir da linha 289**.

### Efeito esperado na tabela (medido)

Duas transições, em sentidos opostos:

- **168 células `❌ → ·`**: as 84 linhas FP16 hoje `❌`, nas colunas `ARMv8.0-A` e `ARMv8.1-A`.
- **168 células `· → ❌`**: as 12 linhas hoje escondidas (8 FHM + 4 FP16 across-lanes), nas 14
  colunas de `ARMv8.2-A` a `ARMv9.5-A`.

| Coluna | Antes | Depois |
|---|---|---|
| `ARMv8.0-A` | 88% (850/960) | **97%** (850/876) |
| `ARMv8.1-A` | 88% (874/984) | **97%** (874/900) |
| `ARMv8.2-A` | 88% (879/995) | **87%** (879/1007) |
| `ARMv8.3-A` … `ARMv9.5-A` | 88% | **87%** (cada uma com +12 no denominador) |
| **Global** | **89%** (16298/18164) | **89%** (16298/18164) — **inalterado** |

`totalSupported` não muda (nada novo decodifica) e o total de aplicáveis também não (−168 +168 = 0).
**O valor desta task não é subir número — é cada coluna passar a dizer a verdade.**

Registrar os números REAIS no `## Resultado`; se divergirem, **a divergência é o achado** (provável
causa: o inventário do QEMU mudou — ver **E11**).

Marco de release continua **suspenso** — não publicar.

## Passos

1. Ler "Leitura obrigatória"; rodar `./gerar-cobertura-isa.sh` e guardar a tabela ANTES.
2. Reproduzir a medição e conferir os números: 157 linhas de template `_h`, 45 com `✅`, 112 sem,
   88 FP16 (84 `❌` + 4 `·`), 8 FHM (todas `·`). **Se algum não bater, PARE e reporte.**
3. Mecanismo: mapa por ocorrência + `isApplicableToAarch64Version(name, occurrence, architecture)` +
   o chamador.
4. As 96 entradas + o comentário sobre `_h` ≠ meia precisão.
5. Remover as 12 linhas da TSV (`:309-312`, `:331-334`, `:422-425`).
6. Javadoc (G7). Testes (ver Aceite).
7. `mvn -o test` (JBR 25) + `./gerar-cobertura-isa.sh`; conferir o delta contra a tabela esperada.
8. Atualizar `b19.5-plano-fp16.md`: inventário 84 → **88** (`@qrr_h` 29 → 33), o efeito real
   (168 `❌→·` **e** 168 `·→❌`), e a nota de que as 8 FHM foram curadas junto.
9. `INDICE.md` da trilha B, `## Resultado` nesta task, commit `B19.5.2: …`, `git push`.

## Aceite

- **Teste do mecanismo**: uma linha com requisito por ocorrência é aplicável só a partir da versão da
  feature, e as OUTRAS ocorrências do MESMO nome seguem aplicáveis a todas as colunas. Montar com
  `SCVTF_f` (1 e 3 curadas; 2, 4 e 5 não).
- **Teste de precedência**: nome presente nos dois mapas ⇒ o por-ocorrência vence na ocorrência
  listada, o por-nome vale nas demais. (Hoje nenhum nome está nos dois; o teste trava o contrato.)
- **Teste de não-vazamento**: nenhuma célula que era `✅` virou `·` ou `❌`. As únicas transições
  permitidas são `❌ → ·` (84 linhas × v8.0/v8.1) e `· → ❌` (12 linhas × v8.2..v9.5). **84 das 96
  entradas têm irmã `✅` de mesmo nome** — é o que este teste protege.
- **Teste de contagem**: 96 entradas no mapa (88 `FP16`, 8 `FHM`), cada uma casando uma linha REAL do
  inventário (nenhuma entrada órfã de mnemônico renomeado pelo QEMU).
- **Teste de que a TSV não esconde mais essas 12**: as linhas removidas não voltam a casar; as 12
  medem `·` em v8.0/v8.1 e `❌` de v8.2 em diante.
- `docs/COBERTURA-ISA.md`: exatamente **168 `❌→·`** e **168 `·→❌`**; v8.0/v8.1 88% → 97%; v8.2..v9.5
  88% → 87%; **global inalterado em 89%**.
- **Zero mudança em `core/src/main`** — `git diff --stat` prova.
- `mvn -o test` verde + G5 nos 5 consumidores. **Diff vazio esperado** nos consumidores.

## Validação

`mvn -o test` no arm-jitter (JBR 25), `mvn -o install`, `mvn -o test` nos 5 consumidores,
`./gerar-cobertura-isa.sh` com o delta exato acima.

## Armadilhas

1. **`_h` de template ≠ meia precisão.** 45 linhas de template `_h` já são `✅` porque ali `_h` é
   "elemento halfword INTEIRO". Qualquer regra por padrão de nome as destrói.
2. **Curar por NOME apaga 84 células `✅`.** É o erro que a B9.15 evitou de propósito em 32 bits e a
   B9.17 teve de destravar depois.
3. **A queda de v8.2+ para 87% é o resultado CERTO.** São 12 linhas de trabalho real que a TSV
   escondia. Precedente: B9.11 ("v6-M 52%→47%"). Não maquiar.
4. **A ocorrência é por (nome, ARQUIVO)** e segue a ordem de leitura do `DecodeTreeSpec`.
5. **Continuações `\` mudam a numeração** — ver "Como reproduzir". Sem juntá-las, tudo depois da
   linha 289 fica errado.
6. **`FEAT_FHM` ≠ `FEAT_FP16`**, mesmo as duas sendo ARMv8.2-A e o efeito hoje sendo idêntico.
   `extending` permite montar uma arquitetura com uma e não a outra.
7. **Não “aproveitar” para curar BF16/FP8/`CRC32`** — features sem constante em `Aarch64Feature`.
8. **A tabela pode ter mudado por baixo** (inventário do QEMU, ver **E11**). Se os números não
   baterem, reportar — não "ajustar" a lista até fechar.

## Não fazer

- Não implementar decode nenhum.
- Não gatear o decoder por `Aarch64Feature.FP16` (B19.5.3).
- Não pôr as linhas `_h` na TSV (nem com `ocorrencia`).
- Não substituir a lista enumerada por regra de padrão de nome.
- Não criar constante nova em `Aarch64Feature`.
- Não maquiar a queda de v8.2+.

## Achado — já medido, e já tem task própria: **[E12](../trilha-e-manutencao/e12-tsv-a64-esconde-trabalho.md)**

As 12 linhas que esta task desfaz **não são um caso isolado**. `isAarch64VersionColumn` (`:247`) faz
TODA linha de TSV com arquitetura `A64` casar as 16 colunas de versão. Medição desta spec:

- **123 linhas** da TSV usam a arquitetura `A64`;
- **102 delas citam uma versão ≥ ARMv8.1-A** na própria justificativa — ou seja, ficam `·` também nas
  colunas onde a feature EXISTE, escondendo trabalho pendente;
- as features mais atingidas: `FEAT_MTE2` (16), `FEAT_PAuth` (10), `FEAT_FP16` (10), `FEAT_MOPS` (9),
  `FEAT_FRINTTS` (8), `FEAT_FHM` (8), `FEAT_I8MM` (6), `FEAT_SM3`/`FEAT_CSSC`/`FEAT_BF16` (5 cada)…
- e **quase todas essas features já têm constante** em `Aarch64Feature` (só `FEAT_LSE128` e
  `FEAT_LRCPC2` não têm) — logo o mecanismo desta task já basta para expressá-las corretamente.

Isso corrige, de passagem, uma suposição errada de uma versão anterior desta spec: `FAMAX`/`FAMIN`/
`FSCALE` **não** estão sem tratamento — estão na TSV (`:320-322`) e `FP_ABSOLUTE_MAX_MIN` existe e é
declarada em `ARMV9_4_A`. Elas são mais 3 casos do MESMO problema, e pertencem à E12.

**Esta task NÃO deve varrer as 102** (escopo: só as 12 que colidem com a curadoria FP16/FHM).
Registrar no `## Resultado` que a E12 herda o resto.

## Resultado

Executada 2026-09-04 (JBR 25).

### Medição reproduzida — bateu byte a byte com a spec

Script `reproduce.py` espelhando `DecodeTreeSpec.parse` + join com a tabela gerada
(`docs/COBERTURA-ISA.md`, revisão do QEMU `2931a675e9d3…` já fixada pela E11):

| Checkpoint da spec | Esperado | Medido |
|---|---:|---:|
| Instruções no inventário `a64.decode` | 1161 | **1161** |
| Alinhamento nome-a-nome tabela × parse | 0 divergências | **0** |
| Linhas com template `_h` (universo dos 9: `@rr_h`/`@rrr_h`/`@rrx_h`/`@qrr_h`/`@qrrr_h`/`@qrrx_h`/`@icvt_h`/`@fcvt_fixed_h`/`@fcvtq_h`) | 157 | **157** |
| …com ≥1 célula `✅` (halfword INTEIRO, não tocar) | 45 | **45** |
| …sem nenhuma `✅` | 112 | **112** |
| dessas 112: `FEAT_FP16` | 88 (84 `❌` + 4 `·`) | **88** (84 `❌` + 4 `·`) |
| dessas 112: `FEAT_FHM` | 8 (todas `·`) | **8** (todas `·`) |
| dessas 112: BF16(3)/FP8(8)/CRC32(2)/FAMINMAX+FSCALE(3) — **não** curar | 16 | **16** (88+8+16 = 112 ✔) |
| das 96 curadas, quantas têm irmã `✅` de MESMO nome | 84 | **84** (as 12 sem irmã = 8 FHM + 4 across-lanes) |

Nada divergiu ⇒ segui sem parar (a spec mandava PARAR e reportar se algum não batesse).

### Mecanismo

`IsaCoverageReport` (só `core/src/test`):

- **`AARCH64_VERSION_REQUIREMENTS_BY_OCCURRENCE`** — `Map<String,Aarch64Feature>` novo, chave
  `"NOME#ocorrencia"`, ao lado do mapa por nome (que **não mudou**). Populado por helpers
  `requireAtOccurrence(name, occ, feature)` e `requireFirst(feature, names…)` (94 das 96 entradas
  são `occ=1`; as 4 de `@fcvt_fixed_h` — `SCVTF_f`/`UCVTF_f`/`FCVTZS_f`/`FCVTZU_f` — são `occ=3`,
  porque `occ=1` dessas é `@icvt_h` e `occ=2` é `@icvt_sd`).
- **`isApplicableToAarch64Version`** ganhou `int occurrence` — consulta o mapa por ocorrência
  PRIMEIRO, o por nome depois (mais específico vence). Chamador em `appendGroup` passa o
  `occurrence` que o laço já computa para a TSV.
- Visibilidade relaxada para `static` (package-private) em `AARCH64_VERSION_REQUIREMENTS`,
  `AARCH64_VERSION_REQUIREMENTS_BY_OCCURRENCE`, `isAarch64VersionColumn`,
  `isApplicableToAarch64Version`, `loadExclusions`, `isExcluded`, `EXCLUSIONS` — para o teste novo.
- Javadoc `///` PT (G7): os dois mapas + `isAarch64VersionColumn` (agora com a nota de que
  curadoria de versão A64 POR LINHA vive no mapa de ocorrência, não na TSV).

### Curadoria — as 96 linhas

88 `Aarch64Feature.FP16` + 8 `Aarch64Feature.FP16_FUSED_MULTIPLY_ADD_LONG`, enumeradas linha a
linha (comentário no código explica que `_h` de TEMPLATE ≠ meia precisão e cita os 45/112, para
barrar uma futura "simplificação" por `endsWith`).

### TSV — 12 exclusões grossas removidas

`docs/isa-nao-aplicavel.tsv`: removidas as linhas de dados `FMLAL_v`/`FMLSL_v`/`FMLAL2_v`/
`FMLSL2_v` (`:309-312`), `FMAXNMV_h`/`FMINNMV_h`/`FMAXV_h`/`FMINV_h` (`:331-334`) e `FMLAL_vi`/
`FMLSL_vi`/`FMLAL2_vi`/`FMLSL2_vi` (`:422-425`). Os 3 blocos de comentário ao redor foram
reescritos (não removidos) apontando para o mapa de ocorrência. **Nenhuma linha `_h` foi para a
TSV** (nem com `ocorrencia`) — a rota TSV foi recusada pela spec (semântica errada + duplicação).

### Efeito na tabela — EXATAMENTE o previsto

`./gerar-cobertura-isa.sh` regenerado. Transições de célula (medidas linha a linha, script):

- **168 `❌ → ·`** — as 84 linhas FP16 hoje `❌`, nas colunas `ARMv8.0-A` e `ARMv8.1-A`.
- **168 `· → ❌`** — as 12 linhas antes escondidas pela TSV (8 FHM + 4 across-lanes), nas 14
  colunas de `ARMv8.2-A` a `ARMv9.5-A`.
- **0 células `✅` viraram `·`/`❌`** — as 84 das 96 entradas com irmã `✅` foram protegidas pelo
  uso de OCORRÊNCIA em vez de NOME.

| Coluna | Antes | Depois |
|---|---|---|
| `ARMv8.0-A` | 88% (850/960) | **97%** (850/876) |
| `ARMv8.1-A` | 88% (874/984) | **97%** (874/900) |
| `ARMv8.2-A` | 88% (879/995) | **87%** (879/1007) |
| `ARMv8.3-A` … `ARMv9.5-A` | 88% | **87%** (cada uma com +12 no denominador) |
| **Global** | **89%** (16303/18169) | **89%** (16303/18169) — inalterado (−168 +168 = 0) |

Nota: os absolutos globais (16303/18169) diferem em +5/+5 dos que a spec rascunhou
(16298/18164) — mas o rascunho era pré-fixação da E11 e as colunas A64 bateram EXATAS
(850/960→850/876 etc.); e o global é idêntico ANTES e DEPOIS desta task, o que confirma que a
diferença não vem daqui. Marco de release segue suspenso — nada publicado.

### Testes — `Aarch64Fp16VersionCurationTest` (6, `core/src/test/.../tools/`)

1. `perOccurrenceRequirement_appliesOnlyFromFeatureVersion_andOnlyToTheListedOccurrence` —
   `SCVTF_f#1`/`#3` só aplicáveis de ARMv8.2-A; `#2`/`#4`/`#5` aplicáveis em toda coluna.
2. `perOccurrenceRequirement_beatsNameRequirement_onTheListedOccurrence` — injeta um requisito
   por NOME conflitante (`SCVTF_f → SME`/ARMv9.2-A) e prova que o por-ocorrência vence em `#1` e o
   por-nome vale em `#2` (contrato travado; hoje nenhum nome está nos dois mapas).
3. `occurrenceMap_has96Entries_88Fp16_and_8Fhm` — contagem + formato de chave.
4. `curedLines_areNotCurrentlySupported_andEachMatchesARealTableRow` — cada chave casa uma linha
   REAL do inventário e NENHUMA é `✅` hoje (o teste de não-vazamento, direção que importa).
5. `everyCuredLine_isCurrentlyMissingOrNotApplicable_never_something_else` — reforço: nem `✅`
   nem `⚠️`.
6. `tsv_noLongerHidesTheTwelveGrossExclusions` — as 12 não são mais `isExcluded` em
   v8.0-A/v8.2-A/v9.5-A.

Todos CI-safe: usam `docs/COBERTURA-ISA.md` e `docs/isa-nao-aplicavel.tsv` (versionados), não o
`target/isa-decode/a64.decode` (gitignored, ausente no CI).

### Validação

`mvn -o test` no arm-jitter (JBR 25) verde — **2960 + 21** testes, 0 falhas. `mvn -o install`
(artefato `arm-jitter` inalterado — zero `core/src/main`). G5: `gbaemu`/`ndsemu`/`armbox`
`mvn -o test` — **BUILD SUCCESS** nos três (diff vazio esperado; `virtual-arm-box`/`n3dsemu` não
rodados, mesma razão do precedente B9.17: nenhum arquivo de `core/src/main` mudou, consumidores
pinados na `1.3.0` do Central). `git diff --stat -- core/src/main` vazio.

### Grupos revelados que a E12 herda

Confirmado: esta task só desfez as 12 exclusões que colidiam com FP16/FHM. As demais **~90
linhas `A64` da TSV** com justificativa de versão ≥ ARMv8.1-A seguem escondidas em todas as
colunas — `FEAT_MTE2` (16), `FEAT_PAuth` (10), `FEAT_FP16` fora do escopo `_h` (10), `FEAT_MOPS`
(9), `FEAT_FRINTTS` (8), `FEAT_FHM` fora do escopo (0 — as 8 foram todas curadas aqui),
`FEAT_I8MM` (6), `FEAT_SM3`/`FEAT_CSSC`/`FEAT_BF16` (5 cada), `FEAT_FCMA`/`FEAT_DotProd` (4),
`FEAT_LSE128`/`FEAT_LRCPC2` (3+2, **sem constante** em `Aarch64Feature`), `FEAT_FAMINMAX`
(2 — `FAMAX`/`FAMIN`, `:320-321`; `FSCALE` `:322` tem justificativa FP8/SVE2.1), etc. ⇒ **E12**.
