# B17.8 — Imediato (bitwise e wide) + multiply **indexado** — 104 encodings

**Trilha:** B · **Repo:** arm-jitter (+ revalidação G5) · **Depende de:** B17.5 (⬜)
**Épico:** `b17-plano-sve.md` · **Status:** ⬜ (spec escrita 2026-09-14, ainda não executada)

## Contexto

A maior task da escada depois da B17.21 e da B17.16 — e a que a rodada de spec mais teve que
corrigir de rótulo.

### Recorte normativo (ver a tabela completa em `b17.1-aarch64-feature-sve-sve2.md`)

| Seção de `sve.decode` | Linhas | Enc |
|---|---|---:|
| `### SVE Bitwise Immediate Group` | 561-569 | 4 |
| `### SVE Integer Wide Immediate - Predicated Group` | 571-584 | 5 |
| `### SVE Integer Wide Immediate - Unpredicated Group` | 906-961 | 24 |
| `#### SVE Multiply - Indexed` (linhas 963-1067) **menos** `FCADD`/`FCMLA` (1069-1081) | 966-1067 | 71 |
| **Total** | | **104** |

⚠️ **Achado desta rodada**: a escada do épico chama esta task de "Imediato" e conta **108**, citando
"o bloco de 99 linhas". Esse "bloco de 99" são na verdade DUAS seções: as 24 linhas de
`### SVE Integer Wide Immediate - Unpredicated Group` **mais** as 75 linhas de
`#### SVE Multiply - Indexed` — um cabeçalho de QUATRO `#` que o agrupamento por `###` do épico
engoliu. Multiply indexado não é imediato nenhum. Duas correções:

- o rótulo passa a ser "**Imediato + multiply indexado**" (a contiguidade de espaço de opcode
  justifica manter juntos; separar exigiria uma task a mais, e a escada é fixa em B17.1-B17.26);
- as 4 linhas FP do fim do bloco indexado (`FCADD`, `FCMLA_zpzzz`, `FCMLA_zzxz` ×2) foram para a
  **B17.14** (FP indexado), que é onde a semântica delas mora ⇒ 108 − 4 = **104**.

### Os encodings reais (amostra; os intervalos completos estão acima)

```
ORR_zzi         00000101 00 0000 ............. .....            @rdn_dbm
EOR_zzi         00000101 01 0000 ............. .....            @rdn_dbm
AND_zzi         00000101 10 0000 ............. .....            @rdn_dbm
DUPM            00000101 11 0000 dbm:13 rd:5
```

```
FCPY            00000101 .. 01 .... 110 imm:8 .....             @rdn_pg4
{
  INVALID       00000101 00 01 ---- 01 1 -------- -----
  CPY_m_i       00000101 .. 01 .... 01 . ........ .....   @rdn_pg4 imm=%sh8_i8s
}
{
  INVALID       00000101 00 01 ---- 00 1 -------- -----
  CPY_z_i       00000101 .. 01 .... 00 . ........ .....   @rdn_pg4 imm=%sh8_i8s
}
```

```
FDUP            00100101 esz:2 111 00 1110 imm:8 rd:5
{
  INVALID       00100101 00    111 00 011 1 -------- -----
  DUP_i         00100101 esz:2 111 00 011 . ........ rd:5       imm=%sh8_i8s
}
{
  INVALID       00100101 00 100 000 11 1 -------- -----
  ADD_zzi       00100101 .. 100 000 11 . ........ .....         @rdn_sh_i8u
}
...
SMAX_zzi        00100101 .. 101 000 110 ........ .....          @rdn_i8s
MUL_zzi         00100101 .. 110 000 110 ........ .....          @rdn_i8s
DOT_zzzz        01000100 1 sz:1 0 rm:5 00000 u:1 rn:5 rd:5      ra=%reg_movprfx
CDOT_zzzz       01000100 esz:2 0 rm:5 0001 rot:2 rn:5 rd:5      ra=%reg_movprfx
```

```
#### SVE Multiply - Indexed
SDOT_zzxw_4s    01000100 10 1 ..... 000000 ..... .....   @rrxr_2 esz=2
SDOT_zzxw_4d    01000100 11 1 ..... 000000 ..... .....   @rrxr_1 esz=3
MLA_zzxz_h      01000100 0. 1 ..... 000010 ..... .....   @rrxr_3 esz=1
SQRDMLAH_zzxz_h 01000100 0. 1 ..... 000100 ..... .....   @rrxr_3 esz=1
USDOT_zzxw_4s   01000100 10 1 ..... 000110 ..... .....   @rrxr_2 esz=2
SQDMLALB_zzxw_s 01000100 10 1 ..... 0010.0 ..... .....   @rrxr_3a esz=2
CDOT_zzxw_s     01000100 10 1 index:2 rm:3 0100 rot:2 rn:5 rd:5  ra=%reg_movprfx
CMLA_zzxz_h     01000100 10 1 index:2 rm:3 0110 rot:2 rn:5 rd:5  ra=%reg_movprfx
SMLALB_zzxw_s   01000100 10 1 ..... 1000.0 ..... .....   @rrxr_3a esz=2
SMULLB_zzx_s    01000100 10 1 ..... 1100.0 ..... .....   @rrx_3a esz=2
SQDMULH_zzx_h   01000100 0. 1 ..... 111100 ..... .....   @rrx_3 esz=1
MUL_zzx_h       01000100 0. 1 ..... 111110 ..... .....   @rrx_3 esz=1
```

Formatos de índice (linhas 270-294), a peça-chave da metade indexada:

```
@rrx_3          ........ .. . ..      rm:3 ...... rn:5 rd:5  &rrx_esz index=%index3_22_19
@rrx_2          ........ .. . index:2 rm:3 ...... rn:5 rd:5  &rrx_esz
@rrx_1          ........ .. . index:1 rm:4 ...... rn:5 rd:5  &rrx_esz
@rrx_3a         ........ .. . .. rm:3 ...... rn:5 rd:5       &rrx_esz index=%index3_19_11
@rrx_2a         ........ .. . .  rm:4 ...... rn:5 rd:5       &rrx_esz index=%index2_20_11
@rrxr_3/_2/_1/_3a/_2a  — idem, com `ra=%reg_movprfx`
```

### Achados desta rodada de spec

1. **`INVALID` é um mnemônico real do inventário** (8 ocorrências nesta task: 2 no grupo predicado,
   6 no unpredicado). O medidor (`DecodeTreeSpec`) as conta como encodings, e elas aparecem em
   `docs/COBERTURA-ISA.md` como linhas `INVALID`. **São padrões que a arquitetura define como
   inválidos** (`esz=00` com o bit de shift ligado) e existem para que o decodetree os recuse ANTES
   de casar a forma geral. Para o medidor ficar `✅`, o decoder tem que **recusar** esses padrões
   explicitamente — ou seja, `INVALID` é literalmente G8 codificado no inventário. Conferir o que o
   probe de `IsaCoverageReport` espera (é o único grupo do projeto com esse caso; ver Armadilha 1).
2. **`@rdn_dbm`/`DUPM` usam o bitmask de 13 bits do A64** (`dbm:13` = N:immr:imms), o MESMO
   codificador que `core/.../decoder64/Aarch64LogicalImmediate.java` já implementa para `AND`/`ORR`
   imediato do A64 — **reuso obrigatório**, não reimplementar.
3. **`%sh8_i8s`/`%sh8_i8u` (`5:9 !function=expand_imm_sh8s|u`)**: 9 bits = `sh:1` + `imm8`, com o
   `sh` deslocando o imediato 8 bits à esquerda. É o imediato "wide" do nome do grupo.
4. **5 famílias de índice diferentes** (`%index3_22_19`, `index:2`, `index:1`, `%index3_19_11`,
   `%index2_20_11`), escolhidas por `esz` e por família de operação. Errar o extrator produz o
   elemento errado do segmento — silenciosamente.
5. **O índice é POR SEGMENTO de 128 bits**, não por vetor: `MUL Zd.h, Zn.h, Zm.h[3]` multiplica
   cada elemento do segmento pelo elemento 3 **daquele** segmento. Em `VL=128` é indistinguível de
   "índice global". Outro caso de VLA invisível em `VL=128`.
6. **`rm` tem 3 ou 4 bits** nas formas indexadas (`rm:3`/`rm:4`), não 5 — o registrador de índice é
   restrito a `Z0-Z7`/`Z0-Z15`. Ler `rm:5` aqui é bug.
7. **Quase tudo do bloco indexado é SVE2** (widening `SQDMLALB`…, `SMULLB`…, `CDOT`/`CMLA`). Só
   `SDOT`/`UDOT` (4 linhas, `FEAT_SVE`), `MLA_zzxz`/`MLS_zzxz`, `MUL_zzx` e `SQDMULH`/`SQRDMULH`
   indexados são SVE base — **conferir linha a linha** contra o manual; gate por linha, não por
   bloco. `USDOT`/`SUDOT` dependem de `FEAT_I8MM` (que `Aarch64Feature.INT8_MATRIX_MULTIPLY` já
   tem).

## Objetivo

As 104 células decodificam e executam, com recusa explícita dos 8 padrões `INVALID`, reuso do
codificador de bitmask do A64 e o índice por segmento correto em qualquer `VL`.

## Inclui

1. **Bitwise immediate** (4): `ORR_zzi`/`EOR_zzi`/`AND_zzi`/`DUPM`.
2. **Wide immediate predicado** (5): `FCPY`, `CPY_m_i`, `CPY_z_i` + os 2 `INVALID`.
3. **Wide immediate não-predicado** (24): `FDUP`, `DUP_i`, `ADD_zzi`/`SUB_zzi`/`SUBR_zzi`/
   `SQADD`/`UQADD`/`SQSUB`/`UQSUB` (+ 6 `INVALID`), min/max imediato, `MUL_zzi`, `DOT_zzzz`,
   `CDOT_zzzz`.
4. **Multiply indexado** (71): dot-product indexado, multiply-add indexado, saturating
   multiply-add indexado, complex dot/mla indexado, multiply-add long indexado, multiply long
   indexado, saturating multiply/multiply-high indexado, `MUL` indexado.
5. Testes (ver Aceite).

## Não inclui

- **`FCADD`/`FCMLA`** (as 4 linhas FP no fim do bloco indexado, `sve.decode` 1069-1081) — **B17.14**.
- **FP indexado** (`FMLA_zzxz`/`FMUL_zzx`) — B17.14.
- **`DUP_x`/`DUP_s`** (broadcast de registrador) — B17.10.
- **Multiply-add não-indexado** (`SMLALB_zzzw`, `#### SVE Integer Multiply-Add (unpredicated)`) —
  B17.22.
- **Emissão nativa/Truffle** — padrão da trilha B.

## Especificação

### Leitura obrigatória antes de codar

- `target/isa-decode/sve.decode` linhas 561-584, 906-1067 INTEIRAS, mais os formatos `@rdn_dbm`,
  `@rdn_pg4`, `@rdn_sh_i8u`, `@rdn_i8s`, `@rdn_i8u`, `@rrx_*`, `@rrxr_*` e os extratores
  `%sh8_i8s|u`, `%index3_22_19`, `%index3_19_11`, `%index2_20_11`.
- `core/src/main/java/dev/vitorsilverio/armjitter/decoder64/Aarch64LogicalImmediate.java` — o
  decodificador de `dbm:13` a **reusar**.
- `core/src/test/java/dev/vitorsilverio/armjitter/tools/IsaCoverageReport.java` e
  `DecodeTreeSpec.java` — para entender exatamente como uma linha `INVALID` é sondada
  (Armadilha 1). **Ler antes de decidir** como recusá-las.
- `core/src/main/java/dev/vitorsilverio/armjitter/advsimd/AdvSimdLanes.java` — `threeSameByElement`
  e `wideningByElement` já fazem "operação por elemento indexado" para AdvSIMD; reuso obrigatório
  onde couber.
- `https://raw.githubusercontent.com/qemu/qemu/master/target/arm/tcg/translate-sve.c`
  (`trans_DUPM`, `trans_CPY_m_i`, `do_zzxz_ool`, `gen_gvec_ool_zzxz`) — via `WebFetch`.
- ARM DDI 0487 — a versão que introduz CADA linha indexada (Achado 7).

## Passos

1. Ler "Leitura obrigatória". Resolver a Armadilha 1 (`INVALID`) **antes** de qualquer código, com
   um probe isolado do medidor.
2. Tabelar, linha a linha, a feature de cada uma das 71 indexadas (Achado 7).
3. Decoder das 104 linhas + recusa explícita dos 8 `INVALID`. Resto → `UNIMPLEMENTED`.
4. Executores, reusando `Aarch64LogicalImmediate` e `AdvSimdLanes` onde possível.
5. Testes; `mvn -o test` + `install` + G5 + `./gerar-cobertura-isa.sh` (zero-diff até B17.26).
6. `INDICE.md`, `## Resultado`, commit `B17.8: ...`, `git push`.

## Aceite

- As 104 linhas decodificam sob as features corretas (as SVE2/`I8MM` recusadas sob `SVE` puro).
- Os **8 padrões `INVALID`** são recusados (`null`/`UNIMPLEMENTED`) e as formas gerais correspondentes
  (`CPY_m_i`, `CPY_z_i`, `DUP_i`, `ADD_zzi`, `SUB_zzi`, `SUBR_zzi`, `SQADD_zzi`, `UQADD_zzi`,
  `SQSUB_zzi`, `UQSUB_zzi`) continuam decodificando para os demais valores — teste do PAR, não só
  da forma geral.
- `DUPM`/`ORR_zzi`/`EOR_zzi`/`AND_zzi` produzem o mesmo bitmask que `Aarch64LogicalImmediate` já
  produz para o A64 (teste cruzado com um encoding A64 equivalente).
- `%sh8_i8s`/`%sh8_i8u`: `sh=1` desloca o imediato 8 bits — teste com `sh=0` e `sh=1`.
- **Índice por segmento**: `MUL_zzx` com `VL >= 256` usa o elemento indexado **de cada segmento de
  128 bits**, não do primeiro — teste obrigatoriamente com `VL >= 256` (Achado 5).
- Os 5 extratores de índice produzem o índice certo para cada `esz` (teste tabelado).
- `rm` restrito a 3/4 bits nas formas indexadas.
- Testado em pelo menos dois `VL`.
- `mvn -o test` verde + G5 verde nos 5 consumidores.

## Armadilhas

1. **`INVALID` é o caso mais estranho de toda a tabela de cobertura.** É a única "instrução" cujo
   `✅` significa *recusar*. Antes de implementar, sondar o medidor com um encoding `INVALID` e
   verificar o que ele considera `SUPPORTED` — se `IsaCoverageReport` marcar `✅` só quando o
   decoder devolve algo não-`UNIMPLEMENTED`, essas 8 linhas são **estruturalmente inalcançáveis** e
   isso precisa virar achado documentado (e provavelmente uma linha em
   `docs/isa-nao-aplicavel.tsv` com a justificativa correta, ou um ajuste no medidor). **Não
   inventar um `Kind` `INVALID` só para pintar a célula de verde** — seria o oposto do G8.
2. **Não reimplementar o bitmask de 13 bits.** `Aarch64LogicalImmediate` já existe (B6.x); uma
   segunda implementação divergente é a receita de bug sutil.
3. **5 extratores de índice** (Achado 4) — errar qual formato cada linha usa é invisível em
   `esz=3`/`index=0` e explode nos demais.
4. **`rm:3`/`rm:4`** (Achado 6): o registrador de índice é restrito.
5. **`DOT_zzzz` usa `sz:1`, não `esz:2`** — bit 22 sozinho seleciona 32 vs 64 bits, com o `u:1`
   selecionando signed/unsigned. Layout diferente do resto do grupo.
6. **`FCPY`/`FDUP` são FP** (imediato de ponto flutuante de 8 bits, o `VFPExpandImm` do A64) mas
   vivem nos grupos de imediato. Reusar o expansor de imediato FP que o A64 já tem (`FMOV`
   imediato, B8.x) — **conferir se existe**; se não existir, é código novo e precisa de teste
   tabelado contra o manual.
7. **`SQDMLALB_zzxw_s` e irmãs usam `0010.0`** — um bit don't-care NO MEIO do opcode, que é parte
   do índice (`%index3_19_11` pega o bit 11). Decodificar `0010x0` como dois opcodes distintos é
   erro; decodificá-lo como opcode fixo perde o bit alto do índice.

## Não fazer

- Não implementar `FCADD`/`FCMLA` (B17.14) nem nenhuma forma FP indexada.
- Não implementar o multiply-add long **não-indexado** (B17.22).
- Não pintar as 8 linhas `INVALID` de verde por um atalho (Armadilha 1).
- Não duplicar `Aarch64LogicalImmediate`.

## Resultado

_(preencher ao concluir a task)_
