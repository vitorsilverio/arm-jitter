# B19.11c — A64: `FEAT_FP8DOT2` (produto escalar FP8 2 vias → meia precisão) — 2 linhas

**Trilha:** B · **Repo:** arm-jitter (+ revalidação G5) · **Depende de:** B19.11, B19.11a
**Épico:** `b19-plano-a64-gap-remanescente.md` · **Status:** ⬜ spec pronta para execução

## Contexto

Task irmã da família FP8 (`b19.11-a64-fp8.md`), registrada no `## Resultado` daquela task e na
varredura de fechamento `b19.9-a64-fechamento.md`, seção "Varredura do que sobrou" (2026-09-10).

Diferente da **B19.11b** (`FEAT_FP8FMA`, feature nova), esta task tem a curadoria de versão JÁ
CORRETA: `Aarch64Feature.FP8_DOT_PRODUCT_2WAY` já existe (declarada em `ARMV9_5_A`,
`arch64/Aarch64Feature.java:147`) e `IsaCoverageReport` já tem
`require(Aarch64Feature.FP8_DOT_PRODUCT_2WAY, "FDOT_hb_v", "FDOT_hb_vi")`
(`core/src/test/java/.../tools/IsaCoverageReport.java:353`) — escrito ANTES da B19.11 rodar. Falta
só **decode + executor**.

### As 2 linhas

| Linha | `a64.decode` | O que faz |
|---|---|---|
| `FDOT_hb_v` | `:1227` | produto escalar FP8 de 2 vias, acumula em meia precisão, vetorial |
| `FDOT_hb_vi` | `:1357` | idem, indexada |

```
1227:FDOT_hb_v       0.00 1110 010 ..... 11111 1 ..... .....  @qrrr_h
1357:FDOT_hb_vi      0.00 1111 01 .. .... 0000 . 0 ..... .....  @qrrx_h
```

(referência de vizinhança, **fora** do escopo desta spec — é a B19.11d:
`1226:FDOT_sb_v` `0.00 1110 000 ..... 11111 1 ..... .....  @qrrr_s`)

### O que `FDOT_hb` faz

`FDOT_hb Vd.4H, Vn.16B, Vm.16B`: para cada lane de destino (meia precisão), soma **2** produtos de
pares de elementos FP8 de `Vn`/`Vm` (o "2-way" do nome) — análogo estrutural a `SDOT`/`UDOT`
(inteiro, B19.23) e a `USDOT`/`SUDOT` (`FEAT_I8MM`, B19.12), mas os operandos são FP8 (convertidos
para float antes de multiplicar/acumular) em vez de inteiros. O formato de cada FP8 vem de `FPMR`
— **confirmar se `FDOT` usa os MESMOS campos que `F1CVTL`/`F2CVTL` (`F8S1`/`F8S2`, via
`fp8SourceFormat1()`/`fp8SourceFormat2()`, já existentes em `Aarch64Core`) ou campos próprios**; não
assumir sem checar o manual/QEMU — a B19.11 avisou textualmente que a semântica de `FPMR` para as
famílias de acumulação (`FMLAL`/`FDOT`) "não foi pesquisada nesta sessão".

### Achado de decode já feito pela B19.11 (reusar, não repetir)

`FDOT_hb_v` vive no MESMO espaço `bit21=0`/opcode(bits[15:11])=`0b1_1111`/`bit10=1` que
`decodeAdvancedSimdFp8ThreeSame` (`decoder64/Aarch64Decoder`) já usa para `FCVTN_bh`/`FCVTN_bs`
(que vivem no opcode vizinho `0b1_1110`) — discriminado pelo campo `a` (bit23): `0`=`FDOT_hb_v`
(**desta task**), `1`=`FMLAL_hb_v` (B19.11b). A B19.11 já escreveu e testou (como regressão
negativa) que este slot NÃO decodifica hoje — é literalmente onde esta task entra, substituindo a
recusa por implementação real. Bits confirmados a partir de `target/isa-decode/a64.decode`:
`FDOT_hb_v` bits[23:21]=`010` (bit23=0/`a`, bit22=1, bit21=0).

**Leitura obrigatória**: `b19.11-a64-fp8.md` (`## Resultado` inteiro), `b19.11a-a64-fpmr.md`
(modelo de `FPMR`), `b19.11d-a64-fp8-dot-4way.md` (task irmã — **ler as duas juntas antes de
implementar qualquer uma**, ver "Escopo compartilhado" abaixo), `b19.12-a64-i8mm.md`/
`b19.23-a64-dotprod-residual.md` (padrão de produto escalar `SDOT`/`UDOT`/`USDOT` já implementado —
mesma forma de acumulação, só a conversão de entrada muda). No código:
`advsimd/AdvSimdLanes.fp8ToFloat`, `executor64/Ir64VectorFpArithmeticExecutor` (procurar o executor
de `SDOT`/`UDOT` vetorial/indexado como referência de estrutura),
`decoder64/Aarch64Decoder#decodeAdvancedSimdFp8ThreeSame` e `#decodeAdvancedSimdIndexedElement`.

## Objetivo

As 2 linhas decodificam e executam sob `ARMV9_5_A`+ (ou a versão real que introduz
`FEAT_FP8DOT2`), gateadas por `Aarch64Feature.FP8_DOT_PRODUCT_2WAY`, e são recusadas antes.

## Escopo compartilhado com a B19.11d (ler as duas specs antes de começar qualquer uma)

`FDOT_hb` (esta task, 2-way→meia precisão) e `FDOT_sb` (B19.11d, 4-way→precisão simples) são o
MESMO algoritmo de produto escalar FP8 parametrizado só por: (a) quantos elementos por lane somar
(2 ou 4) e (b) a largura de acumulação (f16 ou f32). **Recomendação forte: implementar um único
núcleo genérico (`fp8DotProduct(int elementsPerLane, ...)` ou equivalente) reusado pelas duas
tasks**, em vez de duas implementações paralelas — mesmo espírito de reuso que
`VectorArithmeticWideningByElement` e o núcleo `fpFusedMultiplyAddLong*` da B19.13/B13.20 já
praticam no projeto. Se a B19.11d ainda não estiver feita quando esta rodar, construir o núcleo
genérico aqui e deixar pronto para ela consumir (documentar isso no `## Resultado`); se a B19.11d já
estiver feita, estender o núcleo dela em vez de duplicar.

## Inclui

1. **Decode de `FDOT_hb_v`** no espaço já mapeado pela B19.11 (`decodeAdvancedSimdFp8ThreeSame`),
   substituindo a recusa explícita, sem afrouxar `FCVTN_bh`/`FCVTN_bs`/`FMLAL_hb_v` (B19.11b).
2. **Decode de `FDOT_hb_vi`** no bloco de indexadas (B8.19/B19.5.6/B19.11), `size`/opcode a
   confirmar contra o encoding bruto.
3. **Núcleo de produto escalar FP8** (genérico, ver "Escopo compartilhado"), consumindo
   `AdvSimdLanes.fp8ToFloat` para a conversão de cada elemento e o formato vindo de `FPMR`
   (confirmar campo exato antes de codificar).
4. Testes (ver Aceite).

## Não inclui

- **`FDOT_sb_v`/`FDOT_sb_vi`** (4-way) ⇒ **B19.11d** (mas ver "Escopo compartilhado" — o núcleo pode
  nascer aqui).
- **`FMLAL_hb`/`FMLALL_sb`** (acumulação fundida, feature diferente) ⇒ **B19.11b**.
- **As 6 conversões FP8** — já feitas pela B19.11.
- **Emissão nativa.**

## Passos

1. Confirmar **B19.11 ✅** e **B19.11a ✅**. Ler "Leitura obrigatória" e a spec da B19.11d.
2. Confirmar os campos de `FPMR` que `FDOT` consulta (mesmos de `F1CVTL`/`F2CVTL`, ou próprios).
3. Núcleo genérico de produto escalar FP8 (ou estender o da B19.11d se já existir).
4. Decode das 2 linhas.
5. Corpus golden (`-march=...+fp8dot2` ou nome real da flag); se o assembler não aceitar, montar à
   mão e registrar isso.
6. `mvn -o test` + `install` + G5 + `./gerar-cobertura-isa.sh`.
7. `INDICE.md`, `## Resultado`, commit `B19.11c: …`, `git push`.

## Aceite

- As 2 linhas decodificam sob a versão de `FEAT_FP8DOT2` e seguem `unsupported` abaixo.
- **Produto escalar correto**: pelo menos um caso com valores FP8 conhecidos, resultado conferido à
  mão contra a fórmula `soma(fp8ToFloat(a_i) * fp8ToFloat(b_i))` para os 2 elementos.
- **`FDOT_hb_v` × `FCVTN_bh`/`FMLAL_hb_v`** continuam discrimináveis (regressão negativa das duas
  suítes vizinhas, sem edição).
- `docs/COBERTURA-ISA.md`: as 2 células saem de `·`/`❌` para `✅` na versão correta.
- `mvn -o test` verde + G5 verde nos 5 consumidores.

## Armadilhas

1. **Não assumir os campos de `FPMR`** sem confirmar no manual — pode não ser `F8S1`/`F8S2`.
2. **Duplicar o núcleo com a B19.11d** em vez de generalizar — checar a spec irmã antes de escrever
   qualquer código.
3. **Confundir com `SDOT`/`UDOT` inteiros** (B19.23) — a estrutura de acumulação é análoga, mas a
   conversão de entrada (FP8→float) é obrigatória aqui e ausente lá.

## Não fazer

- Não implementar `FDOT_sb`/`FMLAL`/`FMLALL` nesta task (registrar o núcleo compartilhado, se
  construído aqui, para a task irmã consumir).
- Não assumir campos de `FPMR` sem checar o manual.
- Não adicionar caso nativo em `Ir64NativePolicy`.
