# B17.5 — Inteiro não-predicado: aritmética, lógica, shift, multiply-add, misc e `INDEX` — 34 encodings

**Trilha:** B · **Repo:** arm-jitter (+ revalidação G5) · **Depende de:** B17.4 (✅)
**Épico:** `b17-plano-sve.md` · **Status:** ✅ (2026-09-25)

## Contexto

Segundo degrau de conteúdo. São as operações SVE que **não** têm predicado governante — as mais
parecidas com AdvSIMD que a extensão tem, e por isso as que mais reusam `AdvSimdLanes` (RFC B13.2).

### Recorte normativo (ver a tabela completa em `b17.1-aarch64-feature-sve-sve2.md`)

| Seção de `sve.decode` | Linhas | Enc |
|---|---|---:|
| `### SVE Integer Arithmetic - Unpredicated Group` | 455-460 | 6 |
| `### SVE Logical - Unpredicated Group` | 465-479 | 11 |
| `### SVE Bitwise Shift - Unpredicated Group` | 510-518 | 6 |
| `### SVE Integer Multiply-Add Group` | 445-450 | 4 |
| `### SVE Integer Misc - Unpredicated Group` | 531-539 | 3 |
| `### SVE Index Generation Group` | 484-493 | 4 |
| **Total** | | **34** |

⚠️ **A escada do épico diz "45"** para esta task. A contagem real do arquivo é **34** — ver o
"Achado desta rodada de spec" em `b17.1-...md`: a tabela "O número" do épico soma 931 e agrupa por
`###` ignorando os cabeçalhos `##`/`####`. A partição normativa de B17.1 é a referência.

### Os encodings reais

```
ADD_zzz         00000100 .. 1 ..... 000 000 ..... .....         @rd_rn_rm
SUB_zzz         00000100 .. 1 ..... 000 001 ..... .....         @rd_rn_rm
SQADD_zzz       00000100 .. 1 ..... 000 100 ..... .....         @rd_rn_rm
UQADD_zzz       00000100 .. 1 ..... 000 101 ..... .....         @rd_rn_rm
SQSUB_zzz       00000100 .. 1 ..... 000 110 ..... .....         @rd_rn_rm
UQSUB_zzz       00000100 .. 1 ..... 000 111 ..... .....         @rd_rn_rm
```

```
AND_zzz         00000100 00 1 ..... 001 100 ..... .....         @rd_rn_rm_e0
ORR_zzz         00000100 01 1 ..... 001 100 ..... .....         @rd_rn_rm_e0
EOR_zzz         00000100 10 1 ..... 001 100 ..... .....         @rd_rn_rm_e0
BIC_zzz         00000100 11 1 ..... 001 100 ..... .....         @rd_rn_rm_e0

XAR             00000100 .. 1 ..... 001 101 rm:5  rd:5   &rrri_esz \
                rn=%reg_movprfx esz=%tszimm16_esz imm=%tszimm16_shr

# SVE2 bitwise ternary operations
EOR3            00000100 00 1 ..... 001 110 ..... .....     @rdn_ra_rm_ex esz=0
BSL             00000100 00 1 ..... 001 111 ..... .....     @rdn_ra_rm_ex esz=0
BCAX            00000100 01 1 ..... 001 110 ..... .....     @rdn_ra_rm_ex esz=0
BSL1N           00000100 01 1 ..... 001 111 ..... .....     @rdn_ra_rm_ex esz=0
BSL2N           00000100 10 1 ..... 001 111 ..... .....     @rdn_ra_rm_ex esz=0
NBSL            00000100 11 1 ..... 001 111 ..... .....     @rdn_ra_rm_ex esz=0
```

```
ASR_zzi         00000100 .. 1 ..... 1001 00 ..... .....  @rd_rn_tszimm_shr
LSR_zzi         00000100 .. 1 ..... 1001 01 ..... .....  @rd_rn_tszimm_shr
LSL_zzi         00000100 .. 1 ..... 1001 11 ..... .....  @rd_rn_tszimm_shl
ASR_zzw         00000100 .. 1 ..... 1000 00 ..... .....         @rd_rn_rm
LSR_zzw         00000100 .. 1 ..... 1000 01 ..... .....         @rd_rn_rm
LSL_zzw         00000100 .. 1 ..... 1000 11 ..... .....         @rd_rn_rm
```

```
MLA             00000100 .. 0 ..... 010 ... ..... .....   @rda_pg_rn_rm
MLS             00000100 .. 0 ..... 011 ... ..... .....   @rda_pg_rn_rm
MLA             00000100 .. 0 ..... 110 ... ..... .....   @rdn_pg_ra_rm # MAD
MLS             00000100 .. 0 ..... 111 ... ..... .....   @rdn_pg_ra_rm # MSB
```

```
MOVPRFX         00000100 00 1 00000 101111 rn:5 rd:5
FEXPA           00000100 .. 1 00000 101110 ..... .....          @rd_rn
FTSSEL          00000100 .. 1 ..... 101100 ..... .....          @rd_rn_rm
```

```
INDEX_ii        00000100 esz:2 1 imm2:s5 010000 imm1:s5 rd:5
INDEX_ir        00000100 esz:2 1 rm:5 010010 imm:s5 rd:5
INDEX_ri        00000100 esz:2 1 imm:s5 010001 rn:5 rd:5
INDEX_rr        00000100 .. 1 ..... 010011 ..... .....          @rd_rn_rm
```

### Achados desta rodada de spec

1. **`### SVE Integer Multiply-Add Group` é PREDICADO**, apesar do nome do grupo e de estar nesta
   task pela partição do épico. Os formatos são `@rda_pg_rn_rm`/`@rdn_pg_ra_rm` — têm `pg:3`. O
   rótulo "não-predicado" da escada refere-se ao BLOCO, não a estas 4 linhas. Mantidas aqui (a
   partição tem que ser exaustiva e disjunta), mas a sessão de execução precisa do mecanismo de
   predicado da B17.4 — que é exatamente por que esta task **depende de B17.4**.
2. **`MOVPRFX` não é uma instrução comum: é um PREFIXO.** `%reg_movprfx 0:5` aparece em dezenas de
   formatos (`@rdn_rm`, `@rda_rn_rm`, `XAR`, …) — o decodetree modela "operação destrutiva cujo
   `rn` é o próprio `rd`". `MOVPRFX` (aqui) e `MOVPRFX_z`/`MOVPRFX_m` (B17.7) transformam a
   instrução SEGUINTE em construtiva. Ver Armadilha 1: é a armadilha estrutural do épico inteiro.
3. **`XAR`, `EOR3`, `BSL`, `BCAX`, `BSL1N`, `BSL2N`, `NBSL` são SVE2**, não SVE — 7 das 11 linhas
   do grupo lógico. Precisam do gate `Aarch64Feature.SVE2`, não `SVE` (G8 aplicado a feature: sob
   um preset com SVE e sem SVE2, essas 7 têm que ser recusadas).
4. **`INDEX` é a instrução que materializa a VLA**: produz `[start, start+inc, start+2*inc, …]` com
   tantos elementos quantos o `VL` permitir. O QEMU chama `do_index`/`gen_helper_sve_index_*` com
   start e incremento como operandos de 64 bits. É o melhor teste único de VLA da task.
5. **`FEXPA`/`FTSSEL` são auxiliares de FUNÇÃO TRANSCENDENTAL** (expoente e seleção de coeficiente
   trigonométrico) — tabelas fixas do manual, não aritmética derivável. Precisam da tabela real
   (o QEMU tem em `sve_helper.c`, `helper_sve_fexpa_*`). `esz != 0` em ambas.

## Objetivo

As 34 células não-predicadas decodificam e executam corretamente em qualquer `VL`, reusando o
núcleo vetorial compartilhado onde a operação já existe (`AdvSimdLanes`).

## Inclui

1. Aritmética não-predicada (6): `ADD`/`SUB` e as 4 saturantes.
2. Lógica não-predicada (11): `AND`/`ORR`/`EOR`/`BIC` (4, SVE) + `XAR`/`EOR3`/`BSL`/`BCAX`/
   `BSL1N`/`BSL2N`/`NBSL` (7, **gate `SVE2`**).
3. Shift não-predicado (6): por imediato (`tszimm`) e por elementos largos (`_zzw`, `esz != 3`).
4. Multiply-add (4): `MLA`/`MLS` nas formas "escreve o acumulador" e "escreve o multiplicando"
   (`MAD`/`MSB`), **predicadas**.
5. Misc (3): `MOVPRFX` (unpredicated), `FEXPA`, `FTSSEL`.
6. `INDEX` (4): `ii`, `ir`, `ri`, `rr`.
7. Testes (ver Aceite).

## Não inclui

- **`MOVPRFX_z`/`MOVPRFX_m`** (predicados) — vivem em `### SVE Integer Reduction Group`, B17.7.
- Aritmética/shift **predicados** — B17.6.
- Imediatos (`ADD_zzi`, `DUPM`, `ORR_zzi`) — B17.8.
- Multiply **indexado** — B17.8.
- **Emissão nativa/Truffle** — padrão da trilha B.

## Especificação

### Leitura obrigatória antes de codar

- `target/isa-decode/sve.decode` linhas 442-460, 462-493, 507-518, 528-539, e os formatos
  `@rd_rn_rm`, `@rd_rn_rm_e0`, `@rdn_ra_rm_ex`, `@rd_rn_tszimm_shr|shl`, `@rda_pg_rn_rm`,
  `@rdn_pg_ra_rm` (linhas 122-194), mais `%reg_movprfx`, `%tszimm16_esz|shr|shl`.
- O `## Resultado` de **B17.3** (banco `Z`, `VL`) e **B17.4** (predicado, para as 4 linhas de
  `MLA`/`MLS`).
- `core/src/main/java/dev/vitorsilverio/armjitter/advsimd/AdvSimdLanes.java` — `threeSame`,
  `element`/`setElement`, `shiftImmediate`; e `AdvSimdRegisterWords` (a vista por palavras).
  **Reusar** onde a operação já existe é obrigatório (RFC B13.2/D1: cada operação em UM lugar só).
- `https://raw.githubusercontent.com/qemu/qemu/master/target/arm/tcg/translate-sve.c`
  (`do_index`, `trans_MOVPRFX`, `trans_XAR`, `gen_gvec_fn_zzz`) e `sve_helper.c`
  (`helper_sve_fexpa_*`, `helper_sve_ftssel_*`, `helper_sve_index_*`) — via `WebFetch`.
- ARM DDI 0487 — a tabela de `FEXPA` e as regras de `MOVPRFX` (quais instruções podem segui-lo).

## Passos

1. Ler "Leitura obrigatória"; transcrever a tabela de `FEXPA`.
2. Decidir e documentar a estratégia de `MOVPRFX` (Armadilha 1) **antes** de escrever o decoder.
3. Decoder das 34 linhas, com gate `SVE` vs `SVE2` por linha (Achado 3). Resto → `UNIMPLEMENTED`.
4. Executores, reusando `AdvSimdLanes` onde já existe a operação.
5. Testes; `mvn -o test` + `install` + G5 + `./gerar-cobertura-isa.sh` (zero-diff esperado até a
   B17.26; medir para confirmar).
6. `INDICE.md`, `## Resultado`, commit `B17.5: ...`, `git push`.

## Aceite

- As 34 linhas decodificam sob um preset com `SVE` (as 7 lógicas SVE2 **só** com `SVE2`, e são
  recusadas — `null`, não misdecode — sob SVE puro).
- `INDEX_ii`/`ir`/`ri`/`rr` preenchem **todos** os elementos do `VL` (teste em dois `VL`, é o teste
  de VLA desta task).
- `ASR_zzi`/`LSR_zzi`/`LSL_zzi` derivam `esz` e a contagem do par `tszh:tszl:imm3`
  (`%tszimm16_esz`/`%tszimm16_shr|shl`), não de um campo `size` — teste para os 4 `esz`.
- `_zzw` com `esz == 3` é recusado (comentário do arquivo: "Note esz != 3").
- `FEXPA`/`FTSSEL` com `esz == 0` são recusados; os valores batem com a tabela do manual (vetor de
  teste transcrito, não calculado).
- `MLA`/`MLS`/`MAD`/`MSB` respeitam o predicado (elemento inativo não é escrito).
- `MOVPRFX` seguido de uma operação destrutiva produz o mesmo resultado da forma construtiva —
  teste explícito de par de instruções (não só decode isolado).
- `mvn -o test` verde + G5 verde nos 5 consumidores.

## Armadilhas

1. **`MOVPRFX` é a armadilha estrutural do épico.** Ele não é uma operação: é um prefixo que
   transforma a instrução SEGUINTE de destrutiva em construtiva, e a arquitetura restringe QUAIS
   instruções podem segui-lo (um par inválido é UNPREDICTABLE). Duas estratégias possíveis:
   (a) executar como um `MOV Zd, Zn` comum e deixar a instrução seguinte sobrescrever — funciona
   para a maioria dos casos e é o que o QEMU faz na prática; (b) modelar o estado de prefixo.
   **Decidir na sessão, medindo contra o QEMU, e documentar no `## Resultado`.** O que NÃO pode
   acontecer é o lifter de bloco (`StandardIrBlockLifter`) tratar `MOVPRFX` como terminal ou
   reordenar por cima dele — mesmo cuidado que a B15.2 teve com `isTerminal`.
2. **O `.` no campo `esz` nem sempre é `esz`.** `AND_zzz`/`ORR_zzz`/`EOR_zzz`/`BIC_zzz` usam
   `@rd_rn_rm_e0` com os bits 23:22 LITERAIS (`00`/`01`/`10`/`11`) selecionando a OPERAÇÃO, e
   `esz=0` forçado. Ler o padrão, não o formato.
3. **`XAR` tem layout próprio** (`rm:5 rd:5` explícitos, `rn=%reg_movprfx`, `esz`/`imm` derivados de
   `%tszimm16_*`) — não cabe no formato comum. Decodificar campo a campo.
4. **As 7 lógicas ternárias SVE2 têm `esz=0` fixo** mas operam sobre o vetor inteiro (são
   bit-a-bit). Não confundir "esz=0" com "só o byte baixo".
5. **`ADD_zzz` etc. reusam `AdvSimdLanes.threeSame`, mas com `lanes` derivado do `VL`** — o
   parâmetro `lanes` das APIs de `AdvSimdLanes` hoje vale 8/16 (128 bits). Passar `VL/esize` é o
   ponto em que a VLA entra; conferir se `AdvSimdLanes` aceita valores maiores sem suposição
   interna de 128 bits (**medir**, não supor — se não aceitar, é achado a documentar).
6. **`INDEX_ii` tem DOIS imediatos com sinal** (`imm1:s5` = start em bits 9:5, `imm2:s5` = incremento
   em bits 20:16) — a ordem no padrão é `imm2` antes de `imm1`, fácil de trocar.

## Não fazer

- Não implementar `MOVPRFX_z`/`MOVPRFX_m` (B17.7) nem nenhuma forma predicada de aritmética (B17.6).
- Não implementar multiply indexado nem imediatos (B17.8).
- Não decodificar as 7 lógicas SVE2 sob um preset que só tem `SVE`.
- Não duplicar operação que `AdvSimdLanes` já implementa.

## Resultado

**Fechada em 2026-09-25.** Os 34 encodings decodificam e executam, sem nenhuma pendência de encoding. `docs/COBERTURA-ISA.md`
**inalterada** (zero-diff medido: 23523/23523; `sve.decode` segue `NOT_IN_ANY_PRESET` até a B17.26); `docs/COBERTURA-JIT.md`
ganhou o `Kind` novo (157 → 158).

**O que foi feito**

- `decoder64/Aarch64SveDecoder`: o prefixo `0x04` passa a tentar primeiro o grupo inteiro sem predicado
  (`decodeIntegerUnpredicated`) e só depois a contagem de elementos da B17.4. Palavras conferidas contra
  `aarch64-none-elf-as -march=armv9.4-a+sve2+sve2p1`. `XAR`/`EOR3`/`BCAX`/`BSL`/`BSL1N`/`BSL2N`/`NBSL` exigem
  `Aarch64Feature.SVE2` (sob SVE puro devolvem `null` → recusa, G8); o resto exige só `SVE`.
- `ir64/Ir64Op.SveIntegerUnpredicated` (Kind 157): UM record com um `enum Op` de 34 valores (uma por linha do `.decode`).
- `executor64/SveIntegerOps` (novo): elementos via `SvePredicateOps.elementOf/setElementOf`, sempre no `VL` efetivo.
  A lógica bit-a-bit opera por palavra de 64 bits sobre o vetor inteiro (Armadilha 4); `esz` só conta nas demais.

**Decisões**

- **`MOVPRFX` = estratégia (a)**: executado como `MOV Zd, Zn` (é o que o `trans_MOVPRFX` do QEMU faz: `do_mov_z`). Não há estado de
  prefixo; o par `movprfx`+operação destrutiva dá o mesmo resultado da forma construtiva (teste de par de instruções). O lifter
  não trata `MOVPRFX` como terminal (é um `Kind` comum).
- `FEXPA`/`FTSSEL`: tabelas colidas verbatim do `sve_helper.c` (revisão fixada de `gerar-cobertura-isa.sh`, via `curl` +
  leitura do arquivo — não `WebFetch`); ilegais em streaming (`TRANS_FEAT_NONSTREAMING`), via `requireNonStreaming`.

**Achados que a spec não trazia**

1. `XAR` e `EOR3`/`BCAX`/`BSL*` têm campos diferentes do "formato comum": em `XAR` o `tszimm` está em 23:22+20:16 e `Zm` em 9:5;
   nas ternárias `Zm` está em 20:16 e `Zk` em 9:5 — e bits 23:22 escolhem a operação (não `esz`). `EOR3`/`BCAX` só usam os
   seletores `00`/`01` (`10`/`11` não alocados).
2. `tszimm_esz` = posição do bit mais alto de `tsz`; `tsz = 0` é não alocado nas três formas de shift por imediato e no `XAR`.
   Contagem de `ASR`/`LSR`/`XAR` = `(16 << esz) - tszimm`, de `LSL` = `tszimm - (8 << esz)`. `ASR` por `esize` clampa em `esize-1`; `LSR`
   por `esize` zera.
3. `_zzw`: amount vem do doubleword de `Zm` que CONTÉM o elemento; um doubleword com bit 63 ligado é um shift enorme (sem sinal).
4. Um teste da B17.4 usava `0x04200000` como "prefixo 0x04 fora do recorte" — agora é `add z0.b, z0.b, z0.b` legítimo; trocado
   por um buraco real (opcode `000010`, `0x04200800`).

**Pendências nomeadas (nada excluído)**

- **`FPCR.AH` (FEAT_AFP)** não é modelado no core: `FTSSEL` implementa o caso `AH = 0` (inversão de sinal simples).
- `FEXPA` em streaming com `FEAT_SSVE_FEXPA` (feature ainda sem constante em `Aarch64Feature`): hoje é sempre ilegal em streaming.
- Gate SME-only (`aa64_sme_or_sve`/`aa64_sme_or_sve2`): como na B17.4, o decoder exige `FEAT_SVE`/`FEAT_SVE2`; entra com o preset SME sem SVE.

**Testes** (`Aarch64SveIntegerTest`, 163 casos, `VL` 256 **e** 512): decode das 34 linhas + recusa das 7 SVE2 sob SVE puro + 18
encodings não alocados; aritmética (6 operações × 4 `esz`, oráculo `BigInteger`, dados enviesados para os extremos) e limites de
saturação; lógica e ternárias contra a fórmula do manual; `XAR` bit-a-bit; shifts por imediato (13 palavras, incl. shift por
`esize`) e largos (amounts 0…2^63); `MLA`/`MLS`/`MAD`/`MSB` com predicado e bits-lixo nos bytes não-baixos; par `MOVPRFX`+`BSL`;
`FEXPA` contra vetores transcritos das tabelas; `FTSSEL` (`m` 0-7 × 3 `esz`); `INDEX` nas 4 formas (preenche todo o `VL`,
truncamento por `esz`, `XZR`); `VL` efetivo < implementado; trap `EC=0x19` em 10 palavras; `SVL` em streaming; `FEXPA`/`FTSSEL`
`UNDEFINED` em streaming; bloco pelo `StandardIr64BlockLifter`. Mutação de sanidade (BSL1N, sentido do `XAR`, clamp do `ASR`, bit
do `FTSSEL`, troca de `imm1`/`imm2` do `INDEX`): 19 falhas.

**Cobertura (JaCoCo):** `SveIntegerOps`, `Aarch64SveDecoder` e `Ir64Op.SveIntegerUnpredicated`: **100% de linhas e branches**
(1ª medição: 1 linha/1 branch — um `if` de guarda inalcançável, removido).

**Validação:** `mvn -o install` do arm-jitter 5578 + 74 testes, 0 falhas; `./gerar-cobertura-isa.sh` sem diff;
`./gerar-cobertura-jit.sh` regenerado. G5 (após `mvn install`): gbaemu (240, 17 ignorados de sempre), ndsemu (183), armbox (47), virtual-arm-box (87, 5 ignorados de sempre), n3dsemu (221) BUILD SUCCESS.
