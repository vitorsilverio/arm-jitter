# B3.7 — Preset `ARMV7A` + validação armbox com binário gcc hard-float

**Trilha:** B (épico B3, fechamento) · **Depende de:** B3.1–B3.6 · **Repos:** arm-jitter + armbox

## Contexto

Todas as peças do ARMv7-A user-level existem; falta o preset público e a prova
objetiva com binário real (nível 2 e 3 da matriz `docs/VALIDACAO-ARQUITETURAS.md`).
Padrão já estabelecido por B4.0.1 (ARMv6K) e B4.0.2 (Thumb-2): torture test `.s`
auto-verificável + gêmeo `-broken` + binário de compilador real, nos 3 backends.

## Inclui — arm-jitter

Preset novo em `ArmArchitecture`:

```java
public static final ArmArchitecture ARMV7A = extending(ARMV6K_THUMB2, "ARMv7-A",
        ArmFeature.MOVW_MOVT, ArmFeature.MLS_MULTIPLY, ArmFeature.BIT_FIELD,
        ArmFeature.BIT_REVERSE, ArmFeature.DIVIDE, ArmFeature.MEMORY_BARRIERS,
        ArmFeature.VFPV2)
        .withDecoderExtensions(List.of(new VfpDecoder(), new CoprocessorDecoder(),
                new ArmV7MediaDecoder()))
        .withThumb32DecoderExtensions(List.of(/* as 4 de ARMV6K_THUMB2 + */
                new VfpDecoder(), new Thumb2MultiplyDivideDecoder()));
```

Atenção: `extending` HERDA as listas da base, mas `withDecoderExtensions` SUBSTITUI —
monte a lista completa explicitamente (VfpDecoder ANTES de CoprocessorDecoder, regra
da B3.5). Javadoc: user-level only (sem MMU/CP15-VMSA — B4.1), NEON fora, SDIV/UDIV
habilitados (nota v7VE da decisão nº 5 do épico).

## Inclui — armbox (repo `C:\Users\user\IdeaProjects\armbox`)

1. `--arch=armv7a` no `Main`/`Armbox` (plumbing idêntico ao `--arch=thumb2` de
   B4.0.2 — mesmos 2 pontos: `JitRuntimeFactory` + `ArmCore`).
2. `testdata/armv7a-torture.s` escrito à mão (padrão do `armv6k-torture.s`:
   auto-verificável, exit-code único por checagem, `.ltorg` fora do fluxo):
   MOVW/MOVT, UBFX/SBFX/BFI/BFC, RBIT, MLS, SDIV/UDIV (incl. x/0 e MIN/−1), DMB,
   e a seção VFP: VLDR/VSTR/VADD/VMUL/VDIV/VSQRT/VCMP+VMRS APSR_nzcv/VCVT/
   VPUSH/VPOP/VMOV imm/core↔S↔D — cada uma comparando bits contra valor esperado
   pré-calculado (use os mesmos vetores dos testes de B3.4). Gêmeo `-broken`.
3. `testdata/hello-float.c` compilado
   `arm-none-eabi-gcc -march=armv7-a -mfpu=vfp -mfloat-abi=hard -O2 -static`
   (specs nosys ou a libc mínima que `hello.elf` atual usa — copiar a receita do
   `build-testdata.ps1`): calcula algo com double (ex. série de Leibniz 6 termos) e
   imprime o resultado com casas fixas via write() — stdout determinístico.
4. `Armv7TortureTest` espelhando `Thumb2TortureTest`: torture + hello-float nos 3
   backends (JIT/`--interp`/`--check`), stdout/exit idênticos; `-broken` exit ≠ 0.

## Aceite (fecha o épico B3)

- Torture ARMv7-A exit 0 nos 3 backends; `-broken` detecta.
- `hello-float` (gcc hard-float real) stdout idêntico nos 3 backends.
- `mvn -o test` raiz + gbaemu/ndsemu verdes.
- Atualizar: tabela "Arquiteturas e features" do `README.md` do arm-jitter, linha
  ARMv7-A do `ROADMAP.md` (mapa dispositivo→core), matriz
  `docs/VALIDACAO-ARQUITETURAS.md`, índice `tasks/README.md`.

## Armadilhas

- Se o gcc da toolchain emitir NEON mesmo com `-mfpu=vfp` (memcpy vetorizado de
  libc pré-compilada), o binário cai em UNDEFINED — use `-static` com a MESMA libc
  dos testdata atuais e, se aparecer NEON, troque o workload para evitar a rotina
  (registrar no PR qual símbolo) — NÃO implemente NEON de improviso.
- printf de float via libc completa puxa MUITO código — preferir formatação manual
  (multiplicar por 10^k e imprimir inteiro) se a libc dos testdata for a nosys.
- O `--check` (divergence) com FP só é confiável depois de B3.6 (CpuSnapshot já
  compara o banco — B3.3); se divergir, o bug é REAL, não "ruído de NaN": bits são
  determinísticos nos dois backends por construção.
