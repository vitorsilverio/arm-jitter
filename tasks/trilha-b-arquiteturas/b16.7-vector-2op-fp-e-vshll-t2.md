# B16.7 — MVE: vector 2-op de ponto flutuante, `VSHLL` T2 e as formas sobrepostas (narrowing move, `VMULH`, complexos) — 58 encodings

**Trilha:** B · **Repo:** arm-jitter (+ revalidação G5) · **Depende de:** B16.6 (⬜)
**Épico:** `b16-plano-mve-helium.md` · **Status:** ⬜ (spec escrita 2026-09-13, ainda não executada)

## Contexto

Sétimo degrau da escada B16 (partição normativa na spec da **B16.1**). O épico estimou "~47 (resto do
bloco de 92)"; a medição desta rodada fecha em **58**, porque as 14 linhas da seção
`# 2-operand FP` (linhas 772-789) — que o épico havia contado dentro do grupo "Comparações (45)" por
efeito do corte contíguo — pertencem factualmente aqui.

**Esta é a maior task da escada junto com a B16.13.** Ela tem três sub-famílias independentes e é
legítimo, na sessão de execução, quebrá-la em B16.7a/B16.7b se o orçamento não couber (sequenciamento,
nunca exclusão — ver "Regra máxima do projeto" em `tasks/README.md`); o critério de corte natural
está em "Inclui".

### Sub-família 1 — os quatro blocos `{}` sobrepostos (linhas 223-279, 30 linhas)

O comentário do arquivo explica por que `VSHLL` T2 mora aqui e não com os deslocamentos por imediato
(B16.10):

```
# The VSHLL T2 encoding is not a @2op pattern, but is here because it
# overlaps what would be size=0b11 VMULH/VRMULH
{
  VCVTB_SH       111 0 1110 0 . 11 1111 ... 0 1110 0 0 . 0 ... 1 @1op_nosz
  VMAXNMA        111 0 1110 0 . 11 1111 ... 0 1110 1 0 . 0 ... 1 @vmaxnma size=2
  VSHLL_BS       111 0 1110 0 . 11 .. 01 ... 0 1110 0 0 . 0 ... 1 @2_shll_esize_b
  VSHLL_BS       111 0 1110 0 . 11 .. 01 ... 0 1110 0 0 . 0 ... 1 @2_shll_esize_h
  VQMOVUNB       111 0 1110 0 . 11 .. 01 ... 0 1110 1 0 . 0 ... 1 @1op
  VQMOVN_BS      111 0 1110 0 . 11 .. 11 ... 0 1110 0 0 . 0 ... 1 @1op
  VMAXA          111 0 1110 0 . 11 .. 11 ... 0 1110 1 0 . 0 ... 1 @1op
  VMULH_S        111 0 1110 0 . .. ...1 ... 0 1110 . 0 . 0 ... 1 @2op
}
```
e os três blocos gêmeos (`VCVTB_HS`/`VSHLL_BU`/`VMOVNB`/`VQMOVN_BU`/`VMULH_U`;
`VCVTT_SH`/`VMINNMA`/`VSHLL_TS`/`VQMOVUNT`/`VQMOVN_TS`/`VMINA`/`VRMULH_S`;
`VCVTT_HS`/`VMINNMA`/`VSHLL_TU`/`VMOVNT`/`VQMOVN_TU`/`VRMULH_U`).

Formatos envolvidos (linhas 75-78, 101-105, 143-146):

```
@1op      .... .... .... size:2 .. .... .... .... .... &1op qd=%qd qm=%qm
@1op_nosz .... .... .... .... .... .... .... .... &1op qd=%qd qm=%qm size=0
# VSHLL encoding T2 where shift == esize
@2_shll_esize_b .... .... .... 00 .. .... .... .... .... &2shift qd=%qd qm=%qm size=0 shift=8
@2_shll_esize_h .... .... .... 01 .. .... .... .... .... &2shift qd=%qd qm=%qm size=1 shift=16
# 2-operand, but Qd and Qn share a field. Size is in bit 28, but we don't decode it in this format
@vmaxnma  .... .... .... .... .... .... .... .... &2op qd=%qd qn=%qd qm=%qm
```

### Sub-família 2 — complexos FP e dual-accumulate inteiros (linhas 325-350, 14 linhas)

```
{ VCMUL0    111 . 1110 0 . 11 ... 0 ... 0 1110 . 0 . 0 ... 0 @2op_sz28
  VQDMLADH  1110  1110 0 . .. ... 0 ... 0 1110 . 0 . 0 ... 0 @2op
  VQDMLSDH  1111  1110 0 . .. ... 0 ... 0 1110 . 0 . 0 ... 0 @2op }
{ VCMUL180 / VQDMLADHX / VQDMLSDHX }
{ VCMUL90  / VQRDMLADH / VQRDMLSDH }
{ VCMUL270 / VQRDMLADHX / VQRDMLSDHX }
VQDMULLB  111 . 1110 0 . 11 ... 0 ... 0 1111 . 0 . 0 ... 1 @2op_sz28
VQDMULLT  111 . 1110 0 . 11 ... 0 ... 1 1111 . 0 . 0 ... 1 @2op_sz28
```

com `%size_28 28:1 !function=plus_1` (`@2op_sz28`: tamanho 1 ou 2 vindo do bit 28) — note que nesses
blocos o bit 28 deixa de ser o `U` de signed/unsigned e passa a ser tamanho, o que é justamente por
que as formas convivem num `{}`.

### Sub-família 3 — a seção `# 2-operand FP` (linhas 771-789, 14 linhas)

```
# 2 operand fp insns have size in bit 20: 1 for 16 bit, 0 for 32 bit, like Neon FP insns.
%2op_fp_size 20:1 !function=neon_3same_fp_size
# VCADD is an exception, where bit 20 is 0 for 16 bit and 1 for 32 bit
%2op_fp_size_rev 20:1 !function=plus_1

@2op_fp          .... (…) &2op qd=%qd qn=%qn qm=%qm size=%2op_fp_size
@2op_fp_size_rev .... (…) &2op qd=%qd qn=%qn qm=%qm size=%2op_fp_size_rev

VADD_fp      1110 1111 0 . 0 . ... 0 ... 0 1101 . 1 . 0 ... 0 @2op_fp
VSUB_fp      1110 1111 0 . 1 . ... 0 ... 0 1101 . 1 . 0 ... 0 @2op_fp
VMUL_fp      1111 1111 0 . 0 . ... 0 ... 0 1101 . 1 . 1 ... 0 @2op_fp
VABD_fp      1111 1111 0 . 1 . ... 0 ... 0 1101 . 1 . 0 ... 0 @2op_fp
VMAXNM       1111 1111 0 . 0 . ... 0 ... 0 1111 . 1 . 1 ... 0 @2op_fp
VMINNM       1111 1111 0 . 1 . ... 0 ... 0 1111 . 1 . 1 ... 0 @2op_fp
VCADD90_fp   1111 1100 1 . 0 . ... 0 ... 0 1000 . 1 . 0 ... 0 @2op_fp_size_rev
VCADD270_fp  1111 1101 1 . 0 . ... 0 ... 0 1000 . 1 . 0 ... 0 @2op_fp_size_rev
VFMA         1110 1111 0 . 0 . ... 0 ... 0 1100 . 1 . 1 ... 0 @2op_fp
VFMS         1110 1111 0 . 1 . ... 0 ... 0 1100 . 1 . 1 ... 0 @2op_fp
VCMLA0       1111 110 00 . 1 . ... 0 ... 0 1000 . 1 . 0 ... 0 @2op_fp_size_rev
VCMLA90      1111 110 01 . 1 . ... 0 ... 0 1000 . 1 . 0 ... 0 @2op_fp_size_rev
VCMLA180     1111 110 10 . 1 . ... 0 ... 0 1000 . 1 . 0 ... 0 @2op_fp_size_rev
VCMLA270     1111 110 11 . 1 . ... 0 ... 0 1000 . 1 . 0 ... 0 @2op_fp_size_rev
```

Total: 30 + 14 + 14 = **58**.

### Gate de feature: `MVE_FLOAT`, não `MVE_INTEGER`

As formas FP desta task (`VADD_fp`…`VCMLA270`, `VCMUL*`, `VMAXNMA`/`VMINNMA`, `VCVTB_SH`/`VCVTT_SH`/
`VCVTB_HS`/`VCVTT_HS`) exigem `FEAT_MVE_FP`. A **B16.1** criou `ArmFeature.MVE_FLOAT` exatamente
para isso. As formas inteiras da sub-família 1/2 (`VSHLL_*` T2, `VQMOVN*`/`VMOVN*`, `VMAXA`/`VMINA`,
`VMULH`/`VRMULH`, `VQDML*DH*`, `VQDMULL*`) ficam em `MVE_INTEGER`. **Isso é gate por linha, não por
task** — e é a armadilha nº 1.

## Objetivo

As 58 linhas decodificam sob o gate correto (`MVE_INTEGER` ou `MVE_FLOAT`, por linha) e executam com
predicação por elemento, reusando o núcleo `AdvSimdLanes` (que já tem `fpThreeSame`,
`fpComplexAdd`, `fpComplexMultiplyAccumulate`, `maxNum`/`minNum`, `halfBits`/`halfToFloat`,
`shiftWidenImmediate`, `narrow`). 58 células `·`/`❌` → `✅`.

## Inclui

1. **Decode das 58 linhas**, com atenção à prioridade dentro de cada `{}` (padrão mais específico
   primeiro: `VCVTB_SH` tem `bits[19:16]=1111` fixo, `VSHLL_*` tem `bits[17:16]=01`, `VMULH_*` tem
   `size` livre — o catch-all do bloco).
2. **Corte sugerido se a task for quebrada** (sequenciamento, documentar se usado):
   **B16.7a** = sub-famílias 1 e 2 (44 linhas, predominantemente inteiras + 8 `VCVT` de meia
   precisão + 4 `VMAXNMA`/`VMINNMA` + 4 `VCMUL`); **B16.7b** = sub-família 3 (14 linhas, FP puro).
3. **Auditoria de reuso do núcleo** (mesma disciplina da B16.6): para cada linha, dizer se a
   operação já existe em `AdvSimdLanes`/`AdvSimdFpThreeSameOp`/`AdvSimdShiftWidenOp`/
   `AdvSimdNarrowOp` ou se precisa de entrada nova. Candidatos a "não existe":
   `VMAXNMA`/`VMINNMA` (max/min do VALOR ABSOLUTO acumulando em `Qd`, com `qn=%qd` — ver o formato
   `@vmaxnma`), `VMAXA`/`VMINA` (idem, inteiro), `VQMOVUNB`/`VQMOVUNT` (narrow saturante de signed
   para unsigned), `VQDMLADH`/`VQDMLSDH` e variantes `X`/`R` (dual-accumulate com halving e troca de
   metades), `VCMUL*` (complex multiply FP sem acumulação).
4. **Predicação + `FPSCR.QC`** nas formas saturantes (`VQMOVN*`/`VQMOVUN*`/`VQDML*`/`VQDMULL*`).
5. **Meia precisão (`fp16`) real**: `%2op_fp_size` mapeia bit 20 para 16/32 bits. O núcleo tem
   `halfBits`/`halfToFloat`; conferir se o arredondamento/denormais casa com o que o `FPSCR` do
   projeto modela (B3.3/B3.8) — se não casar, documentar como pendência, **não** aproximar.
6. Testes (ver Aceite).

## Não inclui

- As 48 linhas inteiras de 2-op (**B16.6**, dependência desta task).
- `VSHLL` **T1** (linhas 636-646) — é **B16.10**; só a forma **T2** (`shift == esize`, i.e.
  `@2_shll_esize_b`/`_h`) é desta task. Os dois grupos têm o mesmo mnemônico e encodings
  completamente diferentes (Armadilha 3).
- `VCVT` de/para inteiro e ponto fixo, `VRINT*` — **B16.12**. Só as 8 linhas
  `VCVTB_SH`/`VCVTB_HS`/`VCVTT_SH`/`VCVTT_HS` (conversão meia↔simples "bottom/top") são desta task,
  porque vivem nos blocos `{}` desta seção.
- Comparações (B16.8), escalares (B16.9), estreitamento por imediato (B16.11), 1-op/reduções
  (B16.13).
- Emissão nativa/Truffle.

## Especificação

### Leitura obrigatória antes de codar

- `target/isa-decode/mve.decode`, linhas 26-35, 75-105, 137-153, 220-350 e 771-789.
- `tasks/trilha-b-arquiteturas/b16.1-...md` (partição + Armadilha 6), `b16.2-...md`
  (`## Resultado`: predicação) e `b16.6-...md` (`## Resultado`: inventário de reuso do núcleo — esta
  task continua o mesmo inventário).
- `core/src/main/java/dev/vitorsilverio/armjitter/advsimd/`: `AdvSimdLanes.java`
  (`fpThreeSame`, `fpComplexAdd`, `fpComplexMultiplyAccumulate`, `maxNum`/`minNum`, `halfBits`/
  `halfToFloat`, `shiftWidenImmediate`, `narrow`, `saturateToInteger`), `AdvSimdFpThreeSameOp.java`,
  `AdvSimdShiftWidenOp.java`, `AdvSimdNarrowOp.java`, `AdvSimdFpConvertPrecisionOp.java`.
- `core/src/main/java/dev/vitorsilverio/armjitter/core/FpscrRegister.java` — `QC`, `RMODE`, `FZ`.
- QEMU real via `WebFetch`: `target/arm/tcg/translate-mve.c` (`do_2op_fp`, `do_vfp_2op_fp`,
  `trans_VSHLL_BS`/`_BU`/`_TS`/`_TU` e `do_2shift`, `trans_VCVTB_SH` e cia., `trans_VMAXNMA`,
  `do_vcmul`/`do_vcmla`) e `target/arm/tcg/mve_helper.c` (`DO_2OP_FP`, `DO_VCMUL`, `DO_VCMLA`,
  `DO_VQDMLADH`, `DO_VSHLL`, `DO_VQMOVN`, `DO_VMAXNMA`).

## Passos

1. Ler "Leitura obrigatória"; auditar o núcleo contra as 58 e decidir (documentando) se a task vai
   inteira ou em 7a/7b.
2. Implementar o decode respeitando a prioridade dos `{}` e o gate por linha
   (`MVE_INTEGER`/`MVE_FLOAT`).
3. Acrescentar ao núcleo só o que faltar.
4. Ligar ao caminho predicado da B16.2.
5. Testes (ver Aceite).
6. `mvn -o test` + `install` + G5 + `./gerar-cobertura-isa.sh` com delta medido (exatamente 58
   células, ou o subconjunto da metade entregue; **nenhuma célula de B16.6/B16.10/B16.11/B16.12 pode
   mudar**).
7. `INDICE.md`, `## Resultado`, commit `B16.7: ...`, `git push`.

## Aceite

- As 58 linhas decodificam sob o gate correto: um preset com `MVE_INTEGER` **sem** `MVE_FLOAT`
  decodifica as inteiras e **recusa** as FP (teste explícito — é a razão de `MVE_FLOAT` existir).
- Prioridade dos `{}` correta: `VCVTB_SH` (bits[19:16]=1111) não é decodificado como `VSHLL_BS`, e
  `VMULH_S` (o catch-all) só pega o que sobra.
- `@vmaxnma` com `qn = qd`: `VMAXNMA Qd, Qm` acumula em `Qd` (teste de que o operando `Qn` NÃO vem
  do campo `%qn`).
- `@2op_fp_size` vs `@2op_fp_size_rev`: `VADD_fp` com bit20=1 é 16 bits, `VCADD90_fp` com bit20=0 é
  16 bits — o inverso. Teste explícito dos dois (o comentário do arquivo existe porque isso já
  confundiu alguém).
- `VSHLL` T2 desloca por `esize` (8 ou 16) **fixo**, não por um campo de imediato.
- Predicação e `FPSCR.QC` só para lanes ativas nas formas saturantes.
- As asserções pré-existentes de `AdvSimdLanes` (B8.x/B13.2) continuam passando (G1).
- `docs/COBERTURA-ISA.md` com o delta exato; `mvn -o test` verde; G5 verde.

## Armadilhas

1. **Gate por LINHA, não por task.** Misturar `MVE_INTEGER`/`MVE_FLOAT` num gate único faria um
   Cortex-M com Helium inteiro-apenas executar FP vetorial — falso positivo de arquitetura, o mesmo
   tipo de erro que a B12.4 recusou cometer com `Cortex-M3`/`ARMV7M`.
2. **`bit28` muda de significado entre formas do MESMO bloco `{}`**: é `U` (signed/unsigned) em
   `VMULH_S`/`VMULH_U`, e é TAMANHO em `@2op_sz28` (`%size_28 28:1 !function=plus_1`) em
   `VCMUL*`/`VQDMULL*`. Extrair `U` cru e usá-lo para os dois é misdecode silencioso.
3. **`VSHLL` T1 (B16.10) e T2 (esta task) são encodings diferentes com o mesmo mnemônico.** O
   comentário do arquivo é explícito em dois lugares ("the T2 VSHLL encoding is elsewhere in this
   file" / "The VSHLL T2 encoding is not a @2op pattern, but is here because it overlaps what would
   be size=0b11 VMULH/VRMULH"). Se o decoder desta task reivindicar o espaço T1, as 8 células da
   B16.10 saltam para `✅` erradas — medir o delta.
4. **`Thumb2NocpDecoder` e `Thumb2VfpDecoder`**: `VCADD90_fp`/`VCADD270_fp`/`VCMLA*` são
   `1111 110x` — o espaço "extension register load/store" do VFP, que o `VfpDecoder` reivindica
   **sem checar `bits[31:28]`** (bug G8 achado pela B14). Esta é a sub-família MVE com maior risco
   real de ser roubada pelo VFP. Verificar a lista de decoders do preset e, se a colisão for real,
   **reportar** (é candidata a correção na B14), não contornar.
5. **`VQDMLADH`/`VQDMLSDH` e variantes `X`/`R` são "dual accumulate" com troca de metades** —
   semântica cheia de detalhe (halving, rounding, exchange). Transcrever `DO_VQDMLADH` do
   `mve_helper.c`; não derivar de `SQDMULH`.
6. **`fp16`**: o `FPSCR` deste projeto foi modelado para VFP escalar (B3.3/B3.8). Se
   `RMODE`/`FZ`/`DN` não se aplicarem igual em MVE FP, isso é pendência a documentar, não a
   aproximar (G8 aplicado a semântica numérica).
7. **G4** e a regra de `QC` só para lanes ativas.

## Não fazer

- Não implementar `VSHLL` T1 (B16.10) nem `VCVT`/`VRINT` das linhas 798-832 (B16.12).
- Não usar um gate único de feature para inteiro e FP.
- Não derivar semântica de `VQDML*DH*`/`VCMUL*` por analogia; transcrever.
- Não tocar `VfpDecoder` (Armadilha 4).
- Não duplicar operação já presente em `AdvSimdLanes`.

## Resultado

_(preencher ao concluir a task)_
