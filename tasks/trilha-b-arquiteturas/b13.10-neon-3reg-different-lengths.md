# B13.10 — NEON 3-reg-different-lengths (A32): alargamento e estreitamento — 26 linhas

**Trilha:** B · **Repo:** arm-jitter (+ revalidação G5) · **Depende de:** B13.4, B13.8
**Épico:** `b13-plano-neon-a32.md` · **Status:** ⬜

## Contexto

Primeira task do **terceiro frame** do épico B13. Os dois primeiros já estão fechados:

| Frame | Bits | Seção | Estado |
|---|---|---|---|
| `(raw & 0xFE80_0000) == 0xF200_0000` | bit23=**0** | "3-reg-same" | ✅ B13.4/B13.5/B13.6 |
| `(raw & 0xFE80_0010) == 0xF280_0010` | bit23=1, bit4=**1** | "2-reg-and-shift" + `Vimm_1r` | ✅ B13.7/B13.8 (+ B13.9) |
| **bit23=1, bit4=`0`** | | "two registers, or three registers of different lengths" | **B13.10-B13.14** |

O comentário do `neon-dp.decode` (`:387-398`) descreve a estrutura interna deste frame, e é a chave
para dividir as tasks:

> *Within the "two registers, or three registers of different lengths" grouping (`[23,4]=0b10`), bits
> [21:20] are either part of the opcode decode: `0b11` for VEXT, two-reg-misc, VTBL, and
> duplicate-scalar; or they are a size field for the three-reg-different-lengths and
> two-reg-and-scalar insn groups (where size cannot be `0b11`).*

⇒ **`bits[21:20] != 0b11` ⇒ é B13.10 (3-reg-different) ou B13.11 (2-reg-and-scalar)**; `== 0b11` é
B13.12/B13.13/B13.14. Esta task pega a primeira metade.

### As 26 linhas (medidas, o plano estimava ~35)

| Família | Linhas | Mnemônicos |
|---|---:|---|
| Alargando, soma/subtração | 8 | `VADDL_S_3d` `VADDL_U_3d` `VADDW_S_3d` `VADDW_U_3d` `VSUBL_S_3d` `VSUBL_U_3d` `VSUBW_S_3d` `VSUBW_U_3d` |
| Estreitando (half-narrowing) | 4 | `VADDHN_3d` `VRADDHN_3d` `VSUBHN_3d` `VRSUBHN_3d` |
| Diferença absoluta / acumulação | 4 | `VABAL_S_3d` `VABAL_U_3d` `VABDL_S_3d` `VABDL_U_3d` |
| Multiplicação alargando | 10 | `VMLAL_S_3d` `VMLAL_U_3d` `VMLSL_S_3d` `VMLSL_U_3d` `VMULL_S_3d` `VMULL_U_3d` `VMULL_P_3d` `VQDMLAL_3d` `VQDMLSL_3d` `VQDMULL_3d` |

### O espelho A64 (D1 da RFC B13.2)

Praticamente tudo aqui tem irmã A64 **já implementada**: `SADDL`/`UADDL`/`SADDW`/`SSUBL`/`SSUBW`,
`ADDHN`/`RADDHN`/`SUBHN`/`RSUBHN`, `SABAL`/`SABDL`, `SMULL`/`UMULL`/`PMULL`/`SMLAL`/`SMLSL`,
`SQDMULL`/`SQDMLAL`/`SQDMLSL` — feitas por **B8.7/B8.8/B8.19/B8.20** e migradas para o núcleo pela
**E10** (que deu buffer contra aliasing às 6 famílias de largura mista).

⇒ **Esta task é migração + decode, não semântica nova**, exatamente como B13.4-B13.8. Antes de
escrever qualquer laço, achar a operação correspondente em `Ir64VectorArithmeticExecutor` e
**mover para `advsimd/AdvSimdLanes`**, deixando o A64 delegando.

**Leitura obrigatória**: `docs/RFC-NEON-NUCLEO-VETORIAL.md` (B13.2) INTEIRA, a **B13.8**
(`## Resultado` — o precedente mais próximo: estreitamento/alargamento no A32), a **E10**
(`## Resultado` — o aliasing de largura mista, que se aplica a TODAS as 26) e a **B8.20**
(`SQDMULL` escalar / narrow-widen no A64). No código:
`executor64/Ir64VectorArithmeticExecutor` (`executeWidening`, `executeWide`, `executeNarrow`,
`executeWideningByElement` — os nomes que a E10 lista), `advsimd/AdvSimdLanes`,
`advsimd/AdvSimdShiftNarrowOp`/`AdvSimdShiftWidenOp` (o padrão de enum do núcleo),
`decoder/NeonShiftImmediateDecoder` (o precedente de decoder próprio de frame),
`codegen/executor/IrNeonExecutor`, `ir/IrOp.java`, `codegen/jvm/AsmNativePolicy`.

## Objetivo

As 26 linhas decodificam e executam no encoding **A32**, gateadas por `ArmFeature.ADVANCED_SIMD` —
**nenhum preset a declara**, portanto **zero-diff de runtime** e `docs/COBERTURA-ISA.md` **byte a
byte idêntica** (`neon-dp` segue `NOT_IN_ANY_PRESET` até B13.22).

## Inclui

1. **Decoder novo** `decoder/NeonThreeRegDifferentDecoder` (precedente: B13.3/B13.7/B13.8 criaram o
   seu). Frame: bit23=1, bit4=0, **bits[21:20] != 0b11**, e o `opc` (bits[11:8]) escolhe a família.
2. **Enum(s) no núcleo** `advsimd/` para as famílias, espelhando os enums do A64 1:1 (mesmo padrão de
   `AdvSimdShiftNarrowOp`).
3. **Migração D1**: as operações correspondentes saem do `switch` do executor A64 para
   `AdvSimdLanes`, e o A64 passa a delegar. **Zero-diff nas suítes A64** é o Aceite.
4. **`IrOp` novo(s)** + `Kind` contíguo (conferir o próximo livre — B13.9 usa 77) + `permits` +
   `IrBlockExecutor` (2 switches) + `AsmNativePolicy` (`false`) +
   `TruffleCodeEmitterSupportsCoherenceTest` (a contagem de Kinds — sem isso o módulo `truffle/` não
   compila; armadilha herdada de B13.7/B13.8).
5. **Executor A32** em `IrNeonExecutor`, sem escrita destrutiva (VFP32 não zera).
6. Testes (ver Aceite).

## Não inclui

- **2-reg-and-scalar** (`*_2sc`, 19 linhas) ⇒ **B13.11** — mora no mesmo sub-frame
  (`bits[21:20] != 0b11`) e é a irmã imediata desta task.
- **two-reg-misc / conversões / permuta / cripto** (`bits[21:20] == 0b11`) ⇒ B13.12-B13.15.
- **T32** ⇒ B13.16 · **preset com `ADVANCED_SIMD`** ⇒ B13.22 · **emissão nativa** ⇒ C12.8 ·
  **nó Truffle** ⇒ A10.7.
- **`FPSCR.QC`** (tocado por `VQDMLAL`/`VQDMLSL`/`VQDMULL`) — não modelado, paridade com o A64 e com
  B13.5. Task futura própria nos dois lados.

## Especificação

### Frame e campos

```
 31   28 27 25 24 23 22 21 20 19  16 15 12 11  8  7  6  5  4  3   0
1111    001    U  1  D  size    Vn    Vd    opc  N  0  M  0  Vm
```

- Grupo: `(raw & 0xFE80_0010) == 0xF280_0000` — bits[31:25]=`1111001`, bit23=1, **bit4=0**.
- **`bits[21:20]` = `size`, e `size == 0b11` NÃO é desta task** (é B13.12-B13.14) ⇒ devolver `null`
  nesse caso, deixando o espaço para elas (mesma disciplina que a B13.7 usou para `Vimm_1r`).
- `U` = bit24; `opc` = bits[11:8]; `vd`/`vn`/`vm` na convenção `D:Vd`/`N:Vn`/`M:Vm`.
- NEON é incondicional ⇒ `Condition.AL`.

### As três formas de largura (é o que define os registradores)

| Forma | `Vd` | `Vn` | `Vm` | Famílias |
|---|---|---|---|---|
| **Long** (`L`) | `Q` | `D` | `D` | `VADDL`/`VSUBL`/`VABDL`/`VABAL`/`VMULL`/`VMLAL`/`VMLSL`/`VQDMULL`/`VQDMLAL`/`VQDMLSL` |
| **Wide** (`W`) | `Q` | **`Q`** | `D` | `VADDW`/`VSUBW` |
| **Narrow** (`HN`) | `D` | `Q` | `Q` | `VADDHN`/`VRADDHN`/`VSUBHN`/`VRSUBHN` |

**Cada forma tem sua própria regra de registrador ímpar UNDEFINED** — só os operandos `Q` são
checados. Errar aqui é o modo de falha mais provável desta task.

### `VMULL_P` é polinomial

`VMULL.P8` é multiplicação **sem carry** (polinomial em GF(2)), não aritmética. O A64 tem `PMULL`
implementado — reusar. Não é o mesmo laço de `VMULL_S`/`VMULL_U`.

### `VADDHN`/`VRADDHN`: "half narrowing"

Somam dois operandos de largura `2×esize` e escrevem **a metade ALTA** do resultado, estreitada.
`VR*` arredonda antes de estreitar (soma `1 << (esize-1)`). Espelho exato do
`ADDHN`/`RADDHN` do A64.

## Passos

1. Ler "Leitura obrigatória" + `target/isa-decode/neon-dp.decode:386-493`.
2. Mapear cada uma das 26 à operação A64 correspondente. **Se alguma não tiver irmã A64**, é achado:
   registrar e implementar do zero (com a semântica do ARM DDI 0406C).
3. Migração D1 (núcleo) + delegação no A64 → rodar as suítes A64: têm que passar SEM alteração.
4. `IrOp`/`Kind`/dispatch/`AsmNativePolicy`/coerência Truffle.
5. Decoder novo + registro como `DecoderExtension`.
6. Testes. Encodings golden com `arm-none-eabi-as -mfpu=neon -march=armv8-a` (devkitARM).
7. `mvn -o test` (JBR 25) + `install` + G5 + `./gerar-cobertura-isa.sh` com diff **VAZIO**.
8. `INDICE.md` da trilha B (linha B13), `## Resultado`, commit `B13.10: …`, `git push`.

## Aceite

- As **26 linhas** decodificam sob arquitetura de teste com `ADVANCED_SIMD`, com família, `size` e
  registradores corretos; e seguem `UNIMPLEMENTED` em `ARMV7A`/`ARM11_MPCORE` (zero-diff).
- **`size == 0b11` devolve `null`** (não `unimplemented`) — teste provando que o espaço de
  B13.12-B13.14 continua livre, espelhando o teste `immhZeroIsNotClaimedByThisDecoder` da B13.7.
- **Registrador ímpar UNDEFINED por forma**: `Vd` ímpar em `L`; `Vd` **e** `Vn` ímpares em `W`;
  `Vn`/`Vm` ímpares em `HN`. Um teste por forma.
- **Zero-diff A64**: suítes de B8.7/B8.8/B8.19/B8.20/E10 passam sem edição — contagens coladas.
- **Execução**, por família, com padrão conhecido:
  - `VADDL.S8` × `VADDL.U8` no mesmo byte com bit 7 setado (assinado × não);
  - `VADDW` (operando largo + estreito);
  - `VADDHN` × `VRADDHN` num valor onde o arredondamento muda o resultado;
  - `VABAL` **acumula** (pré-preencher `Vd`);
  - `VMULL.P8` polinomial, conferido contra valor calculado à mão;
  - `VQDMULL` saturando em `0x7FFF...`;
  - **`Vd == Vn`** e **`Vd == Vm`** em pelo menos uma família de cada forma (aliasing — E10).
- `docs/COBERTURA-ISA.md` **byte a byte idêntica**.
- `mvn -o test` verde + G5 verde nos 5 consumidores.

## Armadilhas

1. **`size == 0b11` não é desta task** — devolver `null`, não `unimplemented`, ou B13.12-B13.14
   perdem o espaço.
2. **Três formas de largura, três regras de registrador ímpar.** Aplicar a regra de `L` a `W` ou
   `HN` produz UNDEFINED onde não deve (ou aceita o que deveria recusar).
3. **Largura mista ⇒ aliasing.** A E10 achou o bug em 6 famílias A64; ao migrar, **o buffer vem
   junto**. Não reintroduzir a versão sem buffer no núcleo.
4. **`VMULL.P8` é polinomial** — reusar `PMULL` do A64, não o laço inteiro.
5. **`bit4=0` distingue este frame do de B13.7/B13.8.** Sem essa checagem, o decoder engole
   deslocamento por imediato — colisão entre dois decoders (G8).
6. **`AsmNativePolicy` mora em `codegen/jvm/`** e o teste de coerência do Truffle precisa da contagem
   nova (armadilhas herdadas de B13.4-B13.8).
7. **`FPSCR.QC`** continua sem efeito — registrar no `## Resultado`.

## Não fazer

- Não implementar 2-reg-and-scalar (B13.11) nem `size==0b11` (B13.12-B13.14).
- Não antecipar T32 (B13.16).
- Não declarar `ADVANCED_SIMD` em preset (B13.22).
- Não reescrever semântica que o A64 já tem — migrar.
- Não modelar `FPSCR.QC`.

## Resultado

**2026-09-04 — feito.** As 26 linhas (Long 18 + Wide 4 + Narrow 4) decodificam e executam no
encoding A32, gateadas por `ArmFeature.ADVANCED_SIMD` (nenhum preset a declara → zero-diff de
runtime, `docs/COBERTURA-ISA.md` byte-idêntica).

### O que mudou

- **`advsimd/AdvSimdWideningOp`** (novo, 17 constantes: as 16 de `Ir64VectorWideningOp` mais
  `PMULL`, que não tem par no A64 dentro deste enum — `PMULL`/`PMULL2` do A64 é um `IrOp` próprio
  fora desta família; `VMULL.P8` do A32 reusa `AdvSimdLanes#polynomialMultiply8` diretamente).
- **`advsimd/AdvSimdWideOp`** (novo, 4 constantes, mirror de `Ir64VectorWideOp`) e
  **`advsimd/AdvSimdNarrowOp`** (novo, 4, mirror de `Ir64VectorNarrowOp`).
- **`advsimd/AdvSimdLanes`** — 3 métodos novos:
  - `widening(regs, op, esz, outputElements, laneOffset, baseRd, baseRn, baseRm)` — `switch` das 16
    famílias movido VERBATIM de `Ir64VectorArithmeticExecutor#executeWidening` (já com o buffer da
    E10), mais o caso `PMULL` (reusa `polynomialMultiply8`). Buffer `long[]` antes de escrever
    (`Rd` pode ser `Rn`/`Rm`).
  - `wide(regs, op, esz, elements, laneOffset, baseRd, baseRn, baseRm)` — idem de `executeWide`.
  - `narrow(regs, op, esz, elements, laneOffset, baseRd, baseRn, baseRm)` — idem de `executeNarrow`.
  - `saturatingDoublingProduct` (privado, novo) — cópia VERBATIM do homônimo de
    `Ir64VectorArithmeticExecutor`, que MANTÉM a própria cópia (usada por `executeWideningByElement`,
    que não migrou nesta task) — mesma duplicação mínima já aceita para `unsignedBig`/
    `saturateToElement`/`signedSaturatingAdd`/`signedSaturatingSub` desde B13.5.
- **`Ir64VectorArithmeticExecutor`** — `executeWidening`/`executeWide`/`executeNarrow` agora só
  delegam ao núcleo + `sharedWideningOp`/`sharedWideOp`/`sharedHalfNarrowOp` (1:1, sem `default`),
  espelhando `sharedNarrowOp`/`sharedWidenOp` de B13.8.
- **`ir/IrOp`** — records `NeonWidening(op, esz, vd, vn, vm)` (`Kind` 78) / `NeonWide(op, esz, vd,
  vn, vm)` (`Kind` 79) / `NeonNarrow(op, esz, vd, vn, vm)` (`Kind` 80). **Nenhum tem `quad`**: cada
  forma tem largura FIXA de operando (Long: `Vd`=`Q`,`Vn`/`Vm`=`D`; Wide: `Vd`/`Vn`=`Q`,`Vm`=`D`;
  Narrow: `Vd`=`D`,`Vn`/`Vm`=`Q`), mesma disciplina de `NeonShiftNarrowImmediate`/
  `NeonShiftWidenImmediate` (B13.8).
- **`codegen/executor/IrNeonExecutor`** — `executeNeonWidening`/`executeNeonWide`/`executeNeonNarrow`,
  `laneOffset=0` sempre (A32 não tem forma "2"), `outputElements`/`elements = 8 >> esz`.
- **`codegen/executor/IrBlockExecutor`** — os 2 switches ganham os 3 casos.
- **`codegen/jvm/AsmNativePolicy`** — 3 casos → `false` (interpretado, como todo `Kind` vetorial).
- **`decoder/NeonThreeRegDifferentDecoder`** (novo) — frame `bit23=1,bit4=0`; devolve `null` para
  `size==0b11` (espaço de B13.12-14) e para `bit6==1` (espaço de B13.11, "2-regs-plus-scalar" — o
  MESMO subgrupo `size!=0b11` contém as duas famílias, distinguidas só por esse bit, achado medido
  direto no `.decode`); `opc`(bits[11:8])+`U`(bit24) escolhem a família via 3 tabelas
  (`longOperation`/`wideOperation`/`narrowOperation`); checagem de registrador ímpar por FORMA
  (Long: só `Vd`; Wide: `Vd` e `Vn`; Narrow: `Vn` e `Vm`).
- **`truffle/TruffleCodeEmitterSupportsCoherenceTest`** — 78→**81** Kinds, 38→**41** descobertos
  (NEON não tem nó Truffle) + 3 casos em `sampleOp`.
- **`docs/COBERTURA-JIT.md`** regenerado (`./gerar-cobertura-jit.sh`, precisa `mvn -o install`
  antes — achado operacional da B13.9, confirmado de novo aqui): 3 `Kind` novos aparecem como `❌`
  (interpretados) nas duas colunas (ASM/Truffle).

### Achado: `size` é sempre o `esz` do lado ESTREITO, nas TRÊS formas

Medido contra o assembler real (`arm-none-eabi-as -mfpu=neon -march=armv8-a`, devkitARM): para
Long/Wide, `size` é a largura de `Vn`/`Vm` (fonte estreita) — `vaddl.u16` mede `size=1`. Para
Narrow/HN, `size` é a largura de `Vd` (DESTINO estreito) — `vaddhn.i16` mede `size=0` (não `1`
como o nome sugeriria à primeira vista: o sufixo `.i16` do mnemônico denota a largura da FONTE
larga, `esz+1`, não do destino). Confirmado byte a byte contra o `objdump` de 26 instruções reais
— ver a suíte de testes para os valores golden completos.

### Zero-diff A64 (D1 da RFC)

`Ir64VectorArithmeticExecutorTest` e as `Aarch64AdvSimd*DecoderTest` (B8.7/B8.8/B8.19/B8.20/E10)
passam **sem edição nenhuma**.

### Testes novos

`decoder/NeonThreeRegDifferentDecoderTest` (20 testes, novo arquivo):
`b1310EncodingsMatchTheAssembler` (26 encodings golden), zero-diff sem a feature, `null` para
`size==0b11` e para o espaço de B13.11, decode de op/esz/registrador por forma, registrador ímpar
UNDEFINED por forma (Long/Wide/Narrow), `U=1` inválido para os 4 opcodes só-`U=0`, execução por
família (`VADDL` assinado×não-assinado, `VADDW`, `VADDHN`×`VRADDHN` com arredondamento trocando o
carry, `VABAL` acumulando, `VMULL.P8` polinomial conferido à mão, `VQDMULL` saturando), e 2 testes
de aliasing (`Vd`==`Vn`==`Vm` simultâneo na forma Long — o par `D` completo do `Q` de destino
coincide com as DUAS fontes; `Vd` sobrepondo a metade baixa da fonte larga na forma Narrow).

### Validação

- `mvn -o test` (JBR 25), reactor completo: BUILD SUCCESS.
- `mvn -o install`: BUILD SUCCESS.
- `docs/COBERTURA-ISA.md`: **byte-idêntica** (nenhuma mudança de decode medido — `neon-dp` segue
  `NOT_IN_ANY_PRESET`).
- **G5**: `gbaemu` (verde, `JitInterpreterDivergenceTest` sem divergência), `ndsemu` (verde),
  `armbox` (verde), `virtual-arm-box` (verde), `n3dsemu` (verde) — todos `mvn -o test` BUILD
  SUCCESS, nenhum arquivo de consumidor tocado.
