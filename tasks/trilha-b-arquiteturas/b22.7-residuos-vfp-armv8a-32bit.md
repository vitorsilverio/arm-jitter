# B22.7 — Resíduos ARMv8-A de 32 bits expostos pela B14.7: `VRINTR/Z/X`, `VJCVT`, conversões FP16/BF16 do VFPv3, `HALT` no perfil M

**Trilha:** B · **Repo:** arm-jitter (+ revalidação G5) · **Depende de:** B14.7 (✅), B15.6/B15.7 (para o item 4)
**Épico:** `b22-plano-residuos-32-bits.md` (reaberto) · **Status:** ✅ (fechada 2026-09-25)

## Contexto

O épico **B22** ("resíduos de 32 bits") foi dado por concluído em 2026-09-02 (B22.1-B22.6), mas a
**B14.7** (fechada depois, 2026-09-19 — o épico B14 nem existia quando B22 fechou) mediu uma coluna
nova (`v8-A/32`) e expôs **4 famílias de `❌` genuínos**, com destino nomeado explícito no seu
`## Resultado`:

| Grupo/mnemônico | Motivo medido pela B14.7 | Onde vive |
|---|---|---|
| `VRINTR_hp/_sp/_dp`, `VRINTZ_hp/_sp/_dp` (condicional), `VRINTX_hp/_sp/_dp` (condicional) | Dependem de `FPSCR.RMode`; as formas `sp`/`dp` nunca foram implementadas (exclusão explícita de B14.5/B14.6) | `vfp.decode` |
| `VJCVT` | ARMv8.3-A, posterior ao baseline `ARMv8.0-A` do preset `ARMV8A_32` | `vfp-uncond.decode` |
| `VCVT_f32_f16`/`_f16_f32`/`_f64_f16`/`_f16_f64`/`VCVT_b16_f32`/`VCVT_hp_int` | Extensões opcionais de VFPv3/BFloat16, nenhum preset as declara | `vfp.decode` |
| `HLT` sob `ARMv8.1-M+MVE` | `ArmFeature.HALT` não é declarada pelos presets ARMv8-M modernos | `t16.decode`/`a32.decode` |

Isto reabre o épico B22 pelo mesmo motivo que ele existiu da primeira vez: "resíduos de presets de
32 bits que já existem" — os 4 itens acima são exatamente isso, só que descobertos por uma coluna
que nasceu depois do fechamento.

## Objetivo

As 4 famílias acima decodificam e executam corretamente (ou, quando a arquitetura real não define
comportamento — ver Armadilha 2 sobre `VRINTZ`/`VRINTX` condicionais em ARMv8-A — o motivo fica
documentado como não-aplicável, nunca como exclusão silenciosa); a coluna `v8-A/32` de
`docs/COBERTURA-ISA.md` sobe de 94% (736/778) para o valor medido depois desta task.

## Inclui

1. **`VRINTR`/`VRINTZ`/`VRINTX` incondicionais** (`vfp-uncond.decode`, as formas que `VRINTA`/
   `VRINTN`/`VRINTP`/`VRINTM` da B14.5 já implementaram como irmãs) — decode + execução usando
   `FPSCR.RMode` (B14.5 já resolve `RMode` para as 4 formas ARMv8-A "com modo embutido"; `VRINTR`/
   `VRINTZ`/`VRINTX` usam o modo CORRENTE de `FPSCR` em vez de um modo fixo no encoding — ler a B14.5
   e o manual ARM antes de assumir que é a mesma função).
2. **`VJCVT`** (`vfp-uncond.decode`) — conversão FP para inteiro de precisão dupla com arredondamento
   truncado especial (`FJCVTZS`, ARMv8.3-A) — feature nova (`ArmFeature` própria, versão posterior a
   `ARMV8A_32`) ou preset novo `ARMV8_3A_32`, a decidir (ver Especificação).
3. **`VCVT_f32_f16`/`_f16_f32`/`_f64_f16`/`_f16_f64`** (VFPv3 half-precision extension) e
   **`VCVT_b16_f32`/`VCVT_hp_int`** (BFloat16) — decode + execução, reusando o núcleo de conversão
   FP16 que a B14.6/B19.5.1 já criaram se aplicável (medir antes de duplicar).
4. **`ArmFeature.HALT` nos presets ARMv8-M modernos** (`ARMV8M_BASELINE`/`ARMV8M_MAINLINE`/
   `ARMV8_1M`/`ARMV8_1M_MVE`) — `HLT` existe em ambos os perfis A e M do ARM DDI 0487 (achado
   colateral da B14.7); é só uma declaração de feature em preset existente, sem decoder novo (o
   decoder de `HLT` já existe desde B22.1).
5. `docs/COBERTURA-ISA.md`/`docs/isa-nao-aplicavel.tsv`/`docs/VALIDACAO-ARQUITETURAS.md` atualizados;
   delta registrado.
6. Testes (ver Aceite).

## Não inclui

- **`SB`/`CRC32*` de 32 bits sob perfil M** — família irmã, mas de origem B16.14, não B14.7; task
  própria (`b16.15-residuos-loop-branch-e-hints.md`).
- **Formas F16 do NEON A32** (`VCVT_HS_2sh`&cia) — família independente sob `neon-dp.decode`; task
  própria (`b13.24-neon-fp16-aarch32.md`).
- **Emissão ASM nativa/Truffle** dos `IrOp` novos — épicos C12/A10.
- **Release no Maven Central** — bloqueado até 100% (`tasks/README.md`, 2026-08-27).

## Especificação

### Leitura obrigatória antes de codar

- `b14.7-fechamento-coluna-v8a32.md`, `## Resultado`, seção "❌ que a coluna nova expôs" — a tabela
  completa com os 4 grupos, **reconferir contra o código atual** (pode ter mudado desde 2026-09-19).
- `b14.5-vrint-vcvt-modo-explicito.md` e `b14.6-fp16-32bit-vmovx-vins.md`, `## Resultado` de cada —
  o que já foi implementado (`VRINTA/N/P/M`) e o que foi **deliberadamente excluído** e por quê
  (para não reimplementar por engano, e para reusar `RMode`/núcleo FP16 se existir).
- `core/src/main/java/dev/vitorsilverio/armjitter/decoder/VfpDecoder.java` (o decoder de
  `VRINTA/N/P/M` da B14.5, como precedente estrutural) e o `Thumb2VfpDecoder` correspondente.
- `docs/RFC-*` sobre FP16 se existir núcleo compartilhado (`advsimd`/`Aarch64Fp16*`) — B19.5.1
  (`tasks/trilha-b-arquiteturas/b19.5.1-nucleo-meia-precisao.md`, ✅) criou um núcleo A64; medir se
  ele é reusável para VFP de 32 bits ou se é específico do A64.
- `core/src/main/java/dev/vitorsilverio/armjitter/arch/ArmArchitecture.java` e `ArmFeature.java` —
  como declarar `ArmFeature.HALT` num preset existente (aditivo, G3) e como criar um preset
  `ARMV8_3A_32` se o item 2 precisar (espelhar `ARMV8A_32`, B14.1).
- `target/isa-decode/vfp-uncond.decode`/`vfp.decode` (via `IsaCoverageReport`/`gerar-cobertura-isa.sh`)
  — os encodings exatos de `VJCVT`/`VRINTZ`/`VRINTX` condicionais/`VCVT_*f16*`.
- `tasks/trilha-b-arquiteturas/b14.1-preset-armv8a-32bit.md` — precedente de "preset novo aditivo
  sobre `ARMV7A`", a repetir para `ARMV8_3A_32` se necessário.

## Passos

1. Ler "Leitura obrigatória". Remedir os 4 grupos contra o código atual (`./gerar-cobertura-isa.sh`)
   antes de qualquer código.
2. `VRINTR`/`VRINTZ`/`VRINTX` incondicionais: decode + execução usando `FPSCR.RMode` corrente.
3. `VJCVT`: decidir feature/preset (item 2 da Especificação) + decode + execução.
4. Conversões FP16/BF16 do VFPv3: decode + execução, reusando núcleo existente se aplicável.
5. `ArmFeature.HALT` nos 4 presets ARMv8-M modernos — mudança de uma linha por preset + teste.
6. Regenerar `docs/COBERTURA-ISA.md`; auditar `docs/isa-nao-aplicavel.tsv`; atualizar
   `docs/VALIDACAO-ARQUITETURAS.md`.
7. `mvn -o test` (JBR 25) + `install` + G5 nos 5 consumidores.
8. `INDICE.md`, `## Resultado`, `tasks/FILA-EXECUCAO.md`, commit `B22.7: ...`, `git push`.

## Aceite

- As células dos 4 grupos medem `✅` na coluna `v8-A/32` (e `HLT` mede `✅` também em
  `ARMv8.1-M+MVE`), salvo achado documentado de não-aplicabilidade real (com fonte).
- Nenhuma célula das outras colunas regride (conferir por diff explícito).
- `mvn -o test` verde + G5 verde nos 5 consumidores.
- `## Resultado` registra o delta e, para qualquer célula que não fechar, o destino nomeado.

## Armadilhas

1. **`VRINTR`/`VRINTZ`/`VRINTX` usam `FPSCR.RMode` CORRENTE, não um modo fixo no encoding** —
   diferente de `VRINTA`/`N`/`P`/`M` (B14.5), que têm o modo embutido no mnemônico. Confundir os
   dois mecanismos produz resultado silenciosamente errado (G8 adjacente).
2. **`VJCVT` pode exigir um preset de versão posterior a `ARMV8A_32`** (ARMv8.3-A) — não forçar a
   feature no preset `ARMV8A_32` existente (G3); criar preset novo aditivo, mesmo padrão da B14.1.
3. **Núcleo FP16 pode não ser diretamente reusável entre A64 e VFP de 32 bits** — a B19.5.1 construiu
   para AArch64; medir a superfície antes de assumir compartilhamento (mesma disciplina da RFC
   B13.2).
4. **`HALT` em perfil M**: conferir se `HLT` já decodifica corretamente sob esses 4 presets uma vez
   que a feature for declarada — o decoder já existe (B22.1), então este item pode ser só a
   declaração de feature, sem código de decode/execução novo. Não reimplementar `HLT`.
5. **G3/G5**: `ArmFeature.HALT` em presets existentes é aditivo, mas ainda requer G5 completo — um
   preset ganhando uma instrução nova pode, em tese, ser observado por algum consumidor (improvável,
   mas conferir).
6. **Release bloqueada** — medir e registrar, não publicar.

## Não fazer

- Não implementar `SB`/`CRC32*` de perfil M nem NEON FP16 A32 (tasks próprias).
- Não forçar `ArmFeature` nova em preset existente sem checar G3.
- Não publicar release.

## Resultado

**Fechada em 2026-09-25.**

### O que foi feito

1. **`VRINTR`/`VRINTZ`/`VRINTX` (`sp`/`dp`/`hp`)** — `vfp.decode` (espaço CONDICIONAL: a spec dizia
   `vfp-uncond.decode`, mas as 9 linhas moram em `vfp.decode`; o espaço incondicional já tinha
   `VRINT{A,N,P,M}` da B14.5). `VRINTZ` decodifica para o `IrOp.VfpRound`/`VfpRoundHalf` existente com
   `TOWARD_ZERO`; `VRINTR`/`VRINTX` usam o **modo corrente de `FPSCR.RMode`** — representado como
   `direction == null` nos 4 records `VfpRound`/`VfpConvertRounded`/`VfpRoundHalf`/
   `VfpConvertRoundedHalf` (constante `IrOp#FPSCR_ROUNDING_DIRECTION_FIELD`=`0b111` no campo empacotado
   decoder→builder). O executor mapeia `FpRoundingMode`→`AdvSimdLanes.RoundingMode` **explicitamente**
   (`IrVfpExecutor#effectiveDirection`; as duas tabelas não coincidem — a de `FPSCR` não tem "ties away").
   Gate `ArmFeature.ARMV8_FP` (`sp`/`dp`); `hp` sob `FP16_ARITHMETIC`. `VRINTX` = `VRINTR` no valor: o
   `IXC` cumulativo não é modelado por nenhuma família VFP do projeto (documentado no Javadoc).
2. **`VCVTR` (`rz=0`) `sp`/`dp`/`hp`** — achado colateral: a coluna só media `VCVT_*_int` com `rz=1`
   (`FILL_STRATEGIES`), mas `rz=0` (`VCVTR`, VFPv2!) decodificava como `UNIMPLEMENTED`. Agora
   `VfpConvertRounded(null, …)`.
3. **`VCVTB`/`VCVTT`** f16↔f32/f64 + **`VCVT_b16_f32`** (BF16) — `IrOp.VfpConvertHalfPrecision` +
   `enum HalfPrecisionConversion` (`F16_TO_F32`/`F16_TO_F64`/`F32_TO_F16`/`F64_TO_F16`/`F32_TO_BF16`),
   Kind 183. As formas "para half" **preservam a outra metade** do `S` destino. `F64_TO_F16` arredonda
   UMA vez (`AdvSimdLanes#doubleToHalfBits`, agora público) — passar por `float` erra os empates
   (teste `doubleToHalfRoundsOnceNotViaFloat`: `1+2⁻¹¹+2⁻⁴⁰` → `0x3C01`, via float daria `0x3C00`). Gate
   `HALF_PRECISION_FP` **ou** `FP16_ARITHMETIC` (o `ARMV8A_32` só declara a segunda); BF16 sob
   `BFLOAT16`, reconhecido ANTES do gate `FP16_ARITHMETIC` do espaço `_hp` (`FEAT_BF16` não implica
   `FEAT_FP16`).
4. **`VJCVT`** — `IrOp.VfpJavascriptConvert` (Kind 184), `ArmFeature.JAVASCRIPT_CONVERT`. Decisão de
   preset (spec: "feature nova ou preset novo, a decidir"): **preset novo `ARMV8_6A_32`** =
   `ARMV8A_32` + `JAVASCRIPT_CONVERT` + `BFLOAT16` (cumulativo, como as colunas A64), G3 intacto —
   `ARMV8A_32` NÃO ganhou nada. Coluna nova **`v8.6-A/32`** em `IsaCoverageReport`; `VJCVT`/
   `VCVT_b16_f32` entram em `ARM32_VERSION_REQUIREMENTS` → `·` em `v8-A/32`, `✅` em `v8.6-A/32`.
   Semântica conferida contra o QEMU real (`vfp_helper.c`, `HELPER(vjcvt)`, baixado com `curl`):
   `Sd = ToInt32(Dm)` módulo 2³², `FPSCR.{N,Z,C,V} = 0,exato,0,0`, resto do `FPSCR` intacto.
5. **🔴 Bug PRÉ-EXISTENTE achado e corrigido no A64 (`FJCVTZS`, B19.29)**: `Ir64FpExecutor` devolvia
   `Wd=0` em todo overflow (o QEMU e a ARM ARM reduzem módulo 2³²: `4294967301.0` → `5`) e tratava `-0.0`
   como exato (`Z=1`; o QEMU marca `-0.0` como inexato: `inexact |= value == float64_chs(float64_zero)`).
   A lógica agora vive em `AdvSimdLanes#javascriptToInt32`/`#javascriptToInt32IsExact` (fonte única A32+A64).
   O teste `Ir64JavascriptConvertExecutorTest` pinava o comportamento errado ("overflowProducesZero…") —
   reescrito. Zero mudança de cobertura (a célula já era `✅`), mas era corrupção silenciosa de valor.
6. **`ArmFeature.HALT` nos presets ARMv8-M** (`ARMV8M_BASELINE`/`ARMV8M_MAINLINE`; `ARMV8_1M`/
   `ARMV8_1M_MVE` herdam) — só declaração, decoder de `HLT` é o da B22.1 (`ArmV8MHaltPresetTest`).
7. **Curadoria do `docs/isa-nao-aplicavel.tsv`** (comentários "mede ❌"/"não implementado" atualizados;
   `v7-A+NEON` acrescentado a `VRINTR*`/`VRINTZ*`/`VRINTX*`/`VJCVT`/`*_hp`/`VCVT_f32_f16`&cia/
   `VCVT_hp_int`; `v7-A+NEON,v7-R` a `HLT` — ARMv7 não tem essas instruções ARMv8). **39 células de
   `v7-A+NEON` e 2 de `v7-R` passaram de `❌` a `·`** (reclassificação por versão real, não escondeu
   trabalho: as instruções agora existem e medem `✅` na coluna ARMv8 correspondente).

### Medição (docs/COBERTURA-ISA.md)

| | antes | depois |
|---|---|---|
| **global** | 98% (22719/23048) | **98%** (23484/23783) |
| `v8-A/32` | 94% (736/778) | **96%** (749/776) — denominador −2: `VJCVT`/`VCVT_b16_f32` viram `·` |
| `v8.6-A/32` (coluna nova) | — | **96%** (751/778) |
| `ARMv8.1-M+MVE` | 93% (693/742) | 93% (694/742) — `HLT` |
| `v7-A+NEON` | 89% (944/1051) | 93% (944/1012) — curadoria |
| `v7-R` | 89% (594/661) | 90% (594/659) — `HLT` `·` |

Nenhuma célula das colunas antigas regrediu de `✅` (conferido: `v4T`…`v7-A`, `v6-M`, `v7-M` inalteradas).
Gatilhos históricos de release NÃO cruzados (global +0,2pp) — e a release segue reservada para 100%.

### Testes

`VfpB227ResidualsTest` (31: decode+lift+execução, os 4 modos de `FPSCR.RMode`, `VRINTZ` ignora o modo,
gates G8 por feature — `VFP_ONLY`/`HALF_CONV_ONLY`/`BF16_ONLY`/`V80`/`V86`, T32≡A32, bloco, política ASM,
lifter), `AdvSimdLanesJavascriptToInt32Test` (5, inclui o ramo `BigInteger` acima de 2⁶³),
`ArmV8MHaltPresetTest` (2), `Ir64JavascriptConvertExecutorTest` corrigido (+ `-0.0`). Testes antigos que
pinavam "fora de escopo" atualizados: `VfpDecoderTest`, `VfpHalfPrecisionArithmeticTest`,
`IsaCoverageReportV8A32ColumnTest` (agora exige `VRINT*` `✅` em `v8-A/32` e `VJCVT` `·`/`✅`),
`IsaCoverageReportV7RColumnTest`, `ArmArchitectureArmv8a32Test`, coluna nova nas 3 listas de colunas,
`TruffleCodeEmitterSupportsCoherenceTest` (185 Kinds, 119 sem nó Truffle). `docs/COBERTURA-JIT.md`
regenerado (185 `record`s, os 2 novos `❌❌`: decode + interpretado apenas — "Não inclui" da spec).
**`mvn -o test` 4992 + 74 verdes; G5: gbaemu 240, ndsemu 183, armbox 47, virtual-arm-box 87, n3dsemu 221 — todos verdes.**

### Achados abertos (para as próximas sessões — nada aqui bloqueia a B22.7)

- `VMOV_half` (`vfp.decode`) mede `❌` em `v8-A/32`/`v8.6-A/32`: `decodeVmovHalf` exige
  `HALF_PRECISION_FP`, que `ARMV8A_32` não declara (ARMv8.2 FP16 deveria bastar). Mesmo padrão do
  `supportsHalfPrecisionConversion` desta task — correção de 1 linha + teste.
- `VMOV_to_gp`/`VMOV_from_gp` (2 linhas cada, formas de 8/16 bits, só NEON) medem `❌` em `v8-A/32`
  (deveria ser `·`, sem NEON) e em `v7-A+NEON` (aí é gap real de decoder).
- Mapa restante para 100% da TABELA (só medido): 32 bits **259** células (`v7-A+NEON` 68, `v7-R` 65,
  `v8-R` 24, `ARMv8.1-M+MVE` 48, `v8-A/32` 27, `v8.6-A/32` 27) + A64 **40** (PAC/AUT… — B19).
  Grande parte é CURADORIA (instruções ARMv8 contadas em colunas ARMv7: `LDA`/`STL`/`CRC32`/`SMC`/`HVC`/
  `ERET`/`SB`/`CLRM`/scalar-shifts MVE em colunas A) e as `SHA*_3s` de NEON.
- **A tabela NÃO mede** SVE/SVE2 (929 encodings) nem SME (623): `NOT_IN_ANY_PRESET` — "100% da tabela"
  ≠ "100% do ARM" (épicos B17/B18/B21 seguem pendentes).
