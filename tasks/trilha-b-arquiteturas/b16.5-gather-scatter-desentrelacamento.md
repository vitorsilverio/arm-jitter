# B16.5 — MVE: gather/scatter, desentrelaçamento (`VLD2`/`VLD4`/`VST2`/`VST4`) e os `dup` incrementais — 15 encodings

**Trilha:** B · **Repo:** arm-jitter (+ revalidação G5) · **Depende de:** B16.3 (⬜)
**Épico:** `b16-plano-mve-helium.md` · **Status:** ✅ (2026-09-17, ver **Resultado**)

## Contexto

Quinto degrau da escada B16 (partição normativa na spec da **B16.1**). O épico rotula esta task como
"Load/store com base em `Rn[3:1]` — gather/scatter, 34 encodings". A medição desta rodada de spec
mostra que o rótulo e a contagem vinham de um corte contíguo do arquivo: as 34 linhas do intervalo
que o épico rotulou assim (linhas 412-493) são, na verdade, os `dup` incrementais **mais** 28 linhas
de `VMLADAV`/`VMLALDAV`/reduções — que aqui vão para a **B16.13**. O que realmente é
gather/scatter/desentrelaçamento são as linhas 191-204, **11 linhas**, e o que realmente usa
"base em `Rn[3:1]`" são os 4 `dup` incrementais (linhas 412/413/418/419) — daí as **15** desta task.

### O encoding real (`target/isa-decode/mve.decode` — medido)

Gather/scatter por registrador de offsets (linhas 53, 64-69, 190-198):

```
&vldst_sg qd qm rn size msize os
&vldst_sg_imm qd qm a w imm

# scatter-gather memory size is in bits 6:4
%sg_msize 6:1 4:1

@vldst_sg .... .... .... rn:4 .... ... size:2 ... ... os:1 &vldst_sg \
          qd=%qd qm=%qm msize=%sg_msize

# Qm is in the fields usually labeled Qn
@vldst_sg_imm .... .... a:1 . w:1 . .... .... .... . imm:7 &vldst_sg_imm \
              qd=%qd qm=%qn

# gather loads/scatter stores
VLDR_S_sg        111 0 1100 1 . 01 .... ... 0 111 . .... .... @vldst_sg
VLDR_U_sg        111 1 1100 1 . 01 .... ... 0 111 . .... .... @vldst_sg
VSTR_sg          111 0 1100 1 . 00 .... ... 0 111 . .... .... @vldst_sg

VLDRW_sg_imm     111 1 1101 ... 1 ... 0 ... 1 1110 .... .... @vldst_sg_imm
VLDRD_sg_imm     111 1 1101 ... 1 ... 0 ... 1 1111 .... .... @vldst_sg_imm
VSTRW_sg_imm     111 1 1101 ... 0 ... 0 ... 1 1110 .... .... @vldst_sg_imm
VSTRD_sg_imm     111 1 1101 ... 0 ... 0 ... 1 1111 .... .... @vldst_sg_imm
```

Desentrelaçamento (linhas 71-73, 200-204):

```
# Deinterleaving load/interleaving store
@vldst_il .... .... .. w:1 . rn:4 .... ... size:2 pat:2 ..... &vldst_il qd=%qd

VLD2             1111 1100 1 .. 1 .... ... 1 111 .. .. 00000 @vldst_il
VLD4             1111 1100 1 .. 1 .... ... 1 111 .. .. 00001 @vldst_il
VST2             1111 1100 1 .. 0 .... ... 1 111 .. .. 00000 @vldst_il
VST4             1111 1100 1 .. 0 .... ... 1 111 .. .. 00001 @vldst_il
```

`dup` incrementais/decrementais (linhas 397-419) — **a origem literal do rótulo "base em `Rn[3:1]`"
do épico**:

```
# VIDUP, VDDUP format immediate: 1 << (immh:imml)
%imm_vidup 7:1 0:1 !function=vidup_imm

# VIDUP, VDDUP registers: Rm bits [3:1] from insn, bit 0 is 1;
# Rn bits [3:1] from insn, bit 0 is 0
%vidup_rm 1:3 !function=times_2_plus_1
%vidup_rn 17:3 !function=times_2

@vidup   .... .... . . size:2 .... .... .... .... .... qd=%qd imm=%imm_vidup rn=%vidup_rn &vidup
@viwdup  .... .... . . size:2 .... .... .... .... .... qd=%qd imm=%imm_vidup rm=%vidup_rm rn=%vidup_rn &viwdup
{
  VIDUP          1110 1110 0 . .. ... 1 ... 0 1111 . 110 111 . @vidup
  VIWDUP         1110 1110 0 . .. ... 1 ... 0 1111 . 110 ... . @viwdup
}
{
  VCMPGT_fp_scalar 1110 1110 0 . 11 ... 1 ... 1 1111  0110 .... @vcmp_fp_scalar size=2   # B16.8
  VCMPLE_fp_scalar 1110 1110 0 . 11 ... 1 ... 1 1111  1110 .... @vcmp_fp_scalar size=2   # B16.8
  VDDUP            1110 1110 0 . .. ... 1 ... 1 1111 . 110 111 . @vidup
  VDWDUP           1110 1110 0 . .. ... 1 ... 1 1111 . 110 ... . @viwdup
}
```

`%vidup_rn 17:3 !function=times_2` = "`Rn` = bits[19:17] × 2" (bit 0 sempre 0 → só registradores
PARES); `%vidup_rm 1:3 !function=times_2_plus_1` = "`Rm` = bits[3:1] × 2 + 1" (só ÍMPARES). É uma
restrição real do encoding, não conveniência do QEMU.

### Regras confirmadas contra o QEMU (`translate-mve.c`, via `WebFetch` nesta rodada)

- `mve_check_qreg_bank(qmask)` → `qmask < 8` (vale para `Qd` **e** `Qm`).
- "Many instructions check `qd == qm` or `qd == qn` are incompatible (e.g. VQDMULLB, **scatter-gather
  loads**)" — nas formas gather de offsets, `Qd == Qm` é UNDEF (o registrador de offsets seria
  sobrescrito no meio da operação, o que a arquitetura não define).
- `Rn == 15` / `Rn == 13 && w` UNDEF (mesma regra da B16.3).

## Objetivo

As 15 linhas decodificam sob `ArmFeature.MVE_INTEGER` e executam: gather por vetor de offsets
(com/sem sinal, com `os` = offset escalado), gather/scatter com imediato e base vetorial
(`*_sg_imm`, incluindo as formas de 64 bits `VLDRD`/`VSTRD`), load/store com desentrelaçamento de 2
e 4 vias, e os quatro geradores de vetor incremental/decremental (`VIDUP`/`VDDUP`/`VIWDUP`/
`VDWDUP`). 15 células `·`/`❌` → `✅`.

## Inclui

1. **Decode das 3 formas gather/scatter por offsets** (`VLDR_S_sg`/`VLDR_U_sg`/`VSTR_sg`):
   `rn:4`, `%qd`, `%qm`, `size:2` (tamanho do elemento no registrador), `%sg_msize 6:1 4:1`
   (tamanho em memória), `os:1` (offset escalado pelo tamanho). Recusas: `Qd>7`/`Qm>7`, `Qd==Qm`,
   `Rn==15`, `msize > size` (alargar num gather de offsets — **confirmar contra o QEMU** se é UNDEF
   ou válido), `size==3` conforme o manual.
2. **Decode das 4 formas `*_sg_imm`** (`VLDRW`/`VLDRD`/`VSTRW`/`VSTRD`): base é um VETOR (`qm=%qn`,
   note o comentário "Qm is in the fields usually labeled Qn" — **não** trocar), `a:1`/`w:1`
   (add/writeback **no registrador vetorial de base**), `imm:7`. As formas `D` transferem 64 bits
   por lane.
3. **Decode de `VLD2`/`VLD4`/`VST2`/`VST4`**: `@vldst_il` com `rn:4`, `%qd`, `size:2`, `pat:2`
   (qual "pedaço" do grupo de registradores esta instrução move — `VLD2` usa 2 padrões, `VLD4` usa
   4) e `w:1`. **`Qd` é a BASE de um grupo de 2 ou 4 registradores consecutivos** (`Qd..Qd+1` /
   `Qd..Qd+3`), logo `Qd+n > 7` é UNDEF.
4. **Decode de `VIDUP`/`VDDUP`/`VIWDUP`/`VDWDUP`** com `%vidup_rn`/`%vidup_rm` (pares/ímpares) e
   `%imm_vidup` (`1 << (immh:imml)`, ou seja o passo é 1/2/4/8 — **confirmar `vidup_imm` no QEMU**).
5. **Execução** de tudo acima, predicada por elemento (máscara da B16.2) + avanço do `VPT`/`ECI`.
6. Testes (ver Aceite).

## Não inclui

- `VLDR_VSTR` (B16.3) e as formas alargantes (B16.4).
- **`VCMPGT_fp_scalar`/`VCMPLE_fp_scalar`** (linhas 416-417) — dividem o bloco `{}` com
  `VDDUP`/`VDWDUP` mas são **B16.8**. Excluí-los explicitamente do decoder desta task (ver
  Armadilha 4).
- Emissão nativa/Truffle.

## Especificação

### Leitura obrigatória antes de codar

- `target/isa-decode/mve.decode`, linhas 53-73, 190-204 e 397-420 (todos os `&`/`@`/`%` citados
  acima, inteiros).
- `tasks/trilha-b-arquiteturas/b16.1-...md` (partição + Armadilha 6), `b16.3-...md` e `b16.4-...md`
  (`## Resultado` de ambas — decoder/executor de load/store MVE a estender).
- `core/src/main/java/dev/vitorsilverio/armjitter/decoder/NeonLoadStoreDecoder.java` — precedente de
  load/store vetorial com grupos de registradores consecutivos (NEON `VLD2`/`VLD3`/`VLD4` de 32
  bits). **Achado a confirmar na execução**: se a B13.x já implementou `VLD2`/`VLD4` NEON, a
  semântica de entrelaçamento pode ser reusada; o layout de registradores é diferente (NEON usa
  `D`, MVE usa `Q`), mas a permutação é a mesma ideia.
- QEMU real via `WebFetch`: `target/arm/tcg/translate-mve.c` (`trans_VLDR_S_sg`/`_U_sg`/`VSTR_sg`,
  `do_ldst_sg`/`do_ldst_sg_imm`, `trans_VLD2`/`VLD4`, `do_vldst_il`, `trans_VIDUP`/`VIWDUP`/
  `VDDUP`/`VDWDUP`, `vidup_imm`) e `target/arm/tcg/mve_helper.c` (as macros `DO_VLDR_SG`/
  `DO_VSTR_SG`, `DO_VLD2B`… e `DO_VIDUP`/`DO_VIWDUP`).

## Passos

1. Ler "Leitura obrigatória". Reconfirmar no QEMU: (a) `Qd==Qm` UNDEF nos gathers, (b) a regra de
   `msize` vs `size`, (c) `vidup_imm`, (d) como `pat` seleciona o pedaço em `VLD2`/`VLD4`.
2. Implementar decode + recusas de cada uma das 4 sub-famílias, **excluindo** explicitamente
   `VCMPGT_fp_scalar`/`VCMPLE_fp_scalar`.
3. Implementar os executores.
4. Testes (ver Aceite).
5. `mvn -o test` + `install` + G5 + `./gerar-cobertura-isa.sh` com delta medido (exatamente 15
   células; nada de B16.3/B16.4/B16.8/B16.13 pode mudar).
6. `INDICE.md`, `## Resultado`, commit `B16.5: ...`, `git push`.

## Aceite

- As 15 linhas decodificam sob `MVE_INTEGER`; `Qd==Qm` (gather de offsets), `Qd+n>7` (`VLD2`/
  `VLD4`), `Rn==15`, `Qd>7`/`Qm>7` e `Rn` ímpar / `Rm` par nos `dup` são recusados (G8).
- Gather: um vetor de offsets `Qm` produz 4 acessos independentes (testado com offsets fora de
  ordem, para provar que não é um acesso contíguo disfarçado), com `VLDR_S_sg` estendendo sinal e
  `VLDR_U_sg` estendendo zero quando `msize < size`.
- `os=1` escala o offset pelo tamanho do elemento; `os=0` não.
- `*_sg_imm`: a base é o VETOR `Qm` (extraído do campo normalmente chamado `Qn`), e o writeback
  (`w=1`) atualiza esse vetor.
- `VLD2`/`VLD4` desentrelaçam de fato: gravar um padrão conhecido em memória e conferir a
  distribuição pelos 2/4 registradores do grupo; `VST2`/`VST4` fazem o inverso (round-trip).
- `VIDUP`/`VDDUP` produzem `Rn, Rn±passo, Rn±2·passo, ...` e atualizam `Rn`; `VIWDUP`/`VDWDUP`
  envolvem (wrap) em `Rm` — testado o wrap.
- Predicação em todas as sub-famílias; `docs/COBERTURA-ISA.md` com exatamente 15 células novas;
  `mvn -o test` verde; G5 verde.

## Armadilhas

1. **"Qm is in the fields usually labeled Qn"** (comentário literal do arquivo, linha 67) nas formas
   `*_sg_imm`. Reusar `%qm` aqui dá o registrador errado sem nenhum sintoma de decode — é o mesmo
   tipo de armadilha da Armadilha 1 da B16.4.
2. **`Rn` par / `Rm` ímpar nos `dup` é restrição de ENCODING, não convenção.** `%vidup_rn` só
   produz valores pares e `%vidup_rm` só ímpares. Extrair `Rn`/`Rm` de 4 bits crus aqui é erro.
3. **`Thumb2NocpDecoder`/`Thumb2VfpDecoder`**: `bits[27:24] ∈ {1100, 1101}` (as formas `*_sg` e
   `VLD2`/`VLD4` são `111x 1100`/`1111 1101`) caem na **forma 2** do `NOCP` e no espaço de
   extension-register load/store do VFP. Mesmas Armadilhas 1/3 da B16.3 — ordem de registro
   obrigatória, conferida por teste.
4. **`VDDUP`/`VDWDUP` dividem o bloco `{}` com duas `VCMP*_fp_scalar` (B16.8).** O decodetree
   prioriza o padrão mais específico: `VCMPGT_fp_scalar`/`VCMPLE_fp_scalar` têm `bits[21:20]=11`
   fixo e `bits[7:4]` fixos, `VDDUP` tem `bits[7:4]` parcialmente fixos com `size` livre. Máscara
   larga demais nesta task rouba as células da B16.8 — **excluir explicitamente**, mesma técnica das
   máscaras `VLLDM_VLSTM_MASK`/`VSCCLRM_MASK` da B15.2.
5. **`VLD4` move um grupo de 4 `Q` = TODO o meio do banco.** Com só `Q0`-`Q7`, `VLD4` com `Qd=Q6`
   seria `Q6..Q9` — UNDEF. Checar `Qd + grupo - 1 <= 7`, não só `Qd <= 7`.
6. **`pat` não é "qual registrador"**: `VLD2`/`VLD4` são executadas em múltiplas instruções (uma por
   `pat`), cada uma movendo uma fatia. Implementar como se `pat` selecionasse um registrador
   produziria resultado plausível e errado — ler `do_vldst_il` no QEMU.
7. **G4/predicação e writeback**: writeback de `Rn`/`Qm` não é predicado.

## Não fazer

- Não implementar `VCMPGT_fp_scalar`/`VCMPLE_fp_scalar` (B16.8) nem as reduções/`VMLADAV`
  (B16.13), mesmo que o épico as tenha contado neste intervalo.
- Não reusar `%qm` nas formas `*_sg_imm`.
- Não extrair `Rn`/`Rm` de 4 bits crus nos `dup`.
- Não tratar gather como acesso contíguo "otimizado".

## Resultado

**FECHADA 2026-09-17.** As 15 linhas (gather/scatter por offsets + gather/scatter com base
vetorial e imediato + `VLD2`/`VLD4`/`VST2`/`VST4` + `VIDUP`/`VDDUP`/`VIWDUP`/`VDWDUP`) decodificam e
executam sob `ArmFeature.MVE_INTEGER`.

**Bits confirmados por leitura DIRETA de `target/isa-decode/mve.decode`** (via `curl` real, não
`WebFetch` resumido — o resumo cortava trechos longos de código; a rodada usou o arquivo bruto
para recontar cada `@format`/instrução bit a bit, mesma disciplina de off-by-one da B16.3):
`@vldst_sg` (`qd=%qd`, `qm=%qm`, `rn:4`, `size:2`, `msize=%sg_msize` (`bit6`++`bit4`), `os:1`);
`@vldst_sg_imm` (`qm` vem de `%qn`, não `%qm` — Armadilha 1 confirmada); `@vldst_il` (`qd=%qd`,
`rn:4`, `size:2`, `pat:2`, `w:1`, marcador de grupo em `bits[4:0]`); `@vidup`/`@viwdup`
(`%vidup_rn 17:3 !function=times_2`, `%vidup_rm 1:3 !function=times_2_plus_1`, `%imm_vidup 7:1 0:1
!function=vidup_imm` = `1 << x`).

**3 decoders novos**, todos registrados **antes** de `Thumb2NocpDecoder` em `ARMV8_1M_MVE` (mesma
Armadilha 3 da B16.3/B16.4 — os prefixos `111x 1100`/`1111 1101`/`1111 1100`/`1110 1110` colidem
com formas 1/2 de `NOCP`):

1. **`Thumb2MveGatherScatterDecoder`** — as 3 formas de offset (`VLDR_S_sg`/`VLDR_U_sg`/`VSTR_sg`)
   e as 4 formas imediatas (`VLDRW/D_sg_imm`, `VSTRW/D_sg_imm`). Tabela `(msize, size, os)` válida
   transcrita das 3 tabelas `fns[os][msize][size]` reais de `translate-mve.c`
   (`trans_VLDR_S_sg`/`_U_sg`/`VSTR_sg`), medida por leitura direta, não suposta — `VLDR_S_sg`
   nunca tem `msize == size` (sempre alarga), `VLDR_U_sg`/`VSTR_sg` compartilham a mesma forma
   (incluindo o par `msize=3,size=3` da forma de 64 bits). `Qd == Qm` UNDEF nos gathers (offset E
   imediato, quando `load`); `Rn == 15` UNDEF na forma de offsets.
2. **`Thumb2MveInterleavedLoadStoreDecoder`** — `VLD2`/`VLD4`/`VST2`/`VST4`. `Qd + grupo - 1 <= 7`
   (Armadilha 5), `pat > 1` recusado para grupo `2` (`fns[2]`/`fns[3]` são `NULL` reais em
   `trans_VLD2`/`trans_VST2`), `size == 3` recusado sempre (nenhuma tabela real tem entrada).
3. **`Thumb2MveIncrementDupDecoder`** — `VIDUP`/`VDDUP` (sem `Rm`, discriminados por `bits[3:1] ==
   111`) e `VIWDUP`/`VDWDUP` (`Rm` cru nesses mesmos bits, qualquer valor ≠ `111`). `VDDUP` nega o
   `imm` no PRÓPRIO decode (replicando `trans_VDDUP` real: `a->imm = -a->imm`); `VIWDUP`/`VDWDUP`
   NÃO negam — a direção vira o campo `decrement` do `IrOp` (`do_add_wrap`/`do_sub_wrap` reais são
   funções distintas, não uma inversão de sinal). `Rm ∈ {13, 15}` recusado — `15` é na prática
   INALCANÇÁVEL por este encoding (`rmField=0b111` é exatamente o literal que seleciona
   `VIDUP`/`VDDUP`), documentado no teste como achado, não removido (defensivo, espelha o QEMU).

**5 `IrOp` novos + 1 auxiliar** (`IrOp.Kind` 121→127, todos interpretados apenas — "Não inclui"
explícito, mesmo padrão de toda a escada B16): `MveGatherScatterOffset`, `MveGatherScatterImmediate`,
`MveInterleavedLoadStore`, `MveIncrementDup`, `MveWrappingIncrementDup` e `AdvanceEci`.

**Achado que corrige a Aceite implícita da própria task (medido contra o QEMU real, não suposto)**:
`VLD2`/`VLD4`/`VST2`/`VST4` são "beatwise mas **não predicadas**" (comentário literal de
`mve_helper.c`) — usam só `mve_eci_mask`, nunca `mve_element_mask`/`VPR`. O gancho pós-instrução
correto NÃO é `mve_advance_vpt` (que inverteria/deslocaria `VPR.MASK01`/`MASK23` mesmo quando essas
instruções nunca os consultam), e sim `mve_update_and_store_eci`, que cicla só o nibble de `ECI`.
Isso exigiu um `IrOp`/executor **novo** (`AdvanceEci`/`MveVptState.advanceEciOnly`), distinto de
`AdvanceVpt` — reusar `AdvanceVpt` teria corrompido silenciosamente um `VPST` em andamento na
sessão seguinte a um `VLD2`/`VLD4` (bug de fidelidade que só apareceria com um `VPST` ativo
sobrepondo um desentrelaçamento, cenário raro mas real). Testado explicitamente
(`advancesEciOnlyWithoutTouchingVprMask`).

**`VLD2`/`VLD4`/`VST2`/`VST4` transcritos verbatim das tabelas `off[]`/aritmética de bits de
`DO_VLD2B`/`DO_VLD4B`/`DO_VLD2H`/`DO_VLD4H`/`DO_VLD2W`/`DO_VLD4W` (e as 6 formas `VST*` espelho)
de `target/arm/tcg/mve_helper.c`**, em vez de re-derivar a semântica arquitetural do zero — o layout
de bits real (offsets `{0,1,10,11}` etc.) existe por uma otimização de pipelining de hardware (um
acesso de 32 bits por beat, ordem de estruturas não-sequencial) documentada só no código, não no
manual arquitetural; transcrever era a única forma confiável de reproduzir byte a byte. Testado com
round-trip completo (`VLD2`/`VLD4` seguido de `VST2`/`VST4` recuperando os mesmos bytes) para byte,
halfword e word.

**Gather/scatter de 64 bits (`VLDRD_sg`/`VLDRD_sg_imm`/etc.) usa o mesmo truque de par de acessos
de 32 bits que `DO_VLDR64_SG`/`DO_VSTR64_SG` reais** (offset lido só das lanes PARES de 32 bits de
`Qm`, cada resultado de 64 bits composto por dois acessos de 32 bits consecutivos) — implementado
como ramo separado em `executeMveGatherScatterOffset`/`executeMveGatherScatterImmediate` (não uma
generalização do caminho `registerSizeLog2 < 3`, que usa granularidade de elemento real).

**Writeback**: nos gathers de OFFSET não existe (`@vldst_sg` não tem campo `w`). Nos gathers
IMEDIATOS é POR LANE (`if (WB) { m[e] = addr; }` dentro do `if (eci_mask)` real — cada lane ativa
por `ECI` recebe seu próprio endereço calculado de volta em `Qm`, independente do `elementMask` de
`VPT`), diferente do writeback escalar único de `Rn` da B16.3/B16.4. Em `VLD2`/`VLD4`/`VST2`/`VST4`
é escalar em `Rn` (`+= groupSize*16`) e incondicional (G4), mesmo padrão da B16.3/B16.4.

`docs/COBERTURA-ISA.md`: zero-diff, CONFIRMADO — mesma Armadilha 7 da B16.2-B16.4 (`mve.decode`
continua `NOT_IN_ANY_PRESET` até a B16.14). `docs/COBERTURA-JIT.md`: `IrOp.Kind` 121→127 (todos
interpretados, `TruffleCodeEmitterSupportsCoherenceTest` atualizado: 127 `Kind` totais, 61
descobertos sem nó Truffle). `mvn -o test` verde (4085; as mesmas 3 falhas pré-existentes de
mojibake, `Aarch64Fp16VersionCurationTest`×2 + `IsaCoverageReportA64CurationGuardTest`×1) +
`truffle` (73) + `install`. **G5 completo** nos 5 consumidores (gbaemu, ndsemu, armbox,
virtual-arm-box, n3dsemu — todos verdes, zero regressão, reinstalados localmente com o código desta
task).

**Testes**: 3 arquivos de decoder (`Thumb2MveGatherScatterDecoderTest` 18,
`Thumb2MveInterleavedLoadStoreDecoderTest` 13, `Thumb2MveIncrementDupDecoderTest` 8) + 3 arquivos
de execução fim-a-fim (`MveGatherScatterTest` 7, `MveInterleavedLoadStoreTest` 6,
`MveIncrementDupTest` 5) — 57 testes novos, cobrindo cada item do Aceite: gather com offsets fora
de ordem (prova de que não é acesso contíguo disfarçado), extensão de sinal/zero, escalonamento por
`os`, base vetorial + writeback por lane na forma imediata, round-trip de desentrelaçamento byte/
halfword/word, avanço de `ECI` sem tocar `VPR`, `VIDUP`/`VDDUP`/`VIWDUP`/`VDWDUP` com wrap e
predicação por elemento (`Rn` acumula mesmo com lanes mascaradas — só `Qd` usa `mergemask`).

**Pegável a seguir**: `B16.6` (vector 2-op inteiro, depende de B16.2 ✅) é o próximo degrau natural
da escada MVE. `C12.5`/`C12.10` (emissão JIT nativa A64) seguem pegáveis, dimensão 2 do roadmap.
