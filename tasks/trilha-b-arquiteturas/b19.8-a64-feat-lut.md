# B19.8 — A64: `FEAT_LUT` (`LUTI2`/`LUTI4`, consulta de tabela por lane) — 4 linhas

**Trilha:** B · **Repo:** arm-jitter (+ revalidação G5) · **Depende de:** —
**Épico:** `b19-plano-a64-gap-remanescente.md` · **Status:** ✅ 2026-09-05

## Contexto

Degrau pequeno e autocontido do épico B19: as 4 linhas de `FEAT_LUT` (ARMv9.5-A), medidas `❌` nas
16 colunas A64.

```
LUTI2_1b   0100 1110 100 rm:5 0 idx:2  100 rn:5 rd:5   &rrx_e esz=0   (:1381)
LUTI2_1h   0100 1110 110 rm:5 0 idx:3   00 rn:5 rd:5   &rrx_e esz=1   (:1382)
LUTI4_1b   0100 1110 010 rm:5 0 idx:1 1000 rn:5 rd:5   &rrx_e esz=0   (:1384)
LUTI4_2h   0100 1110 010 rm:5 0 idx:2  100 rn:5 rd:5   &rrx_e esz=1   (:1385)
```

Elas moram **ao lado de `TBL`/`TBX`** (`:1379`), no espaço `bit21 = 0` que a **B8.10** já abriu e
trata. É a mesma ideia — indexar uma tabela em registrador — mas com o índice vindo de um campo
IMEDIATO em vez de um vetor de índices.

### O que `LUTI` faz

`LUTI2`/`LUTI4` fazem **consulta de tabela por lane com índices EMPACOTADOS**: cada elemento do
registrador de índices `Rn` carrega vários índices de 2 bits (`LUTI2`) ou 4 bits (`LUTI4`), e o campo
imediato `idx` seleciona **qual** dos grupos empacotados usar nesta instrução. A tabela é `Rm`.

- `LUTI2` — índices de **2 bits** ⇒ tabela de 4 entradas.
- `LUTI4` — índices de **4 bits** ⇒ tabela de 16 entradas.
- Sufixo `_1b`/`_1h`/`_2h` — quantos registradores de tabela e a largura do elemento
  (`esz=0` byte, `esz=1` halfword).

**Transcrever a semântica do ARM DDI 0487 (`FEAT_LUT`, `LUTI2`/`LUTI4`), não deduzir do nome.** O
empacotamento do índice e a seleção pelo imediato são exatamente o tipo de detalhe que a B13.3 já
mostrou não sobreviver a transcrição de memória (a spec dizia `{4,1,2}`, o QEMU real tinha `{2,2,2}`).

**Leitura obrigatória**: a **B8.10** (`## Resultado` — `TBL`/`TBX` e o espaço `bit21=0`), a **B11.4**
(padrão de gate real), e o ARM DDI 0487 na seção `FEAT_LUT`. No código:
`decoder64/Aarch64Decoder#decodeAdvancedSimdExtractPermuteTable` (onde `TBL`/`TBX` são tratadas — é
o vizinho e provavelmente o lugar certo para entrar), `arch64/Aarch64Feature`,
`arch64/Aarch64Architecture.ARMV9_5_A`, `advsimd/AdvSimdLanes`.

## Objetivo

As 4 linhas decodificam e executam sob `ARMV9_5_A`, gateadas por uma feature nova
`Aarch64Feature.LOOKUP_TABLE`, e são recusadas em toda versão anterior.

## Inclui

1. **`Aarch64Feature.LOOKUP_TABLE`** — constante NOVA (`FEAT_LUT`), declarada em `ARMV9_5_A`.
   Javadoc `///` PT. **Conferir a versão real na fonte** antes de declarar (esta spec diz ARMv9.5-A;
   se o manual disser outra, vale o manual e registra-se o achado).
2. **Decode das 4 linhas**, no vizinho de `TBL`/`TBX`, com gate.
3. **Executor** — consulta de tabela por lane. Sem record novo se algum existente servir; caso
   contrário, `Ir64Op` novo + `Kind` novo (próximo contíguo depois de `VECTOR_FP_CONVERT_PRECISION = 95`,
   isto é **96** — conferir, porque outras tasks podem ter consumido antes).
4. **Requisito de versão** em `IsaCoverageReport.AARCH64_VERSION_REQUIREMENTS` para os 4 mnemônicos
   (`LUTI2_1b`, `LUTI2_1h`, `LUTI4_1b`, `LUTI4_2h`) → `LOOKUP_TABLE`, para elas medirem `·` (não
   `❌`) nas 15 colunas anteriores a `ARMv9.5-A`.
5. Testes (ver Aceite).

## Não inclui

- **`LUTI` de SVE/SME** (`sve.decode`/`sme.decode`) — grupos `NOT_IN_ANY_PRESET`, épicos B17/B18.
- **`TBL`/`TBX`** — já implementadas (B8.10); não mexer.
- **Emissão nativa.**

## Especificação

### Discriminação dos 4 encodings

Todos partilham `0100 1110` (bits[31:24]) e `bit21 = 0`. Depois disso:

| Linha | bits[23:22] | campo `idx` | bits fixos após `idx` | `esz` |
|---|---|---|---|---|
| `LUTI2_1b` | `10` | 2 bits | `100` | 0 (byte) |
| `LUTI2_1h` | `11` | 3 bits | `00` | 1 (halfword) |
| `LUTI4_1b` | `01` | 1 bit | `1000` | 0 (byte) |
| `LUTI4_2h` | `01` | 2 bits | `100` | 1 (halfword) |

⚠️ **`LUTI4_1b` e `LUTI4_2h` compartilham bits[23:22] = `01`** e são separadas só pelo tamanho do
campo `idx` e pelos bits fixos que o seguem. Decodificar por igualdade exata do padrão completo, não
por bits[23:22] — é a armadilha 1.

### Colisão com `TBL`/`TBX`

`TBL_TBX` é `0 q:1 00 1110 000 rm:5 0 len:2 tbx:1 00 rn:5 rd:5` — bits[23:21] = `000`. As `LUTI`
têm bits[23:22] ∈ {`10`,`11`,`01`}, isto é **nunca `00`**. Separação limpa por bits[23:22]; conferir
ao inserir.

## Passos

1. Ler "Leitura obrigatória" + `a64.decode:1379-1386` + a seção `FEAT_LUT` do ARM DDI 0487.
2. Feature nova + declaração em `ARMV9_5_A`.
3. Decode + executor.
4. Corpus golden com `aarch64-none-elf-as -march=armv9.5-a+lut` (devkitA64) — **se o assembler não
   suportar `+lut`, montar os encodings à mão a partir do padrão e DIZER isso no `## Resultado`**
   (é aceitável; o padrão do projeto é preferir o assembler quando ele existe).
5. Requisito de versão em `IsaCoverageReport`.
6. Testes. `mvn -o test` + `install` + G5 + `./gerar-cobertura-isa.sh`.
7. `INDICE.md`, `## Resultado`, commit `B19.8: …`, `git push`.

## Aceite

- As 4 linhas decodificam sob `ARMV9_5_A` com `esz`, `idx` e registradores corretos, e seguem
  `unsupported` em `ARMV9_4_A` e abaixo.
- **`TBL`/`TBX` continuam decodificando** — suíte da B8.10 sem edição.
- **`LUTI4_1b` × `LUTI4_2h`**: teste provando que os dois encodings com bits[23:22]=`01` decodificam
  para instruções DIFERENTES (a armadilha 1).
- **Execução**: para cada uma das 4, uma tabela conhecida em `Rm`, índices empacotados conhecidos em
  `Rn`, e o resultado conferido lane a lane contra a fórmula do manual; incluindo um `idx` diferente
  de 0 (prova que o imediato seleciona o grupo certo).
- `docs/COBERTURA-ISA.md`: as 4 viram `✅` em `ARMv9.5-A` e `·` nas 15 anteriores (não `❌`).
- `mvn -o test` verde + G5 verde nos 5 consumidores.

## Armadilhas

1. **`LUTI4_1b` e `LUTI4_2h` têm os mesmos bits[23:22].** Casar o padrão inteiro.
2. **O índice é EMPACOTADO.** Vários índices por elemento de `Rn`, e o imediato `idx` escolhe o
   grupo. Tratar `Rn` como "um índice por lane" (como em `TBL`) dá resultado errado.
3. **Transcrever do manual.** Precedente B13.3: tabela transcrita de memória estava errada.
4. **Feature nova precisa de versão conferida.** Declarar `FEAT_LUT` na versão errada corrompe o
   denominador de 16 colunas.
5. **Sem o requisito em `IsaCoverageReport`**, as 4 medem `❌` em v8.0-v9.4, onde a feature não
   existe — o erro que a B19.5.2/E12 existem para evitar.

## Não fazer

- Não implementar `LUTI` de SVE/SME.
- Não mexer em `TBL`/`TBX`.
- Não declarar a feature em versão não conferida.
- Não adicionar caso nativo em `Ir64NativePolicy`.

## Resultado (2026-09-05)

Implementado como especificado, com uma correção de semântica achada durante a implementação (ver
abaixo) e um desvio de ferramenta (assembler sem suporte, corpus montado à mão).

- **Versão confirmada por fonte primária**: `ARMv9.5-A`/`FEAT_LUT` confirmado contra o commit real
  do QEMU `5fbdd62ee22f929400a623b4a1725dea83b6da70` ("target/arm: Implement LUTI2, LUTI4 for
  AdvSIMD", mesma série de patches de SME2p1/SVE2p1/Armv9.5-A), não só transcrito da spec.
  `Aarch64Feature.LOOKUP_TABLE` novo, declarado em `ARMV9_5_A`.
- **Achado que corrige a spec original**: o texto da task dizia "os índices vêm de `Rn`, a tabela é
  `Rm`" — **invertido**. Lendo o fonte real do QEMU (`target/arm/tcg/translate-a64.c`,
  `do_lut_1`/`TRANS_FEAT`, e `target/arm/tcg/vec_helper.c`, `HELPER(gvec_luti2_b)` → `do_lut_b(zd,
  vm, vn, ...)`), o argumento `indexes` do helper recebe o operando **`Rm`** e `table` recebe
  **`Rn`** — exatamente a MESMA convenção que `VectorTableLookup` (`TBL`/`TBX`, B8.10) já usa neste
  projeto (`Rn`=tabela, `Rm`=índices; conferido em
  `Ir64VectorArithmeticExecutor#executeTableLookup`). Implementado com a convenção CORRETA
  (`Rn`=tabela, `Rm`=índices), documentado no javadoc de `Ir64Op.VectorLookupTable` e no javadoc do
  executor para a próxima sessão não reintroduzir o erro.
- **Decode** (`Aarch64Decoder`): interceptado no topo de `decodeAdvancedSimdExtractPermuteTable`
  (mesmo padrão da B11.4 para `FEAT_RDM` — checado ANTES de `EXT`/permute/`TBL`/copy), gateado por
  `architecture.has(Aarch64Feature.LOOKUP_TABLE)`. Método novo
  `decodeAdvancedSimdLookupTable`: discrimina as 3 famílias por bits[23:21]
  (`ADVSIMD_LUTI2_1B_PATTERN`/`ADVSIMD_LUTI2_1H_PATTERN`/`ADVSIMD_LUTI4_PATTERN`) e, dentro de
  `LUTI4`, `LUTI4_1b` de `LUTI4_2h` pelo campo bits[14:10] inteiro (`idx`+bits fixos juntos, nunca só
  bits[23:22] — a armadilha 1 da spec), com um padrão reservado (`bit12=0` mas `bit13≠1`) que
  continua `unsupported` mesmo com a feature presente (G8, testado). **Zero colisão confirmada**: os
  4 padrões sempre têm bits[23:22] ∈ {`10`,`11`,`01`}, nunca `00`, então caíam no ramo `TBL`
  (`esz≠0` → reservado) antes desta task — mesma prova que a B11.4 fez para `FEAT_RDM`.
- **IR/executor**: `Ir64Op.VectorLookupTable` novo (`Kind.VECTOR_LOOKUP_TABLE = 101` — **não** 96,
  como a spec original supunha; o `Kind` máximo já estava em 100 por causa da B19.6/B8.6,
  confirmado por grep antes de escolher o número, exatamente a checagem que a spec pedia).
  `Ir64VectorArithmeticExecutor#executeLookupTable`: extrai os índices empacotados dos 128 bits
  crus de `Rm` (`low64`/`high64`, nunca cruza a fronteira de 64 bits porque `indexBits` ∈ {2,4}
  sempre divide `64`) e lê a tabela de `Rn`/`(Rn+1)%32` com a MESMA fórmula de
  `tableReg`/`lane` que `executeTableLookup` já usa. Sem `Kind` novo em `Ir64NativePolicy`
  (interpretado, como especificado).
- **Cobertura por versão**: 4 entradas novas em `IsaCoverageReport.AARCH64_VERSION_REQUIREMENTS`.
  `./gerar-cobertura-isa.sh` rodado: as 4 linhas viram `✅` em `ARMv9.5-A` e `·` (não `❌`) nas 15
  versões anteriores. Global 84%→**85%** (16453/19319), `ARMv9.5-A` 901→**905**/1146 — delta
  isolado desta task, sem cruzar os gatilhos de release (suspensos, ver `tasks/README.md`).
  `./gerar-cobertura-jit.sh` também rodado (achado incidental: o script precisa de `mvn install`
  do `core` antes, senão o `exec:java` do módulo `truffle` resolve um jar velho do repositório
  local e o `Kind` novo não aparece na tabela — documentado aqui para a próxima sessão não perder
  tempo com isso) — `VECTOR_LOOKUP_TABLE` aparece `❌`/`❌` (ASM 64 bits 46→ mesmo 46 de 96→102,
  denominador só cresceu).
- **Testes**: `Aarch64LutiDecoderTest` (8 testes: rejeição em `ARMv8.0-A` e `ARMv9.4-A` para as 8
  palavras do corpus, regressão de `TBL`/`TBX`, padrão reservado dentro de `LUTI4` continua
  `unsupported` mesmo com a feature, e as 4 formas decodificando com 2 valores de `idx` cada) +
  `Ir64VectorLookupTableExecutorTest` (4 testes, um por forma, cada um com um padrão de índice
  DIFERENTE por grupo empacotado para que uma seleção de grupo errada produza um resultado
  observável — o de `LUTI4_2h` prova a travessia para `Rn+1`). Corpus **montado à mão**: o
  assembler do devkitA64 disponível nesta sessão (binutils 2.46.0.20260210) rejeita a sintaxe
  AdvSIMD (`luti2 {v0.16b},{v1.16b},v2[1]` → "expected an SVE vector register"), então as palavras
  foram derivadas bit a bit do encoding real do QEMU (mesmo padrão de fallback já previsto na spec).
- `mvn -o test` verde (core 3140 testes + truffle 73 testes) + `mvn -o install`. G5 completo:
  `gbaemu` (240 testes), `ndsemu` (183), `armbox` (47, inclusive a falha pré-existente documentada
  de `Armv7TortureTest` NÃO reproduziu desta vez), `virtual-arm-box` (87) e `n3dsemu` (221) —
  todos `BUILD SUCCESS`, zero-diff esperado (nenhum usa `ARMv9.5-A`).
