# B13.21 — `neon-shared`: BF16 (`VDOT_b16`/`VFMA_b16`/`VMMLA_b16` + `_scal`) — 5 linhas · **fecha o arquivo**

**Trilha:** B · **Repo:** arm-jitter (+ revalidação G5) · **Depende de:** B13.19, B19.7
**Épico:** `b13-plano-neon-a32.md` · **Status:** ⬜

## Contexto

**Última task de `neon-shared.decode`** — com ela o arquivo fica completo (4 + 7 + 3 + 4 + 5 = 23).

```
VDOT_b16       1111 110 00 . 00 .... .... 1101 . q:1 . 0 ....   (:57)
VMMLA_b16      1111 1100 0.00 .... .... 1100 .1.0 ....          (:71)
VFMA_b16       1111 110 0 0.11 .... .... 1000 . q:1 . 1 ....    (:74)
VDOT_b16_scal  1111 1110 0 . 00 .... .... 1101 . q:1 index:1 0 vm:4   (:89)
VFMA_b16_scal  1111 1110 0.11 .... .... 1000 . q:1 . 1 . vm:3         (:99)
```

`FEAT_BF16` (ARMv8.6-A). **Depende da B19.7**, que cria o núcleo de conversão `bfloat16` — o JDK não
tem esse formato (ao contrário de `binary16`), então a conversão com round-to-nearest-even é código
próprio, e escrevê-lo duas vezes seria exatamente o que a RFC B13.2 existe para evitar.

### As três famílias, e onde cada uma já tem irmã

| Família | Irmã A64 | Estado |
|---|---|---|
| `VDOT_b16` / `_scal` | `BFDOT_v` / `BFDOT_vi` | **B19.7** |
| `VMMLA_b16` | `BFMMLA` | **B19.7** |
| `VFMA_b16` / `_scal` | `BFMLAL_v` / `BFMLAL_vi` | **B19.7** |

⇒ se a B19.7 já rodou, esta task é **decode puro** sobre o núcleo dela. É a razão de a dependência
ser obrigatória e não sugerida.

**Leitura obrigatória**: a **B19.7** INTEIRA (`## Resultado` — o núcleo `bfloat16` e as três
famílias), a **B13.19** (`## Resultado` — o `NeonSharedDecoder` e a semântica matricial, que
`VMMLA_b16` reusa com outro tipo de elemento), a **B13.17** (o decoder) e a **E10**. No código:
`advsimd/` (o núcleo `bfloat16` da B19.7), `decoder/NeonSharedDecoder`, `core/ArmFeature`.

## Objetivo

As 5 linhas decodificam e executam em A32/T32, gateadas por uma `ArmFeature` nova (`BFLOAT16`,
espelhando o nome A64) ⇒ zero-diff de runtime e `docs/COBERTURA-ISA.md` byte a byte idêntica.

**E, ao fechar, trocar o `null` residual do `NeonSharedDecoder` por `unimplemented`** — a dívida que
a B13.17 registrou. Esta é a última task do arquivo, então é aqui que ela vence.

## Inclui

1. **`ArmFeature.BFLOAT16`** — constante nova, nenhum preset a declara.
2. **Decode das 5** no `NeonSharedDecoder`, consumindo o último espaço em `null`.
3. **Reuso do núcleo `bfloat16`** da B19.7 — conversão, produto escalar, matricial e
   multiply-accumulate long. **Se algo não estiver no núcleo, pôr lá** (não no decoder A32).
4. **Fechar o arquivo**: o que não for reconhecido por B13.17-B13.21 dentro do frame `neon-shared`
   passa a `unimplemented` (G8). Registrar a dívida como paga.
5. `IrOp`/`Kind`/dispatch/`AsmNativePolicy`/coerência Truffle.
6. Testes (ver Aceite).

## Não inclui

- **As outras 18 linhas do arquivo** (B13.17-B13.20) — pré-requisitos.
- **BF16 do A64** ⇒ B19.7.
- **Preset** ⇒ B13.22 · **nativo/Truffle** ⇒ C12.8/A10.7.

## Especificação

### As três semânticas (todas em `bfloat16`, acumulando em `f32`)

- **`VDOT_b16`** — produto escalar de **pares** de `bf16` por lane de 32 bits:
  `Vd[i] += Vn[2i]*Vm[2i] + Vn[2i+1]*Vm[2i+1]`, com a multiplicação em precisão de `f32`.
- **`VMMLA_b16`** — produto de matriz 2×4 · 4×2 de `bf16` acumulando em 2×2 de `f32`. Mesma forma da
  B13.19, com elemento diferente (`bf16` em vez de `int8`) e **4** em vez de 8 na dimensão interna.
- **`VFMA_b16`** — multiply-accumulate long: lanes `bf16` → acumulador `f32`.

⚠️ **A dimensão interna de `VMMLA_b16` é 4, não 8** (`bf16` tem 16 bits, `int8` tem 8) — copiar a
fórmula da B13.19 sem ajustar produz resultado errado.

### `VMMLA_b16` é só de 128 bits

Como `VSMMLA` (B13.19). `Q` ímpar ⇒ UNDEFINED; não há forma `D`.

## Passos

1. **Confirmar B19.7 ✅**; se não, parar (a dependência é do núcleo, não formal). Ler "Leitura
   obrigatória" + `neon-shared.decode:57-58, 71-75, 89-91, 99-101`.
2. Feature nova + decode das 5.
3. Reuso do núcleo (ou complemento dele).
4. **Fechar o `null` residual do arquivo.**
5. Testes. Golden com `arm-none-eabi-as -march=armv8.6-a+bf16` (se não aceitar, montar à mão e
   registrar).
6. `mvn -o test` + `install` + G5 + `./gerar-cobertura-isa.sh` com diff **VAZIO**.
7. `INDICE.md`, `## Resultado`, commit `B13.21: …`, `git push`.

## Aceite

- As **5 linhas** decodificam com família, `q`, índice e registradores corretos; e seguem
  `UNIMPLEMENTED` sem `BFLOAT16`.
- **Encoding compartilhado**: mesma palavra decodifica igual em A32 e T32.
- **`VMMLA_b16`**: `Q` ímpar ⇒ `UNIMPLEMENTED`; sem forma `D`; matriz **assimétrica** conferida
  elemento a elemento, com **dimensão interna 4** (não 8).
- **Execução**: `VDOT_b16` acumulando pares; `VFMA_b16` acumulando (pré-preencher `Vd`);
  conversão `bf16→f32` exata; **`Vd == Vn`** (aliasing/E10); formas `_scal` com índice nos extremos.
- **Zero-diff A64**: a suíte da B19.7 sem edição (o núcleo é compartilhado).
- **Arquivo fechado**: um encoding não reconhecido dentro do frame `neon-shared` agora devolve
  `UNIMPLEMENTED` (não `null`) — teste explícito, e a dívida da B13.17 registrada como paga.
- `docs/COBERTURA-ISA.md` **byte a byte idêntica**.
- `mvn -o test` verde + G5 verde nos 5 consumidores.

## Armadilhas

1. **Dimensão interna de `VMMLA_b16` é 4**, não 8 como em `VSMMLA`.
2. **Matriz simétrica esconde transposição errada** (herdada de B13.19/B19.12).
3. **`bfloat16` ≠ `binary16`.** Núcleos diferentes; o do `bf16` vem da B19.7 e não existe no JDK.
4. **Esta é a task que fecha o `null` do arquivo** — esquecer deixa um buraco onde encodings
   desconhecidos escapam do G8.
5. **Largura mista ⇒ aliasing** (E10).
6. **`AsmNativePolicy` + coerência Truffle** (armadilha herdada).

## Não fazer

- Não reimplementar a conversão `bfloat16` (usar o núcleo da B19.7).
- Não copiar a fórmula matricial da B13.19 sem ajustar a dimensão.
- Não deixar o `null` residual.
- Não declarar a feature em preset (B13.22).
