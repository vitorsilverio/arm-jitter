# B17.4 — Predicados: lógica, misc, partition break, contagem e element count — 40 encodings

**Trilha:** B · **Repo:** arm-jitter (+ revalidação G5) · **Depende de:** B17.3 (⬜)
**Épico:** `b17-plano-sve.md` · **Status:** ⬜ (spec escrita 2026-09-14, ainda não executada)

## Contexto

Primeiro degrau de conteúdo do épico B17, e **pré-requisito de quase tudo**: sem predicado não há
SVE. Todas as tasks B17.5-B17.25 dependem, direta ou indiretamente, do estado `P<n>`/`FFR` que a
B17.3 criou e das operações que esta task implementa.

### Recorte normativo (ver a tabela completa em `b17.1-aarch64-feature-sve-sve2.md`)

| Seção de `sve.decode` | Linhas | Enc |
|---|---|---:|
| `### SVE Predicate Logical Operations Group` | 795-802 | 8 |
| `### SVE Predicate Misc Group` | 807-832 | 10 |
| `### SVE Partition Break Group` | 837-847 | 7 |
| `### SVE Predicate Count Group` | 852-869 | 9 |
| `### SVE Element Count Group` | 544-559 | 6 |
| **Total** | | **40** |

### Os encodings reais (`target/isa-decode/sve.decode`, medidos nesta rodada)

```
@pd_pg_pn_pm_s  ........ . s:1 .. rm:4 .. pg:4 . rn:4 . rd:4    &rprr_s

AND_pppp        00100101 0. 00 .... 01 .... 0 .... 0 ....       @pd_pg_pn_pm_s
BIC_pppp        00100101 0. 00 .... 01 .... 0 .... 1 ....       @pd_pg_pn_pm_s
EOR_pppp        00100101 0. 00 .... 01 .... 1 .... 0 ....       @pd_pg_pn_pm_s
SEL_pppp        00100101 0. 00 .... 01 .... 1 .... 1 ....       @pd_pg_pn_pm_s
ORR_pppp        00100101 1. 00 .... 01 .... 0 .... 0 ....       @pd_pg_pn_pm_s
ORN_pppp        00100101 1. 00 .... 01 .... 0 .... 1 ....       @pd_pg_pn_pm_s
NOR_pppp        00100101 1. 00 .... 01 .... 1 .... 0 ....       @pd_pg_pn_pm_s
NAND_pppp       00100101 1. 00 .... 01 .... 1 .... 1 ....       @pd_pg_pn_pm_s
```

```
PTEST           00100101 01 010000 11 pg:4 0 rn:4 0 0000
PTRUE           00100101 esz:2 01100 s:1 111000 pat:5 0 rd:4
PTRUE_cnt       00100101 esz:2 1000000111100000010 ...          rd=%pnd
SETFFR          00100101 0010 1100 1001 0000 0000 0000
PFALSE          00100101 0001 1000 1110 0100 0000 rd:4
RDFFR_p         00100101 0 s:1 0110001111000 pg:4 0 rd:4
RDFFR           00100101 0001 1001 1111 0000 0000 rd:4
WRFFR           00100101 0010 1000 1001 000 rn:4 00000
PFIRST          00100101 01 011 000 11000 00 .... 0 ....        @pd_pn_e0
PNEXT           00100101 .. 011 001 11000 10 .... 0 ....        @pd_pn
```

```
BRKPA           00100101 0. 00 .... 11 .... 0 .... 0 ....       @pd_pg_pn_pm_s
BRKPB           00100101 0. 00 .... 11 .... 0 .... 1 ....       @pd_pg_pn_pm_s
BRKA_z          00100101 0. 01000001 .... 0 .... 0 ....         @pd_pg_pn_s
BRKB_z          00100101 1. 01000001 .... 0 .... 0 ....         @pd_pg_pn_s
BRKA_m          00100101 00 01000001 .... 0 .... 1 ....         @pd_pg_pn_s0
BRKB_m          00100101 10 01000001 .... 0 .... 1 ....         @pd_pg_pn_s0
BRKN            00100101 0. 01100001 .... 0 .... 0 ....         @pd_pg_pn_s
```

```
CNTP            00100101 ..    100 000 10 ....     0 .... ..... @rd_pg4_pn
CNTP_c          00100101 esz:2 100 000 10 000 vl:1 1 rn:4 rd:5
FIRSTP          00100101 ..    100 001 10 ....     0 .... ..... @rd_pg4_pn
LASTP           00100101 ..    100 010 10 ....     0 .... ..... @rd_pg4_pn
INCDECP_r       00100101 .. 10110 d:1 10001 00 .... .....     @incdec_pred u=1
INCDECP_z       00100101 .. 10110 d:1 10000 00 .... .....    @incdec2_pred u=1
SINCDECP_r_32   00100101 .. 1010 d:1 u:1 10001 00 .... .....    @incdec_pred
SINCDECP_r_64   00100101 .. 1010 d:1 u:1 10001 10 .... .....    @incdec_pred
SINCDECP_z      00100101 .. 1010 d:1 u:1 10000 00 .... .....    @incdec2_pred
```

```
CNT_r           00000100 .. 10 .... 1110 0 0 ..... .....    @incdec_cnt d=0 u=1
INCDEC_r        00000100 .. 11 .... 1110 0 d:1 ..... .....      @incdec_cnt u=1
SINCDEC_r_32    00000100 .. 10 .... 1111 d:1 u:1 ..... .....    @incdec_cnt
SINCDEC_r_64    00000100 .. 11 .... 1111 d:1 u:1 ..... .....    @incdec_cnt
INCDEC_v        00000100 .. 1 1 .... 1100 0 d:1 ..... .....    @incdec2_cnt u=1
SINCDEC_v       00000100 .. 1 0 .... 1100 d:1 u:1 ..... .....   @incdec2_cnt
```

com `@incdec_cnt ........ esz:2 .. .... ...... pat:5 rd:5 &incdec_cnt imm=%imm4_16_p1` e
`%imm4_16_p1 16:4 !function=plus_1` (o multiplicador é `imm4+1`, 1..16).

### Achados desta rodada de spec

1. **`WHILE*` NÃO está aqui.** As 12 linhas de `WHILE_lt`/`WHILE_gt`/`WHILE_ptr`/`WHILE_*_pair`/
   `WHILE_*_cnt2|4`/`PEXT_*`/`CTERM` vivem em `### SVE Integer Compare - Scalars Group`
   (linhas 874-904) e foram para a **B17.9**, que é onde a tabela normativa as põe. Elas geram
   predicado e setam NZCV — se a B17.9 for executada antes desta, o executor de predicado tem que
   existir; por isso B17.9 depende de B17.4, não o contrário.
2. **`SETFFR`/`RDFFR`/`RDFFR_p`/`WRFFR` estão aqui**, não na B17.17 (que é a task do `FFR` "de
   verdade", isto é, dos loads tolerantes a falha que o ESCREVEM). São 4 das 10 linhas do grupo
   misc. Isso é bom: o QEMU implementa `RDFFR` reusando a operação de predicado
   (`arg_rprr_s alt_a = {… .rn = FFR_PRED_NUM, .rm = FFR_PRED_NUM}`), então sai quase de graça se o
   `FFR` for o 17º predicado (decisão da B17.3).
3. **Flags NZCV de predicado têm fórmula própria** — `predtest`, confirmado por `WebFetch` em
   `sve_helper.c`:
   ```c
   #define PREDTEST_INIT  1
   static uint32_t iter_predtest_fwd(uint64_t d, uint64_t g, uint32_t flags) {
       if (likely(g)) {
           if (!(flags & 4)) {
               flags |= ((d & (g & -g)) != 0) << 31;   /* N = primeiro elemento ativo */
               flags |= 4;
           }
           flags |= ((d & g) != 0) << 1;               /* "Z" (invertido: algum ativo) */
           flags = deposit32(flags, 0, 1, (d & pow2floor(g)) == 0);  /* C = último inativo */
       }
       return flags;
   }
   ```
   Ou seja: **N** = "primeiro elemento ativo do resultado é 1" (`First`), **Z** = "nenhum elemento
   ativo é 1" (`None`, e o bit acumulado é o INVERSO), **C** = "último elemento ativo é 0"
   (`!Last`), **V** = 0 sempre. É a semântica de `PTEST` e de todas as formas com `s:1` (`S`).
   **Transcrever essa função** (e a variante `_bwd`, para `PNEXT`/operações que varrem de trás) é o
   coração desta task.
4. **`PTRUE_cnt`/`CNTP_c`/`%pnd`/`%pnn` são SVE2.1** (predicado-como-contador, `PN8`-`PN15`:
   `%pnd 0:3 !function=plus_8`). São um **estado diferente** de `P0-P15` — um predicado-como-contador
   não é uma máscara de bits, é um par (contagem, invertido). Ver Armadilha 4.

## Objetivo

As 40 células de predicado saem de `❌` (medidas a partir da B17.26) e o projeto ganha o substrato
`PredicateOps` (lógica, `predtest`/NZCV, partition break, contagem) que as 21 tasks seguintes usam.

## Inclui

1. **Lógica de predicado** (8) — `AND`/`BIC`/`EOR`/`SEL`/`ORR`/`ORN`/`NOR`/`NAND`, todas com
   predicado governante `pg` e sufixo `S` (`s:1`) opcional que seta NZCV por `predtest`.
   ⚠️ `EOR_pppp` com `pg = Pd` e `s=1` é o alias `NOTS`; `ORR_pppp` com `rn == rm == pg` é `MOV`.
   Aliases não precisam de `Kind` próprio — mas o executor tem que produzir o resultado certo.
2. **Misc** (10) — `PTEST`, `PTRUE` (com os 32 padrões `pat:5`: `POW2`, `VL1`..`VL256`, `MUL4`,
   `MUL3`, `ALL`), `PTRUE_cnt`, `PFALSE`, `SETFFR`, `RDFFR`, `RDFFR_p`, `WRFFR`, `PFIRST`, `PNEXT`.
3. **Partition break** (7) — `BRKA`/`BRKB` (zeroing e merging), `BRKPA`/`BRKPB`, `BRKN`.
4. **Contagem por predicado** (9) — `CNTP`, `CNTP_c`, `FIRSTP`, `LASTP`, `INCDECP_r`/`_z`,
   `SINCDECP_r_32`/`_r_64`/`_z`.
5. **Element count** (6) — `CNT_r` (`CNTB`/`CNTH`/`CNTW`/`CNTD`), `INCDEC_r`, `SINCDEC_r_32/_64`,
   `INCDEC_v`, `SINCDEC_v`. Todas leem o `VL` que a B17.3 expõe e o padrão `pat:5`.
6. **`SvePredicateOps`** (nome sugerido) — a classe utilitária com `predtest` (fwd e bwd), o
   decodificador de `pat:5` → número de elementos e as primitivas de leitura/escrita de bit de
   predicado por `esz`. É o substrato compartilhado; **cada operação em UM lugar só** (mesma
   disciplina da RFC B13.2/D1).
7. Testes (ver Aceite).

## Não inclui

- **`WHILE*`/`CTERM`/`PEXT`** — B17.9 (ver Achado 1).
- **Loads/stores de predicado** (`LDR_pri`/`STR_pri`) — B17.17/B17.18.
- **Permutação de predicado** (`ZIP1_p`/`PUNPKLO`/`REV_p`) — B17.11.
- **`PSEL`** (broadcast predicate element) — B17.22.
- **Qualquer load que ESCREVE o `FFR`** (`LDFF1`/`LDNF1`) — B17.17.
- **Emissão nativa/Truffle** — padrão da trilha B.

## Especificação

### Leitura obrigatória antes de codar

- `target/isa-decode/sve.decode`, linhas 541-559, 792-869 (citadas acima) e os formatos
  `@pd_pg_pn_pm_s`/`@pd_pg_pn_s`/`@pd_pg_pn_s0`/`@pd_pn`/`@pd_pn_e0`/`@rd_pg4_pn`/`@incdec_*`
  (linhas 109-230).
- O `## Resultado` da **B17.3** — a forma exata de `P<n>`/`FFR` decidida lá.
- `https://raw.githubusercontent.com/qemu/qemu/master/target/arm/tcg/sve_helper.c`
  (`iter_predtest_fwd`/`_bwd`, `compute_brk*`, `helper_sve_cntp`) e `translate-sve.c`
  (`trans_PTRUE`, `trans_RDFFR*`, `do_pfirst_pnext`, `FFR_PRED_NUM`) — via `WebFetch`, **lendo o
  código, não o resumo** (o `WebFetch` desta rodada devolveu resumo para partes do arquivo).
- ARM DDI 0487, tabela de padrões `pat:5` (`SVE predicate constraint`) — os 32 valores, incluindo
  os que a arquitetura define como "sem elementos" (`#uimm5` inválidos ⇒ predicado todo zero, NÃO
  UNDEF; ver Armadilha 2).
- `core/src/main/java/dev/vitorsilverio/armjitter/decoder64/Aarch64Decoder.java` — onde registrar o
  decoder novo e como o gate por `Aarch64Feature` já funciona (B11.4, `FEAT_RDM`, é o gabarito).

## Passos

1. Ler "Leitura obrigatória"; transcrever `iter_predtest_fwd`/`_bwd` e a tabela de `pat:5`.
2. `SvePredicateOps` + testes de unidade dela ANTES de qualquer decode (é o substrato).
3. Decoder SVE novo (`Aarch64SveDecoder` ou família de decoders por grupo — decidir na sessão e
   documentar), gateado por `Aarch64Feature.SVE`, reconhecendo as 40 linhas. **Tudo que não bater
   exatamente cai em `UNIMPLEMENTED`** (G8).
4. Executores.
5. Testes (ver Aceite); `mvn -o test` + `install` + G5 + `./gerar-cobertura-isa.sh` (a tabela
   **não** deve mudar ainda — `sve.decode` só sai de `NOT_IN_ANY_PRESET` na B17.26; medir mesmo
   assim para confirmar zero-diff).
6. `INDICE.md`, `## Resultado`, commit `B17.4: ...`, `git push`.

## Aceite

- As 40 linhas decodificam sob um preset com `Aarch64Feature.SVE` e produzem o resultado correto.
- `PTRUE` com cada um dos 32 padrões `pat:5` produz o predicado certo **em pelo menos dois `VL`**
  (256 e 512, se a RFC B17.2 aprovar VL configurável) — é o teste que prova a disciplina VLA.
- `PTEST` e as formas `S` setam NZCV exatamente como `iter_predtest_fwd` (teste tabelado: predicado
  todo-zero, primeiro-ativo-1, último-ativo-1, misto).
- `RDFFR`/`WRFFR`/`SETFFR` movem o `FFR` de/para predicado; `SETFFR` o deixa todo-1.
- `CNT_r` com `pat=ALL` e `esz=0` devolve `VL/8`; com `esz=3` devolve `VL/64`; `INCDEC_r` com
  `imm4=15` multiplica por 16 (`%imm4_16_p1`).
- `SINCDECP_*`/`SINCDEC_*` saturam (32 e 64 bits, com e sem sinal) — teste nos limites.
- Nenhum encoding vizinho ainda não implementado passa a `✅` por engano (mesma disciplina de
  medição da B15.2/B15.3: medir o delta de `docs/COBERTURA-ISA.md` antes de commitar).
- `mvn -o test` verde + G5 verde nos 5 consumidores.

## Armadilhas

1. **`predtest` não é "compare com zero".** N/Z/C têm significado de `First`/`None`/`!Last` sobre os
   elementos **ativos** (mascarados por `pg`), e `V` é sempre 0. Implementar como flags aritméticas
   normais é um bug silencioso que só aparece em loop VLA real.
2. **`PTRUE` com padrão maior que o `VL` produz predicado TODO ZERO, não exceção.** Ex.: `PTRUE
   p0.b, VL256` com `VL=128` ⇒ nenhum elemento. Tratar como UNDEF quebra código VLA legítimo (é
   justamente assim que o software descobre o `VL`).
3. **`P<n>` é bit-por-BYTE, não bit-por-elemento.** Para `esz>0` só o bit do byte MAIS BAIXO de cada
   elemento é significativo; os demais têm que ser escritos como 0 pelas operações que geram
   predicado, e ignorados pelas que consomem. Errar isso faz `CNTP` contar múltiplo do valor certo.
   (O `expand_pred_s` do QEMU — `[0x01] = 0x00000000ffffffffull` — é a evidência do layout.)
4. **`PTRUE_cnt`/`CNTP_c` são predicado-como-contador (SVE2.1), estado DIFERENTE.** `%pnd 0:3
   !function=plus_8` mostra que o destino é `PN8`-`PN15`, e a codificação é uma contagem, não uma
   máscara. Se a B17.3 não modelou isso, esta task tem duas saídas honestas: (a) modelar o
   predicado-como-contador aqui (escopo maior, documentar) ou (b) deixar essas 2 linhas
   **explicitamente não implementadas**, devolvendo `null` no decoder (`❌` honesto, G8) e
   registrando-as como pendência nomeada desta task. **Não** decodificá-las como `PTRUE`/`CNTP`
   comuns — é o misdecode silencioso que o G8 proíbe.
5. **`BRKA_m`/`BRKB_m` usam `@pd_pg_pn_s0` (`s=0` forçado)** e têm `esz` FIXO no padrão
   (`00`/`10`), diferente de `BRKA_z`/`BRKB_z` (`0.`/`1.`). O bit 22 aqui **não** é `esz` — é parte
   do opcode. Ler o padrão literal, não presumir o layout comum.
6. **`SETFFR`/`RDFFR`/`WRFFR` não têm campo `esz`** — são encodings totalmente fixos. O medidor os
   sonda com os bits fixos; um decoder que exija `esz` os recusa.
7. **`PFIRST` é `@pd_pn_e0`** (esz fixo 0) e `PNEXT` é `@pd_pn` (esz variável) — e as duas são
   destrutivas (`Pdn`). Conferir no manual qual registrador é lido/escrito.

## Não fazer

- Não implementar `WHILE*`/`CTERM`/`PEXT` (B17.9) nem `PSEL` (B17.22).
- Não implementar nenhum load/store (B17.17/B17.18).
- Não decodificar `PTRUE_cnt`/`CNTP_c` como se fossem predicado-máscara (Armadilha 4).
- Não espalhar `128`/`16` como tamanho de predicado — vem do `VL` (G6).

## Resultado

_(preencher ao concluir a task)_
