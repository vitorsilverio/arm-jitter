# B17.21 — SVE2 inteiro II: shift saturante/arredondado, halving, saturating add/sub, widening, accumulate e narrowing — 122 encodings

**Trilha:** B · **Repo:** arm-jitter (+ revalidação G5) · **Depende de:** B17.20 (⬜)
**Épico:** `b17-plano-sve.md` · **Status:** ⬜ (spec escrita 2026-09-14, ainda não executada)

## Contexto

**A maior task da escada** (122 de 929). Como a B17.16, é grande por expansão sistemática
(bottom/top × signed/unsigned × 4 tamanhos), não por 122 semânticas distintas — mas aqui a
expansão tem um eixo que não existe em nenhuma outra extensão ARM: **`B`/`T` (bottom/top)**.

### Recorte normativo (ver a tabela completa em `b17.1-aarch64-feature-sve-sve2.md`)

| Seção de `sve.decode` | Linhas | Enc |
|---|---|---:|
| `### SVE2 saturating/rounding bitwise shift left (predicated)` | 1630-1645 | 12 |
| `### SVE2 integer halving add/subtract (predicated)` | 1647-1656 | 8 |
| `### SVE2 saturating add/subtract (predicated)` | 1666-1675 | 8 |
| `#### SVE2 Widening Integer Arithmetic` | 1677-1748 | 43 |
| `#### SVE2 Accumulate` | 1750-1789 | 18 |
| `#### SVE2 Narrowing` | 1791-1841 | 33 |
| **Total** | | **122** |

(Bate com o "122" da escada do épico.)

**Sugestão de corte 21a/21b**, se o orçamento de uma sessão não comportar: 21a = as 3 seções
predicadas (28) + `#### SVE2 Accumulate` (18) = 46; 21b = widening (43) + narrowing (33) = 76.
As duas metades são independentes (nenhuma linha de uma depende da outra). **Documentar o corte no
`INDICE.md` se for usado.**

### Os encodings reais (representantes de cada sub-família)

```
# saturating/rounding bitwise shift left (predicated) — 12, com formas reversas
SRSHL           01000100 .. 000 010 100 ... ..... .....  @rdn_pg_rm
SRSHL           01000100 .. 000 110 100 ... ..... .....  @rdm_pg_rn # SRSHLR
SQSHL           01000100 .. 001 000 100 ... ..... .....  @rdn_pg_rm
SQRSHL          01000100 .. 001 010 100 ... ..... .....  @rdn_pg_rm
UQRSHL          01000100 .. 001 111 100 ... ..... .....  @rdm_pg_rn # UQRSHLR

# halving add/subtract (predicated) — 8
SHADD           01000100 .. 010 000 100 ... ..... .....  @rdn_pg_rm
SRHADD          01000100 .. 010 100 100 ... ..... .....  @rdn_pg_rm
SHSUB           01000100 .. 010 110 100 ... ..... .....  @rdm_pg_rn # SHSUBR

# saturating add/subtract (predicated) — 8
SQADD_zpzz      01000100 .. 011 000 100 ... ..... .....  @rdn_pg_rm
SUQADD          01000100 .. 011 100 100 ... ..... .....  @rdn_pg_rm
USQADD          01000100 .. 011 101 100 ... ..... .....  @rdn_pg_rm
SQSUB_zpzz      01000100 .. 011 110 100 ... ..... .....  @rdm_pg_rn # SQSUBR
```

```
#### SVE2 Widening Integer Arithmetic (43)
SADDLB          01000101 .. 0 ..... 00 0000 ..... .....  @rd_rn_rm
SADDLT          01000101 .. 0 ..... 00 0001 ..... .....  @rd_rn_rm
…  (add/sub long: 12; interleaved long SADDLBT/SSUBLBT/SSUBLTB: 3; add/sub wide: 8)
SQDMULLB_zzz    01000101 .. 0 ..... 011 000 ..... .....  @rd_rn_rm
PMULLB          01000101 .. 0 ..... 011 010 ..... .....  @rd_rn_rm
…  (multiply long: 8)
SSHLLB          01000101 .. 0 ..... 1010 00 ..... .....  @rd_rn_tszimm_shl
…  (shift left long: 4)
EORBT           01000101 .. 0 ..... 10010 0 ..... .....  @rd_rn_rm
EORTB           01000101 .. 0 ..... 10010 1 ..... .....  @rd_rn_rm
SMMLA           01000101 00 0 ..... 10011 0 ..... .....  @rda_rn_rm_ex esz=2
USMMLA          01000101 10 0 ..... 10011 0 ..... .....  @rda_rn_rm_ex esz=2
UMMLA           01000101 11 0 ..... 10011 0 ..... .....  @rda_rn_rm_ex esz=2
BEXT            01000101 .. 0 ..... 1011 00 ..... .....  @rd_rn_rm
BDEP            01000101 .. 0 ..... 1011 01 ..... .....  @rd_rn_rm
BGRP            01000101 .. 0 ..... 1011 10 ..... .....  @rd_rn_rm
```

```
#### SVE2 Accumulate (18)
CADD_rot90      01000101 .. 00000 0 11011 0 ..... .....  @rdn_rm
SQCADD_rot270   01000101 .. 00000 1 11011 1 ..... .....  @rdn_rm
SABALB          01000101 .. 0 ..... 1100 00 ..... .....  @rda_rn_rm
ADCLB           01000101 .. 0 ..... 11010 0 ..... .....  @rda_rn_rm
SSRA            01000101 .. 0 ..... 1110 00 ..... .....  @rd_rn_tszimm_shr
SRI             01000101 .. 0 ..... 11110 0 ..... .....  @rd_rn_tszimm_shr
SLI             01000101 .. 0 ..... 11110 1 ..... .....  @rd_rn_tszimm_shl
SABA            01000101 .. 0 ..... 11111 0 ..... .....  @rd_rn_rm
```

```
#### SVE2 Narrowing (33)
{
  SQCVTN_sh     01000101 00 1 10001 010 000 ....0 .....  @rd_rnx2 esz=1
  SQXTNB        01000101 .. 1 ..... 010 000 ..... .....  @rd_rn_tszimm_shl
}
SQXTNT          01000101 .. 1 ..... 010 001 ..... .....  @rd_rn_tszimm_shl
…  (3 blocos {} + SQXTUNT: 9)
SQSHRUNB        01000101 .. 1 ..... 00 0000 ..... .....  @rd_rn_tszimm_shr
…  (shift right narrow: 16)
ADDHNB          01000101 .. 1 ..... 011 000 ..... .....  @rd_rn_rm
…  (add/sub narrow high part: 8)
```

### Achados desta rodada de spec

1. **`B`/`T` (bottom/top) é o eixo estrutural da task.** `SADDLB` lê os elementos de índice PAR
   (bottom) de cada par; `SADDLT`, os ÍMPARES (top). O destino tem elementos do DOBRO do tamanho.
   `*B`/`*T` não é "metade baixa/alta do vetor" — é **entrelaçado**. Errar isso inverte metade das
   122 linhas silenciosamente.
2. **`SADDLBT`/`SSUBLBT`/`SSUBLTB` (interleaved long)** misturam os eixos: `BT` = primeiro operando
   bottom, segundo top. Três linhas, três combinações diferentes.
3. **As formas narrowing `*T` PRESERVAM a metade não escrita** — `SQXTNT` escreve só os elementos
   ímpares do destino e deixa os pares intactos. Zerar é o bug natural (mesma armadilha que a
   B16.11 registrou para o MVE: *"Preservar a metade não escrita é SEMÂNTICA"*).
4. **3 blocos `{}` sobrepostos no narrowing** (linhas 1796-1809): `SQCVTN_sh`/`UQCVTN_sh`/
   `SQCVTUN_sh` (SVE2.1, `@rd_rnx2` com `%rn_ax2 6:4 !function=times_2` — lê um PAR de registradores
   `Zn`,`Zn+1`) competem pelo mesmo espaço de `SQXTNB`/`UQXTNB`/`SQXTUNB`. O padrão mais específico
   vem primeiro. **Ordem de decode importa** (mesmo mecanismo do bloco `{}` de `m-nocp.decode` que a
   B15.2/B15.3 enfrentaram).
5. **`SMMLA`/`USMMLA`/`UMMLA` são `FEAT_I8MM`** — `Aarch64Feature.INT8_MATRIX_MULTIPLY`, já
   existente (`ARMV8_6_A`), e a semântica de matriz 8-bit **já existe** no A64/NEON (B13.19). Gate e
   reuso.
6. **`BEXT`/`BDEP`/`BGRP` são `FEAT_SVE2_BitPerm`** — uma feature SEPARADA de `FEAT_SVE2` (anunciada
   em `ID_AA64ZFR0_EL1.BitPerm`). Se a B17.1 não criou `SVE2_BITPERM`, esta task precisa criá-la
   (aditivo, G3-safe) — **não** decodificá-las sob `SVE2` genérico.
7. **`PMULLB`/`PMULLT` são `FEAT_SVE2_PMULL128`** quando `esz=0` (produto de 128 bits) — outra
   sub-feature de `ID_AA64ZFR0_EL1` (`AES`). Conferir; mesma disciplina do Achado 6.
8. **`SSRA`/`USRA`/`SRSRA`/`URSRA`/`SABA`/`UABA` são ACUMULATIVAS** mas usam `@rd_rn_tszimm_shr`/
   `@rd_rn_rm` (formatos **não**-acumulativos) — o arquivo tem o comentário literal
   *"TODO: Use @rda and %reg_movprfx here."*. Ou seja: o destino é implicitamente o acumulador e o
   decodetree do QEMU **não** modela isso. Um decoder que siga o formato ao pé da letra sobrescreve
   em vez de acumular. **É o achado mais perigoso desta task.**
9. **`CADD`/`SQCADD` são adição complexa inteira** (rotação 90/270) — irmãs de `FCADD` (B17.14).
10. **`ADCLB`/`ADCLT` usam o carry** e o comentário diz *"ADC and SBC decoded via size in helper
    dispatch"* — o bit 22 (`esz`) seleciona ADC vs SBC, **não** o tamanho. Um `switch (esz)` comum
    decodifica errado.

## Objetivo

As 122 células decodificam sob as features certas e executam com bottom/top, preservação de metade,
acumulação e saturação corretas, em qualquer `VL`.

## Inclui

1. Shift saturante/arredondado predicado (12), halving (8), saturating add/sub (8) — incl. as 6
   formas reversas.
2. Widening (43): add/sub long, interleaved long, add/sub wide, multiply long, shift left long,
   `EORBT`/`EORTB`, matriz 8-bit, bit-permute.
3. Accumulate (18): `CADD`/`SQCADD`, `SABAL*`/`UABAL*`, `ADCL*`, `SSRA`/`USRA`/`SRSRA`/`URSRA`,
   `SRI`/`SLI`, `SABA`/`UABA`.
4. Narrowing (33): extract narrow (incl. os 3 blocos `{}` SVE2.1), shift right narrow, add/sub
   narrow high part.
5. Testes (ver Aceite).

## Não inclui

- **Multiply/unário/pairwise SVE2** — B17.20.
- **Formas INDEXADAS** das mesmas operações (`SQDMLALB_zzxw`, `SMULLB_zzx`) — B17.8.
- **Multiply-add long NÃO indexado** (`SQDMLALB_zzzw`, `#### SVE Integer Multiply-Add
  (unpredicated)`) — B17.22.
- **SVE2 FP / cripto / memória** — B17.23/B17.24/B17.25.
- **Emissão nativa/Truffle** — padrão da trilha B.

## Especificação

### Leitura obrigatória antes de codar

- `target/isa-decode/sve.decode` **linhas 1630-1841 INTEIRAS** + os formatos `@rdn_pg_rm`,
  `@rdm_pg_rn`, `@rd_rn_rm`, `@rda_rn_rm`, `@rda_rn_rm_ex`, `@rdn_rm`, `@rd_rn_tszimm_shl|shr`,
  `@rd_rnx2` e `%rn_ax2`, `%tszimm16_*`.
- O `## Resultado` de **B17.6** (núcleo predicado, formas reversas) e **B17.20**.
- `tasks/trilha-b-arquiteturas/b16.11-deslocamentos-estreitantes.md` — o MESMO problema de
  bottom/top e preservação de metade, já analisado para o MVE.
- `tasks/trilha-b-arquiteturas/b13.19-neon-shared-i8mm-matricial.md` — `SMMLA`/`UMMLA`.
- `core/src/main/java/dev/vitorsilverio/armjitter/advsimd/AdvSimdLanes.java` —
  `shiftNarrowImmediate`, `shiftWidenImmediate`, `widening`, `wideningByElement`.
- `https://raw.githubusercontent.com/qemu/qemu/master/target/arm/tcg/translate-sve.c`
  (`do_sve2_zzw_ool`, `do_sve2_narrow_extract`, `trans_SSRA`, `trans_ADCLB`, `trans_BEXT`) e
  `sve_helper.c` — via `WebFetch`.
- ARM DDI 0487 — `FEAT_SVE2_BitPerm`, `FEAT_SVE2_PMULL128`, `ADCLB`/`SBCLB`.

## Passos

1. Ler "Leitura obrigatória"; decidir o corte 21a/21b, se aplicável.
2. Resolver as features (Achados 6/7) — criar as constantes que faltarem em `Aarch64Feature`.
3. Transcrever a tabela bottom/top e a lista de acumulativas (Achado 8).
4. Decoder das 122 linhas, com a ordem dos blocos `{}` (Achado 4) e o `esz` de `ADCLB` (Achado 10).
   Resto → `UNIMPLEMENTED`.
5. Executores.
6. Testes; `mvn -o test` + `install` + G5 + `./gerar-cobertura-isa.sh` (zero-diff até B17.26).
7. `INDICE.md`, `## Resultado`, commit `B17.21: ...`, `git push`.

## Aceite

- As 122 linhas decodificam sob as features corretas (`SVE2`, `SVE2_BitPerm`,
  `INT8_MATRIX_MULTIPLY`, e a de `PMULL128`).
- **Bottom/top**: para cada família `*B`/`*T`, um teste com vetor cujos elementos pares e ímpares
  diferem — o resultado de `B` e de `T` é DIFERENTE e bate com o esperado.
- **`*T` narrowing preserva a metade não escrita** — teste com destino pré-preenchido.
- **As 6 acumulativas "sem formato de acumulador"** (`SSRA`/`USRA`/`SRSRA`/`URSRA`/`SABA`/`UABA`)
  ACUMULAM — teste com destino não-zero (Achado 8).
- `ADCLB`/`ADCLT` selecionam ADC vs SBC pelo bit 22, não pelo tamanho.
- `SRI`/`SLI` inserem preservando os bits não deslocados do destino.
- `SMMLA`/`USMMLA`/`UMMLA` batem com o executor A64/NEON equivalente (teste cruzado).
- Os 3 blocos `{}` do narrowing: `SQCVTN_sh` casa antes de `SQXTNB` para o padrão específico, e
  `SQXTNB` continua casando para os demais (teste do PAR, mesma disciplina da B15.2).
- Saturação testada nos limites em todas as famílias saturantes.
- Testado em pelo menos dois `VL`.
- `mvn -o test` verde + G5 verde nos 5 consumidores.

## Armadilhas

1. **Bottom/top é entrelaçado, não metade do vetor** (Achado 1).
2. **As 6 acumulativas com formato errado no `.decode`** (Achado 8) — o comentário `TODO` do
   arquivo é o aviso; seguir o formato literalmente perde a acumulação.
3. **`*T` narrowing preserva** (Achado 3).
4. **Ordem dos blocos `{}`** (Achado 4) — e `@rd_rnx2` lê um PAR de registradores (`%rn_ax2` =
   bits 9:6 × 2), não `Zn`.
5. **Features separadas** (Achados 6/7) — decodificar `BEXT` sob `SVE2` genérico é anunciar suporte
   que o hardware real gateia à parte.
6. **`esz` de `ADCLB` não é tamanho** (Achado 10).
7. **Widening: o destino tem elementos do dobro do tamanho** ⇒ metade dos elementos. Um laço com o
   `esz` do destino percorre o número certo de elementos; um laço com o `esz` da origem percorre o
   dobro e sai do vetor.
8. **`SSHLLB`/`SSHLLT` usam `@rd_rn_tszimm_shl`** (o `esz` vem de `tsz`), e o comentário diz "bit23
   == 0 is handled by esz > 0 in do_sve2_shll_tb" — mais uma restrição que o decodetree não impõe.
9. **Tamanho da task**: 122 linhas é muito para uma sessão sem corte. Preferir 21a/21b a uma sessão
   apressada — o épico inteiro depende da correção desta família (B17.22/B17.23 dependem de B17.20,
   não desta, então o corte não bloqueia ninguém).

## Não fazer

- Não implementar as formas indexadas (B17.8) nem o multiply-add long não-indexado (B17.22).
- Não zerar a metade não escrita das formas `*T`.
- Não decodificar `BEXT`/`BDEP`/`BGRP`/`PMULL` sob `SVE2` genérico sem confirmar a feature.
- Não seguir o formato do `.decode` literalmente nas 6 acumulativas.

## Resultado

_(preencher ao concluir a task)_
