# B17.6 — Inteiro predicado: aritmética binária, shift por imediato e unárias — 68 encodings

**Trilha:** B · **Repo:** arm-jitter (+ revalidação G5) · **Depende de:** B17.4 (✅)
**Épico:** `b17-plano-sve.md` · **Status:** ✅ (2026-09-25)

## Contexto

O núcleo aritmético de SVE: toda operação aqui tem um predicado governante `pg:3` e é **destrutiva**
(`Zdn`), com o elemento inativo **preservado** (merging) — o modelo que diferencia SVE de AdvSIMD.

### Recorte normativo (ver a tabela completa em `b17.1-aarch64-feature-sve-sve2.md`)

| Seção de `sve.decode` | Linhas | Enc |
|---|---|---:|
| `### SVE Integer Arithmetic - Binary Predicated Group` | 302-328 | 20 |
| `### SVE Shift by Immediate - Predicated Group` | 364-389 | 18 |
| `### SVE Integer Arithmetic - Unary Predicated Group` | 391-429 | 30 |
| **Total** | | **68** |

(Bate exatamente com o "68" da escada do épico.)

### Os encodings reais (amostra representativa; as 68 linhas estão nos intervalos acima)

Formatos: `@rdn_pg_rm ........ esz:2 ... ... ... pg:3 rm:5 rd:5 &rprr_esz rn=%reg_movprfx` e o
**invertido** `@rdm_pg_rn ........ esz:2 ... ... ... pg:3 rn:5 rd:5 &rprr_esz rm=%reg_movprfx`.

```
ORR_zpzz        00000100 .. 011 000 000 ... ..... .....   @rdn_pg_rm
ADD_zpzz        00000100 .. 000 000 000 ... ..... .....   @rdn_pg_rm
SUB_zpzz        00000100 .. 000 001 000 ... ..... .....   @rdn_pg_rm
SUB_zpzz        00000100 .. 000 011 000 ... ..... .....   @rdm_pg_rn # SUBR
SMAX_zpzz       00000100 .. 001 000 000 ... ..... .....   @rdn_pg_rm
SABD_zpzz       00000100 .. 001 100 000 ... ..... .....   @rdn_pg_rm
MUL_zpzz        00000100 .. 010 000 000 ... ..... .....   @rdn_pg_rm
SMULH_zpzz      00000100 .. 010 010 000 ... ..... .....   @rdn_pg_rm
# Note that divide requires size >= 2; below 2 is unallocated.
SDIV_zpzz       00000100 .. 010 100 000 ... ..... .....   @rdn_pg_rm
UDIV_zpzz       00000100 .. 010 111 000 ... ..... .....   @rdm_pg_rn # UDIVR
```

```
ASR_zpzi        00000100 .. 000 000 100 ... .. ... .....  @rdn_pg_tszimm_shr
LSL_zpzi        00000100 .. 000 011 100 ... .. ... .....  @rdn_pg_tszimm_shl
ASRD            00000100 .. 000 100 100 ... .. ... .....  @rdn_pg_tszimm_shr
SQSHL_zpzi      00000100 .. 000 110 100 ... .. ... .....  @rdn_pg_tszimm_shl
SRSHR           00000100 .. 001 100 100 ... .. ... .....  @rdn_pg_tszimm_shr
SQSHLU          00000100 .. 001 111 100 ... .. ... .....  @rdn_pg_tszimm_shl
ASR_zpzz        00000100 .. 010 000 100 ... ..... .....   @rdn_pg_rm
ASR_zpzz        00000100 .. 010 100 100 ... ..... .....   @rdm_pg_rn # ASRR
# SVE bitwise shift by wide elements (predicated) — Note these require size != 3.
ASR_zpzw        00000100 .. 011 000 100 ... ..... .....         @rdn_pg_rm
```

```
# SVE unary bit operations (predicated) — Note esz != 0 for FABS and FNEG.
CLS_m           00000100 .. 011 000 101 ... ..... .....         @rd_pg_rn
NOT_zpz_m       00000100 .. 011 110 101 ... ..... .....         @rd_pg_rn
FABS_m          00000100 .. 011 100 101 ... ..... .....         @rd_pg_rn
CLS_z           00000100 .. 001 000 101 ... ..... .....         @rd_pg_rn
# SVE integer unary operations (predicated) — Note esz > original size for extensions.
ABS_m           00000100 .. 010 110 101 ... ..... .....         @rd_pg_rn
SXTB_m          00000100 .. 010 000 101 ... ..... .....         @rd_pg_rn
SXTW_m          00000100 .. 010 100 101 ... ..... .....         @rd_pg_rn
ABS_z           00000100 .. 000 110 101 ... ..... .....         @rd_pg_rn
```

### Achados desta rodada de spec

1. **O sufixo `_m`/`_z` do grupo unário NÃO é merging/zeroing no sentido usual** — as 30 linhas vêm
   em dois blocos idênticos exceto por um bit do opcode (`011`/`001` para as bit-ops, `010`/`000`
   para as extensões). `_m` é a forma **merging** clássica (elemento inativo preservado) e `_z` é a
   forma **zeroing** que a SVE2.1 acrescentou (elemento inativo zerado). Confirmar contra o manual
   antes de implementar: tratar as duas como a mesma semântica é um bug silencioso em metade das
   30 linhas.
2. **`@rdm_pg_rn` existe para as formas "reversas"** (`SUBR`, `SDIVR`, `UDIVR`, `ASRR`, `LSRR`,
   `LSLR`): o MESMO `InstructionKind` do QEMU (`SUB_zpzz`, `SDIV_zpzz`, `ASR_zpzz`) com os
   operandos TROCADOS pelo formato. São 6 das 68 linhas. **O decoder tem que registrar a inversão**
   — decodificar `SUBR` como `SUB` sem trocar os operandos dá o resultado errado silenciosamente.
   O mesmo padrão que a B16.6 encontrou no MVE (`@2op_rev`).
3. **`SDIV`/`UDIV` exigem `esz >= 2`** e `_zpzw` exige `esz != 3`; `FABS_m`/`FNEG_m` exigem
   `esz != 0`. Os comentários do arquivo dizem isso explicitamente — e o decodetree **não** impõe:
   é o tradutor do QEMU que recusa. Ou seja, essas restrições precisam virar recusa explícita no
   decoder (G8), não sair de graça do padrão de bits.
4. **`ASRD` (arithmetic shift right for divide) tem semântica própria** — arredonda para zero, ao
   contrário de `ASR`. Não é `ASR` com outro nome.
5. **`SQSHL_zpzi`/`UQSHL_zpzi`/`SRSHR`/`URSHR`/`SQSHLU` são SVE2**, não SVE (5 das 18 linhas do
   grupo de shift). Gate por `Aarch64Feature.SVE2`.
6. **Merging é o default e é a semântica cara**: `Zdn` inalterado onde `pg` é 0. Isso impede
   implementar a operação como "calcula tudo e escreve tudo" — todo laço de lane precisa do teste
   de predicado. É onde `AdvSimdLanes` **não** serve diretamente (mesmo achado da B16.1: o núcleo
   vetorial da RFC B13.2 não tem predicação).

## Objetivo

As 68 células predicadas decodificam e executam com semântica de merging correta, em qualquer `VL`,
incluindo as 6 formas reversas e as restrições de `esz`.

## Inclui

1. **Aritmética binária predicada** (20): lógica (`ORR`/`EOR`/`AND`/`BIC`), `ADD`/`SUB`/`SUBR`,
   min/max com e sem sinal, `SABD`/`UABD`, `MUL`/`SMULH`/`UMULH`, `SDIV`/`UDIV`/`SDIVR`/`UDIVR`.
2. **Shift predicado** (18): por imediato (`ASR`/`LSR`/`LSL`/`ASRD` + as 5 SVE2), por vetor
   (`_zpzz`, com as 3 formas reversas) e por elementos largos (`_zpzw`).
3. **Unárias predicadas** (30): `CLS`/`CLZ`/`CNT`/`CNOT`/`NOT`/`FABS`/`FNEG` e `ABS`/`NEG`/
   `SXTB`/`UXTB`/`SXTH`/`UXTH`/`SXTW`/`UXTW`, cada uma nas formas `_m` e `_z`.
4. **Extensão do núcleo vetorial com predicação** — ou uma camada `SveLanes` própria sobre
   `AdvSimdRegisterWords` (decidir e documentar; ver Armadilha 5). **Cada operação em UM lugar só**
   (RFC B13.2/D1): se `AdvSimdLanes.threeSame` já sabe fazer `SMAX` em `esz`, a versão predicada
   deve chamá-la por lane, não reimplementar.
5. Testes (ver Aceite).

## Não inclui

- **Reduções** (`SADDV`/`ANDV`/`SMAXV`/`MOVPRFX_z|m`) — B17.7.
- **Formas não-predicadas** — B17.5.
- **Comparações** (`CMPGE_ppzz`…) — B17.9.
- **SVE2 predicado** (`SRSHL`/`SQSHL` por vetor, halving, saturating add/sub, pairwise) — B17.20/B17.21.
  ⚠️ **Só as 5 formas por IMEDIATO** (`SQSHL_zpzi`/`UQSHL_zpzi`/`SRSHR`/`URSHR`/`SQSHLU`) estão
  nesta task, porque vivem no grupo `### SVE Shift by Immediate - Predicated Group`.
- **Emissão nativa/Truffle** — padrão da trilha B.

## Especificação

### Leitura obrigatória antes de codar

- `target/isa-decode/sve.decode` linhas 299-429 INTEIRAS (as 68), mais os formatos `@rdn_pg_rm`,
  `@rdm_pg_rn`, `@rd_pg_rn`, `@rdn_pg_tszimm_shr|shl` e os extratores `%tszimm_esz|shr|shl`
  (linhas 154-194).
- O `## Resultado` de **B17.3** e **B17.4**.
- `core/src/main/java/dev/vitorsilverio/armjitter/advsimd/AdvSimdLanes.java` e
  `AdvSimdThreeSameOp`/`AdvSimdShiftImmediateOp` — o que dá para reusar.
- `tasks/trilha-b-arquiteturas/b13.2-rfc-nucleo-vetorial.md` (decisão D1) e
  `b16.1-fundacao-banco-q-vpr-preset.md`, seção "o núcleo vetorial da B13.2 não tem predicação" —
  o MESMO problema, já analisado uma vez.
- `https://raw.githubusercontent.com/qemu/qemu/master/target/arm/tcg/translate-sve.c`
  (`do_zpzz_ool`, `DO_ZPZZ`, `trans_ASRD`, `do_sve2_zpzi_ool`) e `sve_helper.c` — via `WebFetch`,
  em especial para `ASRD` e para a semântica exata de `_z` vs `_m`.
- ARM DDI 0487 — `SDIV`/`UDIV` (`esz>=2`), `_zpzw` (`esz!=3`), `FABS`/`FNEG` (`esz!=0`).

## Passos

1. Ler "Leitura obrigatória"; **confirmar `_m` vs `_z`** (Achado 1) antes de qualquer código.
2. Decidir a camada predicada do núcleo vetorial (Armadilha 5) e documentar.
3. Decoder das 68 linhas, com as 6 inversões de operando explícitas (Achado 2) e as recusas de
   `esz` (Achado 3). Resto → `UNIMPLEMENTED`.
4. Executores.
5. Testes; `mvn -o test` + `install` + G5 + `./gerar-cobertura-isa.sh` (zero-diff até B17.26).
6. `INDICE.md`, `## Resultado`, commit `B17.6: ...`, `git push`.

## Aceite

- As 68 linhas decodificam sob `SVE` (as 5 de shift-imediato SVE2 **só** sob `SVE2`).
- **Merging**: com `pg` parcialmente ativo, os elementos inativos de `Zdn` ficam **byte a byte
  intactos** — teste explícito com padrão de bits reconhecível (não zeros).
- As 6 formas reversas (`SUBR`/`SDIVR`/`UDIVR`/`ASRR`/`LSRR`/`LSLR`) produzem `op(Zm, Zdn)`, não
  `op(Zdn, Zm)` — teste com operandos assimétricos (o `SUB` de dois valores iguais não pega o bug).
- `SDIV`/`UDIV` com `esz < 2`, `_zpzw` com `esz == 3` e `FABS_m`/`FNEG_m` com `esz == 0` são
  **recusados** (`null`/`UNIMPLEMENTED`), não executados com semântica inventada (G8).
- `ASRD` arredonda para zero (teste com valor negativo — é o caso em que difere de `ASR`).
- `_m` e `_z` diferem observavelmente nos elementos inativos.
- Testado em pelo menos dois `VL`.
- `mvn -o test` verde + G5 verde nos 5 consumidores.

## Armadilhas

1. **As 6 formas reversas** (Achado 2) — o QEMU reusa o mnemônico, o arquivo marca só com um
   comentário (`# SUBR`), e o formato `@rdm_pg_rn` é a única diferença. É o bug mais provável desta
   task inteira.
2. **`%tszimm_esz`/`%tszimm_shr`/`%tszimm_shl`**: o `esz` e a contagem de shift vêm do MESMO campo
   `tsz:imm3` (bits 23:22 + 9:5), com funções diferentes — `shr` extrai `(2*esize) - (tsz:imm3)` e
   `shl` extrai `(tsz:imm3) - esize`. Tratar como campos independentes dá shift errado em todos os
   tamanhos. (Mesma classe de armadilha que a B16.10 encontrou com `%rshift_i3/i4/i5`.)
3. **`esz` inválido não é don't-care.** `tsz == 0` é UNALLOCATED para os shifts; `esz<2` para
   divisão. Precisa de recusa explícita.
4. **`FABS_m`/`FNEG_m` estão no grupo INTEIRO** (`### SVE Integer Arithmetic - Unary Predicated`)
   mas são operações FP (limpam/invertem o bit de sinal). Não dependem de B17.13 — são bit-ops —
   mas o executor precisa saber disso (`esz != 0`, e o "sinal" é o bit mais alto do elemento).
5. **Predicação no núcleo vetorial**: `AdvSimdLanes` não tem. Duas saídas: (a) laço por lane nesta
   task chamando a operação escalar de `AdvSimdLanes` por elemento; (b) acrescentar uma variante
   predicada ao núcleo. A B16.1 deixou a mesma decisão aberta para o MVE — **coordenar**: se a B16
   já tiver resolvido quando esta task rodar, seguir a mesma escolha em vez de criar um terceiro
   mecanismo.
6. **`CNOT`** é "conta se é zero" (`Zd = (Zn == 0) ? 1 : 0`), não `NOT`. E **`CNT_zpz`** é
   popcount por elemento, não a `CNT_r` de element-count da B17.4 — mesmo mnemônico, instruções
   diferentes.
7. **As extensões (`SXTB_m`…`UXTW_m`) exigem `esz` maior que o tamanho de origem** ("Note esz >
   original size for extensions") — `SXTW` com `esz != 3` é inválido.

## Não fazer

- Não implementar reduções (B17.7), comparações (B17.9) nem SVE2 predicado por vetor (B17.20/B17.21).
- Não tratar `_m` e `_z` como a mesma coisa antes de confirmar (Achado 1).
- Não ignorar as inversões de operando.
- Não reimplementar operação que `AdvSimdLanes` já tem.

## Resultado

**Fechada em 2026-09-25.** As 68 linhas decodificam e executam (20 binárias + 18 de shift + 30 unárias), sem pendência de encoding.
`docs/COBERTURA-ISA.md` **inalterada** (23523/23523; `sve.decode` segue `NOT_IN_ANY_PRESET` até a B17.26); `docs/COBERTURA-JIT.md`
ganhou o `Kind` novo (158).

**O que foi feito**

- `ir64/Ir64Op.SveIntegerPredicated` (Kind 158): UM record com `enum Op` de 47 mnemônicos; as formas reversas e `_m`/`_z` são
  campos (`rn`/`rm` trocados, `zeroing`), não `Op`s separados.
- `decoder64/Aarch64SveDecoder.decodeIntegerPredicated`: prefixo `0x04` com `bit 21 = 0` e `bits[15:13]` = `000`/`100`/`101`
  (o `MLA`/`MLS`/`MAD`/`MSB` da B17.5 ocupa os outros); tentado depois do grupo da B17.5 e antes da contagem de elementos.
  Palavras conferidas contra `aarch64-none-elf-as` (devkitA64; as `_z` com `-march=armv9.6-a+sve2p2`).
- `executor64/SveIntegerPredicatedOps` (novo): laço por elemento no `VL` efetivo, predicado lido no bit do byte mais baixo do
  elemento. Reusa `SveIntegerOps.get/set/signExtend/elementMask` (tornados package-private) — cada operação vive num lugar só.
  `AdvSimdLanes` **não** foi reusado: não tem predicação (mesmo achado da B16.1); a Armadilha 5 ficou pela opção (a), laço
  escalar por elemento.

**Achados que a spec não trazia**

1. **`_z` exige `FEAT_SVE2p2`, não SVE2**: no QEMU as 15 `_z` são `TRANS_FEAT(..., aa64_sme2p2_or_sve2p2, ...)`. O Achado 1 da spec
   (confirmar `_m` vs `_z`) resolvido: `_m` = merging, `_z` = zeroing (`desc` bit 0 no helper `DO_ZPZ`); o executor é o mesmo, o
   campo `zeroing` só zera o inativo. `bit 20` do encoding é o discriminador (1 = `_m`); `bit 19` separa bit-ops de inteiras.
2. **Shift por `esize` é válido** (`ASR` clampa em `esize-1`; `LSR`/`ASRD` zeram; `SRSHR`/`URSHR` por `esize` dão 0/bit de sinal) —
   inclusive `URSHR #64` em `.d`, que estoura o `long` (tratado à parte).
3. **`SRSHR`/`URSHR` sem estouro de 64 bits**: `(x >> s) + ((x >> (s-1)) & 1)` em vez de somar o meio-bit antes de deslocar.
4. As quantidades dos shifts por vetor contam com **todos os bits, sem sinal** (`DO_ASR` compara com `sizeof(N)*8-1` como
   `size_t`): um elemento `.d` com o bit 63 ligado é um shift enorme, não um negativo. O `_zpzw` lê `Zm` **por doubleword** e
   o lê ANTES de escrever o grupo (`Zm` pode ser o próprio `Zdn`).
5. `SDIV`/`UDIV`: divisão por zero dá 0 e `INT_MIN / -1 = INT_MIN` (sem exceção, sem saturar).
6. Um teste da B17.5 usava `0x04000061` (`add z1.b, p0/m, z1.b, z3.b`) como "aritmética predicada, ainda recusada" — agora é
   legítimo; a linha saiu da lista de não alocados.
7. Predicado `P` tem 1 bit por **byte**: em `VL=256`/`512` cabe em UMA palavra de 64 bits; setar `w > 0` nos testes estoura.

**Pendências nomeadas (nada excluído)**

- **`FPCR.AH` (FEAT_AFP)** não é modelado no core: `FABS`/`FNEG` implementam o caso `AH = 0`. Com `AH = 1` o QEMU preserva
  NaN (`sve_ah_fabs`/`sve_ah_fneg`).
- Gate SME-only (`aa64_sme_or_sve`/`aa64_sme2p2_or_sve2p2`): como nas B17.4/B17.5, o decoder exige `FEAT_SVE`/`FEAT_SVE2`/`FEAT_SVE2p2`.

**Testes** (`Aarch64SveIntegerPredicatedTest`, 283 casos, `VL` 256 **e** 512): decode de 53 palavras do assembler + contagem exata
de 68/53/48 linhas sob SVE2p2/SVE2/SVE; recusa dos buracos de opcode, das restrições de `esz` (divisão `.b/.h`, `_zzw .d`,
`FABS`/`FNEG .b`, extensões) e de `tsz = 0`; as 20 binárias × `esz` e as 6 de shift por vetor (incl. as 3 reversas com operandos
assimétricos) contra oráculo `BigInteger`, com predicado aleatório e **lixo nos bytes não-baixos**; TODAS as quantidades de
shift por imediato de cada `esz` (9 operações); largos com bit 63 e com `Zm = Zdn`; as 30 unárias `_m`/`_z` (`_z` zera, `_m`
preserva); `ASRD` × `ASR`; extremos (`MIN/-1`, `CLS`/`ABS`/`NEG` do mínimo); `VL` efetivo < implementado; trap `EC=0x19`;
streaming (`SVL=512`); bloco pelo `StandardIr64BlockLifter`. Mutações de sanidade (ASRD sem arredondar, `SRSHR` sem meio-bit,
`_z` ignorado, reversas sem troca, `SQSHLU` de negativo, `CLS` sem `-1`, `UMULH` com sinal, quantidade de shift com sinal): 8/8 mortas.

**Cobertura (JaCoCo):** `SveIntegerPredicatedOps`, `Aarch64SveDecoder` e `Ir64Op.SveIntegerPredicated`: **100% de linhas e branches**.

**Validação:** `mvn -o install` do arm-jitter 5860 + 74 testes, 0 falhas; `./gerar-cobertura-isa.sh` sem diff; `./gerar-cobertura-jit.sh` regenerado. G5: gbaemu (240, 17 ignorados de sempre), ndsemu (183), armbox (47), virtual-arm-box (87, 5 ignorados de sempre), n3dsemu (221) BUILD SUCCESS.
