# Trilha A — Truffle — Índice e dependências

Título, dependências e status de cada task da trilha. Quando o status diz "ver
**Resultado** na task", o histórico completo está no final do arquivo da task. Ver
[../README.md](../README.md) para protocolo de execução e invariantes globais.

| Task | Título | Depende de | Status |
|------|--------|-----------|--------|
| [A0](a0-spike-viabilidade.md) | Spike de viabilidade Truffle | — | ✅ ([relatório](RELATORIO-A0.md): viável; JITta no JBR 25 via Unchained; supera ASM em blocos grandes) |
| [A1](a1-multi-modulo-maven.md) | Build multi-módulo Maven | — | ✅ (parent + `core/` [artifactId `arm-jitter` intacto, jar idêntico] + `truffle/` vazio; gbaemu/ndsemu verdes sem mudar poms) |
| [A2](a2-emissor-minimo.md) | TruffleCodeEmitter mínimo (ALU) | A0, A1 | ✅ — ver **Resultado** na task |
| [A3](a3-cobertura-completa.md) | Cobertura completa de IrOp | A2 | ✅ — ver **Resultado** na task. PENDENTE (aceite #2 da task): usuário rodar divergence-checking com ROM real, ainda não implementado |
| [A4](a4-factory-e-bench.md) | Factory pública + bench 3 JVMs | A3 | ✅ — ver **Resultado** na task |
| [A5](a5-native-image-demo.md) | Demo native-image | A4 | 🟡 PARCIAL — ver **Resultado** na task ([relatório](RELATORIO-A5.md)). **Revalidada pela A7 (2026-07-27): permanece 🟡** — ver nota na linha A7 |
| [A6](a6-especializacao-nos-truffle.md) | Especialização de nós Truffle por `IrOp` (compilação real) | A5 | 🟡 PARCIAL (2026-07-16) — ver **Resultado** na task. Não fechou o binário nativo (bailout SVM) nem a suíte de equivalência sob compilação forçada automatizada — ambos ficam para trabalho seguinte (A7 herda o primeiro com diagnóstico mais preciso) |
| [A7](a7-native-image-revalidacao.md) | Revalidação native-image pós-A6 (medição, fecha aceite #2 da A5) | A6 | 🔴 medida (2026-07-27) — ver **Resultado** na task ([relatório](RELATORIO-A5.md#a7-data-resultado-pós-a6)). Nenhum dos dois critérios de aceite fechou nos dois ambientes simultaneamente → A5 NÃO promovida a ✅, permanece 🟡. Análise da causa raiz SVM e do custo de bootstrap Unchained ficam para sessão de modelo forte dedicada |
| [A8](a8-native-image-otimizacoes.md) | Otimizações native-image: PGO, -O3, GC, tabela startup/RSS | A7 | ✅ (2026-07-31) — ver **Resultado** na task |
| [A9](a9-native-shared-library.md) | arm-jitter como `.dll`/`.so` com API C (`native-image --shared`, módulo `capi/` novo, handles opacos + callbacks MMIO + smoke test em C) — consumo por qualquer linguagem com FFI; pedido do usuário 2026-07-16 | PR1 (backend interpretado): só ambiente GraalVM; PR2 (backend TRUFFLE): A7 verde | PR1 ✅ (2026-07-31) — ver **Resultado** na task. PR2 (backend Truffle) segue bloqueado: depende de "A7 verde", e A7 não fechou (bailout SVM do backend Truffle persiste) — fica para quando esse bailout for corrigido |
| [A10](a10-plano-truffle-completo.md) | ÉPICO: **fazer o Truffle funcionar de verdade** — medição de 2026-09-02: `IrOpNodeFactory` cobre **40 de 73** `Kind` e `TruffleCodeEmitter#supports` devolve `true` SEMPRE, então o backend **quebra com `IllegalStateException`** (não cai no fallback) em 33 Kinds — todo VFP, todo NEON, DSP dual/top-word, `HVC`/`SMC`/`ERET`/`MRS_bank`/`MSR_bank`, bitfield/`RBIT`/`SDIV`, `BKPT`, sysreg do perfil M. Truffle para AArch64 **não existe** (0 nós). Nunca apareceu porque a superfície quebrada nunca foi exercitada (gbaemu=ARMv4T, ndsemu=ARMv5TE, sem VFP). Fecha o achado da RFC B13.2 / pendência 3 da B13.6. Escada A10.1-A10.9 | — | 📋 plano (2026-09-02) |
| [A10.1](a10.1-truffle-supports-honesto.md) | **Correção, não feature** (única task urgente do épico): `TruffleCodeEmitter#supports` passa a consultar `IrOpNodeFactory.supports` novo em vez de `return true`, trocando **crash por fallback** — a infra do conserto (campo `fallback`, `fallbackBlockCount`, o `if (!supports)`) já existe e já está fiada, só nunca é `false`. Requisito central: **fonte única** para a lista de Kinds + **teste de coerência** factory↔supports (sem ele a correção se reintroduz sozinha). Nenhum Kind novo implementado | — | ✅ (2026-09-02) — ver **Resultado** na task |
| **A10.2** (sem arquivo) | **Medir a cobertura Truffle** em `docs/COBERTURA-JIT.md` | C12.1, A10.1 | ✅ **ABSORVIDA pela C12.1 (2026-09-03)** — a C12.1 entregou o relatório com as DUAS colunas Truffle já medidas (`Truffle 32: 40/77` na Tabela A, `Truffle 64: 0/96` na Tabela B, medidas chamando `IrOpNodeFactory.supports` de verdade, não lendo o fonte), mais o teste de guarda `JitCoverageReportGuardTest` que era o ponto da A10.2 ("sem isso a cobertura Truffle volta a apodrecer em silêncio"). Auditado nesta rodada de spec: **não sobrou resíduo** — nenhum documento afirma cobertura Truffle exaustiva (o Javadoc de `TruffleCodeEmitter`/`IrOpNodeFactory` foi corrigido pela própria A10.1, e `docs/TRUFFLE-BACKEND.md` não faz afirmação de cobertura). Task não escrita, e não deve ser: a escada segue em A10.3 |
| [A10.3](a10.3-nos-vfp.md) | Truffle: nós de **VFP** (14 `Kind`) — `VfpOpNode` novo delegando ao executor, no padrão das 7 categorias da A6. O maior bloco descoberto: qualquer binário com ponto flutuante usava o fallback do bloco inteiro. Como a A10.1 fez de `category` a **fonte única**, acrescentar o `Kind` atualiza `create` e `supports` de uma vez. Truffle 32 bits 40/77 → 54/77 | A10.1 ✅ | ✅ (2026-09-04) — ver **Resultado** na task |
| [A10.4](a10.4-nos-bitfield-divide.md) | Truffle: nós de **bitfield/`RBIT`/divisão** (4 `Kind`) — provavelmente cabem no `AluOpNode`. Armadilha real: `SDIV`/`UDIV` por zero devolve **0** no ARM e lança em Java, e `INT_MIN / -1` estoura em Java — a semântica correta já está no executor, então só quebra se alguém reimplementar no nó | A10.1 ✅ | ✅ (2026-09-04) — ver **Resultado** na task |
| [A10.5](a10.5-nos-sistema.md) | Truffle: nós de **sistema** (`Hvc`/`Smc`/`Eret`/`MrsBank`/`MsrBank`/`Breakpoint`, 6 `Kind`) — provavelmente no `SystemOpNode`. **4 deles transferem controle por exceção**, e é exatamente a superfície que a A10.1 descobriu quebrada e que nunca foi exercitada: vale o cuidado da **E7** (exceção de guest escapando para o host). `ERET` tem de ENCERRAR o bloco | A10.1 ✅ | ✅ (2026-09-04) — ver **Resultado** na task |
| [A10.6](a10.6-nos-dsp.md) | Truffle: nós de **DSP** (2 `Kind`) — no `MultiplyOpNode`, onde `DspMultiply` já está. O menor degrau do épico. Armadilhas: a variante `X` troca halfwords, `SMMLA` com arredondamento difere de sem, e a flag `Q` deve reproduzir o que o executor faz (G1 é o oráculo, não o manual) | A10.1 ✅ | ✅ (2026-09-04) — ver **Resultado** na task |
| [A10.7](a10.7-nos-neon.md) | **[REFINAR]** Truffle: nós de **NEON** (11 hoje, crescendo com B13). **Aguarda a RFC da C12.6** — a pergunta (nó por lane × nó que chama `AdvSimdLanes` × não fazer) é a MESMA dos dois backends e decide-se uma vez só, senão o projeto acaba com duas respostas incompatíveis. G1 tem de cobrir as 3 diferenças A32×A64 que o B13 documentou | A10.3, C12.6 | ⬜ |
| [A10.8](a10.8-truffle-aarch64-rfc.md) | **[RFC]** **Truffle para AArch64** — medição da C12.1: **0 de 96**, o backend NÃO EXISTE. RFC entre construir espelho completo / mínimo viável / não construir, com medição em HotSpot **e** GraalVM (o `docs/TRUFFLE-BACKEND.md` registra um caso em que o Truffle foi **5,24× pior**) e contra a linha de base do ASM 64 pós-C12.9. Argumento de peso contra: a escada A10 só DELEGA, e um backend Truffle que delega é o interpretador com overhead de árvore | A10.7, C12.9 | ⬜ |
| [A10.9](a10.9-fechamento.md) | **Fechamento do épico A10**: remedir as 2 colunas Truffle, medir `nativeBlockCount`/`fallbackBlockCount` em workload real (a medição que a A10.2 pediria, feita com o backend completo), e **responder honestamente se o backend ganha de algo** — "sem ganho medido" é resultado válido e tem de ser escrito, com a condição de reabertura. Coordena com a **C12.9** (mesmo arquivo) | A10.3-A10.8 | ⬜ |

Legenda: ⬜ pendente · 🟡 em andamento · ✅ concluída
