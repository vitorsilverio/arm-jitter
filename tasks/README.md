# tasks/ — Spec Driven Development

Cada arquivo aqui é uma task **autocontida**, escrita para ser executada por um agente
sem contexto prévio do projeto. Leia este arquivo inteiro antes de executar qualquer task.

## Protocolo de execução (obrigatório)

1. Leia a task inteira, incluindo **Armadilhas** e **Não fazer**.
2. Verifique a coluna **Depende de** — não execute uma task cujas dependências não
   estejam concluídas (status ✅ no índice abaixo).
3. Leia os arquivos-fonte citados na task ANTES de escrever código. Quando a task diz
   "espelhe o padrão de X", abra X e copie a estrutura, nomes e estilo.
4. Implemente APENAS o que está em "Inclui". Se algo parecer necessário e não estiver
   listado, PARE e pergunte ao usuário em vez de improvisar.
5. Todo comportamento observável novo precisa de teste automatizado no mesmo PR.
6. Valide com `mvn test` (JDK do projeto = JBR 25). Se não puder executar comandos,
   peça ao usuário para rodar e cole o resultado.
7. Ao concluir: atualize o status da task no índice deste README e faça um commit por
   task (mensagem em português, começando com o ID da task, ex.: `B1.2: ...`).

## Invariantes globais (NUNCA violar)

- **G1 — O interpretador é o oráculo.** `InterpretedCodeEmitter` define a semântica.
  Qualquer backend/otimização novo deve produzir estado de CPU idêntico, validado pelo
  `BlockEquivalenceHarness` (`codegen/equivalence/`).
- **G2 — GBA = ARMv4T.** NUNCA aplique instruções ou comportamentos ARMv5+ ao preset
  `ARMV4T`. Todo recurso novo de arquitetura é gateado por `ArmFeature` e habilitado
  apenas nos presets corretos. GBATEK descreve GBA+NDS juntos — cuidado ao ler.
- **G3 — Sem breaking change.** Factories, assinaturas públicas e comportamento default
  não mudam. Recurso novo entra por factory/flag/preset novo.
- **G4 — `Cycle`/`Fetch` nunca recebem guard condicional** no codegen: instrução com
  condição falsa ainda consome ciclo e fetch.
- **G5 — gbaemu e ndsemu são o gate de regressão.** Mudança no arm-jitter exige
  `mvn install` local e suites verdes nos dois consumidores (peça ao usuário se não
  puder rodar).
- **G6 — Sem números mágicos.** Constantes arquiteturais (registradores PC/LR, máscaras,
  offsets) recebem nome.
- **G7 — Javadoc `///` (markdown, Java 25) em toda API pública**, em português.

## Estrutura de uma task

`Contexto → Objetivo → Inclui/Não inclui → Especificação → Passos → Aceite → Validação → Armadilhas`

Tasks marcadas com **[REFINAR]** são especificações de alto nível que devem ser
detalhadas (nova rodada de spec) quando suas dependências concluírem — não execute
uma task [REFINAR] diretamente.

## Issues do GitHub × `tasks/`

Os dois coexistem e não competem:

- **Issue** = um **problema ou pedido observável**, do ponto de vista de quem usa. "O
  Pokémon FireRed tem 3 glitches visuais na batalha." "O ndsemu não boota em INTERPRETED."
  "Queria ROMs recentes no menu." Uma issue descreve **sintoma, repro e evidência**; ela não
  diz como consertar e não tem prazo.
- **Task** (`tasks/*.md`) = uma **especificação executável**, do ponto de vista de quem
  implementa: escopo fechado, `Inclui`/`Não inclui`, passos, aceite, armadilhas. Uma task
  existe porque alguém já decidiu **como** atacar o problema.

Fluxo normal: issue nasce primeiro → quando vira prioridade, uma **sessão de modelo forte**
escreve a task correspondente → a task cita `Fecha: <repo>#<n>` no cabeçalho → o commit que
fecha a task usa `Closes <repo>#<n>` (ou, entre repos diferentes,
`Closes vitorsilverio/<repo>#<n>`).

Casos que **não** viram issue:
- Itens puramente internos de refactor sem sintoma externo.
- Sub-tasks de um épico já especificado (B6.3.1, B6.3.2, ...) — são decomposição de
  implementação, vivem só em `tasks/`.

Casos que **não** viram task (ainda):
- Tudo que está em "Pendências que EXIGEM sessão de modelo forte" — vira **issue** com a
  label `needs-design`, e só vira task depois que alguém desenhar a solução.

**Nunca duplique o corpo.** A issue é o sintoma; a task é a solução; cada uma referencia a
outra por link.

## Índice e dependências

| Task | Título | Depende de | Status |
|------|--------|-----------|--------|
| [A0](trilha-a-truffle/a0-spike-viabilidade.md) | Spike de viabilidade Truffle | — | ✅ ([relatório](trilha-a-truffle/RELATORIO-A0.md): viável; JITta no JBR 25 via Unchained; supera ASM em blocos grandes) |
| [A1](trilha-a-truffle/a1-multi-modulo-maven.md) | Build multi-módulo Maven | — | ✅ (parent + `core/` [artifactId `arm-jitter` intacto, jar idêntico] + `truffle/` vazio; gbaemu/ndsemu verdes sem mudar poms) |
| [A2](trilha-a-truffle/a2-emissor-minimo.md) | TruffleCodeEmitter mínimo (ALU) | A0, A1 | ✅ (`CodegenBackend.TRUFFLE` no core — única mudança lá; `TruffleCodeEmitter`/`TruffleBlockRootNode` no módulo `truffle/`: `RootNode` com `IrOp[] @CompilationFinal` + `@ExplodeLoop`, delegando CADA op a `IrBlockExecutor.executeOp` — zero semântica de ALU/flags reimplementada, G1 intacto; suportado nativamente = ALU cond AL sem ShiftedRegister com MOV/ADD/SUB/AND/CMP + Cycle/Fetch, resto cai no fallback `InterpretedCodeEmitter` inteiro; deps truffle-api/truffle-runtime 25.0.1 só no módulo truffle; equivalência via `BlockEquivalenceHarness` do core [ALU puro nativo / op não suportada→fallback / bloco vazio]; suíte core 467 + truffle 3 verdes, `mvn test` verde na raiz; gbaemu 216 + ndsemu 175 revalidados) |
| [A3](trilha-a-truffle/a3-cobertura-completa.md) | Cobertura completa de IrOp | A2 | ✅ (insight arquitetural: `TruffleBlockRootNode` já delegava CADA op — mesmo as "suportadas" da A2 — a `IrBlockExecutor#executeOp`, o mesmo dispatcher exaustivo usado pelo fallback PER_OP do ASM; o único motivo de blocos caírem no fallback `WHOLE_BLOCK` era o fixup de PC no final do nó ser INCONDICIONAL. Corrigido: `executeBlock` agora acumula `pcChanged` por op — igual ao loop de `IrBlockExecutor#execute` — e só faz o fixup quando nenhuma op mudou o PC. Com isso `TruffleCodeEmitter.supports()` virou `true` sempre: TODO IrOp (ShiftedRegister, condição≠AL, memória, branches, LDM/STM, PSR/SWI/coprocessador, ARMv6/v6K/ARMv5TE) já compila nativamente, sem precisar dos 7 PRs por categoria do plano original — não há especialização de nó por op ainda (isso é ganho de performance/JIT, fora do escopo de correção da A3; ver A4). `fallback`/`InterpretedCodeEmitter` mantidos por compat de API (G3), caminho inatingível na prática. Suite truffle 7 testes verdes cobrindo ALU+ShiftedRegister, 14 cond × 16 NZCV, Load/Store, Branch, Multiply+LDM/STM; core 498 + truffle 7 verdes na raiz do reactor. PENDENTE (aceite #2 da task): usuário rodar divergence-checking com ROM real, ainda não implementado) |
| [A4](trilha-a-truffle/a4-factory-e-bench.md) | Factory pública + bench 3 JVMs | A3 | ✅ (`TruffleJitRuntimeFactory.truffleArmThumb(cacheEntries, hotThreshold[, arch])` no módulo `truffle/`, espelha `JitRuntimeFactory.armThumb` — mesmo pipeline tiered [frio interpretado + quente `TruffleCodeEmitter`], otimizador GBA aplicado no nível do `JitRuntime` [`TruffleCodeEmitter` não recebe otimizador no construtor, diferente do `AsmCodeEmitter`]; 6 testes novos espelhando `JitRuntimeJvmFactoryTest`. Bench REAL coletado (não reaproveitado da A0): emissores de PRODUÇÃO `AsmCodeEmitter`×`TruffleCodeEmitter` em JBR 25.0.3 + Truffle Unchained [`Truffle.getRuntime().getName()=="GraalVM CE"` confirmado — compilação real, não fallback interpretado], mesma sessão/máquina, best-of-5×2M execuções, blocos 20/80/320 instr: ASM vence 5,17× a 20 instr, Truffle vence 1,33× a 80 e 5,5× a 320. **2026-07-11 (sessão seguinte): terceira coluna fechada** — usuário instalou `E:\graalvm-jdk-25.0.3+9.1` (banner "Oracle GraalVM", não GraalVM CE puro — release lista módulos enterprise; distribuição community separada não existe mais desde o GraalVM 21). Bench idêntico rodado com essa JVM [`Truffle.getRuntime().getName()=="Oracle GraalVM"` confirmado — compilação real]: ASM vence 5,24× a 20 e **2,20× a 80** [diferente do JBR, onde Truffle já vence a 80 — o C2 desta build de HotSpot compila o bloco de 80 instr ~4× mais rápido que o C2 do JBR, resultado estável em 3 execuções], Truffle vence 7,18× a 320. Achado novo documentado: o ponto de crossover ASM↔Truffle é JVM-dependente, não um número fixo — recomendação do README ajustada para não assumir "~80 instruções" universalmente. Tabela completa nas 3 linhas [seção "Runtime JIT" do README]. Bench NÃO rodou dentro de gbaemu/ndsemu com ROM real — Truffle não tem NENHUM wiring nos consumidores [fora de escopo, factory é opt-in]; suites gbaemu 216 + ndsemu 175 permanecem verdes sem mudar defaults [G3/G5]; `mvn -o test` na raiz revalidado verde nesta sessão) |
| [A5](trilha-a-truffle/a5-native-image-demo.md) | Demo native-image | A4 | 🟡 PARCIAL ([relatório](trilha-a-truffle/RELATORIO-A5.md)) — repo `armbox` (commit dedicado A5): `Backend.TRUFFLE` novo (`--truffle`) ligado a `TruffleJitRuntimeFactory`; perfil Maven `native` (`native-maven-plugin` 0.11.1) gera `armbox.exe` (39,5MB) com `JAVA_HOME=`GraalVM 25.0.3 Oracle + ambiente MSVC (`vcvars64.bat`, exigido no Windows); backend ASM recusado cedo sob native-image via `ImageInfo.inImageCode()` com mensagem clara (armadilha ✅). **Aceite #1 ✅** (stdout/exit idênticos à JVM para `hello.elf` e `busybox-armv5l` reais). **Aceite #2 🔴 não alcançado**: `TraceCompilation` mostra 0 blocos compilados com sucesso — TODA tentativa de compilar um `TruffleBlockRootNode` contra um bloco ARM real (loop de verdade via busybox, não os blocos sintéticos retos do bench A0/A4) bilateral bailout de partial evaluation, tanto no binário nativo (`FrameWithoutBoxing should not be materialized`) quanto no JBR com Truffle Unchained (`tooDeepInlining`) — causa raiz: `IrBlockExecutor#executeOp` (A3) é um dispatcher único exaustivo sobre TODO `IrOp.Kind`, sem especialização por nó Truffle, e o PE não consegue expandir/podar os ramos para nenhum bloco real; consequência medida: `--truffle` ficou MAIS LENTO que `--interp` (2,76s vs 1,77s, loop de 2000 iterações) porque paga o custo de compilar em background sem nunca ter sucesso. Não é bug de A5 nem específico de native-image — é limitação pré-existente de A2/A3 nunca antes exercitada com blocos reais; recomendação registrada no relatório para uma eventual task futura de especialização de nós Truffle por `IrOp`, fora do escopo desta task. **Revalidada pela A7 (2026-07-27): permanece 🟡** — ver nota na linha A7 |
| [A6](trilha-a-truffle/a6-especializacao-nos-truffle.md) | Especialização de nós Truffle por `IrOp` (compilação real) | A5 | 🟡 PARCIAL (2026-07-16) — implementadas as 7 categorias INTEIRAS da taxonomia da spec (`AluOpNode`, `MultiplyOpNode`, `MemoryOpNode`, `TransferOpNode`, `BranchOpNode`, `SystemOpNode`, `CycleFetchOpNode`), cobrindo as 40 `IrOp.Kind` sem sobrar nenhuma categoria "genérica" (a taxonomia da própria task já esgota o sealed `IrOp`); cada nó guarda o op concreto `@CompilationFinal` e despacha DIRETO ao método do executor de categoria correspondente (switch pequeno de até 11 casos POR nó, nunca o switch de 40 casos de `executeOp`), escolhido na emissão por `IrOpNodeFactory`; `TruffleBlockRootNode` monta um array `@Children IrOpNode[]` em vez do array de dados `IrOp[]`, mantendo `@ExplodeLoop` (item 2 da spec, decisão confirmada). `@TruffleBoundary` aplicado nos 4 pontos de escape reais (memória via `MemoryOpNode`/`TransferOpNode`, `TableBranch`, `Swi`/`Coprocessor`/`StoreReturnState`/`ReturnFromException` em `SystemOpNode`, `Fetch`) — mais grosso que o "só no `AddressSpace.read/write`" literal do item 3, porque `IrExecutionSupport`/os 6 executores vivem no `core/` e NÃO PODEM depender de `truffle-api` (G3/A1 — limite de módulo pré-existente); `ConditionProfile` nos 2 pontos do item 4 (guard por-op quando `condition != AL`, `pcChanged` do fim do bloco). **Desvio de infra necessário e documentado**: os 6 executores (`IrAluExecutor` etc.) e seus métodos eram *package-private* em `core/.../codegen/executor` — impossível chamá-los do módulo `truffle/` sem alguma mudança; resolvido widening a visibilidade para `public` (classes+métodos, sem tocar nenhuma linha de `IrBlockExecutor#execute`/`executeOp`) + 6 getters novos em `IrBlockExecutor` (`aluExecutor()` etc.) — aditivo, G3 intacto, G1 intacto (zero semântica nova). **Aceite 1 (TraceCompilation) ✅ no JBR 25 + Truffle Unchained**: mesmo workload de A5 (`busybox sh -c` com loop real) — **137 `opt done`, 0 `opt failed`** (antes: 0/0, só bailout); saída do programa (`done 5000`) correta sob compilação real. **Aceite 1 🔴 native-image**: rebuildado com GraalVM 25.0.3+MSVC (mesma receita da A5) — MESMO bailout `FrameWithoutBoxing should not be materialized` persiste (916 `opt failed`, 0 `opt done`) — a especialização de nós resolveu o bailout no pipeline JVMCI/HotSpot (JBR) mas NÃO no pipeline SVM/Enterprise Truffle Compiler do native-image; são pipelines de compilação Truffle distintos, e a causa raiz sob SVM continua sem diagnóstico — fica para a A7, que já é a task dedicada a fechar native-image. Aceite 2 (suíte truffle/equivalência): 606 core + 13 truffle testes verdes (`mvn -o test`, JBR 25); a suíte formal com compilação FORÇADA (thresholds baixos + `CompilationFailureAction=Throw`, pedida pela Armadilha 1) não foi automatizada nesta sessão (exigiria perfil surefire novo com module-path Unchained) — mitigado por evidência prática equivalente: o programa busybox rodou até o fim com resultado correto sob 137 blocos REALMENTE compilados. Aceite 3 (ganho): NO NATIVE-IMAGE segue mais lento (`--truffle` ~2,6s vs `--interp` ~1,8s, 2000 iterações — igual à A5, pois lá a compilação ainda falha); NO JBR+Unchained também mais lento em wall-time de processo curto (~3,3-4,9s vs ~2,2s) mas essa medição é dominada pelo custo fixo de carregar os módulos Truffle/Graal via classpath Unchained num processo de vida curta, não comparável 1:1 à medição de processo único da A5 — o ganho estrutural (compilação passou a ACONTECER) não se traduziu em ganho de wall-time neste workload curto; não invalidada a hipótese de ganho em blocos/traces grandes de vida longa (A4), só não demonstrada aqui. `mvn -o test` verde no reactor completo + gbaemu (216, sem divergência) + ndsemu (175) revalidados verdes (G5, só por precaução — nenhuma semântica mudou). **Resumo do que fechou vs não fechou**: fechou o objetivo central (compilação REAL de blocos ARM reais, antes 0/0 nos dois ambientes, agora 137/0 no JBR) e G1/G3 intactos; não fechou o binário nativo (mesmo bailout de A5, agora sabidamente um problema SVM-específico não relacionado ao dispatcher) nem a suíte de equivalência sob compilação forçada automatizada — ambos ficam para trabalho seguinte (A7 herda o primeiro com um diagnóstico mais preciso). |
| [A7](trilha-a-truffle/a7-native-image-revalidacao.md) | Revalidação native-image pós-A6 (medição, fecha aceite #2 da A5) | A6 | 🔴 medida (2026-07-27, [relatório](trilha-a-truffle/RELATORIO-A5.md#a7-data-resultado-pós-a6)) — rebuild completo com A6 mergeada (`mvn -o install` JBR 25 + `armbox.exe` via GraalVM 25.0.3+MSVC) confirmado, aceite #1 (corretude `hello.elf`/`busybox-armv5l`) ✅ mantido nos dois ambientes/backends. **Aceite #2 (TraceCompilation) misto**: JBR 25+Unchained agora COMPILA de verdade (1812 `opt done`, 0 `opt failed` — A6 confirmada, não é artefato pontual da sessão dela); native-image reproduz o MESMO bailout `FrameWithoutBoxing should not be materialized` da A5 byte a byte (0 `opt done`, 903 `opt failed`) — a especialização de nós de A6 resolveu o pipeline JVMCI mas não teve nenhum efeito sobre o pipeline SVM/Enterprise Truffle Compiler, causa raiz sob SVM ainda sem diagnóstico. **Aceite de ganho de tempo 🔴 falha nos DOIS ambientes** (loop de 2000 iterações, best-of-5): native-image `--truffle` 3307ms vs `--interp` 2164ms; JBR+Unchained `--truffle` 3713ms vs `--interp` 2508ms (JBR mais lento apesar de compilar de verdade — wall-time de processo curto dominado pelo custo fixo de bootstrap dos módulos Unchained, não comparável ao ganho estrutural). **Nenhum dos dois critérios de aceite fechou nos dois ambientes simultaneamente → A5 NÃO promovida a ✅, permanece 🟡.** `mvn -o test` verde (1322 core + 13 truffle, JBR 25). Análise da causa raiz SVM e do custo de bootstrap Unchained ficam para sessão de modelo forte dedicada (fora do escopo de medição desta task, conforme a própria task instrui) |
| [A8](trilha-a-truffle/a8-native-image-otimizacoes.md) | Otimizações native-image: PGO, -O3, GC, tabela startup/RSS | A7 | ✅ (2026-07-31, armbox `b1411f6`) — 5 variantes medidas (baseline/`-O3`/`-O3 -march=native`/`--gc=G1`/`--pgo=<profile> -O3`), mesma máquina/sessão, best-of-5 para startup+throughput, `PeakWorkingSet64` por polling para RSS, corretude `hello.elf`+`busybox echo hi` nos 2 backends. `--gc=G1` falhou no build com erro claro (`only supported on Linux AMD64 and AArch64` — resultado válido, não bloqueio, registrado). **PGO+`-O3` venceu (ou empatou) as 4 métricas simultaneamente** — startup 33,0ms (empate técnico com `-march=native` 33,5ms), throughput truffle 2101ms (melhor, vs 2175ms de `-march=native`), throughput interp 1466ms (melhor, vs 1536ms), RSS 97,3MB (ÚNICA variante abaixo do baseline 101,8MB) — promovido a default do perfil `native` do armbox (`pom.xml`), perfil comitado em `armbox/native-profile/default.iprof` com receita de regeneração no README. `-march=native` descartado apesar de próximo (binário não-portável, não venceu PGO em nenhuma métrica). Tabela completa + comandos reproduzíveis no README do armbox. `mvn -o test` verde (arm-jitter + armbox, JBR 25); G5 não se aplica (nenhum arquivo Java/IR tocado, só `pom.xml`/build config do armbox e README) |
| [A9](trilha-a-truffle/a9-native-shared-library.md) | arm-jitter como `.dll`/`.so` com API C (`native-image --shared`, módulo `capi/` novo, handles opacos + callbacks MMIO + smoke test em C) — consumo por qualquer linguagem com FFI; pedido do usuário 2026-07-16 | PR1 (backend interpretado): só ambiente GraalVM; PR2 (backend TRUFFLE): A7 verde | PR1 ✅ (2026-07-31, ambiente GraalVM+MSVC ficou disponível nesta máquina — ver `tasks/FILA-EXECUCAO.md`) — módulo `capi/` novo (irmão de `core/`/`truffle/`) com `ArmJitterCApi`/`CoreHandle`/`CallbackOpenBus`/`MmioRead·WriteFunctionPointer`; API v1 completa (`aj_create`/`destroy`/`map_ram`/`write`/`read`/`set_mmio_callbacks`/`get·set_register`/`get·set_cpsr`/`set_pc`/`run_cycles`/`set_irq_line`/`save·load_state`/`last_error`), backend `INTERPRETED_IR` só (`backendId=1` devolve erro claro, reservado para PR2). **Achado real de native-image** (fora do escopo de qualquer bug pré-existente, específico desta task): um campo estático inicializado com `WordFactory.nullPointer()` (`globalErrorBuffer`) faz o `native-image` tentar SIMULAR o inicializador de classe em tempo de build e falhar com `Unsupported kind: Object` em **todo** `@CEntryPoint` da classe (`SimulateClassInitializerSupport` não sabe colocar um valor `Word` no heap da imagem) — corrigido removendo o inicializador explícito (campo estático `Word` fica implicitamente zero-inicializado, que É o ponteiro nulo em runtime sob SVM; o mesmo NÃO se aplica a inicializadores de campo de INSTÂNCIA, que rodam em runtime via `new`, não simulados). PR1 do smoke test (`capi/src/test/c/smoke.c` + `capi/build-and-run-smoke.ps1`) cobre: programa ARM real (soma sem SWI, termina por orçamento de ciclos) até R0 conferido, callback MMIO (read devolve constante do C, write registra endereço/tamanho/valor do último byte), save/load state round-trip, e handle inválido devolvendo erro claro sem crash — todas as 19 checagens passam contra o `.dll` real compilado com `cl.exe`/GraalVM 25.0.3. `mvn -o test`/`install` verdes (JBR 25, capi/ compila sem GraalVM instalado — só o perfil `native-lib` exige). README ganhou a seção "Biblioteca nativa (C API)". PR2 (backend Truffle) segue bloqueado: depende de "A7 verde", e A7 não fechou (bailout SVM do backend Truffle persiste, ver índice da A7 acima) — fica para quando esse bailout for corrigido. |
| [B0](trilha-b-arquiteturas/b0-rfc-ir-64bit.md) | RFC: IR de 64 bits (para AArch64) | — | ✅ ([RFC](../docs/RFC-IR-64BIT.md) **APROVADA 2026-07-10: Opção B** — IR-64 paralelo + `Aarch64Core` irmão + `AddressSpace64`; §5 em vigor p/ B1–B3, ex.: monitor LDREX/STREX de B1.4 com endereço `long`; B6/F1+ destravados quando priorizados) |
| [B1.1](trilha-b-arquiteturas/b1.1-armv6-features-preset.md) | ArmFeatures + preset ARMV6K | — | ✅ (10 features + `ARMV6K` via `extending`; zero-diff de runtime; gbaemu/ndsemu verdes) |
| [B1.2](trilha-b-arquiteturas/b1.2-armv6-extend-reverse.md) | SXT/UXT, REV, UMAAL | B1.1 | ✅ (decoder gateado + IR + interpretador; extend via ShiftedRegister ROR, UMAAL como `LongMultiply.accumulateDouble`; ASM rejeita até B1.6 — equivalência PER_OP provada; suites arm-jitter 389 + gbaemu 215 + ndsemu 175 verdes) |
| [B1.3](trilha-b-arquiteturas/b1.3-armv6-simd-media.md) | SIMD paralelo, GE flags, SAT, USAD8, PKH | B1.1 | ✅ (2 PRs: GE\[3:0\] no CPSR + `IrOp.ParallelAlu` com enums próprios; SEL/`IrOp.Sel`, PKH via `IrOp.Alu`+ShiftedRegister, SSAT/USAT(16)/`IrOp.Saturate` com Q sticky, USAD(A)8/`IrOp.AbsDiffSum`; DCE/ConstantFold/FlagMerge atualizados; ASM rejeita até B1.6 — equivalência PER_OP provada; MSR v4T/v5TE pinado por teste; suite 441 verde) |
| [B1.4](trilha-b-arquiteturas/b1.4-armv6-exclusive.md) | LDREX/STREX + monitor de exclusividade | B1.1 | ✅ (monitor no `ArmCore` com endereço `long` — §5 da RFC IR-64; `IrOp.LoadExclusive/StoreExclusive/ClearExclusive`; STREX checa o monitor ANTES de escrever; exceção/CLREX limpam o monitor; `CpuSnapshot` inclui o monitor para o harness de equivalência; ASM rejeita até B1.6 — equivalência PER_OP provada; gating EXCLUSIVE_WORD/EXCLUSIVE_SIZED e UNPREDICTABLE→UNDEFINED testados; suite 455 verde) |
| [B1.5](trilha-b-arquiteturas/b1.5-armv6-system.md) | CPS, SRS/RFE, SETEND, hints WFI/WFE | B1.1 | ✅ (decoder gateado com UNDEFINED explícito sem a feature, espelhando LDREX/STREX/CLREX; CPS reusa `ArmCore.setCpsr` — mesmo caminho de banco de MSR/exceção — e é NOP em modo User; SRS/RFE empilham/restauram LR+SPSR atuais nos 4 modos de endereçamento, UNDEFINED em User/System; SETEND expõe o bit E novo do CPSR e bloqueia acesso de dados com E=1 (`UnsupportedOperationException` central em `IrExecutionSupport`); WFI vira `core.halt()`; YIELD/WFE/SEV/hint genérico continuam sem IR nova (mask=0 já é no-op); ASM rejeita até B1.6; suíte arm-jitter 467 + gbaemu 216 + ndsemu 175 verdes) |
| [B1.6](trilha-b-arquiteturas/b1.6-armv6-asm-nativo.md) | Emissão nativa ASM das ops v6 | B1.2–B1.5 | ✅ (3 PRs) — PR1 extend/reverse/UMAAL (B1.2): bytecode direto (I2B/I2S/AND/`Integer.reverseBytes`) + helpers `extendByte16`/`reverseHalfwords`/`reverseSignedHalfword`; UMAAL soma RdLo/RdHi como duas parcelas u32 no `LMUL` já emitido. PR2 paralelas+GE+PKH+saturação+USAD (B1.3): helpers `parallelAlu`/`sel`/`saturate`/`absDiffSum` em `AsmRuntimeHelpers` (GE e Q sticky como efeito colateral no core, espelhando o padrão ARMv5TE); PKHBT/PKHTB viram bytecode direto (AND+OR). PR3 LDREX/STREX/CLREX (B1.4): helpers `loadExclusive`/`markExclusive`/`exclusiveMonitorCovers`/`clearExclusiveMonitor`; STREX checa o monitor ANTES de qualquer escrita via branch no bytecode (mesma ordem do interpretador — falha não toca a memória), formas doubleword (LDREXD/STREXD) desenroladas inline (mark + 2×loadWord/storeWord). `AsmNativePolicy` só rejeita mais as instruções de sistema da B1.5 (CPS/SETEND/SRS/RFE/WFI — raras/kernel-only, decisão documentada); `supportedAluOpcodes()` virou `EnumSet.allOf`. Equivalência exaustiva 14 cond × 16 NZCV por grupo de op (`ArmV6ExtendReverseNativeEquivalenceTest` + `ArmV6ParallelSaturateNativeEquivalenceTest` + `ArmV6ExclusiveNativeEquivalenceTest`, esta última cobrindo B/H/D + falha de monitor) + `perOpFallbackOpCount()==0` nos blocos sintéticos. Suites arm-jitter 498 + gbaemu 216 (JitInterpreterDivergenceTest sem divergência) + ndsemu 175 verdes em todas as 3 PRs |
| [B1.7](trilha-b-arquiteturas/b1.7-armv6-unaligned-access.md) | Acesso desalinhado ARMv6+ (`UNALIGNED_ACCESS`; hoje a rotação v4T é incondicional — corrupção silenciosa em binários v6+) | — | ✅ (`ArmFeature.UNALIGNED_ACCESS` novo, adicionado a `ARMV6K` [e por herança `ARMV6K_THUMB2`], NÃO a `ARMV4T`/`ARMV5TE` [G2]. Interpretado: `IrExecutionSupport` ganhou 4 métodos novos — `readWordForLoad`/`readHalfwordForLoad`/`writeWordForStore`/`writeHalfwordForStore` — que forkam por feature SEM tocar `read32Arm7`/`read16Arm7`/`write32Arm7`/`write16Arm7` (que continuam intocados e são os únicos métodos que LDM/STM/LDRD/STRD/LDREX/STREX/SWP/RFE/SRS/PUSH/POP chamam — excluídos da feature "sem mudança" por construção, já que essas instruções sempre passam endereço pré-alinhado). Acesso atravessado composto por 4×`read8`/`write8` independentes (nunca por um `read32`/`write32` largo recortado) — prova a fronteira de página/região por construção, sem depender de como o `AddressSpace` concreto trataria uma leitura/escrita larga desalinhada. `LDR`/`LDRH` com destino no PC excluídos explicitamente (`loadsToProgramCounter`, item 4 da task) — só `IrMemoryExecutor.executeLoad`/`executeStore` (o LDR/STR/LDRH/STRH "normal") chamam os métodos novos; `executeLoadLiteral` inalterado (endereço sempre alinhado por construção, fork seria no-op). `LDRSH` desalinhado sob a feature NÃO herda o quirk v4T de virar leitura de byte — lê o halfword atravessado inteiro e sinal-estende normalmente (comportamento real ARMv6+, diferente do v4T pinado à parte). ASM: mesmo fork em `AsmRuntimeHelpers` (`loadWordCrossed`/`loadHalfCrossed`/`loadHalfSignedCrossed`/`storeWordCrossed`/`storeHalfCrossed` novos, ponto único de emissão em `AsmBlockCompiler.emitLoad`/`emitStore`) — `AsmBlockCompiler` ganhou o campo `unalignedAccess` (mesmo padrão de `loadPcInterworks`/`LOAD_PC_INTERWORKING`), passado por um novo construtor de 3 argumentos; `AsmCodeEmitter` decide a partir de `architecture.has(UNALIGNED_ACCESS)`. **Achado durante a implementação**: o caminho legado de STORE já não fazia rotação nenhuma (só LOAD rotaciona em ARMv4T/v5TE) — `write32Arm7`/`write16Arm7` sempre delegaram direto a `AddressSpace.write32/write16` sem alinhar; usando `TestAddressSpace` (que força alinhamento por halfword, não por word inteira) isso produz um comportamento legado sutil e pré-existente [offset 1 coincide com offset 0; offsets 2 e 3 coincidem entre si] — pinado exatamente como é (não "corrigido", G2). Testes novos: `ArmV6UnalignedAccessTest` [12 — pin do legado rotacionado/quirk LDRSH por offset 1/2/3, valores atravessados concretos batendo o exemplo da task [`11 22 33 44 55` → `LDR@+1`=`0x55443322`], sinal-estensão de halfword atravessado, `LDR pc` mantendo alinhamento legado sob a feature, pin de STORE legado + atravessado, fronteira de página real via `PagedAddressSpace` [páginas de 256B mapeadas em dois arrays separados]] + `ArmV6UnalignedAccessNativeEquivalenceTest` [4 — LDR/STR e LDRH/STRH desalinhados, 14 condições × 16 NZCV, `LDR pc` excluído, `perOpFallbackOpCount()==0`]. Suites arm-jitter 606 core + 13 truffle + gbaemu 216 [`JitInterpreterDivergenceTest` sem divergência] + ndsemu 175 verdes [zero-diff, feature não usada lá]. armbox: `armv6k-torture.s` ganhou as checagens 27/28 [`LDR`/`STR` desalinhado real via `--arch=armv6k`, mesmo vetor de bytes do exemplo da task], recompilado com `build-testdata.ps1` [devkitARM `-march=armv6k`]; suíte armbox 26 verde nos 3 backends)|
| [B1.8](trilha-b-arquiteturas/b1.8-armv6-be8-data-endianness.md) | Suporte real a BE8 (`CPSR.E=1`, acesso de dados big-endian) — revoga a exceção MVP da B1.5 | B1.5–B1.7 | ✅ 2026-08-15 (motivada pela F3/`virtual-arm-box`: kernel Linux ARMv6K real usa `SETEND BE` cedo no boot. Confirmado contra ARM DDI 0406C A2.9: BE8 = "byte-invariant" — fetch de instrução SEMPRE little-endian, só acesso de DADO multi-byte troca a ordem de composição/decomposição [`Integer.reverseBytes` para word, troca dos 2 bytes para halfword]; byte único é invariante por definição. Achado real: como `IrOp.Load`/`Store`/`MultipleTransfer`/`LoadExclusive`/`StoreExclusive` já eram TODAS ops nativas ASM desde B1.6 [`AsmNativePolicy` nunca as rejeitou], o caminho nativo rodava com `CPSR.E=1` SEM NENHUMA checagem [nem lançava, ao contrário do interpretado] — silenciosamente produzia valor little-endian errado; corrigido junto. Troca de bytes condicionada a `core.cpsr().isBigEndian()` em tempo de EXECUÇÃO [não em tempo de compilação/preset] nos 8 métodos primitivos de `IrExecutionSupport` [`read32Arm7`/`read16Arm7`/`write32Arm7`/`write16Arm7` + os 4 `readCrossed*`/`writeCrossed*` de B1.7] e nos 9 espelhos ASM de `AsmRuntimeHelpers` [`loadWord`/`loadHalf`/`loadHalfSigned`/`storeWord`/`storeHalf` + as 4 variantes `*Crossed`] — `loadByte`/`storeByte`/`read8Arm7`/`write8Arm7` INTOCADOS [invariantes por definição]. Como todo acesso de dados do core [LDR/STR/LDRH/STRH simples e desalinhado, LDRD/STRD, LDM/STM/PUSH/POP/SRS/RFE, LDREX/STREX, SWP, transferências VFP] passa por esses mesmos primitivos compartilhados, BE8 passa a funcionar corretamente para TODOS eles sem nenhum código extra — não foi preciso "cortar escopo" para LDR/STR/LDRH/STRH como a task previa como plano B; nenhuma mudança em `AsmBlockCompiler`/`AsmNativePolicy` foi necessária. `checkLittleEndianData` [a exceção da B1.5] removido. G2/G3 automáticos: só presets com `SETEND_BIG_ENDIAN_DATA` [ARMV6K/ARMV6K_THUMB2/ARMV7A/ARM11_MPCORE] conseguem decodificar `SETEND` para setar E=1 — ARMV4T/ARMV5TE [GBA/NDS] nunca chegam lá, então o código novo é sempre a identidade nesses presets; com E=0 [default de TODO preset, inclusive os que têm a feature] o código novo também é sempre a identidade. Teste pré-existente `dataAccessWithBigEndianThrowsClearly` [B1.5] substituído — não lança mais, comportamento revogado de propósito. Testes novos: `ArmV6Be8DataEndiannessTest` [12 — word/halfword trocados vs. E=0, ida-e-volta STR+LDR, `LDRB`/`STRB` invariantes [teste negativo explícito], fetch de instrução permanece little-endian após `SETEND BE`, `LDM`/`STM` com words trocadas independentemente [prova que o primitivo compartilhado cobre "de graça", não assumido], regressão G3 pinando o exemplo da B1.7 com E=0] + `ArmV6Be8NativeEquivalenceTest` [3 — LDR/STR alinhado e desalinhado [`UNALIGNED_ACCESS`+E=1 juntos] e LDRH/STRH, 14 condições × 16 NZCV cada, equivalência interpretado×ASM]. Suítes: arm-jitter 1367 core+truffle [`mvn -o test`] + `mvn -o install` OK. **G5 validado**: gbaemu verde, ndsemu verde [zero-diff, nenhum dos dois usa BE8], armbox 40/41 verde — a 1 falha [`Armv7TortureTest`, `ArrayIndexOutOfBoundsException` em `VfpRegisters`] é a MESMA pré-existente já documentada pela B1.7/sessão F3 [subsistema VFP não tocado por esta task, não é regressão]. `virtual-arm-box`/F3/`Raspi1BootTest` NÃO tocado [repo/sessão separados, conforme escopo] — a sessão seguinte consome esta versão via o `.m2` local recém-publicado) |
| [B2](trilha-b-arquiteturas/b2-thumb2.md) | Thumb-2 (decoder 32-bit + IT blocks) — épico | B1.6 | 🟡 refinado em B2.1–B2.5 (2026-07-11); nenhuma sub ainda implementada |
| [B2.1](trilha-b-arquiteturas/b2.1-thumb2-infra.md) | Infra: fetch 32-bit + gate `THUMB2` + desambiguação vs BL/BLX legado | B2 (B1.6) | ✅ (`ThumbDecoder` ganha checagem `top5=hw1[15:11] ∈ {11101,11110,11111}`; desambiguação real é por hw2[15:14] — `0b11` identifica a família "branch with link" (BL/BLX imediato) independente de J1/J2, então é robusta ao range estendido do Thumb-2, não só ao caso legado J1=J2=1; quando bate, cai no caminho ARMv4T/v5T de dois halfwords inalterado. `ArmArchitecture` ganha `thumb32DecoderExtensions()` reaproveitando a interface `DecoderExtension` existente [raw = os dois halfwords combinados] — vazio até B2.2 registrar a primeira categoria; até lá todo candidato genuíno de 32 bits [fora do espaço BL/BLX] vira UNDEFINED controlado com fetch atômico dos dois halfwords, sem exceção de "extensão ausente". Top5=11101/11111 como entrada standalone continuam delegando ao caminho legado sempre que nenhuma extensão reivindica — ainda 100% do espaço hoje, já que a lista está vazia; fica pronto para B2.2/B2.3 plugarem Load/store double+exclusive+table-branch e Load/store single+hints+coprocessor sem tocar aqui de novo. Nenhum preset atual habilita `THUMB2`, então o novo código é inerte em produção (G2/G3). 5 testes novos (`ThumbTwoDecoderTest`): BL e BLX-imediato legados idênticos com THUMB2 ligado, 2 candidatos genuínos UNDEFINED com fetch atômico provado via `raw()`, regressão nos 3 presets sem a feature. Suite arm-jitter verde (`mvn test` raiz) + gbaemu + ndsemu revalidados verdes) |
| [B2.2](trilha-b-arquiteturas/b2.2-thumb2-dataproc.md) | Data-processing 32-bit (modified immediate, MOVW/MOVT, shifts) | B2.1 | ✅ (`Thumb2DataProcessingDecoder` novo, plugado via `thumb32DecoderExtensions`: modified-immediate com `ThumbExpandImm_C` [4 casos de replicação + carry-out via o mesmo truque `valor<0` do rotate-imm ARM], `MOVW`/`MOVT` [`MOVT` como `IrOp.MoveTop` novo — não é um padrão `Alu`, escreve só bits 31:16], `ADD`/`SUB` plain-binary com `Rn=PC`→`ADR`/`Rn=SP` genérico, e a forma registrador com shift imediato. `ORN` novo em `IrOpCode`/`InstructionKind` [sem equivalente ARM clássico]; MOV/MVN via alias `Rn=PC`; TST/TEQ/CMN/CMP via alias `Rd=PC,S=1`; demais `Rd=PC` ou `Rn=PC` fora desses aliases → UNDEFINED [UNPREDICTABLE]. ASM nativo NÃO incluído [mesmo padrão B1.2-B1.5 até B1.6] — `AsmNativePolicy` rejeita `IrOp.MoveTop` e o opcode `ORN` explicitamente. **Fix de infra descoberto durante a task** [pertencia a B2.1 mas só se manifestava com uma extensão real registrada]: `StandardIrBuilder`/`StandardIrBlockLifter#instructionWidth` sempre devolviam 2 para THUMB — corrigido para 4 quando `raw()<0` [candidato Thumb-2 de 32 bits genuíno sempre tem o bit mais alto ligado, halfword isolado nunca], senão `endPc`/fetch/pc-advance do lifter ficavam quebrados para qualquer instrução de 32 bits real. 14 testes novos [`Thumb2DataProcessingDecoderTest`]: os 4 casos do `ThumbExpandImm` + carry-out, `MOVW`+`MOVT` compondo uma constante de 32 bits, `ADD Rd,SP,#imm` e `ADR` [+ e -], forma registrador LSL e RRX comparadas registro-a-registro e flag-a-flag contra o ARM clássico equivalente, bloco misto no fallback `PER_OP` sem divergência do interpretado [mesmo harness de B1.2-B1.5, prova `perOpFallbackOpCount()>0`], regressão G2 sem `THUMB2`. Suite arm-jitter 517 + gbaemu 216 + ndsemu 175 verdes. op4 reservados de `top5=0b11101` [register-form] caíam no fallback legado incorreto pré-existente de B2.1, não fechado aqui — **fechado em B2.2.2** [`DecoderExtension#claimsEncodingSpace`, virou UNDEFINED explícito]. Rejeição de `Rm=SP` nesta forma AUDITADA e confirmada correta em B2.2.1) |
| [B2.2.1](trilha-b-arquiteturas/b2.2.1-thumb2-dataproc-register-form-sp.md) | Auditar `Rm=SP` na forma registrador do data-processing Thumb-2 | B2.2 | ✅ (fonte primária confirma: ARM DDI 0406C A5.3.1 declara `if d IN {13,15} || n IN {13,15} || m IN {13,15} then UNPREDICTABLE` uniformemente para AND/BIC/ORR/ORN/EOR/ADC/SBC/RSB/ADD/SUB [register] Thumb T2/T3 — Rm=SP genuinamente UNPREDICTABLE, sem exceção nos casos de alias de Rd/Rn; corroborado pelo comentário do grupo MVE colidente no decodetree do QEMU `t32.decode` ["Rm==13 or 15 ... UNPREDICTABLE cases for MOVS/ORRS"]. Rejeição de B2.2 estava CORRETA — não era uma simplificação desnecessária. Nenhuma mudança de comportamento; comentário do decoder trocado de "não verificado" para a citação precisa. Suite arm-jitter 531 + gbaemu 219 + ndsemu 175 verdes, sem testes novos [nenhum comportamento observável mudou]) |
| [B2.2.2](trilha-b-arquiteturas/b2.2.2-thumb2-reserved-encoding-undefined.md) | Fechar fallback incorreto de `op4` reservados sob `top5=0b11101` | B2.1, B2.2 (idealmente após B2.3, ver Armadilhas) | ✅ **Opção (b) do enunciado**: `DecoderExtension` ganhou `claimsEncodingSpace(int raw)` (default `false`, preserva o contrato anterior para quem não tem essa ambiguidade — ex. `CoprocessorDecoder`), distinguindo "não é meu espaço" (`tryDecode` devolve `null` e `claimsEncodingSpace` devolve `false`) de "é meu espaço estruturalmente reconhecido, mas esse sub-encoding é reservado/não implementado" (`tryDecode` devolve `null`, `claimsEncodingSpace` devolve `true`). `ThumbDecoder#tryDecodeThumb32`: quando nenhuma extensão reivindica via `tryDecode` mas alguma reivindica via `claimsEncodingSpace`, o candidato vira UNDEFINED — igual ao tratamento já existente de `top5=0b11110` (unificado numa condição só: `top5==0b11110 || anyExtensionClaims(raw32)`); sem NENHUMA extensão que reivindique (caso dos testes legados `BL`/`BLX`, lista de extensões vazia), o comportamento é bit-a-bit idêntico a antes — prova de que a ambiguidade real com o sufixo `BL`/`BLX` (que só existe quando bits do próprio halfword coincidem com um espaço realmente registrado, sem contexto para desambiguar) não foi alterada. Implementado tanto em `Thumb2DataProcessingDecoder` (prefixo de 7 bits `0b1110101`, cobrindo `PKH`/MVE/demais `op4` reservados) quanto em `Thumb2LoadStoreDecoder` (`top8` de "Load/store single" + `EXTRA_TOP7` de "Load/store dual/exclusive/table branch"/"Load/store multiple", cobrindo as formas unprivileged/`TBB`/`TBH`/listas com SP-PC já excluídas em B2.3) — as duas extensões de `top5=0b11101` feitas ANTES de fechar o contrato, como a Armadilha pedia, sem retrabalho. 1 teste novo (`Thumb2DataProcessingDecoderTest`: `op4` reservado sob o prefixo vira `UNIMPLEMENTED`, não mais `LONG_BRANCH_SUFFIX`); 3 testes existentes de `Thumb2LoadStoreDecoderTest` que documentavam a limitação conhecida (`t4UnprivilegedFormIsNotClaimedAsLoad`, `stmWideWithPcInListIsNotClaimedAsStoreMultiple`, `ldmWideWithStackPointerInListIsNotClaimedAsLoadMultiple`) fortalecidos com `assertEquals(UNIMPLEMENTED, ...)` (antes só `assertNotEquals` do resultado errado, já que o fallback era indeterminado) e comentários atualizados para refletir a resolução; regressão de `ThumbTwoDecoderTest` (BL/BLX legado com THUMB2 ligado) e G2 sem `THUMB2` intactas. Suite arm-jitter 554 (553+1) + gbaemu 216 + ndsemu 175 verdes |
| [B2.3](trilha-b-arquiteturas/b2.3-thumb2-loadstore.md) | Load/store 32-bit (offset 12-bit, LDRD/STRD, LDM/STM.W) | B2.1 | ✅ (`Thumb2LoadStoreDecoder` novo, plugado via `thumb32DecoderExtensions`: `LDR`/`STR`/`LDRB`/`STRB`/`LDRH`/`STRH`/`LDRSB`/`LDRSH` nas formas T3 [imediato de 12 bits, sempre `P=1,U=1,W=0` — a armadilha do enunciado confirmada contra o QEMU `t32.decode`: nunca misturar com o P/U/W explícito do T4], T4 [imediato de 8 bits, `W=1`→4 modos pré/pós-indexados com writeback, `P=1,U=0,W=0`→offset negativo sem writeback complementando o T3; `P=1,U=1,W=0` unprivileged `LDRT`/`STRT`/... explicitamente fora de escopo] e T2 [`[Rn,Rm,LSL #imm2]`, implementado para as 8 variantes de tamanho/sinal — mais amplo que o "LDR/STR" literal do enunciado, já que o encoding real do ARM ARM e o QEMU confirmam a mesma forma para todas]; forma literal `Rn=PC` das 5 variantes de load com prioridade sobre T3/T4/T2 [ida confirmada contra o QEMU: "Load, unsigned (literal) overlaps all other load encodings"], reusando o mesmo alinhamento de 4 bytes do Thumb-1 — `IrOp.LoadLiteral` ganhou `sizeBytes`/`signed` para isso [antes só word unsigned]. `LDRD`/`STRD` Thumb-2 gateado por `ArmFeature.LDRD_STRD` [igual ao ARM clássico, encoding diferente]: achado principal da task — ao contrário do ARM clássico [`Rt2` sempre `Rt+1`, um único campo no encoding], o Thumb-2 tem `Rt`/`Rt2` como campos de 4 bits INDEPENDENTES [confirmado no manual e no QEMU `@ldstd_ri8: rt:4 rt2:4`], então pares arbitrários e não-adjacentes são válidos — `IrOp.DoubleTransfer` ganhou um campo `second` explícito [antes hardcoded `first+1`], usado por todos os executores/ASM/DCE/policy. `LDM.W`/`STM.W`/`PUSH.W`/`POP.W` com lista completa de 16 registradores reusam `LOAD_MULTIPLE`/`STORE_MULTIPLE` sem nenhum decode especial para `PUSH`/`POP` [mesmo alias que o ARM clássico já usa: `STMDB SP!`/`LDMIA SP!`]; só suportam IA/DB [não IB/DA, restrição real do Thumb-2 confirmada no QEMU]; UNPREDICTABLE tratado como UNDEFINED [`Rn=PC`, lista com SP, `STM` com PC na lista, <2 registradores, writeback com a base na lista]; `POP.W` com PC reusa o mesmo `IrExecutionSupport#loadToPc` gateado por `ArmFeature.LOAD_PC_INTERWORKING` que o `LDM`/`Pop` clássico já usa [nenhum código de interworking novo]. **Bug de infra achado e corrigido durante a task**: uma tentativa de generalizar o UNDEFINED explícito de `ThumbDecoder#tryDecodeThumb32` [hoje só cobre `top5=0b11110`] para os 3 valores de `top5` quebrou os testes legados de `BL`/`BLX` — revertida; o gap permanece e agora está documentado com mais precisão em B2.2.2 [ver a entrada acima]. Sem emissão ASM nativa NOVA — mas como a task reusa `IrOp.Load`/`Store`/`MultipleTransfer` sem alterar sua forma [só `DoubleTransfer`/`LoadLiteral` ganharam campos, propagados a todos os backends por completude/correção, não por escolha de "adicionar ASM"], essas formas herdam a emissão nativa já existente desde B1.6 automaticamente [decisão registrada, não é regra nova]. Equivalência PER_OP validada com o mesmo harness de B1.x/B2.x [`Thumb2LoadStoreDecoderTest`, bloco misto LDR-T3+STR-T4+LDRD]. 22 testes novos: cada forma T2/T3/T4 com vetor concreto, LDRSB/LDRSH sign-extend, literal com PC desalinhado [bit 1] validando alinhamento de 4 bytes e U-bit explícito, LDRD/STRD com par não-adjacente ida-e-volta, LDM.W 16 registros, PUSH.W/POP.W como alias STMDB/LDMIA, POP.W-com-PC interworking COM e SEM `LOAD_PC_INTERWORKING`, gates LDRD_STRD/G2. Suite arm-jitter 553 [531+22] + gbaemu 219 [sem divergência JIT×interpretado] + ndsemu 175 verdes) |
| [B2.4](trilha-b-arquiteturas/b2.4-thumb2-branches-it.md) | Branches (B.W/BL/BLX/CBZ/CBNZ/TBB/TBH) + IT block (maior risco) | B2.1 | ✅ (implementadas as 3 decisões de desenho fechadas na spec: **D1** — ITSTATE\[7:0\] mora em `CpsrRegister` [bits 26:25/15:10, split via `IT_LOW_*`/`IT_HIGH_*`, espelhando `GE_FLAGS_*`], threaded localmente pelo `StandardIrBlockLifter`/`ArmInterpreter.stepWithResult` via `ItState` [novo, algoritmo shift-register do QEMU], e entra em `BlockKey`/`JitRuntime` [chave do cache + tag do inline cache] quando não-zero, para que um mesmo PC lifted com ITSTATE de entrada diferente nunca reaproveite condições por-op erradas [coberto por teste dedicado]; **D2** — `BL`/`BLX` Thumb-2 reaproveitam 100% o caminho legado prefix/suffix, zero decode novo, só teste de regressão; **D3** — `TBB`/`TBH` via `IrOp.TableBranch` novo [executor dedicado em `IrBranchExecutor`, delega leitura de memória aos helpers de load existentes, valor da tabela nunca observável em registrador]. `CBZ`/`CBNZ` via `IrOp.CompareBranchZero` novo [nunca afeta NZCV]. `Thumb2BranchDecoder` novo [`B.W` T3/T4 + `TBB`/`TBH`] plugado em `ARMV6K_THUMB2` junto de `Thumb2DataProcessingDecoder`; `Thumb2LoadStoreDecoder`/`Thumb2MiscDecoder` deliberadamente NÃO plugados [colisão real documentada no javadoc de `ARMV6K_THUMB2`: `Thumb2LoadStoreDecoder` reivindica greedily o "fantasma" formado ao reler o segundo halfword de um par `BL`/`BLX` legado — investigação fica para uma task de fechamento de preset separada]. As 3 UNPREDICTABLEs CONSTRAINED específicas do IT block [IT aninhado, branch não-última-instrução, IT como última instrução de outro IT] tratadas como "continua normalmente" [precedente QEMU], não UNDEFINED — correção sobre a convenção geral do projeto, documentada explicitamente. Entrada em exceção limpa o ITSTATE do CPSR ativo [`ArmCore`]; o SPSR já preserva o ITSTATE de origem para o retorno. 25 testes novos [`Thumb2BranchesItTest`]: expansão ITSTATE contra oráculo shift-register independente, NV→AL, guard verdadeiro/falso [incl. ciclo/fetch de instrução pulada, G4, e ausência de side-effect de memória], equivalência ASM×interpretado sob condição vinda de IT, `BlockKey`/tag do IC diferindo por ITSTATE, mesmo PC lifted duas vezes com ITSTATE diferente produzindo condições baked diferentes [D1], exceção no meio de um IT block salvando/restaurando via SPSR, `B.W` condicional/incondicional [curto, longo positivo/negativo, fora do alcance de 16 bits], regressão `BL`/`BLX` legado com THUMB2 ativo, `CBZ`/`CBNZ` [tomado/não-tomado, sem tocar flags], `TBB`/`TBH` [incl. offset dobrado do halfword, valor não exposto], G2 sem THUMB2, os 3 casos CONSTRAINED UNPREDICTABLE. Suite arm-jitter 580 [555+25] + gbaemu 216 + ndsemu 175 verdes. **Fecha o épico B2** [B2.1-B2.5 todas ✅ agora] — exceto a decisão pendente de plugar `Thumb2LoadStoreDecoder`/`Thumb2MiscDecoder` no preset, registrada explicitamente como fora do escopo desta task) |
| [B2.5](trilha-b-arquiteturas/b2.5-thumb2-misc.md) | Misc: hints, barriers (DMB/DSB/ISB), MSR/MRS Thumb-2 | B2.1 (+ B1.5) | ✅ (`Thumb2MiscDecoder` novo, plugado via `thumb32DecoderExtensions` para a forma de 32 bits [`MRS`/`MSR` registrador + `DMB`/`DSB`/`ISB`, confirmados contra o QEMU `target/arm/tcg/t32.decode` — "Hints, and CPS"/"Miscellaneous control"/`MRS_reg`/`MSR_reg`], mais decode inline no `ThumbDecoder` para o espaço de hints de 16 bits (`0xBF00`, `t16.decode`) que hoje não tinha NENHUM tratamento [nem UNDEFINED explícito — caía no fallback genérico]. Armadilha do enunciado confirmada: os hints Thumb-2 NÃO são os mesmos encodings do Thumb-1 [esse espaço só existe desde ARMv6T2] — decode novo dos dois lados, reusando o `IrOp.PsrTransfer`/`WaitForInterrupt` de B1.5 sem duplicar semântica [`NOP`/`YIELD`/`SEV`/reservado → `MSR(#imm)->CPSR` com máscara vazia, mesmo truque implícito que o ARM clássico já usa; `WFI` → `WAIT_FOR_INTERRUPT`]. `ArmFeature.MEMORY_BARRIERS` novo [ARMv7] só gateia `DMB`/`DSB`/`ISB`; `IrOp.MemoryBarrier` novo, documentado como NOP observável sob a premissa explícita de core single-thread sem reordenação especulativa. Gating fino: `WFI`/hints exigem `WAIT_HINTS` além de `THUMB2` [sem a feature, UNDEFINED explícito, espelhando o precedente do `WFI` ARM de B1.5]; `NOP` e `MRS`/`MSR` só exigem `THUMB2`. Fora de escopo (documentado): `CPS`/`CLREX`/`SB` de 32 bits e as formas banked de `MRS`/`MSR` [mesmo espaço de bits, mas não pedidos no Objetivo] caem no UNDEFINED controlado já existente; IT block [`mask≠0000` no mesmo opcode de 16 bits dos hints] fica para B2.4, sem alteração. Bug de decode achado e corrigido durante a implementação: posição do bit `r` [CPSR/SPSR] de `MRS_reg` estava a 4 bits do lugar certo [confundido com o prefixo fixo do nibble vizinho] — pego pelo teste de ida-e-volta contra o MRS ARM clássico. 14 testes novos [`Thumb2MiscDecoderTest`]: hints de 16 e 32 bits sem efeito observável, `WFI` [16 e 32 bits] halting o core, gate `WAIT_HINTS`/`MEMORY_BARRIERS` ausente → UNDEFINED, `DMB`/`DSB`/`ISB` sem efeito além de ciclo/fetch [G4], `MRS`/`MSR` ida-e-volta byte-a-byte contra o ARM clássico [CPSR e SPSR, mask `_f` vs `_fc`], IT block ainda UNDEFINED, regressão G2 sem `THUMB2`. Sem emissão ASM nativa [`AsmNativePolicy` rejeita `IrOp.MemoryBarrier`, mesmo padrão das outras instruções de sistema desde B1.5/B2.2]. Suite arm-jitter 531 + gbaemu 219 + ndsemu 175 verdes. **Não fecha o épico B2 ainda**: apesar do texto original da task supor B2.1-B2.5 como a lista completa, B2.3 [load/store 32-bit] e B2.4 [branches + IT block] seguem ⬜ no índice — o aceite do épico depende delas também) |
| [B2.6](trilha-b-arquiteturas/b2.6-thumb2-preset-fechamento.md) | Fechar o preset Thumb-2: BL/BLX vira decode 32-bit + plugar as 4 extensões | B2.1-B2.5 | ✅ (D2 de B2.4 revogada sob `THUMB2`, exatamente como a spec mandava: `ThumbDecoder#tryDecodeThumb32` agora decodifica `BL`/`BLX` imediato como instrução ÚNICA de 32 bits [`InstructionKind.LONG_BRANCH_32` novo] em vez de delegar ao par prefixo+sufixo de 16 bits — `decode()` nunca mais é chamado no endereço do halfword de sufixo em código são, então o "fantasma" documentado desde B2.1/B2.4 deixa de existir estruturalmente. `StandardIrBuilder` eleva a instrução única para o MESMO par de `IrOp` (`ThumbBlPrefix`+`ThumbBlSuffix`) que o caminho legado já produzia — zero semântica nova, reusa executor+ASM existentes; `Cycle`/`Fetch` emitidos duas vezes [um por halfword] para bater exatamente com o que o par legado emitia [G4]. Sufixo isolado [`top5∈{0b11101,0b11111}`] sob `THUMB2` sem nenhuma extensão que reivindique vira UNDEFINED incondicional agora [a delegação ao caminho legado, que só existia por causa do fantasma, foi removida]. **Bug pego durante a implementação** [não estava na spec]: o primeiro rascunho do check de BL/BLX aplicava a máscara `hw2[15:14]==0b11` para QUALQUER `top5`, o que capturava erroneamente `TBB`/`TBH` [`top5=0b11101`, cujo segundo halfword `0xF000-0xF01F` também bate com `0b11==15:14` por coincidência] — corrigido restaurando o guard `top5==0b11110` [o único `hw1` real de BL/BLX Thumb-2, ARM DDI 0406C A8.8.25], descoberto pelos testes de TBB/TBH de `Thumb2BranchesItTest` quebrando. **Segundo bug pego nesta sessão**: a distinção BL(1)/BLX(0) usa `hw2[12]` [confirmado contra as máscaras legadas `0xF800`/`0xE800`, que só diferem nesse bit], não `hw2[14]` como uma leitura literal do texto da spec sugeria — `bit14` é sempre `1` para qualquer candidato que já passou pelo `BRANCH_WITH_LINK_MASK` [`bits15:14==0b11`], então usá-lo classificaria tudo como BL; corrigido e documentado no código com a citação cruzada às máscaras legadas. Preset `ARMV6K_THUMB2` reescrito com as 4 extensões juntas [`Thumb2DataProcessingDecoder`+`Thumb2LoadStoreDecoder`+`Thumb2BranchDecoder`+`Thumb2MiscDecoder`] + `MEMORY_BARRIERS`, via uma arquitetura intermediária só-features [`ARMV6K_THUMB2_FEATURES`] para resolver a dependência circular do construtor de `Thumb2LoadStoreDecoder`/`Thumb2MiscDecoder` [que recebem a própria `ArmArchitecture` para gatear `LDRD_STRD`/`WAIT_HINTS`/`MEMORY_BARRIERS`]. 9 testes novos/reescritos em `ThumbTwoDecoderTest` [decode único, equivalência de estado final ASM×legado para BL e BLX, os 2 testes de reprodução do fantasma pedidos no Passo 1 — um para a colisão com `Thumb2LoadStoreDecoder` `top8=0xF8`, outro para a colisão JÁ LATENTE no preset de 2 extensões pré-B2.6 com `Thumb2DataProcessingDecoder` `REGISTER_FORM_PREFIX`], `ArmArchitectureTest` atualizado para 4 extensões, `Thumb2BranchesItTest` atualizado [BL/BLX vira 1 `core.step()`, não 2], `ArmV6kThumb2PresetIntegrationTest` novo com o bloco misto de aceite [`LDR.W`+`IT EQ`+`ADDEQ.W`+`DMB`+`BL`, harness `BlockEquivalenceHarness` interpretador×ASM guard verdadeiro/falso, mais uma checagem end-to-end de estado final; `TBB`/`TBH` contra o preset real já cobertos por `Thumb2BranchesItTest`]. Suítes arm-jitter 590 [586+4] + gbaemu 219 + ndsemu 175 verdes. **armbox** [repo irmão]: `testdata/thumb2-torture.s` estendido com checagens 11-20 [`LDR.W`/`STR.W` T3/T4 com writeback, `LDRD`/`STRD` par não-adjacente, `LDM.W`/`PUSH.W`/`POP.W`, `DMB`/`DSB`/`ISB`, `MRS`/`MSR` de 32 bits, `TBB`, e um `BL` real entre funções com `.word 0xF890F890` logo após um `b` que pula por cima dele — o endereço de retorno real de um `bl`/`bx lr` não pode conter dados crus, então o "fantasma" é simulado com um skip-branch em vez de quebrar a chamada real]. **Bug de alinhamento achado e corrigido durante a extensão do `.s`**: a tabela de bytes do `TBB` tinha 3 entradas [ímpar] sem padding, desalinhando TUDO que vinha depois em 1 byte [`bl` e `helper` caíam em endereços ímpares, quebrando Thumb] — corrigido com um 4º byte de padding + `.align 1`, confirmado via `objdump -d` antes e depois. Recompilado com `build-testdata.ps1` [reconstrói os 6 binários]; roda exit 0 nos 3 backends [JIT/`--interp`/`--check`] via `Thumb2TortureTest`; `thumb2-torture-broken.elf` [não modificado nesta task, já cobria o "teste do teste"] continua exit 77. Suíte armbox 26 verde) |
| [B2.7](trilha-b-arquiteturas/b2.7-thumb2-paridade-encodings.md) | **Paridade Thumb-2** (MUL.W/UMULL/shifts-reg/extend/REV/CLZ/SEL/QADD/paralelas/SSAT + LDREX/STREX.W + CPS.W + MCR/MRC Thumb) — sem isto binário v7 real morre na 1ª multiplicação; 3 PRs | B2.6 | ✅ 2026-07-16/17 (PR1+PR2+PR3, fecha o épico B2.7 — espaço `0xFA` + `SSAT`/`USAT`/`SSAT16`/`USAT16`; PR2 multiply/`0xFB`; PR3 `LDREX`/`STREX`.W + `CLREX`.W + `CPS`.W + `MCR`/`MRC` Thumb — ver notas abaixo). **PR3 (2026-07-17)**: `Thumb2LoadStoreDecoder` ganhou `decodeExclusiveWordOrSized` dentro do subgrupo `EXTRA_TOP7`/`LDRD_MARKER_BIT` que antes devolvia `null` para `P=0,W=0` (o comentário do PR3 na spec dizia "`LDRD_MARKER_BIT=0`" mas o bit real é `1` — a auditoria confirmou contra o QEMU `t32.decode` que `STREX`/`LDREX`/`STREXB/H/D`/`LDREXB/H/D` TÊM `raw[22]=1`, igual a `LDRD`/`STRD`, e são discriminados de `LDRD`/`STRD` por `P=0,W=0`, exatamente o ramo que já tinha o comentário "pertence a LDREX/STREX/TBB/TBH" — a spec só nomeou o bit errado, o ramo estava certo): `LDREX`/`STREX` word decodificados com o imediato `imm8×4` que o ARM clássico NÃO tem (`%imm8x4`, MESMO campo/macro do `LDRD`/`STRD`, confirmado no QEMU `@strex_i`/`@ldrex_i`) — **exigiu adicionar um campo `offset` a `IrOp.LoadExclusive`/`IrOp.StoreExclusive`** (B1.4), zero no ARM clássico e nas formas B/H/D, precedente idêntico ao `second` que `IrOp.DoubleTransfer` ganhou em B2.3; `IrMemoryExecutor`/`AsmBlockCompiler` (`emitLoadExclusive`/`emitStoreExclusive`) atualizados para somar o offset ao endereço, `StandardIrBuilder` passa `instruction.immediate()` (0 para ARM clássico, sempre) — G3 intacto (record widening aditivo, não breaking). `LDREXB/H/D`/`STREXB/H/D` (gate `EXCLUSIVE_SIZED`, discriminados de `TBB`/`TBH` pelo campo `op4`=`raw[7:4]`, QEMU `@strex_0`/`@strex_d`/`@ldrex_0`/`@ldrex_d`) — achado real confirmado no ARM DDI 0406C: mesmo com `Rt2` sendo um campo de 4 bits INDEPENDENTE no encoding Thumb-2 (diferente do ARM clássico), `LDREXD`/`STREXD` continuam UNPREDICTABLE se `Rt2≠Rt+1`, então `IrOp.LoadExclusive`/`StoreExclusive` (par `dst`/`dst+1` implícito) não precisaram de um campo `second` como `DoubleTransfer` — só a decodificação valida `Rt` par e `Rt2==Rt+1`. `Thumb2MiscDecoder` ganhou `CLREX` de 32 bits (encoding totalmente fixo `0x8F2F` no mesmo `hi`/top-byte das barreiras, gate `EXCLUSIVE_SIZED` — mesma feature do ARM clássico) e `CPS` de 32 bits (mesmo `hi` dos hints, distinguido por `imod`/`M`≠0, MESMO empacotamento de `immediate` que o `CPS` ARM clássico usa — `StandardIrBuilder` não precisou de nenhuma mudança para os dois, `IrOp.ChangeProcessorState`/`ClearExclusive` de B1.4/B1.5 reusados sem duplicar semântica). `Thumb2CoprocessorDecoder` novo (casca fina, `decoder/`): delega a `CoprocessorDecoder` (ARMv5TE, MCR/MRC — mesmo layout de bits `hw1=0xEE` confirmado contra o QEMU `@mcr`) e reescreve só o `InstructionSet` de `ARM` para `THUMB` via um `DecodedInstruction#withInstructionSet` novo (mesmo precedente de `withCondition`, aditivo) — necessário porque `CoprocessorDecoder` sempre marca `ARM` e o resto do pipeline (`instructionWidth`/`isThumb32`/chave de bloco) precisa da tag correta; plugado como a 7ª extensão de `ARMV6K_THUMB2`, sem gate próprio (mesmo padrão do `CoprocessorDecoder` original). 18 testes novos: `Thumb2LoadStoreDecoderTest` (+8 — ida-e-volta `LDREX`/`STREX` word com offset zero, `STREX.W` com offset≠0 [a forma que não existe em ARM clássico, vetor dedicado pedido pela task], `LDREXB/H/D`/`STREXB/H/D` ida-e-volta, gates ausentes→UNDEFINED, preset só-`EXCLUSIVE_WORD`, `TBB`/`TBH` não engolido pelo espaço de exclusivos), `Thumb2MiscDecoderTest` (+5 — `CLREX.W` abre o monitor, gate ausente, `CPS.W` ida-e-volta mudando I/F e mudando modo, gate ausente), `Thumb2CoprocessorDecoderTest` novo (+5 — `MCR`/`MRC` Thumb-2 ida-e-volta com bus fake `CapturingCp15`, gate G2, bloco misto `LDREX`+`STREX`+`CLREX`+`MCR`+`MRC` com `perOpFallbackOpCount()==0` [todas as 5 ops já são ASM-nativas desde B1.6], regressão `BL`/`BLX` de 32 bits). `ArmArchitectureTest`/`ArmV6kThumb2PresetIntegrationTest` atualizados de 6→7 extensões. Suíte arm-jitter 675 (657+18) core + 13 truffle verdes (`mvn -o test`, JBR 25). **G5 validado**: `mvn -o install` do arm-jitter seguido de `mvn -o test` nos dois consumidores — gbaemu 239 verde (`JitInterpreterDivergenceTest` sem divergência em 20M blocos) + ndsemu 175 verde — zero regressão. Oráculo QEMU `target/arm/tcg/t32.decode` baixado via `curl` (raw.githubusercontent.com) e lido linha a linha, seção "Load/Store Exclusive, Load-Acquire/Store-Release, and Table Branch" — `STLEX*`/`LDAEX*`/`STL*`/`LDA*` (ARMv8 store-release/load-acquire) confirmados fora de escopo (mesmo espaço de bits que o ARM clássico também não implementa) e deliberadamente deixados UNDEFINED. **PR2 (2026-07-17)**: `Thumb2MultiplyDecoder` novo, plugado em `ARMV6K_THUMB2` como a 6ª extensão Thumb-2, cobrindo o prefixo `0xFB` inteiro: `MUL.W`/`MLA.W` (sentinel `Ra=1111`→MUL, mesmo achado do PR1), `SMULL`/`UMULL`/`SMLAL`/`UMLAL` (`IrOp.LongMultiply`), `UMAAL` (gate `UMAAL`), `USAD8`/`USADA8` (gate `PACK_SATURATE`) e as multiplicações DSP ARMv5TE — `SMLA<x><y>`/`SMUL<x><y>`/`SMLAW<y>`/`SMULW<y>`/`SMLAL<x><y>` (gate `DSP_MULTIPLY`, `IrOp.DspMultiply`) — zero IR nova, mesmo executor+ASM de B1.2/B1.6. **Achado real**: ao contrário do ARM clássico (onde `SMUL<x><y>`/`SMULW<y>` têm `op2` PRÓPRIO no encoding, distinto de `SMLA<x><y>`/`SMLAW<y>`), o Thumb-2 reusa o MESMO bit-pattern `family`/`op` para as formas com/sem acumulador, distinguindo pelo sentinel `Ra=1111` (confirmado no QEMU `t32.decode`: `SMULBB`/`SMULWB`/... usam `@rn0dm`, que fixa esse campo em `1111`; `SMLABB`/`SMLAWB`/... usam `@rnadm`, campo livre) — mesmo truque de `MUL.W`/`MLA.W`, generalizado para todo o grupo DSP; traduzido para o `op2`/`x` que `IrAluExecutor#executeDspMultiply` já espera (op2=0↔SMLA, op2=3↔SMUL sem acumulador, op2=1 com x=0/1↔SMLAW/SMULW, op2=2↔SMLAL de 64 bits, sempre acumula). `MLS`/`SMLAD`/`SMLSD`/`SMLALD`/`SMLSLD`/`SMMLA`/`SMMLS`/`SDIV`/`UDIV` (ARMv7, fora de escopo — ver B3.2) ficam reservados via `claimsEncodingSpace` (todo o prefixo `0xFB`), caindo em UNDEFINED controlado em vez de UNIMPLEMENTED silencioso ambíguo (mesmo protocolo de B2.2.2); comentário no decoder documenta que B3.2 deve estender esta MESMA classe com essas formas, não criar uma nova. Rd/Rn/Rm ∈ {13,15} → UNDEFINED em todo o espaço `0xFB` (mesma citação de B2.2.1/B2.7-PR1). 2 testes pré-existentes de contagem de extensões (`ArmArchitectureTest`/`ArmV6kThumb2PresetIntegrationTest`) atualizados de 5→6. 33 testes novos (`Thumb2MultiplyDecoderTest`): ida-e-volta contra o ARM clássico para cada grupo (MUL/MLA, os 4 kinds de long-multiply, UMAAL, USAD8/USADA8, as 4 combinações B/T de `SMLA<x><y>`, `SMUL<x><y>` sem acumulador, `SMLAW<y>`/`SMULW<y>`, as 4 combinações de `SMLAL<x><y>`), gate-ausência por feature (`UMAAL`/`PACK_SATURATE`/`DSP_MULTIPLY`), rejeição Rd/Rn/Rm=SP/PC, formas reservadas (MLS/SMLAD/SDIV) confirmadas UNDEFINED e não silenciosamente decodificadas, G2 sem `THUMB2`, bloco misto interpretado×ASM com `perOpFallbackOpCount()==0`, regressão `BL`/`BLX` de 32 bits (B2.6) com a nova extensão plugada. Suíte arm-jitter 657 core + 13 truffle verdes (`mvn -o test`, JBR 25). **G5 validado**: `mvn -o install` do arm-jitter seguido de `mvn -o test` nos dois consumidores — gbaemu verde (`JitInterpreterDivergenceTest` sem divergência em 20M blocos) + ndsemu verde — zero regressão. PR1 (2026-07-16) — oráculo QEMU `target/arm/tcg/t32.decode` baixado via `curl` e lido linha a linha (WebFetch resumido por outro modelo se mostrou impreciso para o arquivo inteiro, então a leitura direta do `.decode` foi a fonte real). `Thumb2RegisterDataProcessingDecoder` novo (`decoder/`), plugado em `ARMV6K_THUMB2` como a 5ª extensão Thumb-2 (junto de Data-processing/Load-store/Branch/Misc de B2.1-B2.6): shift por registrador (`LSL.W`/`LSR.W`/`ASR.W`/`ROR.W` — reusa os MESMOS `InstructionKind.LSL/LSR/ASR/ROR` do shift Thumb-1 por registrador, já que a semântica "src2=quantidade" do `IrOpCode` correspondente já existia), extensão com rotação/acumulador (`SXTAH`/`UXTAH`/`SXTAB16`/`UXTAB16`/`SXTAB`/`UXTAB`, gate `EXTEND_ROTATE`), `REV`/`REV16`/`REVSH` (gate `BYTE_REVERSE`), `CLZ` (gate `CLZ`), `SEL` (gate `PARALLEL_SIMD`), `QADD`/`QSUB`/`QDADD`/`QDSUB` (gate `SATURATING`) e as 36 formas de aritmética paralela ARMv6 (gate `PARALLEL_SIMD`) — todas reempacotadas para o MESMO formato de `DecodedInstruction`/IR que o decoder ARM clássico (`ArmDecoder`) já produz, zero IR nova, reusando o executor+ASM nativos desde B1.6. `Thumb2DataProcessingDecoder` (B2.2) ganhou `SSAT`/`USAT`/`SSAT16`/`USAT16` no grupo plain-binary existente (`top5=0b11110`), gate `PACK_SATURATE` — exigiu adicionar um campo `ArmArchitecture` ao construtor (antes sem estado), mesmo padrão de `Thumb2LoadStoreDecoder`/`Thumb2MiscDecoder` desde B2.3/B2.5; `SSAT16`/`USAT16` reaproveitam DELIBERADAMENTE o mesmo bit-pattern de `SSAT`/`USAT` com `sh=1` e shift totalmente zero (ambiguidade real de hardware, não um bug — mesma prioridade do QEMU). **Bug real encontrado e corrigido durante a implementação**: o predicado de detecção da forma de extensão (`isExtendForm`, marcador fixo `nibble[7:6]==0b10`) coincidia EXATAMENTE com os 2 bits altos de todo `nibble[7:4]` no intervalo `{8,9,A,B}` — o mesmo intervalo usado por `QADD`-family/`REV`-family/`SEL`/`CLZ` logo abaixo no dispatch; como o predicado não validava `nibble[23:20]<=5` (o intervalo real de extensão) e devolvia `null` cedo demais, TODA instrução `REV`/`CLZ`/`SEL`/`QADD` era engolida silenciosamente antes de alcançar o dispatch correto — pego pelos testes de ida-e-volta contra o ARM clássico (`revRev16RevshMatchArmClassic` inicialmente lançava `ArrayIndexOutOfBoundsException`, sintoma de um decode totalmente errado). Corrigido validando o intervalo de `nibble[23:20]` já no predicado `isExtendForm`. `StandardIrBuilder#operand` também precisou de um pequeno desvio: a forma de shift por registrador é um candidato Thumb-2 de 32 bits (`raw()<0`) mas usa os MESMOS `InstructionKind` do shift Thumb-1 por registrador — sem tratamento especial, caía por engano no ramo de "forma com shift imediato" do `Thumb2DataProcessingDecoder` (decoder DIFERENTE, nunca produz esses kinds); resolvido com `isRegisterControlledShiftKind` guardando a entrada nesse ramo. Rd/Rn/Rm ∈ {13,15} tratado como UNPREDICTABLE→UNDEFINED em todo o espaço `0xFA` (mesma citação de B2.2.1). 23 testes novos (`Thumb2RegisterDataProcessingDecoderTest`): ida-e-volta contra o ARM clássico para cada grupo (os 4 shtys do shift por registrador, `SXTAH` com acumulador+rotação, `UXTB16` sem acumulador, `REV`/`REV16`/`REVSH`, `CLZ`, `SEL`, as 4 formas `QADD`, amostra de 4 das 36 paralelas cobrindo família/variante, `SSAT`/`USAT`/`SSAT16`/`USAT16`), gate-ausência por feature (`EXTEND_ROTATE`/`BYTE_REVERSE`/`CLZ`/`SATURATING`/`PARALLEL_SIMD`/`PACK_SATURATE`), rejeição Rd/Rn/Rm=SP, G2 sem `THUMB2`, bloco misto interpretado×ASM com `perOpFallbackOpCount()==0`, regressão `BL`/`BLX` de 32 bits (B2.6) com a nova extensão plugada. Suíte arm-jitter 637 (614+23) core + 13 truffle verdes (`mvn -o test`, JBR 25). **G5 validado nesta sessão** via `mvn -o install` do arm-jitter seguido de `mvn -o test` nos dois consumidores: gbaemu 239 verde (`JitInterpreterDivergenceTest` sem divergência em 20M blocos) + ndsemu 175 verde — zero regressão. |
| [B2.8](trilha-b-arquiteturas/b2.8-pld-pli-nop.md) | PLD/PLI como NOP (ARM + Thumb-2) — memcpy de libc v7 emite PLD; hoje UNDEFINED | B2.6 (lado Thumb) | ✅ 2026-07-17 (`ArmFeature.PRELOAD_HINTS` novo, adicionado a `ARMV6K` — deliberadamente NÃO a `ARMV5TE`, G3/menor superfície — e herdado por `ARMV6K_THUMB2`. **ARM** (`ArmDecoder`): 6 máscaras exatas para `PLD`/`PLDW`/`PLI` nas formas imediato/literal e registrador, transcritas bit a bit do QEMU `a32-uncond.decode` ("Preload instructions") via um script Node (evita erro de aritmética manual — o `mask`/`value` de cada uma das 6 linhas foi gerado programaticamente a partir do padrão `1111 0101 -101 ...` literal, não deduzido de cabeça). **Thumb-2** (`Thumb2LoadStoreDecoder`): carve-out cirúrgico ANTES da rejeição/decode normal de `Rt=PC` — `Rt=1111` nas formas LDRB-shaped/LDRH-shaped (unsigned) e LDRSB-shaped (signed) vira hint; a LDRSH-shaped (signed) com `Rt=PC` é um "reserved hint, behaves as nop" GENÉRICO do QEMU (não nomeado `PLD_*`/`PLI_*`) e foi deliberadamente deixada fora (não generalizado); `LDR Rt,PC` (sizeL=101) continua um load real (regressão coberta). **IR**: nenhum record novo — reusa o mesmo truque de `MSR`(imediato)→CPSR com máscara de campo vazia que os hints de B1.5/B2.5 já usam (`InstructionKind.MSR`, fieldMask=0, valor=0) — o endereço nunca é computado, então um PLD para um endereço não mapeado não lança exceção (testado diretamente contra `TestAddressSpace` fora dos limites). 7 testes novos: `ArmDecoderTest` (+2 — as 6 formas ARM sem efeito observável/sem tocar o endereço, gate ausente→UNDEFINED), `Thumb2LoadStoreDecoderTest` (+5 — T3/T4/T2-registrador/literal/PLI sem efeito observável, `LDR Rt,PC` não afetado pelo carve-out — regressão de interworking, gate ausente→UNDEFINED, equivalência interpretado×ASM com bloco misto PLD+LDR). Suíte arm-jitter 682 core (675+7) + 13 truffle verdes (`mvn -o test`, JBR 25). **G5 validado**: `mvn -o install` seguido de `mvn -o test` nos dois consumidores — gbaemu 243 verde (`JitInterpreterDivergenceTest` sem divergência em 20M blocos) + ndsemu 175 verde — zero regressão. Oráculo QEMU `target/arm/tcg/a32-uncond.decode`/`t32.decode` baixados via `curl` (raw.githubusercontent.com) e lidos linha a linha, seção "Preload instructions" (ARM) e o bloco de prioridade `{ PLD ... LDRB_ri ... }` (Thumb-2, que confirma a nota "Load, unsigned (literal) overlaps all other load encodings" também vale para o carve-out de hint).
| [B3](trilha-b-arquiteturas/b3-armv7a-vfp.md) | ARMv7-A user-level + VFP — ÉPICO refinado 2026-07-15 | B2.6 | ✅ FECHADO 2026-07-23 (B3.1–B3.7 todas ✅ — ver B3.7 para o resumo final e o preset público `ARMV7A`; reaberto pontualmente pela B3.8 em 2026-08-15) |
| [B3.8](trilha-b-arquiteturas/b3.8-fpscr-rmode-fz.md) | FPSCR: RMode e FZ de verdade (revisita a decisão nº 3 do B3) | B3.3, B3.4 | ✅ 2026-08-15 — `FpscrRegister` aceita `RMode`/`FZ`/`LEN`/`STRIDE` sem lançar; `DirectedFpRounding` implementa os 4 modos de arredondamento para `ADD`/`SUB`/`MUL`/`DIV`/`MLA`/`MLS`/`NMUL`/`SQRT` via arredondamento dirigido (curto-circuito de sinal contra valor exato/quase-exato); `FZ` (flush-to-zero) real nas entradas e resultado; `LEN`/`STRIDE` aceitos e armazenados MAS SEM semântica de vetor executada (decisão de escopo explícita — nenhum consumidor real conhecido usa modo vetor de propósito); `mvn -o test` raiz verde + gbaemu/ndsemu verdes (G5, nenhum dos dois grava FPSCR não-default hoje, G3 preservado); desbloqueia o crt0/newlib de binários `.3dsx` reais no n3dsemu (ver `tasks/FILA-EXECUCAO.md`, achado da G2) |
| [B3.1](trilha-b-arquiteturas/b3.1-armv7-inteiro-arm.md) | Inteiro v7 encodings ARM (MOVW/MOVT, MLS, bitfield, RBIT, SDIV/UDIV, barreiras) | B2.6 | ✅ 2026-07-17 (`ArmFeature.MOVW_MOVT`/`MLS_MULTIPLY`/`BIT_FIELD`/`BIT_REVERSE`/`DIVIDE` novos; `IrOp.BitFieldExtract`/`BitFieldInsert`/`BitReverse`/`Divide` + `Multiply.subtractFromAccumulator` [MLS], executados em `IrAluExecutor`, DCE atualizado [`ConstantFoldPass`/`FlagMergePass` não precisaram — só tratam `IrOp.Alu`, e nenhuma destas 4 ops novas é uma]; `AsmNativePolicy` rejeita as 4 [interpretado até B3.6, mesmo padrão B1.x pré-B1.6]. **Desvio real do plano original da spec, descoberto pelo Passo 0 obrigatório**: a task previa uma `ArmV7MediaDecoder implements DecoderExtension` plugada só nos testes — mas TODAS as 8 instruções (`MOVW`/`MOVT`/`MLS`/`SBFX`/`UBFX`/`BFI`/`BFC`/`RBIT`/`SDIV`/`UDIV`/`DMB`/`DSB`/`ISB`) colidem com dispatches genéricos do `ArmDecoder` que retornam ANTES de alcançar o loop de extensões no fim de `decode()`: `MOVW`/`MOVT` caem no dispatch ALU genérico [opcode 0x8/0xA = TST/CMP com S=0]; `MLS` cai no bloco de halfword-transfer [`raw&0x0E000090==0x00000090`]; `SBFX`/`UBFX`/`BFI`/`BFC`/`RBIT`/`SDIV`/`UDIV` [todos com bits27:26=01] caem no bloco de LDR/STR imediato, cujo ramo interno `!immediateOffset && bit4` já devolve UNIMPLEMENTED para esse padrão [mesmo espaço "Media instructions" do ARM DDI 0406C A5.4, adjacente ao de LDR/STR]; `DMB`/`DSB`/`ISB` [espaço incondicional cond=1111] também caem nesse mesmo bloco de LDR/STR e decodificavam como um `LOAD` incorreto. A spec já prescrevia exatamente este contingente ["PARE e registre... o carve-out no ArmDecoder base precisa ser gateado por feature, espelhando BLX/CLZ"] — todas as 13 instruções da tabela viraram carve-outs diretos em `ArmDecoder` [mesmo padrão de `BLX`/`CLZ`/`UMAAL`/exclusivos], posicionados antes dos blocos que as engoliriam; nenhuma classe `ArmV7MediaDecoder`/`DecoderExtension` foi criada. `Thumb2DataProcessingDecoder#decodeMoveWide` (B2.2) passou a exigir `MOVW_MOVT` além de `THUMB2` (item da spec); `ARMV6K_THUMB2_FEATURES` ganhou `MOVW_MOVT` para não regredir (teste pré-existente `Thumb2DataProcessingDecoderTest` ajustado). 17 testes novos (`ArmV7MediaDecoderTest`): sonda do Passo 0 [as 13 formas UNIMPLEMENTED sob `ARMV6K` puro — provou o desvio acima], vetores concretos [MOVW+MOVT compõem 32 bits; MLS 10−3×4=−2; UBFX 0xABCD1234 lsb4/width8→0x23; SBFX com sinal vs UBFX sem sinal no mesmo campo; BFC limpa/BFI insere sem tocar o resto; RBIT 0x80000001→0x80000001 palíndromo; SDIV trunca para zero, /0→0, MIN_VALUE/−1→MIN_VALUE sem exceção; UDIV como não-assinado; MOVW/MOVT com Rd=PC→UNDEFINED; DMB/DSB/ISB NOP observável], gate ausente→UNIMPLEMENTED, G2 [nada decodifica em ARMV4T/ARMV5TE], e equivalência interpretado×ASM(PER_OP) com `perOpFallbackOpCount()>0` [sem emissão nativa ainda]. Suíte arm-jitter 698 core [682+16, +1 ajustado] + 13 truffle verdes (`mvn -o test`, JBR 25). **G5 validado**: `mvn -o install` seguido de `mvn -o test` nos dois consumidores — gbaemu verde (`JitInterpreterDivergenceTest` sem divergência em 20M blocos) + ndsemu verde — zero regressão) |
| [B3.2](trilha-b-arquiteturas/b3.2-armv7-inteiro-thumb2.md) | Inteiro v7 encodings Thumb-2 | B3.1 | ✅ 2026-07-17 — `SBFX`/`UBFX`/`BFI`/`BFC` acrescentados a `Thumb2DataProcessingDecoder` (mesmo grupo "plain binary immediate" de `MOVW`/`MOVT`/`ADD`/`SUB`/`SSAT`/`USAT`, `top5=0b11110`, novos `op4` `0b1010`/`0b1110`/`0b1011` — vagos e sem colisão com os já usados); `MLS`/`SDIV`/`UDIV` acrescentados a `Thumb2MultiplyDecoder` (mesmo espaço `0xFB`, `family=0/op=1` para `MLS`, `family=9`/`family=0xB` com `op` fixo `0xF` para `SDIV`/`UDIV`); `RBIT` acrescentado a `Thumb2RegisterDataProcessingDecoder` (família `REV`, `op=0xA`, `InstructionKind.BIT_REVERSE` — PRÓPRIO, distinto de `BYTE_REVERSE`/REV-family, mesmo gate `ArmFeature.BIT_REVERSE` do encoding ARM clássico). Todos os campos de bits confirmados linha a linha contra o QEMU `target/arm/tcg/t32.decode` via `curl` direto (seções "Saturate, bitfield" / "Multiply and multiply accumulate" / "Long multiply..." / "Data-processing (two source registers)"), não deduzidos — a armadilha do enunciado sobre `imm3:imm2` não-contíguo (`raw[14:12]`+`raw[7:6]`) confirmada e implementada via `%imm5_12_6` do QEMU. Zero IR nova: reusa 100% o mesmo `InstructionKind`/executor/ASM-fallback que o encoding ARM clássico de B3.1 já usa (G1); nenhum preset atual habilita as features novas (`BIT_FIELD`/`MLS_MULTIPLY`/`DIVIDE`/`BIT_REVERSE` só chegam em `ARMV7A`, B3.7), então o código novo é inerte em produção (G2/G3). Sem emissão ASM nativa (mesma decisão de B3.1, prevista para B3.6). Testes novos: `Thumb2DataProcessingDecoderTest` (+6 — `UBFX`/`SBFX` e `BFC`/`BFI` ida-e-volta contra o ARM clássico com os MESMOS vetores concretos de B3.1 [`0xABCD1234`→`0x23`, `0x80`→sinal `0xFFFFFF80` vs sem sinal `0x80`, `BFC` limpa `[11:4]`→`0xFFFFF00F`, `BFI` insere sem tocar o resto→`0xFFFFFABF`], gate ausente→UNDEFINED, G2); `Thumb2MultiplyDivideDecoderTest` novo (+13 — `MLS`/`SDIV`/`UDIV` ida-e-volta com os mesmos vetores de B3.1 [`10-3*4=-2`, `-7/2=-3` truncado, divisão por zero→0, `MIN_VALUE/-1` sem overflow, `UDIV` unsigned `0xFFFFFFFF/2=0x7FFFFFFF`], gates ausentes, rejeição SP/PC, vizinho reservado `family=0x2` [SMLAD, ARMv7 ainda fora de escopo] confirmado UNDEFINED e não decodificado como MLS/DIVIDE por engano, G2, bloco misto interpretado×ASM com `perOpFallbackOpCount()>0`); `Thumb2RegisterDataProcessingDecoderTest` (+2 — `RBIT` ida-e-volta com o mesmo vetor palíndromo de B3.1 [`0x80000001`→`0x80000001`], gate PRÓPRIO ausente distinto de `BYTE_REVERSE`). **Armadilha do enunciado sobre as comparações ARM-clássico confirmada na prática**: `ArmArchitecture.ARMV6K` puro NÃO tem `BIT_FIELD`/`MLS_MULTIPLY`/`DIVIDE`/`BIT_REVERSE` (só chegam em `ARMV7A`), então os testes de ida-e-volta usam arquiteturas de teste dedicadas também no lado "ARM clássico" da comparação (não só no lado Thumb-2) — descoberto pelos 8 testes que falharam na primeira rodada (ARM clássico decodificava UNIMPLEMENTED/register=0 silenciosamente sem a feature, mascarando a comparação). Suítes arm-jitter 745 core (724+21) + 13 truffle, gbaemu 239, ndsemu 179 verdes (`mvn -o test` + `mvn -o install` + suítes dos dois consumidores) |
| [B3.3](trilha-b-arquiteturas/b3.3-vfp-banco-registradores.md) | VFP: banco S/D + FPSCR no core | — | ✅ (`VfpRegisters` [32 `S` + views `d`/`sFloat`/`dDouble`, sempre bits crus] e `FpscrRegister` [espelha `CpsrRegister`; `setValue` lança `UnsupportedOperationException` para `RMODE`≠00/`FZ`=1/`LEN`≠0/`STRIDE`≠0, cumulativos aceitos sem side-effect de operação FP] novos em `core/`; `ArmCore` ganha os campos `vfp`/`fpscr` + accessors, sempre alocados (sem preset habilitando ainda). `ArmCore.saveState`/`loadState` ganharam um `STATE_VERSION` interno próprio (`1`=pré-B3.3, `2`=com VFP) — streams antigos (versão 1) carregam com o banco VFP zerado em vez de lançar exceção; isso é ortogonal ao versionamento de formato completo que `GbaConsole`/`NdsConsole` já tinham por cima (full-reject), que também foi bumpado [`SAVE_STATE_VERSION` 3→4 e 2→3 respectivamente] porque o layout de bytes de `ArmCore` mudou. `CpuSnapshot` inclui o banco S completo + FPSCR na captura/`assertEqualTo` (destrava detecção de divergência de FP em B3.6). `ArmFeature.VFPV2` nova, nenhum preset a usa ainda (B3.7). Testes novos: `VfpRegistersTest` [overlap S/D, views bit-exatas incl. NaN payload, save/load, reset], `FpscrRegisterTest` [NZCV/DN/cumulativos, rejeição dos 4 modos não suportados, `setNzcv` isolado], `ArmCoreVfpStateTest` [round-trip + stream legado versão 1 zera o banco sem exceção], `CpuSnapshotTest` [3 casos de divergência/igualdade]. Suites arm-jitter 710 core + 13 truffle, gbaemu 239, ndsemu 175 verdes) |
| [B3.4](trilha-b-arquiteturas/b3.4-vfp-ir-interpretador.md) | VFP: IrOps + executor interpretado | B3.3 | ✅ 2026-07-17 — 10 records novos em `IrOp.java` (`VfpAlu`/`VfpMoveImmediate`/`VfpCompare`/`VfpConvert`/`VfpLoad`/`VfpStore`/`VfpMultipleTransfer`/`VfpCoreTransfer`/`VfpCorePairTransfer`/`VfpSystemTransfer`, `Kind` 44-53) + 2 enums (`VfpOperation`, `VfpConversion`); `IrVfpExecutor` novo em `codegen/executor/` espelhando `IrMemoryExecutor`, plugado em `IrBlockExecutor` (campo/construtor + os dois switches `execute`/`executeOp` + getter `vfpExecutor()`, mesmo padrão A6). Aritmética via `VfpRegisters.sFloat`/`dDouble` (decisão nº 3 do épico): `VMLA`/`VMLS`/`VNMUL` **nunca** usam `Math.fma` (duas operações `float`/`double` arredondadas separadamente, testado contra `Math.fma` explicitamente); `VNEG`/`VABS` manipulam o bit de sinal via `Integer.MIN_VALUE`/`MAX_VALUE` (single) e `Long.MIN_VALUE`/`MAX_VALUE` (double), nunca `0-x`/`Math.abs` (preserva payload de NaN e `-0.0`); `VSQRT` usa `Math.sqrt` direto (corretamente arredondado). `VCVT` para inteiro COM sinal reaproveita o narrowing nativo do Java (`(int)`), que já implementa round-toward-zero + saturação MIN/MAX + NaN→0 idêntico ao VFP; SEM sinal usa um clamp manual em `long` (`toUnsignedInt32`, já que Java não tem `uint32`). `VfpCompare` grava só `FPSCR.NZCV` (nunca o CPSR) com a tabela exata dos 4 quadrantes; `VMRS APSR_nzcv` (`VfpSystemTransfer` com `read=true,armRegister=15`) é o único caminho que copia `FPSCR.NZCV`→`CPSR.NZCV`, sem tocar Q/GE/IT/modo (decisão nº 4 do épico). Load/store de dupla precisão reusa o layout little-endian 2-words de `IrMemoryExecutor#executeDoubleTransfer` (metade baixa no endereço menor). `AsmNativePolicy` rejeita os 10 records novos (ASM nativo só em B3.6). `DeadCodeEliminationPass` ganhou `regUse`/`regDef` explícitos para as formas que tocam registrador ARM (`VfpLoad`/`VfpStore`/`VfpMultipleTransfer`/`VfpCoreTransfer`/`VfpCorePairTransfer`/`VfpSystemTransfer` — protege escritas ALU anteriores de serem eliminadas por engano); as formas 100%-VFP (`VfpAlu`/`VfpMoveImmediate`/`VfpCompare`/`VfpConvert`) já eram opacas pelo `default -> 0` existente, já que `isDead` só mira `IrOp.Alu`. `ConstantFoldPass`/`FlagMergePass` não precisaram de código novo (já só casam `IrOp.Alu` — todo `Vfp*` atravessa intacto, mesma barreira que `Coprocessor`/`Swi` já tinham), só javadoc documentando a decisão. `IrVfpExecutorTest` novo (31 testes): os 9 itens da task — ADD/SUB/MUL/DIV single+double, `MLA`/`MLS` provados `!=Math.fma` com vetor concreto, `NEG`/`ABS` com payload de NaN e `-0.0`, `SQRT(-1)=NaN` + `sqrtf(2)=0x3FB504F3`, os 4 quadrantes de `VCMP` (incl. NaN), cada membro de `VfpConversion` (incl. NaN→0, `1e30f`→`MAX_VALUE`, `-1.5`→U32=0, `4294967040.0`→U32=`0xFFFFFF00`), double ida-e-volta na memória + `VLDM`/`VSTM` IA/DB com writeback, `VMRS APSR_nzcv` preservando Q/GE/modo, condição falsa pulando a op mas consumindo `Cycle`/`Fetch` (G4). Suítes: arm-jitter 776 core (745+31) + 13 truffle, gbaemu 240 (17 skipped, pré-existente) + ndsemu 179 verdes (`mvn -o test` + `mvn -o install` + suítes dos dois consumidores, JBR 25) — zero regressão |
| [B3.5](trilha-b-arquiteturas/b3.5-vfp-decoder.md) | VFP: decoder CP10/11 ARM+Thumb-2 | B3.4 | ✅ 2026-07-22 — `VfpDecoder` novo (`decoder/`), oráculo QEMU `target/arm/tcg/vfp.decode` (baixado via `curl` e citado linha a linha nos comentários, não deduzido). Reivindica `bits[27:24] ∈ {1100,1101,1110}` com `bits[11:8] ∈ {1010,1011}` (CP10/CP11; `1001`/meia-precisão fora do gate, nunca chega ao corpo); dentro disso decodifica: aritmética de 3 registradores (`op1` = bits `23,21,20`, `bit6` seleciona MLA/MLS, MUL/NMUL, ADD/SUB; `VNMLA`/`VNMLS`, `op1=001`, fora de escopo — sem `VfpOperation` correspondente); a família `op1=111` (`bit6=0`→`VMOV_imm`, `bit6=1`→2-operando/compare/convert por `opc2`=bits[19:16]) — achado da task: as conversões (`opc2` 7/8/C/D) têm Vd/Vm em precisões DIFERENTES um do outro (`@vfp_dm_ds`/`@vfp_dm_sd` do QEMU: `VCVT_sp`/`VCVT_dp` invertem qual lado é `S`/`D`; `VCVT_int`/`VCVT_*_int` sempre mantêm um lado fixo em single — o campo `size` descreve só um dos dois lados, nunca ambos), resolvido caso a caso (não um único `doublePrecision` compartilhado, como as formas de 3 registradores/`VCMP`/`VMOV_reg` legitimamente têm). `VMOV_single`/`VMSR_VMRS` (bit4=1, mesmo formato de bits de `MRC`/`MRRC`) e `VMOV_64_dp` (bit4=1, formato `MCRR`/`MRRC`) exigidos a decodificar ANTES de `CoprocessorDecoder` na lista de extensões (ordem de registro, documentada e testada — sem isso `CoprocessorDecoder` capturaria essas instruções primeiro). `D:Vd > 15` (só 16 `D` em v2/v3-d16) validado em TODOS os pontos que decodificam um registrador double (3-registros, 2-operando, `VMOV_imm`, `VLDR`/`VSTR`, `VLDM`/`VSTM` incl. o ÚLTIMO registrador da lista, `VMOV_64_dp`) — UNDEFINED, não silencioso. `VLDM`/`VSTM` com `imm8` ímpar em dupla (`FLDMX`/`FSTMX` deprecated) também UNDEFINED, não arredondado. `Thumb2VfpDecoder` casca fina (mesmo padrão de `Thumb2CoprocessorDecoder`/`CoprocessorDecoder`): o raw32 Thumb-2 tem layout BIT-IDÊNTICO ao ARM com cond forçada a `1110` (confirmado no cabeçalho do próprio `vfp.decode`), então a mesma lógica de bits decodifica os dois — só reescreve o `InstructionSet`. `StandardIrBuilder` ganhou os 10 casos `VFP_*` (novos `InstructionKind`, cada um documentando seu empacotamento de campos — `signedAccess` reaproveitado para `doublePrecision`, `link` para a direção da transferência) + `vfpExpandImm` (fórmula `vfp_expand_imm` do QEMU, verificada bit a bit — **achado**: o vetor "31.0→imm8 0x4F" do enunciado da task está errado/transposto, `VFPExpandImm(0x4F,32)=0x3E780000≈0.242`; `0x3F` é que expande para `31.0f`, confirmado contra a fórmula do QEMU e usado nos testes). `StandardIrBlockLifter#isTerminal` ganhou os 10 kinds novos no ramo `false` (nenhum toca o PC). `VfpDecoderTest` novo (29 testes) cobrindo os 6 itens do enunciado: cada grupo single+double, `VFPExpandImm` (4 vetores, com a correção documentada), execução ponta-a-ponta `VLDR`+`VADD`+`VCMP`+`VMRS APSR_nzcv` decidindo `BGE`, gate `VFPV2` (`MRC p10` sem a feature / `MRC p15` com a feature continuam no `CoprocessorBus`), ida-e-volta ARM×Thumb-2 (`VADD.F64`, `VLDR`), UNPREDICTABLE/fora-de-escopo→UNDEFINED (`VNMLA`/`VNMLS`, `VMSR` não-FPSCR, `imm8` ímpar em dupla, `VCVTR`, `VCVT_fix`). Sem emissão ASM nativa (B3.6 libera, `AsmNativePolicy` já rejeitava todo `Vfp*` desde B3.4). Suítes arm-jitter 812 core (incl. os 29 novos) + 13 truffle, gbaemu 240 + ndsemu 179 verdes (`mvn -o test` raiz + `mvn -o install` + suítes dos dois consumidores, JBR 25) — zero regressão |
| [B3.6](trilha-b-arquiteturas/b3.6-vfp-asm-nativo.md) | VFP + inteiro v7: emissão ASM nativa (2 PRs) | B3.5 | ✅ PR1 2026-07-22, PR2 2026-07-23 — **PR1** (inteiro v7 de B3.1/B3.2): `AsmNativePolicy` libera `BitFieldExtract`/`BitFieldInsert`/`BitReverse`/`Divide`/`MoveTop`/`MemoryBarrier` e `Multiply` (MLS incluso, sem mais checar `subtractFromAccumulator`). `AsmBlockCompiler` ganha `emitBitFieldExtract` (ISHL+ISHR/IUSHR, o mesmo truque do interpretado), `emitBitFieldInsert` (máscara `(((1<<width)-1)<<lsb)` pré-computada no emit; BFC pula o passo de inserção por inteiro em vez de OR com zero), `emitBitReverse` (`Integer.reverse`, intrínseco JIT — mesmo padrão do REV/`reverseBytes` de B1.6), `emitDivide` (guard do divisor 0 ANTES do `IDIV`, ordem confirmada pela Armadilha da task; `Integer.MIN_VALUE/-1` não precisa de guard — `IDIV` da JVM já devolve MIN sem trap), `emitMoveTop` (AND 0xFFFF + OR imediato«16, preserva a metade baixa). `emitMultiply` ganhou o branch MLS: pilha tem `[produto, acumulador]` após empilhar o acumulador — precisa de `SWAP` antes do `ISUB` para subtrair na ordem certa (`acumulador − produto`), não óbvio na primeira tentativa. `countAccesses` (register cache) ganhou os 5 casos nativos com registrador (BitField*/BitReverse/Divide/MoveTop; MemoryBarrier não tem registrador). 2 testes pré-existentes de B3.1/B3.2 que fixavam o comportamento PRÉ-B3.6 (`perOpFallbackOpCount() > 0`) tiveram a asserção invertida para `== 0`: `Thumb2MultiplyDivideDecoderTest` (suíte normal) e `ArmV7MediaDecoderTest$PerOpEquivalence` (achado: essa classe estática aninhada NÃO é descoberta pelo `mvn test` normal — surefire só casa `*Test`/`Test*` no nome da classe top-level; só rodou explicitamente via `-Dtest='ArmV7MediaDecoderTest$PerOpEquivalence'` — verificação manual necessária, não pega em `mvn -o test` da raiz). Suíte arm-jitter 812 core + 13 truffle verdes (`mvn -o test`, JBR 25) + `mvn -o install` + gbaemu (`JitInterpreterDivergenceTest` sem divergência em 20M blocos) + ndsemu verdes — zero regressão. **PR2** (VFP de B3.4/B3.5): `AsmNativePolicy` libera os 10 records `IrOp.Vfp*` — cobertura completa, nenhuma rejeição remanescente. Bytecode direto (caminho quente) para `VfpAlu` ADD/SUB/MUL/DIV (via `sFloat`/`dDouble` + `FADD`/`DADD`/... nativos da JVM + `setSFloat`/`setDDouble`, que já usam `floatToRawIntBits`/`doubleToRawLongBits` por dentro — nunca a forma não-raw, que canonicalizaria NaN), NEG/ABS (XOR/AND só no bit de sinal via `s`/`d` crus, sem conversão de tipo — evita completamente a armadilha do NaN), COPY (cópia crua), `VfpLoad`/`VfpStore` (reusa os MESMOS helpers `loadWord`/`storeWord` de LDR/STR — dupla precisão via `packDoubleWords` novo), `VfpCoreTransfer` (lê/escreve o registrador ARM pelo register cache via `emitReadRegister`/`emitStoreRegister`, sem spill). Helper estático em `AsmRuntimeHelpers` para o resto: `vfpAluCold` (MLA/MLS/NMUL/VSQRT — MLA/MLS seguem NUNCA usando `Math.fma`, mesma regra de B3.4), `executeVfpCompare`, `executeVfpConvert`, `executeVfpMultipleTransfer`, `executeVfpCorePairTransfer`, `executeVfpSystemTransfer` (incl. o caso `VMRS APSR_nzcv`) — os 3 últimos tocam registrador(es) ARM DIRETAMENTE no core (por índice arbitrário, fora do register cache) e por isso são cercados por `emitSpilled` no ponto de despacho, exatamente o mesmo tratamento que `PsrTransfer`/`Coprocessor` já tinham (a "cautela com o register cache" da task — não havia um cache de flags separado para se preocupar, CPSR/FPSCR nunca são cacheados em local JVM). `GuestToHostMapper` ganhou 9 bindings novos (`vfp()`, `s`/`setS`/`d`/`setD` crus + `sFloat`/`setSFloat`/`dDouble`/`setDDouble`). `VfpNativeEquivalenceTest` novo (15 métodos): equivalência exaustiva por grupo (14 condições × cada família, vetores NaN payload/±0.0/subnormal/MIN_VALUE-(-1)/x÷0), property test de 64 floats aleatórios (seed fixa) × todas as 11 `VfpOperation` × single/double, `perOpFallbackOpCount() == 0` num bloco sintético com os 10 kinds, e 14 condições × VCMP+VMRS incl. `APSR_nzcv`. Os `IrOp` são montados à mão (mesmo padrão de `IrVfpExecutorTest`) em vez de decodificados — `ARMV7A` (B3.7) ainda não existe, e `AsmNativePolicy`/o interpretador não gateiam por arquitetura, só o decoder. Suíte arm-jitter 826 core (812+14) + 13 truffle verdes (`mvn -o test`, JBR 25) + `mvn -o install` + gbaemu 240 (`JitInterpreterDivergenceTest` sem divergência em 20M blocos, 17 skipped pré-existentes) + ndsemu 183 verdes — zero regressão. Fecha B3.6 por completo (PR1+PR2); B3.7 (preset `ARMV7A` + armbox torture) segue como próxima task da trilha. |
| [B3.7](trilha-b-arquiteturas/b3.7-preset-armv7a-armbox.md) | Preset `ARMV7A` + armbox torture + gcc hard-float (fecha B3) | B3.1–B3.6 | ✅ 2026-07-23 — `ArmArchitecture.ARMV7A` novo (`extending(ARMV6K_THUMB2, ...)`): `MOVW_MOVT`/`MEMORY_BARRIERS` já herdados de `ARMV6K_THUMB2_FEATURES`, mas `MLS_MULTIPLY`/`BIT_FIELD`/`BIT_REVERSE`/`DIVIDE` (demais features do "inteiro v7" de B3.1) NÃO são herdados de nenhum preset anterior — precisaram ser declarados aqui explicitamente, junto com `VFPV2` novo. **Desvio real do plano original, mesmo padrão de honestidade de B3.1**: a spec previa `.withDecoderExtensions(List.of(new VfpDecoder(), new CoprocessorDecoder(), new ArmV7MediaDecoder()))` — mas `ArmV7MediaDecoder` nunca existiu (B3.1 já desviou disso, ver a entrada dela); o preset só precisa de `VfpDecoder`/`CoprocessorDecoder` (nesta ordem — regra de B3.5) mais as features certas, já que as 13 instruções "media" v7 são carve-outs diretos em `ArmDecoder`. `withThumb32DecoderExtensions` lista de novo as 7 extensões de `ARMV6K_THUMB2` (substituição, não soma) mais `Thumb2VfpDecoder` posicionado ANTES de `Thumb2CoprocessorDecoder`. **Quebra-cabeça do ovo e da galinha resolvido com `ARMV7A_FEATURES` privado** (mesmo padrão de `ARMV6K_THUMB2_FEATURES`): `VfpDecoder`/`Thumb2VfpDecoder` recebem `ArmArchitecture` no construtor para gatear `VFPV2`, e `ARMV7A` ainda não existe nesse ponto. Lado armbox: `--arch=armv7a` no `Main` (mesmo padrão de `--arch=thumb2`); `testdata/armv7a-torture.s` (28 checagens auto-verificáveis: MOVW/MOVT, UBFX/SBFX/BFC/BFI, RBIT, MLS, SDIV/UDIV incl. x/0 e MIN_VALUE/-1, DMB, e a seção VFP — VMOV imediato single/double + transferência core↔S/D, VADD/VMUL/VDIV/VSQRT [`sqrtf(2)=0x3FB504F3`, mesmo vetor exato de `IrVfpExecutorTest`/B3.4], VCMP+VMRS APSR_nzcv, VCVT int↔float, VLDR/VSTR, VPUSH/VPOP) + gêmeo `-broken`; `testdata/hello-float.c` (série de Leibniz de 6 termos em `double`, formatação manual sem printf de float). **Armadilha do enunciado resolvida diferente do previsto**: a spec temia NEON via memcpy vetorizado de libc pré-compilada mesmo sob `-mfpu=vfp` — a mitigação adotada foi mais forte que "trocar o workload": `hello-float.c` segue o MESMO padrão bare-metal de todo o resto do testdata (`-nostdlib`, `_start` próprio via `naked`, syscalls cruas via `svc #0`, sem CRT/libc/libgcc nenhuma — divisão por constante 10 inline pelo próprio gcc) — zero libc linkada, então o risco de NEON não existe estruturalmente, confirmado via `objdump`/`nm` (só 2 símbolos, `_start`/`c_main`, nenhum registrador `q`). Toolchain real exigiu 2 ajustes não previstos na spec: `SDIV`/`UDIV` não existem em `-march=armv7-a` base (é a extensão opcional "integer divide", nota v7VE) — usado `-march=armv7ve`; `VMOV` imediato é VFPv3, não VFPv2 real — usado `.fpu vfpv3-d16` no assembler (nome real da variante de 16 registradores `D` que corresponde ao que `VfpDecoder` já valida como "`D:Vd>15` é UNDEFINED"). **Bug REAL encontrado e corrigido pela validação N3** (exatamente o cenário que a Armadilha 3 da spec previa: "se divergir, o bug é real"): `hello-float.elf` divergia nos 3 backends (JIT e interpretado davam respostas DIFERENTES e ambas erradas) — causa raiz: `IrOp.VfpLoad`/`VfpStore`/`VfpMultipleTransfer` não tinham o campo `baseValueOverride` que `IrOp.Load`/`Store` já tinham para o idioma `Rd,[pc,#imm]` (literal pool do `gcc`) — sem ele, `base`=PC era lido AO VIVO em `core.register(15)`, que só reflete o viés `+8` do PC arquitetural DEPOIS que o bloco termina, então duas `VLDR Dx,[pc,#imm]` consecutivas liam os literais TROCADOS entre si. Fix: campo novo em `IrOp.java` (3 records) + `StandardIrBuilder` passando `baseValueOverride(instruction)` (helper já existente, reusado) + `IrVfpExecutor` usando `support.registerValue(...)` em vez de `core.register(op.base())` direto + `AsmBlockCompiler`/`AsmRuntimeHelpers` (emissão nativa) com o mesmo tratamento de `Load`/`Store`. 3 novos testes de regressão (`VfpDecoderTest` ida-e-volta decode+execução com `Rn=PC`, `VfpNativeEquivalenceTest` provando que o override — não o PC ao vivo — dirige o endereço). Depois do fix, os 3 backends (JIT/interp/check) concordam bit-a-bit: `0.744012` (Leibniz de 6 termos), confirmado contra cálculo de referência Java (`Double.doubleToRawLongBits`). Suíte arm-jitter (core+truffle) + armbox (incl. `Armv7TortureTest` novo: torture 28/28 + `-broken` exit 77 + hello-float 3 backends idênticos) + `mvn -o install` + gbaemu + ndsemu todos verdes (JBR 25) — zero regressão. |
| [B4.0](trilha-b-arquiteturas/b4.0-runner-user-mode.md) | Runner Linux user-mode (estilo qemu-user) | — | ✅ (repo `armbox`: hello + busybox echo/sh; sem fork/pipes. 2026-07-11: só validado com ARMv5TE até agora, apesar do README do armbox prometer ARMv6K/Thumb-2/ARMv7 — daí B4.0.1/B4.0.2 abaixo; padrão a repetir como B4.0.3+ quando B2.3-B2.5/B3/B6 fecharem) |
| [B4.0.1](trilha-b-arquiteturas/b4.0.1-armbox-validar-armv6k.md) | Validar ARMv6K de verdade no armbox (binário real) | B4.0, B1.1-B1.6 | ✅ (repo armbox, commit `e8ee7c8`: `--arch=armv5te\|armv6k` [default armv5te, plumbing até os 2 `JitRuntimeFactory`+`ArmCore`]; `testdata/armv6k-torture.s` — torture test ARMv6K escrito à mão, auto-verificável [26 checagens, exit-code único por checagem], cobrindo SXTB/UXTAH com rotação, REV/REV16/UMAAL, SADD16/UQSUB8/UADD8+SEL, PKHBT/SSAT/USAD8, LDREX/STREX/CLREX [sucesso E as duas formas de falha do monitor forçadas explicitamente], CPS/SETEND/WFI — roda exit 0 nos 3 backends [JIT/`--interp`/`--check`]. **WFI**: armbox é user-mode puro sem timer/IRQ — decisão explícita documentada: `Armbox.run` chama `ArmCore#wake()` sempre que observa o core HALTED após uma fatia de blocos [sem `setInterruptLine`/vetor de exceção], espelhando o Linux real [WFI em user-mode só estala até o próximo tick do timer do kernel] — WFI vira no-op observável. `armv6k-torture-broken.s` = "teste do teste" [valor esperado deliberadamente errado, prova exit≠0]; `hello-armv6k.s` = sinal complementar de compilador [`-march=armv6k` sem instrução nova]. Nenhum bug do arm-jitter revelado — os 3 backends bateram de primeira contra os vetores esperados [único bug foi no `.s` do torture test, pool de literais caindo no fluxo de execução, corrigido reposicionando `.ltorg`]. Suite armbox 23 testes verde) |
| [B4.0.2](trilha-b-arquiteturas/b4.0.2-armbox-validar-thumb2.md) | Validar Thumb-2 de verdade no armbox (binário real) | B4.0.1, B2.1-B2.2 | ✅ (arm-jitter: novo preset público `ArmArchitecture.ARMV6K_THUMB2` — `extending(ARMV6K, ..., THUMB2).withThumb32DecoderExtensions(List.of(new Thumb2DataProcessingDecoder()))`, nome deliberadamente NÃO sugere completude [não é o ARMv7-A de B3]; comentário registra a convenção de acrescentar `Thumb2LoadStoreDecoder`/`Thumb2MiscDecoder` quando uma task futura ampliar a cobertura do armbox. armbox: `--arch=thumb2` no `Main`/`Armbox`; `testdata/thumb2-torture.s` (handwritten, `.syntax unified`+`.w` forçado, confirmado via `objdump -d`) cobrindo modified-immediate com carry-out, MOVW+MOVT, ADD/ADR com SP/PC, forma registrador com shift incl. RRX; `Thumb2TortureTest` (espelha `ArmV6TortureTest` de B4.0.1) roda nos 3 backends + `thumb2-torture-broken.elf` prova que o harness detecta regressão (exit 77). **Bug real achado e corrigido durante a task**: a primeira versão do `.s` usava `mrs r5, cpsr` para extrair o carry-out — MRS é do grupo B2.5 (`Thumb2MiscDecoder`), fora do escopo do preset desta task (só `Thumb2DataProcessingDecoder`); o binário caía em UNDEFINED e o armbox (sem vetor de exceção real, hospedeiro user-mode) segfaultava lendo o endereço 0x4. Corrigido substituindo por `adc r5, r6, #0` (r6=0) — lê o carry via ALU comum, dentro do escopo B2.1-B2.2, sem depender de MRS. Suítes arm-jitter (raiz) + armbox 26 (22+4 novos) + gbaemu + ndsemu revalidadas verdes) |
| [B4.0.3](trilha-b-arquiteturas/b4.0.3-armbox-validar-thumb2-completo.md) | armbox: Thumb-2 de compilador real (gcc/busybox) | B2.6 ✅, **B2.7 (incl. PR3 = MCR/MRC Thumb — o gap que B4.0.4 confirmou), B1.7 ✅, B2.8**, B4.0.4 ✅, B4.0.4.1 ✅ | 🟡 PARCIAL (2026-07-24) — `testdata/hello-thumb2.c` (repo armbox): struct com bitfields (`UBFX`/`SBFX`/`STRH`), `STRD` explícito, switch denso que o gcc compila para `TBB`, qsort pequeno recursivo (`IT` blocks) — `gcc -march=armv7-a -mthumb -Os -nostdlib -static`, `objdump` confirma `ldr.w`/`strd`/`ldmia.w`/`tbb`/`it`/`bl`. Rodado com `--arch=armv7a` (não `--arch=thumb2`), fallback previsto pela própria task: bitfields exigem `ArmFeature.BIT_FIELD`, que `ARMV6K_THUMB2` não tem (só `ARMV7A`). **Achado real de bug corrigido no arm-jitter**: `ArmArchitecture.ARMV7A` construía `Thumb2DataProcessingDecoder`/`Thumb2RegisterDataProcessingDecoder`/`Thumb2MultiplyDecoder` passando `ARMV6K_THUMB2_FEATURES` (sem `BIT_FIELD`/`BIT_REVERSE`/`MLS_MULTIPLY`/`DIVIDE`) no construtor em vez de `ARMV7A_FEATURES` — `UBFX`/`SBFX`/`RBIT`/`SDIV`/`UDIV`/`MLS` em encoding **Thumb-2** viravam `UNDEFINED` mesmo com o preset `ARMV7A` tendo as features certas (ARM-mode nunca teve esse bug — usa a `architecture` real passada a cada chamada, não uma cópia guardada no construtor). Sem o fix, `armbox` segfaultava lendo o vetor 0x4 (undefined-instruction, sem handler em user-mode) já no início do `hello-thumb2.elf`. Corrigido trocando as 5 construções para `ARMV7A_FEATURES`; teste de regressão novo `ArmV7aThumb2PresetIntegrationTest` (decodifica contra o preset PÚBLICO `ARMV7A`, não um preset sintético — os testes de unidade existentes de B3.1/B3.2 não pegavam esse bug de fiação por construírem seus próprios presets já corretos). `Thumb2CompilerBinaryTest` novo (armbox): os 3 backends concordam, mesmo checksum hex, exit 0. Matriz de validação atualizada (`ARMV6K_THUMB2` N3 🟡, `ARMV7A` N3 ganha a nota do achado). Suítes arm-jitter (raiz) + gbaemu + ndsemu + armbox revalidadas verdes. **Item 3 da task (busybox Thumb-2) NÃO fechado**: sem binário pré-compilado Thumb-2 nos releases oficiais de busybox.net (só ARM mode); buildar da fonte exige um toolchain `arm-linux-*` real (musl/glibc) — os cross-toolchains do musl.cc são ELF Linux e não rodam neste ambiente Windows/MSYS2 sem WSL com uma distro configurada, e o devkitARM instalado é bare-metal (`arm-none-eabi`, sem libc de userspace). Fica pendente para sessão com toolchain `arm-linux-*` disponível — task continua aberta até isso fechar. |
| [B4.0.4](trilha-b-arquiteturas/b4.0.4-armbox-tls-tpidruro.md) | armbox: TLS v7 (TPIDRURO via `CoprocessorBus`) — musl v7 lê TLS por MRC, hoje explode | — | ✅ (repo armbox: `ArmboxCp15` novo em `linux/`, instalado via `core.setCoprocessorBus` só para arquiteturas com `ArmFeature.EXTEND_ROTATE` [marcador de ">= ARMv6K"; único ponto real de `new ArmCore` no armbox — o overload de 4 argumentos delega ao de 6, então "os 2 pontos de `--arch`" da task acabaram sendo o mesmo site]; atende `MRC`/`MCR p15,0,Rt,c13,c0,{2,3}` [TPIDRURW/TPIDRURO]; TPIDRURO lê a MESMA word que `ARM_set_tls` já escreve [`KuserHelpers.TLS_VALUE_ADDRESS`] — kuser helper e CP15 sempre concordam, campo único, sem duplicação. **Achado durante a implementação, não estava na spec**: `IrSystemExecutor#executeCoprocessor` só intercepta e entrega uma exceção ARM Undefined limpa ao guest [PC pro vetor] quando `bus.handles(coprocessor)` é `false`, ANTES de chamar `read`/`write` — uma vez que `handles(15)` precisa ser `true` [para atender TPIDRURO/TPIDRURW], o método sempre devolve `false` [bloco continua] depois de chamar `read`/`write`, não importa o que aconteça dentro; e `CoprocessorBus` não dá acesso ao `ArmCore` para o barramento sinalizar a exceção ele mesmo. Confirmado experimentalmente: uma primeira tentativa injetando `ArmCore` e chamando `core.requestException(UNDEFINED)` dentro de `read()` para registrador não suportado NÃO interrompia o bloco — a exceção era "pedida" mas as instruções seguintes do bloco (já decodificadas) continuavam rodando com o registrador destino contaminado, e o processo saía com o código de saída da PRÓXIMA `svc` do bloco em vez de segfaultar no vetor [medido: exit 0 em vez do esperado]. **Decisão**: registrador de CP15 não suportado [ou escrita em TPIDRURO, read-only] lança `IllegalStateException` — falha alto, não "engole" o acesso [aceite da task], mas não é bit-a-bit idêntico à entrega de exceção Undefined real ao guest; ArmCore não é injetado no barramento. Reportado como limitação real de `CoprocessorBus`/`IrSystemExecutor` para uma implementação parcial de um coprocessador que `handles()==true` — menor que o gap do Thumb-2 abaixo, arm-jitter NÃO alterado [fora de escopo desta task]. **Gap confirmado da Armadilha #1**: nenhum decoder Thumb-2 para o espaço `MRC`/`MCR` [`0xEE`] existe no arm-jitter — `CoprocessorDecoder` só está plugado via `decoderExtensions()` [ARM-mode, herdado desde `ARMV5TE`], nunca via `thumb32DecoderExtensions()`; `ARMV6K_THUMB2` registra só as 4 extensões de B2.1-B2.6 [DataProcessing/LoadStore/Branch/Misc], nenhuma cobre coprocessador. Sob `--arch=thumb2`, um `mrc` real emitido em Thumb [musl Thumb-2] cai em UNDEFINED por falta de decode, não por falta deste CP15 — bloqueia B4.0.3 mesmo com esta task fechada; gap reportado, NÃO corrigido [instrução explícita da task]. 4 testes novos [`ArmboxCp15Test`, 3 backends INTERPRETED/JIT/TRUFFLE]: TPIDRURO round-trip via `set_tls` [ARM-mode, prova os dois caminhos concordam], TPIDRURW write+read, registrador não suportado falha alto, `ARMV5TE` sem barramento continua Undefined [G3]. Suíte armbox 30 verde [26 existentes + 4 novos], `mvn -o test` raiz com JBR 25) |
| [B4.0.4.1](trilha-b-arquiteturas/b4.0.4.1-coprocessor-handles-fino.md) | `CoprocessorBus.handles` fino por registrador — Undefined limpo de guest para CP15 parcial (gap 1 do relatório da B4.0.4; decisão fechada: predicado ANTES de read/write, com default retrocompatível) | — | ✅ (`CoprocessorBus` ganhou `handles(coprocessor, opcode1, crn, crm, opcode2)` default delegando ao `handles(coprocessor)` grosso [G3 — retrocompatível]; `IrSystemExecutor#executeCoprocessor` e `AsmRuntimeHelpers#executeCoprocessor` [emissão ASM nativa de `IrOp.Coprocessor`, nativa desde sempre] passaram a consultar a forma fina, mesmo caminho UNDEFINED já existente. Testes novos em `CoprocessorTest` [bus fake grosso=true/fino só c13 → registrador reivindicado funciona, não-reivindicado vira Undefined sem exceção Java vazando] + `CoprocessorFineHandlesEquivalenceTest` novo [interpretado × ASM equivalentes nos dois cenários, G1]. **armbox** [commit separado]: `ArmboxCp15` sobrepõe a forma fina [só `c13,c0,{2,3}`]; registrador não reivindicado agora entra pelo caminho Undefined normal do core [`ArmboxCp15Test.unsupportedCp15RegisterStaysUndefined` trocado de `assertThrows(IllegalStateException)` para `SEGFAULT_EXIT_CODE`, igual ao caso ARMv5TE sem barramento]; mensagem de `unsupported()` [agora só alcançável por bug do executor] trocada para "bug: executor não consultou handles fino". Suítes arm-jitter 610 core [606+4: 2 em `CoprocessorTest` + 2 em `CoprocessorFineHandlesEquivalenceTest`] + 13 truffle + gbaemu 216 + ndsemu 175 + armbox 30 [26+4, sem teste novo — `ArmboxCp15Test` ajustado] verdes, `mvn -o test`/`install` com JBR 25) |
| [B4.0.5](trilha-b-arquiteturas/b4.0.5-armbox-fork-pipes.md) | armbox fase 3: fork/execve/pipes/wait (scripts shell reais como corpus N3) | B4.0 ✅ (B4.0.4 recomendada antes) | ⬜ |
| [B4.1](trilha-b-arquiteturas/b4.1-mmu-softmmu.md) | MMU/softmmu full-system — refinado em B4.1.1–B4.1.5; [RFC-SOFTMMU](../docs/RFC-SOFTMMU.md) aprovada 2026-07-15 (**não depende mais de B3**: hospedeiro versatilepb+ARM1176/ARMv6K) | B1.6 | ✅ (épico completo 2026-08-14) **B4.1.1 ✅ 2026-07-26** — `memory/mmu/TranslatingAddressSpace` novo (wrapper `AddressSpace`, G3: nenhum consumidor existente tocado): page-walk short-descriptor ARMv6 (L1 4096×1MiB — fault/coarse-page-table/seção; L2 coarse 256×4KiB — fault/página grande 64KiB/página pequena 4KiB; sem subpages, sem XN/APX/TEX/cache attrs — fora do pedido da B4.1.1), domínios (`DomainAccess`: NO_ACCESS/CLIENT/RESERVED-como-NO_ACCESS/MANAGER, `DACR` de 16×2 bits) e AP clássico de 2 bits (User vs Privileged); micro-TLB direto-mapeada 2×256 (instrução/dados separadas, granularidade fixa de 4KiB mesmo para seções/páginas grandes, tag por ASID) com `pageWalkCount()` provando que HIT não relê L1/L2 — permissão/domínio são reavaliados a cada acesso (hit ou miss) contra o `DACR`/modo atuais, sem precisar invalidar TLB quando eles mudam (comportamento real de hardware). API pública: `setTtbr0`/`setAsid`/`setDacr`/`setPrivileged`/`invalidateTlbAll`/`invalidateTlbByMva`/`pageWalkCount`/`fetch16`/`fetch32`. `MemoryTranslationException` (unchecked, sem stack trace, RFC §3) + `FaultStatus` (6 códigos `FS[3:0]` reais do ARMv6) lançados pelo wrapper — só a exceção em si, SEM captura/entrada de abort nos motores nem CP15 (isso é B4.1.2/B4.1.3, não tocado). `TranslatingAddressSpaceTest` novo (11 testes): identidade, remapeamento, permissão privilegiada-escreve/usuário-só-lê na mesma página com AP `USER_READ_ONLY`, domínio NO_ACCESS bloqueando apesar de AP full-access, domínio MANAGER liberando apesar de AP no-access, HIT não gera walk novo, `invalidateTlbAll`/`invalidateTlbByMva` forçando novo walk seletivamente, translation fault em L1 vazio. `mvn -o test` raiz verde (1136 core + 13 truffle); gbaemu/ndsemu não tocados/revalidados (G5 não se aplica — nenhum arquivo compartilhado por eles foi alterado, mesmo precedente de B6.1-B6.3.4). **B4.1.2 ✅ 2026-07-26** — `memory/mmu/Cp15VmsaCoprocessor` novo (`CoprocessorBus`, composição não herança, mesmo precedente do `ArmboxCp15`): liga `MCR`/`MRC` (`opcode1=0`) aos controles da B4.1.1 — `SCTLR` (`c1,c0,0`: só bits `M`/MMU-enable e `V`/vetores-altos têm efeito, sincronizados com `TranslatingAddressSpace.setMmuEnabled` novo e `ArmCore.setHighVectors`; resto RAZ/WI por não ter estado próprio guardado), `TTBR0`/`TTBR1`/`TTBCR` (`c2`: só `TTBR0` liga em `setTtbr0`, os outros dois só armazenamento — wrapper assume `TTBCR.N=0`), `DACR` (`c3`→`setDacr`), `DFSR`/`IFSR`/`DFAR`/`IFAR` (`c5`/`c6`: só armazenamento, preenchimento real de abort é B4.1.3), `c7` (manutenção de cache + barreiras `ISB`/`DSB`/`DMB`: NOP observável em escrita, RAZ em leitura — sem cache/pipeline modelados), TLB unificada `c8,c7,{0,1}` (`TLBIALL`/`TLBIMVA`→`invalidateTlbAll`/`invalidateTlbByMva`), `CONTEXTIDR` (`c13,c0,1`: 8 bits baixos→`setAsid`, valor completo guardado). `TranslatingAddressSpace` ganhou `setMmuEnabled`/`mmuEnabled` (aditivo, default `true` preserva o comportamento da B4.1.1 sem CP15; o construtor do `Cp15VmsaCoprocessor` desliga explicitamente para simular `SCTLR.M=0` no reset real de hardware) — quando desligada, tradução vira passthrough identidade (sem walk/TLB/permissão), igual ao hardware antes do software habilitar a MMU. `Cp15VmsaCoprocessorTest` novo (8 testes): unidade direta (TTBR0/DACR/CONTEXTIDR forwarding+read-back, SCTLR.M liga/desliga `mmuEnabled`, SCTLR.V liga `core.highVectors()`, TLB ops forçando/não-forçando walk novo, `c7` NOP, predicado fino rejeitando registrador fora de escopo) **+ o aceite explícito da task**: sequência real `TTBR0→DACR→SCTLR.M=1` escrita por instruções `MOV`/`MCR` reais executadas via `ArmCore.step()` no interpretador muda a tradução (verificado lendo/escrevendo pelo `TranslatingAddressSpace` depois). Fora de escopo documentado no Javadoc da classe: sincronizar `setPrivileged` a partir de troca de modo do `core` a cada instrução (o `ArmCore` ainda não tem gancho de troca de modo; o wrapper só vira `AddressSpace` *live* dos motores na B4.1.3) e captura de `MemoryTranslationException`/entrada de abort (B4.1.3). `mvn -o test` raiz verde (1144 core + 13 truffle); gbaemu/ndsemu não tocados/revalidados (G5 não se aplica, mesmo precedente de B4.1.1). **B4.1.3 ✅ 2026-07-26** — `ArmCore.enterMemoryAbort` novo: converte `MemoryTranslationException` em `PREFETCH_ABORT`/`DATA_ABORT`, preenchendo FAR/FSR via `MemoryAbortListener` novo (mesmo padrão aditivo de `CoprocessorBus`/`BkptDispatcher`, `Cp15VmsaCoprocessor` agora também o implementa) antes de `setProgramCounter`+`requestException` — `instructionAddress` é o endereço da PRÓPRIA instrução faltosa (não o sequencial seguinte, ao contrário de SWI), já que `AProfileExceptionModel` soma o deslocamento +4/+8 a partir dele. Captura nos DOIS motores: `ArmCore#executeSingleInstruction` (interpretador de um passo, usa o `pc` já capturado antes do decode/lift/execute — cobre fetch e dados da mesma instrução) e `IrBlockExecutor#execute` (motor único compartilhado por bloco interpretado inteiro E fallback PER_OP do JIT — `try` cercando o laço quente inteiro sem custo quando não lança nada; no `catch`, o endereço da instrução faltosa é o do próximo `IrOp.Fetch` a partir do índice corrente, já que toda instrução termina SEMPRE com `Cycle`+`Fetch`, G4). **JIT nativo** (`AsmBlockCompiler`): um ÚNICO `visitTryCatchBlock` de `MemoryTranslationException` cerca todo o laço de emissão por-op (custo zero no bytecode gerado quando não lança); o endereço da instrução dona de cada op é computado em TEMPO DE COMPILAÇÃO (`computeInstructionAddresses`, mesma técnica de scan-para-trás a partir do próximo `Fetch`) e gravado num slot fixo (`FAULT_PC_LOCAL`, acima de toda a faixa dinâmica do register cache) via `LDC`+`ISTORE` a cada op; o handler faz `emitCacheFlush` — os locais do register cache SOBREVIVEM ao unwind dentro do MESMO frame JVM, então registradores já escritos antes da falta (ex. no meio de um LDM desenrolado) chegam ao core de qualquer forma — e chama `core.enterMemoryAbort(...)` antes de retornar com os ciclos parciais. **Achado real corrigido no meio do trabalho** (fora do escopo original da task, mas sem ele NENHUMA falta de busca de instrução seria possível): `TranslatingAddressSpace.fetch16`/`fetch32` (B4.1.1) já existiam mas ninguém os chamava — `ArmDecoder`/`ThumbDecoder` liam `read16`/`read32` direto, então a busca de instrução nunca passava pela TLB de INSTRUÇÃO nem podia gerar `PREFETCH_ABORT` de verdade; corrigido com `AddressSpace.fetch16`/`fetch32` novos (default delega a `read16`/`read32` — ZERO mudança de comportamento para todo barramento existente, G3) e os 3 pontos de busca de instrução dos dois decoders chamando `fetch16`/`fetch32` em vez de `read16`/`read32`. **MultipleTransfer base-restored (RFC §3) cai de graça nos dois motores**: o writeback da base já era emitido DEPOIS do laço de registradores em ambos (`IrTransferExecutor`, `AsmBlockCompiler#emitMultipleTransferInline`), então uma falta no meio do laço simplesmente nunca alcança a linha de writeback — nenhuma mudança extra precisou ser feita ali. Testes novos: `ArmCoreMemoryAbortTest` (DATA_ABORT/PREFETCH_ABORT com FAR/FSR reais via `Cp15VmsaCoprocessor`, `SUBS PC,LR` retomando a instrução faltosa), `Cp15VmsaCoprocessorTest` (`onDataAbort`/`onPrefetchAbort`), `MultipleTransferInterpreterTest` (LDM base-restored), `MemoryAbortEquivalenceTest` novo (G1: mesmo bloco lifted de uma instrução real rodando por `InterpretedCodeEmitter` E `AsmCodeEmitter` via `BlockEquivalenceHarness`/`CpuSnapshot`, precisa terminar no MESMO estado após o abort — Load e LDM). `FaultingAddressSpace` novo em `support/` (decorator de teste que injeta a exceção num endereço configurado, sem precisar montar page tables reais — essas já são cobertas por `TranslatingAddressSpaceTest`, B4.1.1). `mvn -o test` raiz verde; gbaemu + ndsemu + armbox revalidados verdes (G5 se aplica desta vez: `ArmCore`/`IrBlockExecutor`/`AsmBlockCompiler`/decoders são compartilhados). **B4.1.4 ✅ 2026-07-26** — `translationGeneration` (RFC-SOFTMMU §5) no `BlockKey`/`JitRuntime`: `AddressSpace` ganhou o default `translationGeneration()` (aditivo, retorna `0` — mesmo precedente de `fetch16`/`fetch32` da B4.1.3, zero mudança de comportamento para todo consumidor sem MMU, G3); `TranslatingAddressSpace` ganhou o contador próprio, incrementado SÓ em `setTtbr0`/`setAsid`/`invalidateTlbAll` (as três operações que trocam qual tradução um VA já resolvido passa a produzir — `setDacr`/`setPrivileged`/`invalidateTlbByMva` não bumpam, mudam só política/uma página). `BlockKey` ganhou o 4º campo `translationGeneration` (mesma técnica de tag do `itState` da B2.4, com os dois construtores de compatibilidade preservados e testados: `(pc, instructionSet)` e `(pc, instructionSet, itState)` continuam defaultando `translationGeneration=0`). `JitRuntime.execute` lê `core.memory().translationGeneration()` uma vez por chamada e usa o valor em três pontos: (1) entra no `BlockKey` do lift, então uma troca de geração é um MISS natural no `BlockCache` — o bloco da geração anterior nunca é encontrado pela chave nova, sem invalidação explícita; (2) o inline cache de dispatch agora esvazia TOTALMENTE (mesmo mecanismo já existente para `BlockCache.generation()`) quando a geração de tradução muda — necessário porque uma troca de processo NÃO remove blocos do `BlockCache` (gerações diferentes coexistem sob chaves diferentes), só o flush do IC evita servir um slot preenchido pela geração anterior; (3) o guard do laço de encadeamento (`chain fast path`) também checa a geração de tradução a cada hop — sem isto, um `MCR` que escrevesse `TTBR0`/`CONTEXTIDR`/`TLBIALL` NO MEIO de uma corrente encadeada continuaria servindo blocos do IC compilados para o mapeamento anterior pelo resto do orçamento de ciclos (achado da própria task, não coberto explicitamente pela RFC — a corrente de encadeamento é código mais novo que a RFC original). `JitRuntimeTranslationGenerationTest` novo (aceite explícito da task): monta duas tabelas L1 short-descriptor à mão mapeando o MESMO VA para código físico DIFERENTE (`MOV R0,#10` vs `MOV R0,#20`), simula duas "trocas de processo" via `TranslatingAddressSpace.setTtbr0`+`invalidateTlbAll`, e prova que cada troca executa o código correto (nunca o bloco stale da geração anterior) tanto no primeiro lookup quanto no inline-cache-hit subsequente; também documenta explicitamente o trade-off aceito pela RFC ("não ASID por bloco... per-ASID caching fica como otimização futura medida"): revisitar uma tabela de página já vista antes conta como geração NOVA (contador global monotônico) e recompila em vez de reencontrar o bloco antigo — correto, apenas não ótimo. `mvn -o test` raiz verde (1157 core + 13 truffle); gbaemu revalidado verde (240 testes) e ndsemu revalidado verde (183 testes) + bench `JUS.nds` ASM_BYTECODE ~63 fps (2 execuções, ~105-106% realtime) — SEM regressão frente ao último número registrado (~59,5 fps), dentro (e acima) do ±2% exigido (G5 se aplica: `BlockKey`/`JitRuntime`/`AddressSpace` são infraestrutura compartilhada). **B4.1.5 ✅ 2026-08-14 (2 sessões) — FECHA O ÉPICO B4.1** — repo novo `virtual-arm-box` (ex-`linuxbox`): hospedeiro `versatilepb`-like com RAM 128MiB (`PagedAddressSpace`, C3), `Pl011Uart` (console, máquina de estados transcrita de `hw/char/pl011.c`), `Sp804DualTimer` (`hw/timer/arm_timer.c`), `Pl190Vic` (`hw/intc/pl190.c`), `AtagsBuilder` (protocolo ATAGs clássico: CORE/MEM/INITRD2/CMDLINE/NONE) — todos sobre `TranslatingAddressSpace`+`Cp15VmsaCoprocessor` (B4.1.1-B4.1.4, usados como estão, nenhuma mudança de escopo). **Desvio forçado documentado** (sem toolchain `arm-linux-*` nesta máquina, mesmo bloqueio de B4.0.3/B6.2): usa um kernel REAL pré-compilado (`vmlinuz-3.2.0-4-versatile`, build Debian, ARM926EJ-S/ARMv5TE, board `versatile`/ATAGs — não um `versatile_defconfig` self-built, que hoje exige Device Tree e não existe mais em ARM1176/ARMv6K puro) + `busybox-armv5l` real (binário estático ARM-mode oficial do busybox.net — o achado que destrava onde B4.0.3/B6.2 ficaram parciais, que precisavam de Thumb-2/AArch64 inexistentes lá) via `initramfs.cpio.gz` (formato newc escrito à mão, sem `cpio` disponível). Ver `testdata/README.md` do repo `virtual-arm-box` para a proveniência completa. **2 bugs reais do arm-jitter encontrados e corrigidos rodando o kernel** (commit separado, fora de B4.1.1-B4.1.4): (1) `ArmArchitecture.ARMV5TE` não tinha `ArmFeature.PRELOAD_HINTS` (só `ARMV6K` herdava) — PLD/PLDW/PLI existem desde a ARMv5TE de verdade (ARM DDI 0406C), sem a feature um `PLD` real de `arch/arm/lib/copy_page.S` virava UNDEFINED e travava o boot; movido para o conjunto base da `ARMV5TE` (G3: aditivo). (2) `JitRuntime.execute`/`executeTiered` chamavam `lift(...)` em 3 pontos sem `try/catch` de `MemoryTranslationException` — a falta de tradução na busca da PRIMEIRA instrução de um bloco NUNCA-antes-visto (antes do bloco existir, portanto antes da proteção de B4.1.3 em `IrBlockExecutor`/`AsmBlockCompiler`, que só cerca a EXECUÇÃO de um bloco já lifted) escapava e derrubava o processo hospedeiro; os 2 pontos síncronos agora capturam e chamam `core.enterMemoryAbort`, o de promoção em background desiste da promoção esta vez. **Aceite objetivo da task NÃO alcançado**: o boot não chega a um shell `busybox` interativo — depois das 2 correções, o kernel descomprime de verdade e salta para código pós-MMU em endereço virtual (`0xc0...`), mas trava num loop de `PREFETCH_ABORT` recursivo ao redor da página de vetores altos (`0xffff0000`) cuja causa raiz não foi isolada nesta sessão (não é mais falta de decode conhecida). `VersatilePbBootTest` novo verifica o marco real alcançável hoje (mensagem `"Uncompressing Linux... done, booting the kernel."` nos dois backends, INTERPRETED e JIT — não inclui `--check`, que só valeria a pena com o boot completo) + testes de unidade novos por periférico (`Pl011UartTest`/`Sp804DualTimerTest`/`Pl190VicTest`/`AtagsBuilderTest`). `mvn -o test` verde em virtual-arm-box + arm-jitter raiz + gbaemu + ndsemu + armbox (G5 aplica-se às 2 correções, infraestrutura compartilhada). Pendência clara para uma sessão futura: isolar a causa do loop de abort na página de vetores (candidatos: conteúdo da página de vetores não populado ainda neste ponto do boot real vs. um bug na tradução/paginação da MMU para essa faixa de VA) — ou, alternativamente, obter um toolchain `arm-linux-gnueabihf-*`/ambiente WSL para compilar um `versatile_defconfig` mainline real com Device Tree e fechar o desvio arquitetural (ARM1176/ARMv6K da RFC) por completo. **SESSÃO 2 (2026-08-14) fechou o aceite**: o kernel boota até um shell `busybox` INTERATIVO nos dois backends e responde a comando digitado (`VersatilePbBootTest` reescrito: espera `/ #`, digita `echo LINUX"BOX-SHELL-OK"` byte a byte — o FIFO RX de 16 posições do PL011 descarta uma linha inteira empurrada de uma vez — e exige a saída SEM as aspas, para o eco do tty sozinho não passar no teste); ~18s de boot interpretado. **4 causas raiz, nenhuma na página de vetores** (ela era só o sintoma): (1) `Cp15VmsaCoprocessor` só atendia a TLB unificada `c8,c7,*`, mas ARM926EJ-S/ARMv5 tem TLBs de instrução e dados SEPARADAS e todo `flush_tlb_*` do kernel usa `c8,c5,*`/`c8,c6,*` → UNDEFINED antes de `early_trap_init` popular `0xffff0000`, vetor cheio de zeros (`andeq r0,r0,r0` = NOP), execução escorrega a página inteira e cai em `0xffff1000` não mapeada = o abort recursivo; corrigido com 4 operações novas por face em `TranslatingAddressSpace` (`invalidateInstruction/DataTlbAll`/`...ByMva`, só a de INSTRUÇÃO bumpa `translationGeneration` — mapeamento de dados não invalida bloco compilado, RFC §5) + `c8,c{5,6,7},{0,1,2}` no CP15 (inclui `TLBIASID`). (2) `TranslatingAddressSpace.setPrivileged` nunca era sincronizado com o modo do core (documentado como "fora de escopo" desde B4.1.2) → TODO acesso era privilegiado, uma escrita de USUÁRIO em página somente-leitura passava, e o *copy-on-write* do `fork()` do Linux nunca disparava: pai e filho compartilhavam fisicamente a pilha e se corrompiam (todo filho do shell morria com SIGSEGV). Corrigido com `ModeChangeListener` novo no `ArmCore` (padrão aditivo de `MemoryAbortListener`, G3), disparado em `setCpsr` e `switchMode`; `Cp15VmsaCoprocessor` o implementa e repassa `mode != USER`. (3) SÓ no backend JIT/tiered (que executa BLOCOS desde a 1ª visita, enquanto o não-tiered interpreta instrução a instrução até esquentar — bisecção com `hotThreshold` gigante provou que não era emissão ASM): `StandardIrBlockLifter` terminava o bloco quando a leitura ADIANTADA estourava o barramento (`IndexOutOfBoundsException`) mas deixava escapar `MemoryTranslationException` — o lift de um bloco que só ENCOSTA na página seguinte entregava um `PREFETCH_ABORT` no PC de INÍCIO do bloco, o kernel consertava o endereço errado e voltava para o mesmo lugar (`INFO: task kworker/0:0 blocked for more than 120 seconds`). Corrigido tratando as duas exceções igual (bloco não-vazio termina; bloco vazio propaga, aí a falta É da instrução que a CPU vai executar); JIT passou a bootar em ~12s, mais rápido que o interpretado. (4) Do lado do `virtual-arm-box`: o `busybox-armv5l` de `testdata/` é HARD-FLOAT (`EF_ARM_ABI_FLOAT_HARD` no `e_flags`) e usa `VPUSH {d8-d14}` real não gateado por `HWCAP_VFP` → SIGILL no PID 1; o hospedeiro passou a montar o core com `ARMV5TE + ArmFeature.VFPV2` + `VfpDecoder` = ARM926EJ-S com a VFP9-S opcional, exatamente o que o `-cpu arm926` do QEMU modela (sem preset novo no arm-jitter). Testes de regressão novos em `StandardIrBlockLifterTest` (leitura adiantada faltando termina o bloco; falta na 1ª instrução propaga), `Cp15VmsaCoprocessorTest` (faces de TLB independentes; privilégio sincronizado fazendo escrita de usuário em `AP=01` faltar com `SECTION_PERMISSION` sem tocar a memória) e `TranslatingAddressSpaceTest`. `Main` do virtual-arm-box ganhou drenagem não bloqueante de `stdin` para o UART0. Suítes verdes: arm-jitter 1327 core + 13 truffle, virtual-arm-box, gbaemu 244, ndsemu 183, armbox 41 (G5 aplica-se). **Desvios que permanecem** (falta de toolchain, não bug): kernel Debian 3.2 + ATAGs em vez de `versatile_defconfig` mainline + DTB, e ARM926EJ-S/ARMv5TE em vez do ARM1176/ARMv6K da RFC decisão 2 — mesmo bloqueio de B4.0.3/B6.2/B6.6.6. |
| [B5](trilha-b-arquiteturas/b5-3ds.md) | 3DS: lado arm-jitter (monitor exclusivo global B5.1 + preset MPCore B5.2) — refinada 2026-07-15 | B3.3–B3.6 (só B5.2; B5.1 já executável) | 🟡 **B5.1 ✅ 2026-07-16** — lógica extraída para `core/ExclusiveMonitor.java`, compartilhável via `ArmCore.setExclusiveMonitor` (default = monitor próprio, zero-diff); reserva é POR CORE dentro do monitor compartilhado (`IdentityHashMap<ArmCore,Reservation>`, não um único slot global — necessário para o cenário (c) da spec, reservas em endereços distintos não podem se derrubar); `STREX`/escrita comum de QUALQUER core que bata a região consome/abre a reserva, sucesso só para o dono (`ArmCore.exclusiveMonitorCovers`/`notifyOrdinaryWrite`); hook de escrita comum ligado em TODOS os pontos de store de baixo nível dos 2 backends (`IrExecutionSupport.write{8,16,32}Arm7` + crossed word/halfword do `UNALIGNED_ACCESS`, `AsmRuntimeHelpers.store{Byte,Half,Word}` + crossed) — não só o `IrOp.Store`/`STR` simples da spec, para cobrir também LDM/STM, LDRD/STRD e SWP sem lacuna. 4 testes novos (`ExclusiveMonitorSharedTest`, cenários a-d da spec) + suíte `ArmV6ExclusiveAccessTest`/`ArmV6ExclusiveNativeEquivalenceTest` (B1.4) intacta. `mvn -o test` raiz (614+13) + gbaemu (239) + ndsemu (175) verdes. B5.2 segue bloqueada por B3.3-B3.6 (VFP). — **B5.2 ✅ 2026-07-23** — preset `ArmArchitecture.ARM11_MPCORE` (`extending(ARMV6K, "ARM11-MPCore", VFPV2)` + `withDecoderExtensions(VfpDecoder, CoprocessorDecoder)`, mesmo padrão ovo-e-galinha de `ARMV7A_FEATURES`/`ARMV6K_THUMB2_FEATURES` para o construtor de `VfpDecoder` precisar da arquitetura já com `VFPV2`). Sem Thumb-2 (nenhuma chamada a `withThumb32DecoderExtensions`, herda a lista vazia de `ARMV6K` — `THUMB2` fica de fora do feature set, então `ThumbDecoder` mantém o par legado `LONG_BRANCH_PREFIX`/`LONG_BRANCH_SUFFIX` para `BL`/`BLX` em vez do fechamento de instrução única da B2.6). 5 testes novos em `ArmArchitectureTest` (features herdadas de ARMv6K + VFPV2; decodifica VADD.F32 via `VFP_ALU`; `BL`/`BLX` cai no par legado, não em `LONG_BRANCH_32`; CP15 não ganha extensão nova além de `VfpDecoder`+`CoprocessorDecoder` herdado). **Fecha o épico B5 por completo.** `mvn -o test` raiz + gbaemu + ndsemu verdes. |
| [B6](trilha-b-arquiteturas/b6-aarch64.md) | AArch64 — refinado: B6.1/B6.2 executáveis; B6.3 decomposta em B6.3.1-B6.3.4 (2026-07-24), TODAS ✅ (2026-07-26); B6.4 ganhou spec própria (2026-07-26, `b6.4-aarch64-asm-backend.md`, 3 PRs) — PR1/PR2/PR3 ✅, épico B6.4 FECHADO 2026-07-26; B6.5 decomposta em B6.5.1-B6.5.4 (2026-07-26, espelha B3.3-B3.6/VFP32); B6.6 decomposta em B6.6.1-B6.6.6 (2026-07-26, uma sub-task a mais que o precedente B4.1.1-B4.1.5 porque A64 não tem MCR/MRC — acesso a registrador de sistema via MRS/MSR é pré-requisito novo, B6.6.1) | B0 ✅ | 🔶 B6.1 ✅ (2026-07-24) — esqueleto IR-64/`Aarch64Core`/decoder/interpretador (fatia data-processing imediato + branches). Pacotes novos `ir64/` (`Ir64Op` selado: `Alu64`/`MoveWide`/`PcRelative`/`Branch64`/`CompareBranch64`/`Svc`/`Cycle`/`Fetch`, SEM `condition()` universal — só `Branch64` carrega condição de fato), `core64/` (`Aarch64Core` EL0-only, `long[31]` X0-X30 + SP + PC fora do banco, `PstateRegister` NZCV próprio, NÃO reusa `CpsrRegister`), `memory/AddressSpace64` (mesma forma de `AddressSpace` com `long`, + adapter `.wrapping(AddressSpace)`), `decoder64/Aarch64Decoder` (`ADR`/`ADRP`, `ADD`/`SUB` imediato, `MOVZ`/`MOVN`/`MOVK`, `B`/`BL`/`B.cond`, `CBZ`/`CBNZ`/`TBZ`/`TBNZ`, `BR`/`BLR`/`RET`, `SVC`; `logical imm`/bitfield/extract/loads-stores ficam fora, B6.3/B6.2), `executor64/Ir64BlockExecutor` (interpretador direto, sem cache/JIT). Oráculo de decode: corpus REAL montado com `aarch64-none-elf-as`/`objdump` do devkitA64 já instalado na máquina (`core/src/test/resources/aarch64/corpus.s`+`.bin`+`.objdump.txt`, versionados); 2 bugs de decode pegos pelo próprio corpus antes do merge (top-level class shift errado, array de registradores 1 curto). Testes: `Aarch64DecoderCorpusTest` (30 vetores campo a campo), `Ir64BlockExecutorTest` (XZR-como-destino, ADRP alinhando 4 KiB, MOVK compondo endereço 64 bits, CBZ W vs X, branches/flags), `Aarch64CoreTest`. Suíte completa do repo (`mvn test`) verde. B6.2 (loads/stores+ELF64+armbox) fica para task própria — **B6.2 🟡 PARCIAL (2026-07-24)**: loads/stores completos no arm-jitter (`Ir64Op.Load64`/`Store64`/`LoadStorePair`/`LoadLiteral64`, tamanhos B/H/W/X+sign-extend, unsigned-offset/`LDUR`/pre-index/post-index/registrador+extend UXTW·LSL·SXTW·SXTX, `LDP`/`STP` — o idioma de prólogo/epílogo — e `LDR (literal)`/`LDRSW (literal)`; decode do grupo `x1x0` (`ARM DDI 0487 C4.1.3`) verificado campo a campo contra corpus REAL montado com `aarch64-none-elf-as`/`objdump`, apêndice do mesmo `corpus.s`/`.bin`/`.objdump.txt` de B6.1, offsets `0x94`-`0x114`; `AddressSpace64` ganhou `read64`/`write64` default, composição little-endian de dois `read32`/`write32`) + armbox (`--arch=aarch64` no `Main`, pacote novo `aarch64/`: `Elf64Loader`/`Elf64Image` para ELF64 estático `EM_AARCH64`, `Aarch64GuestMemory` paginada em `Map<Long,byte[]>` — não um array como a de 32 bits, endereço de 64 bits não cabe —, `Aarch64LinuxAbi` com a tabela de syscall arm64 genérica [números DIFEREM do ARM32: `write`=64/`exit`=93/`exit_group`=94/`brk`=214/`mmap`=222], `Aarch64LinuxGuest` implementando `Aarch64SvcHandler` direto [~10 syscalls: write/read/close/brk/munmap/ioctl/set_tid_address/getpid/getuid.../uname/rt_sigaction/rt_sigprocmask], `Aarch64LinuxMachine` rodando só via `Ir64BlockExecutor` interpretado — SEM JIT/tiered para A64 ainda, isso é B6.4). **Aceite #1 fechado**: `hello-aarch64.s` escrito à mão (`svc` write+exit cruas, sem libc) montado com `aarch64-none-elf-gcc -nostdlib -static` (devkitA64, bare-metal — resolve o toolchain sem precisar de `aarch64-linux-*`/musl real, já que o binário não usa runtime nenhum do toolchain) produz ELF64 `ET_EXEC`/`EM_AARCH64` válido; `armbox --arch=aarch64 hello-aarch64.elf` imprime a mensagem e sai com código 42, idêntico ao esperado (`Aarch64HelloTest` novo, espelha `RealElfTest`). **Aceite #2 (busybox aarch64) NÃO fechado, mesmo bloqueio documentado em B4.0.3 item 3**: busybox.net não publica binário estático aarch64/arm64 (só `armv8l`, que é ARM 32-bit rodando em silício v8 — ISA errada) e o toolchain devkitA64 instalado é bare-metal (`aarch64-none-elf`, sem libc de userspace para compilar da fonte) — sem uma fonte confiável de busybox estático arm64 real ou um toolchain `aarch64-linux-*` (musl/glibc), fica pendente para sessão com esse ambiente disponível, igual ao item 3 de B4.0.3. Suítes arm-jitter (core+truffle) + armbox + gbaemu + ndsemu revalidadas verdes (G5), `mvn -o test`/`install` com JBR 25 |
| [B6.3.1](trilha-b-arquiteturas/b6.3.1-aarch64-logical-imm-alu-register.md) | AArch64: logical immediate (`DecodeBitMasks`) + ALU registrador (shifted/extended) — cria o dispatch novo de classe top-level "Data Processing — Register" | B6.2 🟡 | ✅ (2026-07-24/25) — `Logical (immediate)` (`AND`/`ORR`/`EOR`/`ANDS`) via `case 0b00` novo em `decodeDataProcessingImmediate` (`decodeLogicalImmediate`), reaproveitando `Ir64Op.Alu64`/`Ir64AluOp.AND` já existentes (D2: `ANDS` não é um opcode novo, é `AND` com `setFlags=true` — `logicalWithFlags` já força `C=0,V=0`, sem cálculo de carry/overflow); campo `dbm` (`N:immr:imms`) decodificado por `DecodeBitMasks` transcrito de `translate-a64.c:5224-5286` (`Aarch64LogicalImmediate.decodeBitMasks`, classe utilitária dedicada no `decoder64/`) — validado por **property test EXAUSTIVO** (`2×64×64=8192` combinações de `N,imms,immr`) contra uma SEGUNDA implementação independente por array de bits + rotação por indexação módulo (não a mesma fórmula de shift-e-OR), `Aarch64LogicalImmediateTest`; `N=1` com `sf=0` e `s==levels` tratados como `UnsupportedOperationException` (UNDEFINED de verdade, não "não implementado"). Dispatch novo `isDataProcessingRegisterClass`(bit27=1&&bit25=1)/`decodeDataProcessingRegister` (D1, mesmo padrão de `isLoadsAndStoresClass` já usado para resolver a ambiguidade de bits[28:26]) — só preenche `Add/subtract (shifted/extended register)` nesta task; `CSEL`/`2-source`/`3-source` continuam `unsupported`, prontos para B6.3.2/B6.3.3 adicionarem `case`s sem tocar no dispatch em si. Dois records de IR novos (D3, não uma extensão de `Alu64` — modos de operando com regras de `SP` genuinamente diferentes): `Ir64Op.AluShiftedRegister` (`ADD`/`SUB`/`ADDS`/`SUBS`, `Rd`/`Rn`/`Rm` nunca `SP`, enum novo `Ir64ShiftType` LSL/LSR/ASR — `ROR` reservado para `ADD`/`SUB`) e `Ir64Op.AluExtendedRegister` (`Rn` sempre `Rn|SP`, `Rd` `Rd|SP` só sem `S`, enum novo `Ir64AluExtendType` com as 8 combinações tamanho×sinal — NÃO reaproveita o `Ir64ExtendType` de 4 valores do load/store de B6.2, contrato documentado diferente). Executor novo (`executeAluShiftedRegister`/`executeAluExtendedRegister`) reaproveitando `addWithFlags`/`subWithFlags` já existentes. Corpus real estendido (apêndice do mesmo `corpus.s`/`.bin`/`.objdump.txt`, offsets `0x118`-`0x184`, `aarch64-none-elf-as`/`objdump` reais do devkitA64) cobrindo os casos de canto pedidos (elemento de 64 bits `N=1`, elemento de 2/4 bits, rotação não trivial, `LSL`/`LSR`/`ASR` com quantidade não-zero, as 8 extensões, `SP` como `Rd`/`Rn` na forma extended, `XZR` como destino em `ADDS`/`SUBS`). **Achado real reportado, NÃO corrigido (fora do "Inclui" desta task — ver pendência #7 do README)**: `Ir64BlockExecutor#executeAlu` (B6.1, forma imediata) resolve `Rd|SP`/`Rn|SP` checando só a flag booleira, nunca o índice do registrador (`==31`) como o pseudocódigo real do manual e `readBaseRegister`/`writeBaseRegister` (mesmo arquivo) exigem — confirmado com um teste descartável que `add x4,x5,#0x123` grava em `SP` em vez de `X4`; o código NOVO desta task (`AluExtendedRegister`) implementa a checagem CORRETA por índice, não copia o padrão do B6.1. `mvn -o test` verde (1022 testes, JBR 25); gbaemu/ndsemu **não revalidados** (aceite da própria task confirma que G5 não se aplica aqui — nenhum arquivo 32-bit tocado, mesmo precedente de B6.1/B6.2) |
| [B6.3.2](trilha-b-arquiteturas/b6.3.2-aarch64-csel-bitfield.md) | AArch64: `CSEL`/`CSINC`/`CSINV`/`CSNEG` (+ aliases) + `UBFM`/`SBFM`/`BFM` (+ aliases) | B6.3.1 ✅ | ✅ (2026-07-25) — `CSEL`/`CSINC`/`CSINV`/`CSNEG` via `case` novo em `decodeDataProcessingRegister` (fixo de 9 bits `011010100` em `[29:21]` + bit11=0, distinguido de `Add/subtract register` pelo grupo de 5 bits `[28:24]` diferente — D1 da task): record novo `Ir64Op.ConditionalSelect` (enum `Ir64ConditionalSelectOp`), `Rd`/`Rn`/`Rm` sempre normais (nunca `SP`), executor `executeConditionalSelect` só LÊ `PSTATE` via `evalCond` (nunca escreve NZCV) e resolve `CSEL`/`CSINC`/`CSINV`/`CSNEG` pelo caminho geral sem atalho para `CSET`/`CSETM`/`CINC`/`CINV`/`CNEG` (aliases confirmados no corpus real: mesmo opcode com `src1==src2` ou `==XZR` e condição já invertida pelo assembler). `SBFM`/`BFM`/`UBFM` via `case 0b10` novo em `decodeDataProcessingImmediate` (`decodeBitfield`, mesmas posições de bit de `Logical (immediate)`, checagem `N==sf` idêntica; `opc==0b11`/`EXTR` continua `unsupported`, fora de escopo): record novo `Ir64Op.Bitfield` (enum `Ir64BitfieldOp`) com `immr`/`imms` CRUS (decisão D2, sem pré-cálculo de `pos`/`len` no decoder — auditoria mais fácil contra o pseudocódigo do manual/QEMU). Executor `executeBitfield` com um único cálculo de `pos`/`len` (dois casos, `imms>=immr` e `imms<immr`) compartilhado pelos 3 opcodes — só a política de preenchimento fora do campo muda (`SBFM`=sinal, `UBFM`=zero, `BFM`=preserva `Rd`, testado explicitamente com `Rd` pré-populado). Os 11 aliases do épico (`UBFX`/`SBFX`/`BFI`/`BFXIL`/`LSL`/`LSR`/`ASR`/`UXTB`/`UXTH`/`SXTB`/`SXTH`/`SXTW`) e os 5 aliases de `CSEL` (`CSET`/`CSETM`/`CINC`/`CINV`/`CNEG`) cobertos de graça, sem nenhum `case` de decode dedicado — confirmado por corpus real (`aarch64-none-elf-as`/`objdump`, devkitA64), apêndice do mesmo `corpus.s`/`.bin`/`.objdump.txt`, offsets `0x188`-`0x1f8`, incluindo um vetor extra (`csneg x25,x26,xzr,eq`) para exercitar `CSNEG` com `src2=XZR` na execução. `mvn -o test` verde (1074 testes, JBR 25); gbaemu/ndsemu **não revalidados** (G5 não se aplica — nenhum arquivo 32-bit tocado, mesmo precedente de B6.1/B6.2/B6.3.1) |
| [B6.3.3](trilha-b-arquiteturas/b6.3.3-aarch64-mul-div.md) | AArch64: `MADD`/`MSUB` (+ aliases `MUL`/`MNEG`), `SDIV`/`UDIV` | B6.3.1 ✅ | ✅ (2026-07-25) — `MADD`/`MSUB` via `case` novo em `decodeDataProcessingRegister` (fixo `11011000` em `[28:21]`, `o0`(15) seleciona `MADD`/`MSUB`): record novo `Ir64Op.MultiplyAccumulate` (`subtract`/`dst`/`src1`/`src2`/`accumulator`/`wide`, nenhum campo `SP`/`setFlags` — nenhuma das duas famílias afeta NZCV nem aceita `SP`, `ARM DDI 0487 C6.2` confirmado). `MUL`/`MNEG` (aliases com `Ra=XZR`) cobertos de graça, SEM `case`/atalho dedicado no executor (D2 da task, mesmo raciocínio de `CSET`/`CSETM` em B6.3.2): o caminho geral já produz o resultado certo porque `XZR` lê `0`. Executor (`executeMultiplyAccumulate`) lê CADA operando-fonte via `core.xForWidth` explicitamente (D2 — nunca confia no invariante de zero-extensão do atalho de codegen do QEMU) e faz a multiplicação/soma em `long` puro (overflow silencioso de `long*long` de Java já é a truncagem módulo `2^64` exigida pela arquitetura). `SDIV`/`UDIV` via `case` novo (fixo `11010110` em `[28:21]` + opcode `00001` em `[15:11]`, bit10 seleciona `UDIV`(0)/`SDIV`(1)): record novo `Ir64Op.Divide` (`signed`/`dst`/`src1`/`src2`/`wide`). Executor (`executeDivide`) checa divisor `0` ANTES de dividir (retorna `0`, SEM exceção arquitetural — diferente do `/`/`%` de Java, que lançaria `ArithmeticException`); `UDIV` via `Long.divideUnsigned`/`Integer.divideUnsigned` (nunca `/` comum, que daria resultado errado com o bit mais alto setado); `SDIV` com overflow (`MIN_VALUE / -1`) deixado acontecer via `/` de Java, que já trunca sem lançar (mesma convenção de complemento-de-dois); `SDIV` em `W` faz sign-extend EXPLÍCITO dos operandos de 32 bits antes de dividir (`readDivideOperand`), `UDIV` em `W` já vem zero-estendido "de graça" via `xForWidth`. Corpus real estendido (apêndice do mesmo `corpus.s`/`.bin`/`.objdump.txt`, offsets `0x1fc`-`0x228`, `aarch64-none-elf-as`/`objdump` reais do devkitA64) cobrindo `MADD`/`MSUB`/`MUL`(alias)/`MNEG`(alias) em `W` e `X`, e `SDIV`/`UDIV` em `W` e `X`; testes de execução cobrindo divisor `0` sem exceção, `SDIV` com `MIN_VALUE / -1` (wide e narrow) truncando sem exceção, `UDIV` com operando de bit mais alto setado (confirma divisão sem sinal real), e zero-extensão do resultado de `MADD` em `W`. `SMADDL`/`SMSUBL`/`UMADDL`/`UMSUBL` (subgrupo vizinho "3-source" com `op0` diferente) confirmadas fora do escopo fechado do épico pela própria spec — NÃO implementadas. `mvn -o test` verde (1097 testes: 1074 anteriores + 23 novos, JBR 25); gbaemu/ndsemu **não revalidados** (G5 não se aplica — nenhum arquivo 32-bit tocado, mesmo precedente de B6.1/B6.2/B6.3.1/B6.3.2) |
| [B6.3.4](trilha-b-arquiteturas/b6.3.4-aarch64-exclusive-monitor.md) | AArch64: `LDXR`/`LDAXR`/`STXR`/`STLXR` + `Aarch64ExclusiveMonitor` (sibling do monitor de B1.4/B5.1) | B6.2 🟡 | ✅ (2026-07-26) — `Aarch64ExclusiveMonitor` novo em `core64/` (D1: sibling ESTRUTURAL de `core.ExclusiveMonitor`, não generalização — `IdentityHashMap<Aarch64Core, Reservation>`, mesmos nomes de método `markExclusive`/`consumeIfCovered`/`notifyOrdinaryWrite`/`clear`/`isArmed`/`address`/`sizeBytes`, SEM comparação de valor no consumo — mesma simplificação single-core do monitor de 32 bits). `Aarch64Core` ganha `exclusiveMonitor` próprio por padrão + `setExclusiveMonitor`/`markExclusiveMonitor`/`exclusiveMonitorCovers`/`clearExclusiveMonitor`/`notifyOrdinaryWrite`/`exclusiveMonitorAddress`/`exclusiveMonitorSizeBytes` (espelhando a API pública de `ArmCore`/B1.4/B5.1 1:1 nos nomes). Decode: `decodeExclusive` novo em `Aarch64Decoder` (`case SUBCLASS_EXCLUSIVE_ATOMIC` deixou de lançar `unsupported` incondicional) cobrindo as 4 mnemônicas — `LDXR`/`STXR` compartilham o MESMO encoding `@stxr` que `LDAXR`/`STLXR` (D0: só o bit `lasr` difere, decodificar metade dos valores não seria coerente); `sz` reaproveita a MESMA codificação/posição de bit de `Ir64MemSize`/`SINGLE_SIZE_*` já usada por `LDR`/`STR` (B6.2); `LDXP`/`STXP`/`CAS`/`LDAR`/`STLR` (mesmo subgrupo, valores diferentes do campo `form`) continuam `unsupported` — confirmado por teste de regressão negativo com os 6 valores de `form` fora de escopo. Dois records novos em `Ir64Op` (D2): `LoadExclusive`(`rt`/`rn`/`size`/`acquireRelease`) e `StoreExclusive`(`rs`/`rt`/`rn`/`size`/`acquireRelease`) — `acquireRelease` é NOP observável no interpretador (mesma convenção de `IrOp.MemoryBarrier` no IR de 32 bits), carregado só para um futuro emissor nativo. Executores novos em `Ir64BlockExecutor`: `executeLoadExclusive` (lê memória + marca o monitor) e `executeStoreExclusive` (consulta o monitor ANTES de qualquer escrita — mesma armadilha crítica de `STREX`/B1.4: falha não pode ter efeito colateral de memória). **Auditoria da Especificação #2 cumprida**: `executeStore`/`executeLoadStorePair` (B6.2) ganharam a chamada a `core.notifyOrdinaryWrite` que faltava (sem ela, o teste de escrita comum abrindo o monitor falha) — achado real da task, não incidental. Limpeza do monitor em entrada de exceção fica pendência EXPLÍCITA (B6.1 não tem modelo de exceção síncrona/assíncrona para A64 ainda). Corpus real estendido (apêndice do mesmo `corpus.s`/`.bin`/`.objdump.txt`, offsets `0x22c`-`0x268`, `aarch64-none-elf-as`/`objdump` reais do devkitA64) cobrindo as 4 mnemônicas × 4 tamanhos (B/H/W/X). Teste novo `Aarch64ExclusiveAccessTest` (executor64/) espelha `ArmV6ExclusiveAccessTest` (precedente B1.4): sucesso/falha do `STXR`/`STLXR`, tamanho incompatível, loop de incremento atômico convergindo (usando ADD shifted-register para contornar o bug pendente #7 do README, não corrigido aqui — fora do "Inclui"), `LDAXR`/`STLXR` idênticos a `LDXR`/`STXR`, e a integração `notifyOrdinaryWrite` com `STR` comum. `mvn -o test` verde (JBR 25); gbaemu/ndsemu **não revalidados** (G5 não se aplica — nenhum arquivo 32-bit tocado, mesmo precedente de B6.1/B6.2/B6.3.1-3; `core.ExclusiveMonitor` intocado). **B6.3 (Base ISA inteira restante) 100% FECHADA** com esta sub-task (B6.3.1-B6.3.4 todas ✅) — próximo passo natural do épico B6 maior é B6.4 (backend ASM 64-bit) ou B6.5/B6.6, mas isso fica para o usuário priorizar (nenhuma spec nova escrita/executada aqui). Aceite #2 do ÉPICO B6.3 completo ("busybox sh -c no armbox64") segue BLOQUEADO no usuário (mesmo bloqueio de B6.2 aceite #2, `tasks/FILA-EXECUCAO.md` seção 🧑) — `LDXR`/`STXR` são pré-requisito funcional real desse aceite (futex/spinlock de glibc/musl), mesmo sem poder validá-lo agora |
| [B6.4](trilha-b-arquiteturas/b6.4-aarch64-asm-backend.md) | AArch64: backend ASM 64-bit, dividido em 3 PRs (spec própria escrita 2026-07-26) | B6.3 ✅ | 🟡 **PR1 ✅ (2026-07-26)** — esqueleto: `ir64/Ir64Block`+`Ir64BlockLifter`/`StandardIr64BlockLifter` (novos, faltavam desde B6.1 — decoder A64 já devolve `Ir64Op` direto, então o lifter é mais simples que o 32-bit, sem estágio `DecodedInstruction`); `Ir64BlockExecutor` ganha `executeOp`/`executeBlock` (aditivos, delegam às `execute*` privadas já existentes — G1 por construção). **Decisão D0 registrada**: `JitRuntime`/`BlockCache`/`BlockKey` 32-bit NÃO são reusáveis (amarrados a `ArmCore`/`InstructionSet`/`itState`/`translationGeneration` da MMU 32-bit) — pacote-irmão `jit64/` novo (`CompiledBlock64`/`BlockKey64`/`BlockCache64`/`JitRuntime64`), deliberadamente SEM inline cache/encadeamento/tiering em background/invalidação por escrita (nenhum consumidor A64 real ainda que justifique replicar às cegas — pendência explícita). `ExecutionThreshold` (`jit/`, 32-bit) É reaproveitado direto (genérico o bastante). `codegen64/` novo: `Ir64CodeEmitter`, `InterpretedIr64CodeEmitter` (oráculo, delega a `executeBlock`), `Asm64CodeEmitter` (política `WHOLE_BLOCK`); `codegen64/jvm64/`: `Ir64NativePolicy` (só `Alu64`/`MoveWide`/`PcRelative`/`Branch64`/`CompareBranch64`/`Cycle`/`Fetch` nesta PR — `Svc`/loads-stores/B6.3.x ficam para PR2/PR3), `Aarch64GuestToHostMapper` (reaproveita `HostMethodBinding` genérico do pacote 32-bit), `Ir64AsmRuntimeHelpers`, `Ir64BlockCompiler` (bytecode ASM real via `ClassWriter`). **Decisão D-ASM registrada, honesta sobre o alcance**: o bytecode gerado RECONSTRÓI o record `Ir64Op` exato (campos constantes de compilação) e chama `Ir64AsmRuntimeHelpers.executeOp` — o MESMO despacho do interpretador (G1 por reuso direto, não reimplementação) — em vez de inlinar aritmética num cache de registrador como o `AsmBlockCompiler` 32-bit maduro (2493 linhas); **este PR1 NÃO entrega ganho de performance sobre o interpretado** para o conjunto coberto, só o pipeline+equivalência — o registrador-cache fica para uma PR futura, explicitamente fora do escopo desta sessão. `Jvm64BlockLoader` mora em `codegen/jvm/` (não `codegen64/jvm64/`, única exceção à convenção — reaproveita `JvmBlockClassLoader` package-private). `codegen/equivalence/`: `Aarch64CpuSnapshot`/`EquivalencePair64`/`EquivalencePairFactory64`/`BlockEquivalenceHarness64` novos (reaproveitam `EquivalenceMismatchException` genérico direto). Testes novos: `StandardIr64BlockLifterTest` (terminal em `Branch64`/`CompareBranch64`, corte em `maxInstructions`), `Ir64BlockExecutorBlockTest` (`executeBlock` produz o MESMO estado que `step()` chamado N vezes), `Ir64NativePolicyTest`, `BlockEquivalenceHarness64Test` (aceite do PR1: `InterpretedIr64CodeEmitter`×`Asm64CodeEmitter` idênticos para ADD/SUB/AND/ORR/EOR com/sem flags, `SP` como destino, MOVZ/MOVN/MOVK, ADR/ADRP, B incondicional/condicional tomado/não-tomado, BR/BLR/RET, CBZ/CBNZ/TBZ/TBNZ tomado/não-tomado, bloco reto multi-instrução terminando em branch), `JitRuntime64Test` (frio até o threshold, compila e cacheia depois, cache reusado). `mvn -o test` verde (JBR 25, 1182 core + 13 truffle — 25 testes novos); gbaemu/ndsemu **não revalidados** (G5 não se aplica — nenhum arquivo 32-bit tocado, `Ir64BlockExecutor` só ganhou métodos aditivos, mesmo precedente de B6.1-B6.3.4). **PR2 ✅ (2026-07-26)** — estende `Ir64NativePolicy.supports` com `LOAD64`/`STORE64`/`LOAD_STORE_PAIR`/`LOAD_LITERAL64`/`SVC`; `Ir64BlockCompiler` ganha `constructLoad64`/`constructStore64`/`constructLoadStorePair`/`constructLoadLiteral64`/`constructSvc` (mesmo padrão D-ASM do PR1: reconstrói o record `Ir64Op` exato e delega a `Ir64AsmRuntimeHelpers.executeOp`). **Achado que simplificou o PR2**: como o acesso à memória e ao `Aarch64SvcHandler` já acontece inteiramente DENTRO de `Ir64BlockExecutor#execute` (mesmo caminho para os dois backends), nenhum binding novo em `Aarch64GuestToHostMapper` foi necessário — PR2 não emite nenhuma instrução de memória, só reconstrói o record e despacha, ao contrário do que a spec cogitava ("D4... loads/stores de PR2 adicionam o resto"). Único detalhe novo de bytecode: `Ir64Op.Load64`/`Store64.extendType()` é `null` fora de `REGISTER_OFFSET` — `emitEnumConstantOrNull` novo trata isso (`ACONST_NULL` vs `GETSTATIC` do enum). Testes novos: `Ir64NativePolicyTest.supportsPr2OpSet`/`rejectsOpsOutsidePr2Scope`; `BlockEquivalenceHarness64Test` ganha `listOfPr2KindsAllNativelySupported`, `storeThenLoadOffsetRoundTrip`, `loadSignedByteSignExtends` (LDRSB sign-extend), `storePreAndPostIndexWriteback` (writeback de `Rn|SP` idêntico nos dois backends), `loadStoreRegisterOffsetAddressing` (a única forma que carrega `rm`/`extendType`, prova o `emitEnumConstantOrNull`), `loadStorePairRoundTrip` (STP/LDP 64-bit), `loadLiteralReadsAbsoluteAddress`, `svcDispatchesToInstalledHandler` (mesmo `Aarch64SvcHandler` instalado nos dois cores do par, prova que o dispatch reconstruído chega ao mesmo handler). `mvn -o test` verde (JBR 25, core 1188 + truffle 13). gbaemu/ndsemu **não revalidados** (G5 não se aplica — nenhum arquivo 32-bit tocado, mesmo precedente do PR1). **PR3 ✅ (2026-07-26)** — fecha o conjunto B6.3.1-B6.3.4 nativamente: `Ir64NativePolicy.supports` ganha `ALU_SHIFTED_REGISTER`/`ALU_EXTENDED_REGISTER`/`CONDITIONAL_SELECT`/`BITFIELD`/`MULTIPLY_ACCUMULATE`/`DIVIDE`/`LOAD_EXCLUSIVE`/`STORE_EXCLUSIVE` — com isso TODO `Ir64Op.Kind` existente é suportado nativamente, nenhum bloco A64 cai mais no interpretado por falta de `case`; `Ir64BlockCompiler` ganha `constructAluShiftedRegister`/`constructAluExtendedRegister`/`constructConditionalSelect`/`constructBitfield`/`constructMultiplyAccumulate`/`constructDivide`/`constructLoadExclusive`/`constructStoreExclusive` (mesmo padrão D-ASM dos PRs 1/2: reconstrói o record `Ir64Op` exato e delega a `Ir64AsmRuntimeHelpers.executeOp` — nenhuma aritmética nova em bytecode bruto, `Ir64BlockExecutor#execute` já tinha os `case`s desde B6.3.1-B6.3.4, então nenhum arquivo de execução precisou mudar). Testes novos: `Ir64NativePolicyTest.supportsPr3OpSet`; `BlockEquivalenceHarness64Test` ganha `listOfPr3KindsAllNativelySupported` + 7 testes de equivalência (`aluShiftedRegisterAddWithFlagsAndLsl`, `aluExtendedRegisterReadsAndWritesStackPointer` — cobre a resolução de `SP` POR ÍNDICE, `conditionalSelectAllFourOpcodes` — CSEL/CSINC/CSINV/CSNEG no mesmo bloco, `bitfieldUbfmSbfmBfm`, `multiplyAccumulateMaddAndMsub`, `divideSignedUnsignedAndByZero` — incl. divisor `0`, `loadExclusiveThenStoreExclusiveSucceeds` + `storeExclusiveWithoutReservationFails` — prova que os dois backends concordam nos caminhos de sucesso E falha do monitor de exclusividade). `mvn -o test` verde (JBR 25, core 1197 + truffle 13); `mvn -o install` verde (os 2 módulos). gbaemu/ndsemu **não revalidados** (G5 não se aplica — nenhum arquivo 32-bit tocado, mesmo precedente dos PRs 1/2). **Registrador-cache de verdade e bench busybox-aarch64 (aceite agregado do épico, "busybox ≥3× interpretador") CONTINUAM PENDENTES** — o registrador-cache é trabalho de otimização sem consumidor A64 real medido ainda (mesma disciplina D0/D-ASM do PR1: não replicar às cegas), e o bench segue bloqueado no mesmo ambiente de `tasks/FILA-EXECUCAO.md` (busybox aarch64 real ou toolchain `aarch64-linux-*`) — ver a seção 🧑 lá. |
| [B6.5.1](trilha-b-arquiteturas/b6.5.1-aarch64-fp-register-bank.md) | AArch64: banco de registradores FP escalar (`Aarch64FpRegisters`, V0-V31 só bits 63:0, sem FPCR/FPSR) — 1ª das 4 sub-tasks de B6.5 (rodada de spec 2026-07-26) | B6.1 ✅ | ⬜ |
| [B6.5.2](trilha-b-arquiteturas/b6.5.2-aarch64-fp-ir-interpretador.md) | AArch64: `Ir64Op`s de FP (`Fp64Alu`/`Fp64MoveImmediate`/`Fp64Compare`/`Fp64Convert`) + executor interpretado | B6.5.1 ✅ | ✅ (2026-07-27) — 4 records novos em `Ir64Op` (`Fp64Alu`/`Fp64MoveImmediate`/`Fp64Compare`/`Fp64Convert`, enums `Fp64Operation`/`Fp64Conversion`), sem campo `Condition` (D1 — mesma disciplina de todo `Ir64Op` desde B6.1). **`Kind` renumerado**: a spec previa `20`-`23` contíguos, mas B6.6.1-B6.6.4 (intermediárias) já tinham reivindicado `20`-`22` — usados `23`-`26` (`FP64_ALU`/`FP64_MOVE_IMMEDIATE`/`FP64_COMPARE`/`FP64_CONVERT`), documentado no javadoc do `Kind`. `executor64/Ir64FpExecutor.java` novo (package-private, métodos estáticos — sem estado próprio, diferente de `IrVfpExecutor`/32-bit, já que nenhuma operação desta fatia toca memória, D3: sem `Fp64Load`/`Fp64Store`/`Fp64MultipleTransfer`): `Fp64Alu` via `core.fp().sFloat/dDouble` (Java puro, round-to-nearest fixo); `NEG`/`ABS` via manipulação do BIT DE SINAL (nunca `-x`/`Math.abs`, mesma armadilha de `IrVfpExecutor`); `MOV` (registrador↔registrador) é cópia de bits crus. `Fp64Compare` escreve `PSTATE.NZCV` DIRETO (Fatos de referência #1 da task — diferente do VFP32, que precisa de um segundo passo `VMRS APSR_nzcv`); `signalOnQuietNaN` carregado sem efeito observável, **auditado contra `IrVfpExecutor#executeVfpCompare`** (que também não o consulta — confirma a hipótese "CONFERIR" da task, nenhum core desta família modela traps de FP). `Fp64Convert` via cast direto do Java (`(double)`/`(float)`), correto por IEEE 754 nos dois sentidos. `Ir64BlockExecutor.execute` ganha os 4 `case`s novos delegando a `Ir64FpExecutor`. Nenhum pass de otimização A64 existe ainda (confirmado por busca) — item 3 da Especificação (documentar para não tocar os `Kind`s novos sem tratamento explícito) só precisou de javadoc, sem criar pass. Testes novos (`Ir64FpExecutorTest`, 20 testes): ADD/SUB/MUL/DIV single+double, NEG/ABS preservando payload de NaN e distinguindo ±0.0 (single e double), MOV registrador↔registrador com payload de NaN (single e double), MOV imediato (single e double, bits já resolvidos), os 4 quadrantes de `FCMP` (eq/lt/gt/unordered) + `compareWithZero`, `FCVT` F32↔F64 incl. preservação de "quietness" de NaN, `Kind`s contíguos `23`-`26`, e confirmação de que nenhuma op de FP altera o PC. `mvn -o test` verde (JBR 25, core 1274 = 1254 + 20 novos; truffle 13). G5 não se aplica (nenhum arquivo 32-bit tocado, mesmo precedente de B6.1-B6.6.5). **Não inclui** (fora do "Inclui" da task, ver Armadilhas): `Fp64Load`/`Fp64Store`/`Fp64MultipleTransfer` (D3), `SQRT`/`MLA`/`MLS`/`NMUL`, conversão inteiro↔float (`SCVTF`/`UCVTF`/`FCVTZS`/`FCVTZU`), `FMOV Rt,Sn` core↔FP — gaps reais que podem bloquear o aceite agregado do épico B6.5 ("musl printf de float") mesmo com B6.5.1-B6.5.4 completas, registrados na task. Próximo passo: B6.5.3 (decoder) fica elegível |
| [B6.5.3](trilha-b-arquiteturas/b6.5.3-aarch64-fp-decoder.md) | AArch64: decoder da classe "Data Processing — Scalar FP" (`bit26=1`, ramo novo em `decodeDataProcessingRegister`) | B6.5.2 | ✅ |
| [B6.5.4](trilha-b-arquiteturas/b6.5.4-aarch64-fp-asm-nativo.md) | AArch64: emissão ASM nativa de FP (extensão de `Ir64NativePolicy`/`Ir64BlockCompiler`) | B6.5.3, B6.4 ✅ | ✅ (2026-07-27) — **fecha o épico B6.5 por completo**. `Ir64NativePolicy.supports` ganha os 4 `case`s novos (`FP64_ALU`/`FP64_MOVE_IMMEDIATE`/`FP64_COMPARE`/`FP64_CONVERT` → `true`); `Ir64BlockCompiler` ganha `constructFp64Alu`/`constructFp64MoveImmediate`/`constructFp64Compare`/`constructFp64Convert`, mesmo padrão mecânico D-ASM dos `construct*` de B6.4 (reconstrói o record `Ir64Op` exato, campo a campo, delega a `Ir64AsmRuntimeHelpers.executeOp` — o MESMO despacho do interpretador, nenhuma aritmética `FADD`/`DADD` inlinada, D1 da spec: consistência com TODO o resto do `Ir64BlockCompiler`, que também não tem register-cache). D2 confirmada na implementação: `Aarch64GuestToHostMapper` NÃO precisou de nenhum binding novo — `Ir64AsmRuntimeHelpers.executeOp` já tem acesso ao `Aarch64Core` inteiro (inclui `fp()`) por dentro do helper compartilhado, YAGNI evitado como a spec previu. Testes novos: `Ir64NativePolicyTest.supportsB654FpOpSet` + 7 testes novos em `BlockEquivalenceHarness64Test` (ADD/SUB/MUL/DIV single+double, NEG/ABS/MOV preservando payload de NaN, `FMOV` imediato nos 4 vetores canônicos single+double, `FCMP`/`FCMPE` nos 4 quadrantes NZCV incl. `compareWithZero`, `FCVT` nas 2 direções incl. perda de precisão, property test de 50 rodadas com seed fixa cobrindo as 4 operações × single/double) — todos provam `InterpretedIr64CodeEmitter`×`Asm64CodeEmitter` produzindo o MESMO `Aarch64CpuSnapshot` (banco `V` completo, B6.5.1). `mvn -o test` verde (JBR 25, core 1322 = 1314 + 8 novos; truffle 13); `mvn -o install` verde. G5 não se aplica (nenhum arquivo 32-bit tocado, mesmo precedente de todas as sub-tasks A64). **Sem meta de performance** (D1, disciplina explícita herdada de B6.4) — este PR não muda o número do bench "busybox ≥3× interpretador", que segue bloqueado no usuário (toolchain/binário aarch64, mesma pendência 🧑 de B6.2/B6.4). **Fecha B6.5 com o gap de `SCVTF`/`UCVTF`/loads-stores FP ainda aberto** (herdado de B6.5.2) — o aceite agregado do épico ("musl printf de float") pode continuar bloqueado mesmo com as 4 sub-tasks fechadas |
| [B6.6.1](trilha-b-arquiteturas/b6.6.1-aarch64-system-register-access.md) | AArch64: acesso a registrador de sistema (`MRS`/`MSR (register)`) — pré-requisito da MMU, sem equivalente 32-bit (A64 não tem MCR/MRC) — 1ª das 6 sub-tasks de B6.6 (rodada de spec 2026-07-26) | B6.1 ✅ | ✅ (2026-07-27) — `Aarch64SystemRegisterId` novo em `ir64/` (9 registradores EL1 "gerais": `SCTLR_EL1`/`TTBR0_EL1`/`TCR_EL1`/`MAIR_EL1`/`ESR_EL1`/`FAR_EL1`/`VBAR_EL1`/`ELR_EL1`/`SPSR_EL1`, `FPCR`/`FPSR` deliberadamente fora — D3) + `Ir64Op.SystemRegister` (D1: `read`/`register`/`rt`, `Kind.SYSTEM_REGISTER=20`; sem forma `W` — confirmado que o bit alto da instrução é parte do prefixo fixo do encoding, não um `sf`). Decoder: encoding `SYS`/`MRS`/`MSR` verificado campo a campo contra `aarch64-none-elf-as`/`objdump` (devkitA64) ANTES de codificar (mesma disciplina de pin de B6.3.x) — prefixo fixo 10 bits em bits[31:22]=`1101010100`, `L`(21), `op0`(20:19, só `!=0` coberto — `op0=0` é hint/barreira/MSR-imediato, `op0=1` é `SYS`/`SYSL` de manutenção, ambos fora de escopo), `op1`(18:16)/`CRn`(15:12)/`CRm`(11:8)/`op2`(7:5)/`Rt`(4:0); resolução `op0:op1:CRn:CRm:op2`→`Aarch64SystemRegisterId` feita NO DECODER (D1), combinação fora da lista devolve `null`→`UnsupportedOperationException` documentada como "não implementada ainda", não UNDEFINED real. `Aarch64SystemRegisterBus` novo em `core64/` (D2, sibling estrutural de `CoprocessorBus` 32-bit — não herança) + `Aarch64Core.systemRegisterBus`/`setSystemRegisterBus` (default `none()`, `NoSystemRegisters` sibling de `NoCoprocessor`). Executor novo `executeSystemRegister`: checa `handles()` ANTES de `read`/`write` (registrador sem hospedeiro lança `UnsupportedOperationException` do próprio executor — `Aarch64Core` ainda não tem modelo de exceção síncrona para entregar Undefined ao guest como o mundo 32-bit faz). Corpus real estendido (apêndice do mesmo `corpus.s`/`.bin`/`.objdump.txt`, offsets `0x26c`-`0x280`, `aarch64-none-elf-as`/`objdump` reais cobrindo `MRS`/`MSR` de `SCTLR_EL1`/`TTBR0_EL1`/`VBAR_EL1`). Testes: `Aarch64DecoderCorpusTest` (6 vetores do corpus + combinação fora da lista + `op0=0`/`op0=1` fora de escopo) e `Aarch64SystemRegisterTest` novo (executor64/, bus falso só para teste — sem bus/registrador não atendido/ida-e-volta `MSR`→`MRS`/`XZR` como destino descarta escrita/`XZR` como origem escreve `0`). `mvn -o test` verde (JBR 25, core 1221 + truffle 13); `mvn -o install` verde. gbaemu/ndsemu **não revalidados** (G5 não se aplica — nenhum arquivo 32-bit tocado, mesmo precedente de B6.1-B6.5.1). Mecanismo pronto para B6.6.3 instalar um `Aarch64SystemRegisterBus` real de MMU sem tocar decoder/IR/executor de novo |
| [B6.6.2](trilha-b-arquiteturas/b6.6.2-aarch64-translating-address-space.md) | AArch64: `TranslatingAddressSpace64` — page-walk VMSA64 (4KiB granule, VA 48 bits, 4 níveis L0-L3) + micro-TLB, independente de B6.6.1 | B6.1 ✅ | ✅ (2026-07-27) — `memory/mmu/TranslatingAddressSpace64.java` novo, sibling estrutural de `TranslatingAddressSpace` (32-bit): page-walk L0→L1→L2→L3 (bit0=válido, bit1=bloco(0, só L1/L2)/tabela-ou-página(1); bloco em L0 tratado como falha reservada), permissão `AP[2:1]` (bit baixo=EL0 permitido, bit alto=somente-leitura — achado real de projeto: em VMSA64 o bit de somente-leitura é UNIVERSAL, afeta EL1 e EL0 igual, diferente do ARMv6 `AP=0b10` que dava RW privilegiado + RO usuário) + `PXN`/`UXN` (execute-never privilegiado/não-privilegiado, sem equivalente 32-bit), micro-TLB 256 entradas (mesma estrutura do precedente, chave VA de 64 bits, sem domínios — VMSA64 não tem), `translationGeneration` (incrementa em `setTtbr0`/`invalidateTlbAll`, não em `setTcr`/`setMair` — D5; ASID de 16 bits embutido em `TTBR0_EL1[63:48]`, não um `setAsid` separado). `FaultStatus64`/`MemoryTranslationException64` novos (sibling de `FaultStatus`/`MemoryTranslationException`, mas `ESR_EL1.ISS` `DFSC`/`IFSC` já embute o NÍVEL do walk — `TRANSLATION_FAULT_L0..L3`, `PERMISSION_FAULT_L1..L3`, valores não conferidos contra o manual nesta rodada, ver Armadilhas). `AddressSpace64.translationGeneration()` ganhou default `0` (D4, aditivo, zero mudança para armbox `Aarch64LinuxMachine`). `TranslatingAddressSpace64Test` novo (13 testes: identidade, remapeamento, bloco L1 1GiB, bloco L2 2MiB, página L3 4KiB, permissão `AP` EL0-negado, `PXN`/`UXN` bloqueando fetch, TLB hit não refaz walk, `invalidateTlbAll`/`invalidateTlbByMva`, falta de tradução nos 4 níveis, `translationGeneration`). `mvn -o test` verde (JBR 25). G5 não se aplica — pacote novo aditivo, nenhum arquivo 32-bit tocado (mesmo precedente de B6.1-B6.6.1). Pronto para B6.6.3 instalar como `AddressSpace64` real de um hospedeiro |
| [B6.6.3](trilha-b-arquiteturas/b6.6.3-aarch64-system-register-mmu-bridge.md) | AArch64: `Aarch64VmsaSystemRegisters` ligando B6.6.1↔B6.6.2 + decode mínimo de `TLBI VMALLE1` | B6.6.1, B6.6.2 | ✅ (2026-07-27) — `memory/mmu/Aarch64VmsaSystemRegisters.java` novo (D2, espelho de `Cp15VmsaCoprocessor`, composição não herança): liga `SCTLR_EL1` (só bit `M`, sem bit `V` — A64 não tem vetores altos), `TTBR0_EL1`/`TCR_EL1`/`MAIR_EL1` a `TranslatingAddressSpace64` (B6.6.2); `ESR_EL1`/`FAR_EL1`/`VBAR_EL1`/`ELR_EL1`/`SPSR_EL1` só armazenamento (B6.6.4 os consome de verdade). Construtor desliga a MMU (mesmo reset real de hardware do precedente). **Achado real D1 confirmado nesta rodada** (`aarch64-none-elf-as`/`objdump` reais, devkitA64): `TLBI VMALLE1`/`VMALLE1IS` são `SYS` (`op0=1`), NÃO `MRS`/`MSR` (`op0∈{2,3}`) — mesmo prefixo de 10 bits/L/op1/CRn/CRm/op2/Rt de B6.6.1, só o campo `op0` muda de subgrupo; `DSB`/`ISB`/`DMB` (`op0=0`, CRn=`0b0011`, op2∈{4,5,6}) também decodificadas, NOP observável (mesmo precedente 32-bit de `IrOp.MemoryBarrier`). `Ir64Op.SystemInstruction` novo (`Kind.SYSTEM_INSTRUCTION=21`) + `Ir64SystemInstructionOp{TLBI_ALL,BARRIER}` — `VMALLE1`/`VMALLE1IS` colapsam no mesmo valor (sem múltiplos cores modelados, `IS` não tem efeito extra). `Aarch64SystemRegisterBus` ganhou `invalidateTlbAll()` default NOP (mesmo barramento único que o hospedeiro já instala em `Aarch64Core`, já que `TLBI` não é uma "leitura/escrita de registrador" mas ainda é uma ação de nível de sistema); executor novo `executeSystemInstruction` despacha `TLBI_ALL`→`bus.invalidateTlbAll()` (sem checar `handles()` — default NOP já cobre "sem hospedeiro") e `BARRIER`→NOP puro. Decoder: `decodeBranchExceptionSystem` reestruturado para checar o prefixo comum PRIMEIRO e só then ramificar em `op0` (barreira/`SYS`/`MRS`-`MSR`), 2 métodos novos (`decodeSystemInstructionBarrier`/`decodeSystemInstructionSys`); `TLBI` per-VA (`VAE1`) e `SYSL` continuam `unsupported` (regressão negativa testada com encodings reais). Corpus real estendido (apêndice do mesmo `corpus.s`/`.bin`/`.objdump.txt`, offsets `0x284`-`0x294`). Testes: `Aarch64DecoderCorpusTest` (+7: 5 vetores do corpus + `TLBI VAE1`/`SYSL` fora de escopo) e `Aarch64VmsaSystemRegistersTest` novo (`memory/mmu/`, 7 testes: `TTBR0`/`TCR`/`MAIR` ida-e-volta, `SCTLR.M` liga/desliga MMU, `ESR`/`FAR`/`VBAR`/`ELR`/`SPSR` armazenamento, `TLBI` forçando novo walk via `pageWalkCount()`, `TLBI` sem bus instalado não lança, barreira NOP não mexe no PC, e o ACEITE explícito: sequência real `TTBR0_EL1→TCR_EL1→SCTLR_EL1.M=1` via `MOVZ`+`MSR` reais rodando em `Ir64BlockExecutor.step()` liga a MMU e um acesso subsequente percorre a tabela de páginas de verdade). `mvn -o test` verde (JBR 25, core 1249 + truffle 13); `mvn -o install` verde. gbaemu/ndsemu **não revalidados** (G5 não se aplica — nenhum arquivo 32-bit tocado, mesmo precedente de B6.1-B6.6.2). Épico B6.6 progride: falta B6.6.4 (aborts/exceção EL0→EL1) |
| [B6.6.4](trilha-b-arquiteturas/b6.6.4-aarch64-precise-aborts-el1.md) | AArch64: modelo mínimo de exceção EL0→EL1 (`Aarch64ExceptionState`) + `ERET` + aborts precisos no interpretador | B6.6.3 | ✅ (2026-07-27) — `core64/Aarch64ExceptionState.java` novo (D1: `sp1`/`elr1`/`spsr1`/`esr1`/`far1`/`vbar1`/`inEl1`), fiado como ÚNICA fonte de verdade em `Aarch64Core` (`exceptionState()`), inclusive redirecionando `Aarch64VmsaSystemRegisters` (B6.6.3) a delegar `ESR_EL1`/`FAR_EL1`/`VBAR_EL1`/`ELR_EL1`/`SPSR_EL1` para lá em vez de campos próprios (evita duas cópias divergindo quando o guest lê via `MRS` depois de um abort real — `core`, antes só "reservado por simetria", virou consumidor real). `sp()`/`setSp()` resolvem `SP_EL0`/`SP_EL1` automaticamente por `inEl1` (zero mudança nos chamadores existentes). `Aarch64Core.enterMemoryAbort` (D2, espelho de `ArmCore.enterMemoryAbort`): preenche `ESR_EL1` (`EC=0x24` data/`0x20` instrução, CONFERIDO contra `ARM DDI 0487 D17.2.30` — a task citava `0x21`, que é "sem troca de EL", incorreto para o caso EL0→EL1 real) + `IL=1` + `ISS[5:0]`=`FaultStatus64.code()`, `FAR_EL1`, `ELR_EL1←instructionAddress`, `SPSR_EL1←PstateRegister.toSpsrFormat()` (novo par `toSpsrFormat`/`setFromSpsrFormat`, NZCV em `[31:28]`, mesma posição do CPSR ARM32), abre o monitor de exclusividade (fecha a pendência de B6.3.4) e salta para `VBAR_EL1 + 0x400` (única entrada usada da tabela de 16×0x80 bytes do `ARM DDI 0487 D1.10` — "Synchronous, lower EL, AArch64"; as outras 15 não têm consumidor nesta task). `ERET` decodificado (`Ir64Op.ExceptionReturn` novo, record dedicado — não reaproveita `SystemInstruction`, semântica muda PC/PSTATE diferente de TLBI/barreira; encoding `opc=0b0100` no mesmo formato fixo de `BR`/`BLR`/`RET`, CONFERIDO via `aarch64-none-elf-as`/`objdump`, devkitA64: `eret`→`0xD69F03E0`) executado por `Ir64BlockExecutor`. Captura de `MemoryTranslationException64` em `Ir64BlockExecutor.step`/`executeBlock` (D3 — só interpretador; ASM/`jit64` fica pendência explícita). Testes novos (`Aarch64MemoryAbortTest`, 3 casos): data abort real via `TranslatingAddressSpace64` (não decorator sintético) → EL1 → `ERET` → continuação bem-sucedida; instruction abort com `EC` distinto do data abort; `SP_EL1` separado de `SP_EL0` através de entrada/saída de exceção. `mvn -o test` verde (1265 testes, 0 falhas) no reactor completo. G5 não se aplica (`ArmCore`/32-bit não referencia `core64` — zero impacto em gbaemu/ndsemu, confirmado por grep). |
| [B6.6.5](trilha-b-arquiteturas/b6.6.5-aarch64-translation-generation.md) | AArch64: `translationGeneration` em `jit64/BlockKey64`/`JitRuntime64` | B6.6.4, B6.4 ✅ | ✅ (2026-07-27) — espelho direto de B4.1.4 (32-bit), mas menor: `jit64/` não tem inline cache nem encadeamento (B6.4 D0), então só o ponto de lookup/lift de `JitRuntime64.execute` existe hoje (D2/D3). `BlockKey64` ganhou 2º campo `int translationGeneration` (D1) com construtor de compatibilidade `BlockKey64(long pc)` preservando `translationGeneration=0` (todo chamador existente de B6.4 continua compilando). `JitRuntime64.execute` passou a ler `core.memory().translationGeneration()` uma vez por chamada e usar no `BlockKey64` do lookup/lift — troca de `TTBR0_EL1` vira MISS natural no `BlockCache64`. Javadoc de pendência (D3) registrado em ambos os arquivos: se uma PR futura adicionar IC/encadeamento a `jit64/`, precisa lembrar de checar `translationGeneration` nesses pontos novos também. `JitRuntime64TranslationGenerationTest` novo (espelho de `JitRuntimeTranslationGenerationTest`): duas tabelas de página L0-L3 completas (4 níveis reais) mapeando o mesmo VA para código físico diferente, troca via `TranslatingAddressSpace64.setTtbr0`+`invalidateTlbAll`, cada troca executa o `MOVZ` certo, nunca o bloco stale da geração anterior; + teste do construtor de compatibilidade. `mvn -o test` verde (core 1254 + truffle 13); `mvn -o install` verde. G5 não se aplica — `BlockKey64`/`JitRuntime64`/`AddressSpace64` são exclusivos do mundo A64, confirmado por grep (nenhum arquivo 32-bit referencia essas classes). Épico B6.6 quase fechado: só falta B6.6.6 (bloqueada no usuário) |
| [B6.6.6](trilha-b-arquiteturas/b6.6.6-aarch64-virt64-host.md) | AArch64: hospedeiro `virt64` (repo novo) — kernel arm64 mínimo até shell | B6.6.5 | 🧑 bloqueada no usuário (toolchain/kernel real, mesmo bloqueio de B6.2/B4.1.5) — EM ESPERA (não cancelada), converge com o mesmo bloqueio de fundo da F11, agora atacado por B6.6.7 |
| [B6.6.7](trilha-b-arquiteturas/b6.6.7-aarch64-el1-kernel-surface.md) | AArch64: superfície mínima de EL1 para kernel real (system regs restantes + `WFI`/`WFE`/`HVC`/`SMC` + IRQ) | B6.6.4 | ✅ (2026-08-18) — priorizada pelo usuário em resposta direta ao bloqueio achado pela F11. `Aarch64SystemRegisterId` estendido: `CurrentEL`/`MPIDR_EL1`/`MIDR_EL1`/`ID_AA64PFR0_EL1`/`ID_AA64ISAR0_EL1`/`ID_AA64MMFR0_EL1`/`ID_AA64DFR0_EL1`/`TPIDR_EL1` resolvidos INTRINSECAMENTE por `Aarch64Core` (achado real: antes desta task, `MRS` sem `Aarch64SystemRegisterBus` instalado lançava até para `CurrentEL`, que um kernel lê incondicionalmente cedo demais para qualquer host já estar configurado); timer genérico (`CNTFRQ_EL0`/`CNTPCT_EL0`/`CNTP_TVAL_EL0`/`CNTP_CTL_EL0`/`CNTP_CVAL_EL0`, `op0=3,op1=3` — tabela DIFERENTE da EL1 "geral" `op1=0`) continua host-pluggable via o bus existente. `WFI` decodificado com semântica própria (`Aarch64Core` reaproveita `CpuSleepState` direto, mesma disciplina "genérico o bastante" de `ExecutionThreshold`); `WFE`/`NOP`/`YIELD`/`SEV`/`SEVL` viram NOP puro (sem event-stream modelado); `HVC`/`SMC` decodificadas como `Ir64Op.PrivilegedCall` novo, devolvem `PSCI_RET_NOT_SUPPORTED` em `X0` sem entrar em EL1 (sem EL2/EL3 modelados, GIC/PSCI ficam por conta do hospedeiro consumidor). `ERET` já existia (B6.6.4) — CONFIRMADO sem mudança de decode. Mecanismo mínimo de IRQ: `Aarch64Core.interruptLine`/`enterIrq` (espelho de `enterMemoryAbort`, mas assíncrono — `ELR_EL1←PC atual`, `ESR_EL1`/`FAR_EL1` intocados, vetor `VBAR_EL1+0x480`) + `PstateRegister` ganhou o bit `I` de `DAIF` (`toSpsrFormat`/`setFromSpsrFormat` estendidos, mesma posição do CPSR ARM32) + `Ir64BlockExecutor.step`/`executeBlock` checam IRQ/sleep no topo (espelho de `ArmCore#servicePendingIrq`, 32-bit); `enterMemoryAbort` (B6.6.4) também passou a mascarar IRQ na entrada (gap corrigido para consistência arquitetural). Achado colateral: `Aarch64VmsaSystemRegisters` (B6.6.3) tinha `switch` EXAUSTIVO sobre o enum — quebrava a build ao estendê-lo; ganhou `default` explícito, continua sem atender timer/identidades (fora do seu escopo de MMU/exceção). Todos os encodings novos CONFERIDOS via `aarch64-none-elf-as`/`objdump` reais (devkitA64), apêndice do mesmo `corpus.s`/`.bin`/`.objdump.txt`, offsets `0x318`-`0x380` — incl. correção de um teste antigo que esperava o `NOP` de `label1` (offset `0x90`) como "não suportado" (agora é NOP de verdade) e dois testes negativos novos (`CLREX`/`BRK`, ainda fora de escopo). `Aarch64IrqAndCpuIdentityTest` novo (`executor64/`). `mvn -o test` verde (core 1380 + truffle 13); `mvn -o install` local feito. G5 não se aplica (nenhum arquivo 32-bit tocado, mesmo precedente de toda a trilha B6). **Destrava F11** (retomar `Raspi364Machine`) **e** B6.6.6 (mesmo bloqueio de fundo) — ver `tasks/FILA-EXECUCAO.md` |
| [B7](trilha-b-arquiteturas/b7-m-profile.md) | Perfil M / Cortex-M — ÉPICO refinado 2026-07-15; decisão fechada na [RFC-M-PROFILE](../docs/RFC-M-PROFILE.md) (`ExceptionModel` plugável, sem core irmão) | B2.6 | ✅ ÉPICO CONCLUÍDO (B7.1–B7.5, 2026-07-23) |
| [B7.1](trilha-b-arquiteturas/b7.1-exception-model-refactor.md) | Extrair `ExceptionModel` (refactor zero-diff) | B2.6 | ✅ (2026-07-17) — `ExceptionModel` novo (`interceptsBranch`/`branchIntercepted`, default `false`/lança) + `AProfileExceptionModel` recebendo o corpo íntegro de `ArmCore#handleException` (entrada de exceção, vetores, ITSTATE etc. — zero mudança de lógica); `ArmCore` ganha `exceptionModel`/`setExceptionModel`/`exceptionModel()`, default `AProfileExceptionModel`, e `requestException`/`servicePendingIrq` delegam a ele. Intercept plugado nos 3 caminhos que escrevem PC vindo de dado: `IrBranchExecutor#executeBranchExchange` e `IrExecutionSupport#loadToPc` (interpretado — cobre BX, LDR/LDR-literal/POP/LDM com PC); `AsmRuntimeHelpers#branchExchange` e um `loadToPc` novo (ASM — **achado da task**: o ASM não tinha um único ponto de entrada para load-to-PC como o interpretado, `loadToPcArm4`/`loadToPcArm5` eram invocados diretamente em 3 emissões distintas do `AsmBlockCompiler`, LDR/LDR-literal/POP-inline THUMB, além da via `executePop`/`executeMultipleTransfer`; unificado num `loadToPc(core,value,interwork)` público só para esses 4 caminhos, mantendo `loadToPcArm4` cru e sem intercept para `MOV pc,...`, que nunca interworka nem intercepta — espelha exatamente o `alignAndSetPc` do interpretado). Suítes arm-jitter 716 core + 13 truffle + gbaemu + ndsemu verdes; bench `Main JUS.nds 600 bench` rodou normal (16,9 fps avg, DISPCNT não-zero) — custo do intercept é um método default retornando `false` constante, dentro do ruído|
| [B7.2](trilha-b-arquiteturas/b7.2-mprofile-exception-model.md) | `MProfileExceptionModel`: MSP/PSP, xPSR, stacking, EXC_RETURN | B7.1 | ✅ (2026-07-23) — `MProfileException` (números arquiteturais fixos) + `MProfileExceptionModel` novos em `core/`: MSP/PSP como sombra do R13 ativo (`ArmCore.SP` alargado de `private` p/ package-private, mesmo tratamento de `LR` na B7.1), CONTROL(SPSEL)/PRIMASK/VTOR, `enterException(core, MProfileException)` empilhando R0-R3/R12/LR/ReturnAddress/xPSR (realinhamento a 8 bytes com bit STKALIGN, xPSR remontado por reaproveitamento direto dos bits NZCVQ/GE/IT do `CpsrRegister` — mesmas posições, só T deslocado p/ bit 24 e IPSR nos bits 8:0), `EXC_RETURN` nos 3 padrões válidos. **Retorno de exceção e o UNDEFINED de `BX`/`BLX` p/ bit0=0 reusam o MESMO par `interceptsBranch`/`branchIntercepted` da B7.1** (`MProfileExceptionModel.interceptsBranch` reconhece candidato EXC_RETURN OU bit0=0) — zero mudança nova nos 3 backends de branch/load-to-pc, tudo já vinha plugado. `SVC` sob perfil M desvia do `SwiDispatcher` via hook novo `ExceptionModel.handlesSupervisorCall()` (default `false`), checado em `IrSystemExecutor.executeSwi` E `AsmRuntimeHelpers.executeSwi` (G1, os 2 backends). `ArmFeature.M_PROFILE` adicionado (documentado como não-consumido ainda — igual ao precedente `VFPV2`/B3.3 — nenhum preset/decoder existe até a B7.4). Sem NVIC (B7.3): testes chamam `enterException` direto ou via `SVC`. 6 testes novos (`MProfileExceptionModelTest`): layout do frame, ida-e-volta Thread(PSP, com realinhamento de 4)→handler(MSP)→`BX LR`, handler→handler encadeado (retorno também em cadeia), `EXC_RETURN` inválido→UsageFault, ITSTATE preservado, equivalência interpretado×ASM (prova adicional que o `SwiDispatcher` registrado NUNCA é chamado sob o modelo M). Suítes arm-jitter (core+truffle) + gbaemu + ndsemu verdes (G5) |
| [B7.3](trilha-b-arquiteturas/b7.3-scs-nvic-systick.md) | SCS/NVIC/VTOR/SysTick memory-mapped na lib | B7.2 | ✅ (2026-07-23) — `MProfileSystemControl` novo em `core/`: VTOR/ICSR/AIRCR/SHPR1-3/SHCSR/NVIC(ISER/ICER/ISPR/ICPR/IABR/IPR, até 32 IRQs)/SysTick(CSR/RVR/CVR) por offset relativo a `0xE000E000`, decodificador fino em cima do `MProfileExceptionModel` (que ganhou o estado de verdade: `pending[]`/`priority[]`/`externalIrqEnabled[]`/pilha de exceções ativas). `ExceptionModel` ganhou `hasPendingException()`/`enterPendingException(core)` (default `false`/lança, mesma técnica de `interceptsBranch` da B7.1) e `ArmCore.servicePendingIrq` passa a consultar os dois: linha de IRQ do perfil A (inalterado) OU o novo hook do perfil M. Prioridade efetiva fixa p/ NMI(-2)/HardFault(-1), 8 bits configuráveis p/ o resto; preempção só entra com prioridade estritamente mais urgente que a ativa (Thread mode = a mais baixa possível); PRIMASK mascara tudo >= 0. `enterException` virou aceitar número bruto (não só o enum) p/ IRQs externas entrarem pelo mesmo caminho de empilhamento da B7.2, com push/pop na pilha de ativas em entrada/retorno. SysTick: `tick(cycles)` decrementa CVR, recarrega de RVR, seta COUNTFLAG (limpa em QUALQUER leitura de CSR) e pende SYSTICK se TICKINT — **achado da task**: a primeira versão só detectava o cruzamento por zero quando um único `tick()` consumia o período inteiro de uma vez, então alimentar 1 ciclo por chamada (padrão realista de host) nunca disparava a recarga; corrigido para detectar o cruzamento em qualquer granularidade de chamada. 7 testes novos (`MProfileSystemControlTest`): VTOR, NVIC habilita/pende/limpa, prioridade com preempção aninhada + NMI, PRIMASK, PendSV via ICSR entra só depois que a Thread libera, SysTick recarrega 2x em `tick(250)` + granularidade de 1 ciclo/chamada, e uma integração "mini kernel" (loop + handler Thumb reais rodando via `core.step()`, contador em RAM bate o número de recargas). Ficou tudo em `core/` (não moveu p/ pacote `mprofile/` — a alternativa "ou" do enunciado; SP/LR do `ArmCore` são package-private e a mudança de pacote não trazia benefício nesta fase). Suítes arm-jitter (core+truffle) + gbaemu + ndsemu verdes (G5) |
| [B7.4](trilha-b-arquiteturas/b7.4-presets-armv6m-armv7m.md) | MRS/MSR SYSm + CPS M + presets `ARMV6M`/`ARMV7M` | B7.2 (pleno c/ B3.2) | ✅ (2026-07-23) — `IrOp.MProfileSystemRegister` (kind 54) + `InstructionKind.MPROFILE_MRS`/`MPROFILE_MSR` novos: `MRS`/`MSR` na forma SYSm (número de registrador especial), distinta da forma A-profile CPSR/SPSR+máscara que continua em `PsrTransfer`. `Thumb2MiscDecoder` ganha ramo M-profile em `decodeMrs`/`decodeMsr` gateado por `ArmFeature.M_PROFILE` (com M_PROFILE, `lo[7:0]` deixa de precisar ser zero e vira SYSm; sem ela, comportamento A-profile bit a bit intacto, G3); SYSm fora da tabela → UNDEFINED, e BASEPRI/BASEPRI_MAX/FAULTMASK (17/18/19) gateados no decode pela feature nova `ArmFeature.M_FAULT_MASKING` (ausente no v6-M). `ThumbDecoder` ganha o `CPS` de 16 bits (`0xB660` T1, encoding NOVO — não existia em A nem M antes) gateado por `M_PROFILE`, reusando `InstructionKind.CPS`/`IrOp.ChangeProcessorState` (empacotamento idêntico ao CPS ARM/Thumb-2); `CPS f` exige `M_FAULT_MASKING`. Executor: `IrSystemExecutor.executeMProfileSystemRegister` delega a dois métodos novos do `MProfileExceptionModel` (`readSystemRegister`/`writeSystemRegister`, tabela SYSm completa: APSR/IAPSR/EAPSR/XPSR compõem NZCVQ+GE do CPSR + IPSR; IPSR/EPSR/IEPSR RAZ/WI na escrita; MSP/PSP via `readMsp`/`writeMsp`/`readPsp`/`writePsp` novos com a regra de "SP ativo vs sombra"; PRIMASK/BASEPRI/FAULTMASK/CONTROL; BASEPRI_MAX só abaixa o limite; EPSR modelado como 0), e `executeChangeProcessorState` ganha ramo M-profile (CPS toca só PRIMASK/FAULTMASK, ignora A/modo) ANTES do caminho A-profile. Estado novo no modelo: `basepri`/`faultmask` (armazenamento simples, NÃO integrados à preempção — documentado, fica p/ task futura, mesmo padrão do SHCSR inerte da B7.3). Wiring dos switches exaustivos: `IrBlockExecutor` (2 pontos), `AsmNativePolicy` (→ false, interpretado), `DeadCodeEliminationPass` (regUse/regDef: MSR lê o reg ARM fonte, MRS escreve o reg ARM destino — correção real de DCE, não cosmética), `StandardIrBlockLifter.isTerminal` (não-terminal). Presets `ARMV6M` (Thumb-1 + só Misc/Branch de 32 bits — v6-M NÃO tem Thumb-2 largo, `THUMB2` aqui é só o fetch) e `ARMV7M` (Thumb-2 completo + divide/bitfield + `M_FAULT_MASKING`; **sem VFP**, fora de escopo). 21 testes novos (`MProfileSystemRegisterTest` 13 fim-a-fim via `core.step()` incl. G1 interp×ASM; `ArmArchitectureMProfilePresetsTest` 8: v6-M rejeita LDR.W/ADD.W que v7-M aceita, gating SYSm/CPS-f, SVC entra pela exceção M, e diff de features vazio nos presets A-profile). Suítes arm-jitter (core 868 + truffle 13) + gbaemu (240) + ndsemu (183) verdes (G5). **Desvio documentado**: o `CPS` de 16 bits foi implementado via reuso de `InstructionKind.CPS`+`ChangeProcessorState` (opção (b) do plano) em vez de IR nova, roteando por `exceptionModel instanceof MProfileExceptionModel` no executor — mais simples e zero duplicação de semântica |
| [B7.5](trilha-b-arquiteturas/b7.5-runner-bare-metal.md) | armbox `--machine=cortex-m` + firmware torture + semihosting (fecha B7) | B7.3 ✅, B7.4 ✅ | ✅ (2026-07-23) — **fecha o épico B7 por completo.** armbox ganha um segundo modo de máquina (`dev.vitorsilverio.armbox.baremetal.CortexMMachine`, `--machine=cortex-m`): mapa fixo flash `0x0`/RAM `0x20000000` (`--ram-size`, default 1 MiB)/SCS `0xE000E000` (`MProfileSystemControl`) sobre um `PagedAddressSpace` (C3 — primeiro consumidor real fora dos testes dele), boot lendo MSP/PC direto dos 2 primeiros words da tabela de vetores (VTOR=0 por padrão), aceita `.elf` (PT_LOAD pelos endereços físicos — loader próprio e pequeno, não reusa `Elf32Loader` porque este é acoplado a uma `GuestMemory` de mapeamento dinâmico/ABI Linux, incompatível com o mapa fixo daqui) e `.bin` cru em `0x0`. **Semihosting novo no arm-jitter** (não existia hook para `BKPT`): `IrOp.Breakpoint`/`InstructionKind.BREAKPOINT`/`ArmFeature.BREAKPOINT` (ARMv5T+, ausente só em `ARMV4T` — G2) + decode Thumb `1011 1110 imm8`; `BkptDispatcher`/`BkptHandler`/`BkptFallbackHandler` novos em `core/` (mesmo padrão do `SwiDispatcher`) + `ArmCore.setBkptDispatcher`/`bkptDispatcher()` (aditivo, G3); interpretado via `IrSystemExecutor.executeBreakpoint` (delega ao dispatcher, ou `UNDEFINED` sem um instalado — comportamento pré-task preservado); ASM permanece com fallback interpretado (`AsmNativePolicy` → `false`, mesmo padrão de toda op nova/rara desde B1.x). armbox implementa `SYS_WRITE0`/`SYS_WRITEC`/`SYS_EXIT` (`BKPT 0xAB`) como um `BkptHandler`. **Achado de bug real no arm-jitter** (categoria "+ arm-jitter só se sair bug" da task): `DivergenceCheckingCodeEmitter` (`--check`) sempre criava o core scratch com `AProfileExceptionModel` default, nunca com o `MProfileExceptionModel` real — qualquer bloco com `BX`/`POP` de `EXC_RETURN`, `MRS`/`MSR` SYSm, `CPS` ou `WFI` sob perfil M divergia por definição (o scratch não reconhecia o retorno de exceção nem replicava PRIMASK/MSP/PSP-sombra). Corrigido tratando TODO bloco como "hostDependent" (só a referência roda, sem comparação por bloco) quando `core.exceptionModel() instanceof MProfileExceptionModel` — mesmo precedente do SWI/Coprocessor, decisão calculada no lambda de execução (não mais no `emit()`) porque só ali o `core` real está disponível; granularidade de comparação por bloco sob perfil M fica documentada como limitação para uma task futura (exigiria dar ao `MProfileExceptionModel` um snapshot/restore próprio). Firmware: `cortexm-torture.s`/`cortexm-torture-m0.s` (handwritten, `arm-none-eabi-as -mcpu=cortex-m3`/`-mcpu=cortex-m0`, linker script `flash.ld` novo com `KEEP(*(.isr_vector))`) cobrindo reset+MSP, `SVC` em MSP e depois PSP (troca via CONTROL.SPSEL), SysTick (RVR curto+TICKINT+contador), PendSV pendido de DENTRO do handler de SysTick entrando só depois (prioridade igual não preempta), PRIMASK segurando+liberando a entrega, MRS/MSR MSP/PSP/CONTROL/PRIMASK ida-e-volta e (só m3) MOVW/MOVT/SDIV/UDIV/UBFX/LDREX+STREX; gêmeo `-broken` (SDIV com valor esperado errado) prova o harness. `hello-cortexm.c` é o sinal de compilador real (gcc puro, tabela de vetores como array de ponteiros de função em C — bit Thumb correto automático, sem `.word` cru). Exit code é booleano (0/1), não a bitmask crua de checagens — **achado**: `System.exit`/o exit code do processo trunca mod 256, e uma bitmask com bit ≥ 8 setado (ex. bit 12 = 4096) vira silenciosamente 0 na shell. `CortexMTortureTest` novo (6 testes: m3 e m0 nos 3 backends, `-broken` detecta, `.bin` cru, regressão `linux-user` intacto). Suítes arm-jitter (core) + armbox + gbaemu + ndsemu verdes (G5). `docs/VALIDACAO-ARQUITETURAS.md` atualizado (N1-N3 ✅ para `ARMV6M`/`ARMV7M`, N4 ainda ⬜ — nenhum consumidor real ainda) + README do armbox (seção nova) |
| [C0](trilha-c-perf/c0-superblocos-trace-jit.md) | Superblocos → loop-superbloco (alavanca nº 1) | — | ✅ ÉPICO CONCLUÍDO (C0.1–C0.4) |
| [C0.1](trilha-c-perf/c0-impl-loop-superbloco.md) | Harness lockstep de runtime | — | ✅ |
| [C0.2](trilha-c-perf/c0-impl-loop-superbloco.md) | Detector de ciclo + contadores | C0.1 | ✅ (acha os loops da medição nos jogos reais) |
| [C0.3](trilha-c-perf/c0-impl-loop-superbloco.md) | Emissor do loop-superbloco + integração | C0.1, C0.2 | ✅ (bench: MKDS +16%, SM64DS +44%, JUS +39% — os dois últimos ACIMA de realtime) |
| [C0.4](trilha-c-perf/c0-impl-loop-superbloco.md) | Validação de jogo + A/B + default (ndsemu) | C0.3 | ✅ (boots frios ×4 ON==OFF + gameplay validado na GUI; default ON no backend ASM — ndsemu cc65fab) |
| [C1](trilha-c-perf/c1-flags-nzcv-locals.md) | Flags NZCV em locals JVM (⚠️ prioridade rebaixada) | — | ❌ FECHADA SEM IMPLEMENTAR — fechamento original (2026-07-11 cedo) citava só o profile de 2026-07-08, pré-C0, sem remedir depois do que mudou; **REAVALIADA com dados novos na mesma data (2026-07-11, pós-C0/superblocos, backend `asm superblocks` = default de produção hoje)**: JFR de 120s em DOIS jogos via savestate (MKDS corrida 782 amostras, JUS intro Mobiclip 459 amostras) — folhas de flag/condição (`condEq`/`condNe`/`evalCond`) ficam em **~0,4% das amostras nos dois**, dispatch (`JitRuntime.execute`) continua o maior item isolado identificável (~12-14%, caiu de ~26% pré-superbloco mas não some). A hipótese de "superblocos mudariam o perfil o suficiente" não se confirmou. Conclusão de 2026-07-08 REAFIRMADA com medição fresca, não mais suposição herdada. É a task de MAIOR RISCO de corretude da trilha C (invariantes de flush/reload "que não podem falhar") — sem ganho medido (agora 2×) e com risco alto, fechada de novo; reabrir só com profile NOVO mostrando sinal diferente (jogo/cenário novo, ou mudança grande de arquitetura) |
| [C2](trilha-c-perf/c2-logic-flags-shifter-nativo.md) | Carry-out do shifter nativo em ops lógicas S | — | ✅ (helpers `shiftedOperandCarry`/`doXxxS`; policy só rejeita dst15+S nas ALU; property test 32 combos × n=0..255 × 2 carries; asmcheck JUS 800 zero divergências; JUS bench 76,5→81,7 fps +6,8%) |
| [C3](trilha-c-perf/c3-paged-address-space.md) | PagedAddressSpace O(1) | — | ✅ utilitário no arm-jitter (`memory/PagedAddressSpace`: `mapRam`/`mapHandler`/`mapMirror`/`unmap`, `WriteListener` por página só dispara em página marcada por `setContainsCode` — mesma ideia do índice página→blocos do `BlockCache`, open-bus configurável, waitstates por página com `providesAccessCycles()` esperto; 13 testes novos). **Microbench honesto (loop manual, if-chain sintético raso é enganoso — refeito com 8 branches como o mapa real do GBA): paged ~19-26% mais rápido que o if-chain realista** (0,74-0,81x o tempo). Suite core 480 verde; gbaemu 216 + ndsemu 175 revalidados (ainda NÃO adotado por eles — "Adoção" é PR separado por hospedeiro, fora do escopo desta task) |
| [C4](trilha-c-perf/c4-chain-budget-pos-boot.md) | Chain budget pós-boot (repo ndsemu) | — | ✅ FECHADA (ndsemu 85e4b36 + cf7b5c2: bench 256/64 = MKDS +6% SM64DS +7,6% JUS +11,4%; boots ×4 OK; gameplay GUI validado 2026-07-10 → 256/64 é o DEFAULT) |
| [C5](trilha-c-perf/c5-gbaemu-chaining.md) | Chaining no gbaemu (repo gbaemu) | — | ✅ (gbaemu `GbaConsole.CHAIN_CYCLE_BUDGET=32`, conservador — bem abaixo de 1 scanline/1232 ciclos; aplicado nos dois backends para manter `JitInterpreterDivergenceTest` válido; bench headless 5 jogos: +9,1% a +41,0%; gba-tests + suite 216 verdes. **VALIDAÇÃO DE GAMEPLAY (2026-07-11, usuário, 3 jogos):** a travadinha de compilação que o ASM dava antes SUMIU com chaining — confirmado. **Achados novos, registrados a pedido do usuário, SEM investigação (por escolha dele, não falta de tempo):** (1) Pokémon (FireRed) tem glitches gráficos em batalha rodando ASM+chaining — possível regressão do caminho ASM, escopo do C5; (2) animação da BIOS + áudio lentos — usuário confirmou que acontece TAMBÉM em `INTERPRETED`, então NÃO é regressão desta task nem do ASM; é um bug pré-existente e mais amplo, versão em que começou desconhecida. **Decisão do usuário: `INTERPRETED` segue DEFAULT do gbaemu** [já era assim desde 2026-06-21 por causa do corte de input do JIT — reforçado agora também por precisão]; ASM fica reservado para quando precisar de mais desempenho, não como default) |
| [C6](trilha-c-perf/c6-paged-address-space-gbaemu.md) | Adotar `PagedAddressSpace` no gbaemu (beneficia INTERPRETED default) | C3 ✅ | ✅ (gbaemu `64e08c1`) `GbaBus` reimplementado sobre `PagedAddressSpace` (composição): EWRAM/IWRAM viram páginas de RAM verdadeiras via `mapRam`+`mapMirror`; BIOS/Paleta/VRAM/OAM/ROM+EEPROM/SRAM/I-O viram um handler por bloco (`MemorySpaceGroup`, reproduz a ordem de prioridade de registro de hoje), reaproveitando os MESMOS dispositivos sem mudar comportamento observável. `PAGE_SHIFT=15` (32KB) — 4KB chegou a REGREDIR o bench (tabelas maiores/esparsas, pior localidade de cache no caminho quente de EWRAM/IWRAM), achado registrado no commit. Decisões documentadas: waitstates de GAME_PAK continuam calculados dinamicamente via WAITCNT (fora da tabela estática de páginas do utilitário, que é por página fixa — não serviria ao caso dinâmico); `InvalidationAwareAddressSpace` continua envolvendo o bus inteiro sem usar o `WriteListener`/`setContainsCode` por página do utilitário (o `JitRuntime.invalidate()` já tem seu próprio gate O(1) por bitset — duplicar o rastreio de "página com código" nas duas camadas seria redundante e arriscaria desync, sem ganho medido). Suite gbaemu 216 verde (`JitInterpreterDivergenceTest` sem divergência, gba-tests, IRQ/timer/DMA). Bench headless dos 5 jogos (3 execuções, médias) nos dois backends: **-8% a -29%** (interp: -8% a -20%; asm+chain: -15% a -29%), sem regressão em nenhum jogo. **Validação de gameplay do usuário concluída 2026-07-16** (A/B real: build pós-C6 vs. worktree no commit pai `942b664`, mesmos 5 jogos): os 3 problemas observados no boot+save (chiado de áudio no SMW, glitch de batalha do Pokémon agora também visível em INTERPRETED, canal de áudio baixo/acelerado no Metroid) **já existiam no build pré-C6 — confirmados pré-existentes, não são regressão desta task**. Achado relevante registrado à parte: o glitch de batalha do Pokémon antes atribuído só ao caminho ASM (ver C5) TAMBÉM ocorre em INTERPRETED — contradiz a atribuição anterior, fica como pendência de investigação futura (não corrigido aqui, fora de escopo de C6) |
| [C7](trilha-c-perf/c7-paged-address-space-ndsemu.md) | Adotar `PagedAddressSpace` no ndsemu (ARM9+ARM7, substitui cache de bancos VRAM) | C3 ✅, C6 recomendada antes | ⬜ |
| [C8](trilha-c-perf/c8-interpretador-perf-gbaemu.md) | **Perf do INTERPRETADO** — o caminho de produção do gbaemu (INTERPRETED é default POR FIDELIDADE, decisão do usuário: IRQ de H/V-blank/STAT precisa de granularidade por instrução; JIT de bloco quebra Pokémon em batalha). Fase 1 medir, fase 2 um candidato por PR | C6 recomendada antes | 🟡 Fase 1 (medir) ✅ 2026-07-17 — `InterpretedThroughputBenchTest` novo (espelha `ChainCycleBudgetBenchTest` de C5, `useJit=false`), ANTES publicado: pokefirered 612ms, smw 635ms, castlevania 150ms, metroid 227ms, mariokart 301ms (50M ciclos cada). JFR de 120s (`InterpretedProfileMain`, harness manual não-teste) rodado em FireRed interpretado — **savestate de batalha não disponível nesta sessão** (nenhum `.ss` de batalha em `roms/`), perfil coletado em boot+overworld em vez disso (limitação registrada, não bloqueante: o hot path do dispatcher do interpretador não deveria variar por fase do jogo). Top do profile confirma a expectativa da spec: `IrBlockExecutor.execute` (o switch de dispatch) domina (~5000 de ~8400 amostras), seguido por `IrAluExecutor`/`IrMemoryExecutor`/`GbaBus$MemorySpaceGroup.owner` (waitstate lookup, candidato #4). **Decisão: fase 2 começa pelo candidato #1 (dispatch), como a spec já previa** — próxima sessão implementa e re-mede. Suites arm-jitter (sem mudança) + gbaemu (`mvn -o test`, `JitInterpreterDivergenceTest` sem divergência) verdes; ndsemu não revalidado nesta sessão (nenhum arquivo de arm-jitter tocado). **2026-07-17 (sessão seguinte): candidato #1 (dispatch) implementado e MANTIDO** — `IrOp.kind()` (chamada virtual megamórfica sobre ~40 subtipos selados) deixou de ser chamado por op a cada execução do bloco; `IrBlock` agora resolve um `int[] kindsArray` uma única vez na construção (paralelo a `operationsArray()`, mesmo padrão de imutabilidade), e `IrBlockExecutor#execute` faz `switch (kinds[i])` em vez de `switch (op.kind())` — o tableswitch em si não mudou, só a origem do discriminador. `executeOp` (fallback PER_OP do ASM) não foi tocado — continua usando `switch (op)` por padrão de tipo, fora do escopo do candidato. Ganho medido: 10 execuções de `InterpretedThroughputBenchTest` antes/depois (para absorver ruído de máquina — desvio de até ±20% run-a-run no mesmo binário) — pokefirered 929→810ms, smw 832→657ms, castlevania 172→148ms, metroid 280→258ms, mariokart 365→304ms, **agregado −15,6%** (bate a meta ≥15% do Aceite). Suítes: arm-jitter 745+13, gbaemu 240 (`JitInterpreterDivergenceTest`: "No divergence in 20000000 blocks"; `GbaTestRomSuiteTest`/`ArmTestRomTest`/`ThumbTestRomTest` verdes), ndsemu 179 — todas verdes. **2026-07-17 (sessão seguinte): candidato #2 investigado, sem PR (nada a mudar)** — verificado no código (`JitRuntime#execute`, caminho não-tiered usado por `interpretedArmThumb`, `hotThreshold=1`): o `lift` só roda na primeira execução de cada `pc` (quando `blockCache.getOrNull(key)` ainda é `null`); a partir daí o `IrBlock` já emitido fica no `BlockCache` para sempre (ou até invalidação por SMC) e é reusado por chave — não há re-lift em cache hit. Consistente com o profile da fase 1, que não mostrou nenhum frame de decode/lift entre os mais quentes. **Decisão: candidato #2 fechado como "já coberto", zero código.** Candidato #4 (`GbaBus$MemorySpaceGroup.owner`) implementado em seguida, por ser o próximo item do profile com evidência real (não o #3, que o profile também não credenciava — `evalCond` já é um `switch` sobre enum, um tableswitch tão barato quanto a checagem "block é todo AL" proposta, sem chamada extra a evitar). Bucket de I/O (o mais populoso e mais acessado do barramento: timers, DMA, som, vídeo, teclado, serial competindo pelo mesmo grupo) ganhou uma tabela endereço→dono pré-computada na montagem (`GbaBus#mapIoBucket`, `MemorySpaceGroup` aceita `fastOwners`/`fastBase` opcionais), substituindo a varredura linear de `owner()` por um índice de array — reproduz EXATAMENTE a mesma regra de prioridade do `owner()` original (mesmo `members`, mesma ordem), só resolvida uma vez em vez de por acesso; buckets pequenos (≤2 membros via `probeBucketMembers`) não usam a tabela (não compensa). Ganho medido: 5 execuções de `InterpretedThroughputBenchTest` antes/depois isoladas (ruído de máquina alto, mesma metodologia do candidato #1) — agregado médio 2350ms→2194ms, **≈ −6,6%** (acima do piso de 2% do Aceite, mas sozinho não bateria os 15% — a meta agregada já estava satisfeita pelo candidato #1). Suíte gbaemu 240 verde (`mvn -o test`, `JitInterpreterDivergenceTest` sem divergência, `GbaTestRomSuiteTest`/`ArmTestRomTest`/`ThumbTestRomTest` verdes); nenhum arquivo de arm-jitter tocado nesta sessão (mudança é 100% gbaemu, `GbaBus.java`), ndsemu não revalidado (não usa `GbaBus`). **Fase 2 encerrada por aqui** (meta ≥15% batida pelo #1, #2 sem custo real, #4 deu ganho extra positivo; candidato #3 descartado por análise — sem chamada extra a cortar — e #5 exigiria evidência de alocação que o profile desta sessão não coletou). **Validação do usuário 2026-07-17: FireRed continua com velocidade normal (sempre teve) — sem regressão perceptível de fidelidade/timing** das otimizações da fase 2; ganho de desempenho bem-vindo mesmo sem gargalo perceptível no hardware do usuário. **C8 FECHADA.** |
| [C9](trilha-c-perf/c9-jit-fastmem-ndsemu.md) | Fastmem: load/store direto no array da página no bytecode JIT (ndsemu; gbaemu FORA — é interpretado) | C7 | ⬜ |
| [C10](trilha-c-perf/c10-jit-warmstart-ndsemu.md) | Warm-start do JIT: persistir PCs quentes por ROM + pré-compilar no load (ataca a queixa "demora a esquentar") | — | ✅ (2026-07-17) — arm-jitter: `BlockCache#hotKeys(max)` (só blocos COMPILADOS, ordem decrescente de acertos) + `JitRuntime#hotBlockKeys(max)` (delega) + `JitRuntime#precompile(Collection<BlockKey>, AddressSpace)` novos; `precompile` é no-op no modo não-tiered (`coldEmitter==null`), e chaves já presentes/com memória que não decodifica mais (ROM diferente/self-modified) são ignoradas — o próprio `lift` é a validação, sem exceção especial. **Desvio documentado da assinatura literal da spec**: `precompile` recebe um `AddressSpace memory` extra (a spec só listava `Collection<BlockKey> keys` — sem uma fonte de memória não há como fazer o lift; `JitRuntime` não guarda um `AddressSpace` persistente). ndsemu: `dev.vitorsilverio.ndsemu.jit.HotBlockStore` novo (texto simples versionado `NDSEMU-HOTPCS v1` + guarda de hash CRC32 da ROM) grava `<rom>.hotpcs` (`hotBlockKeys(512)` dos dois cores) no fechamento do app (shutdown hook), ao trocar de ROM e nos 2 fluxos de save-state (`saveStateQuick`/`saveStateToFile`); recarrega e chama `precompile` de forma SÍNCRONA (mesma thread da emulação — `BlockCache` não é thread-safe para acesso concorrente com `execute`, então rodar de uma thread nova violaria isso; o "background" do warm-start já vem do pool de compilação existente dentro do `precompile`) ao carregar ROM e nos 2 fluxos de load de save-state. Testes novos: arm-jitter `BlockCacheHotKeysTest` (3) + `JitRuntimeWarmStartTest` (5, incl. round-trip de compilação em background via polling); ndsemu `HotBlockStoreTest` (4: round-trip fim-a-fim populando runtimes/memórias FRESCOS antes de qualquer execução real, hash divergente ignorado, arquivo corrompido ignorado, arquivo ausente ignorado). Suítes verdes: arm-jitter 724 core + 13 truffle, ndsemu 179, gbaemu (regressão, não usa nada disto — ver FILA-EXECUCAO). **PENDENTE de validação do usuário** (não medido nesta sessão, sem ambiente de ROM real/headless disponível): Aceite #1 (medição objetiva de warmup no MKDS via savestate, antes/depois) e Aceite #2 (asmcheck JUS com precompile ativo + boot dos 4 jogos com/sem `.hotpcs` real) — a spec explicitamente permite registrar isso como pendência em vez de inventar números. Armadilha do filtro de chaves IWRAM/RAM instável (v1) também NÃO implementada — não há telemetria real desta sessão para decidir o filtro; anotado, não fechado |
| [C11](trilha-c-perf/c11-savestate-restore-jit-frio.md) | **Restore de save state deixa o JIT frio >10 min** (relato do usuário, SM64DS 2026-07-15) — diagnóstico já encaminhado no spec (`loadState` só faz `blockCache().clear()`; working set in-game re-aquece do zero); Fix A = embutir chaves quentes no `.ss` + `precompile` | — (compartilha APIs com C10) | ✅ **fase 2 concluída (2026-07-16, sessão de modelo forte)**: confirmado por leitura de fonte que `LoopSuperblockDetector.promoted`/`pending` (não só `JitRuntime.superblockHeads`) também sobrevivem ao `clear()` — `isPromoted(headPc)` bloqueia PARA SEMPRE a reconfirmação de um head já promovido antes do save, então o loop de maior valor de uma cena nunca mais vira superbloco depois de um restore, mesmo com seus blocos individuais recompilando normalmente. **Fix C implementado como fix PRIMÁRIO** (não só higiene, como a spec original supunha): `JitRuntime.reset()` novo no arm-jitter — limpa `blockCache()` + `superblockHeads` + reinstala um `LoopSuperblockDetector` fresco quando superblocos estão ligados; `NdsConsole.loadState` (ambos os runtimes) e `GbaConsole.loadState` trocam `blockCache().clear()` por `reset()` (gbaemu é no-op hoje — INTERPRETED default não liga superblocos — mantido por paridade). 2 testes novos em `LoopSuperblockTest` provando o bug (`blockCacheClearAloneLeavesSuperblockHeadPermanentlyBlocked`) e a correção (`resetAllowsSuperblockHeadToBeRebuiltAfterRestore`). **Re-medição com o `.ss` real do usuário (SM64DS, mesmo harness da fase 1)**: cenário frio (agora via `reset()`) parte de 23,7ms/frame e converge para a faixa do cenário quente (~11,6-13,2ms/frame, antes ~17-20ms/frame e o gap NUNCA fechava) já entre os frames 1200-2400 (20-40s de jogo) — dentro do critério de aceite (≤60s, perto do ideal ≤15s). **Decisão de modelo forte: Fix A/B NÃO são necessários** — Fix C sozinho fecha o gap medido; não implementados nesta sessão (documentado como decisão, não como pendência). Boot dos 4 jogos de referência (SM64DS via o bench + MKDS/JUS/Platinum headless) sem regressão. `asmcheck` de JUS mostrou uma divergência ASM×interpretador pré-existente e não relacionada (`ASM_CHECK` não liga superblocos — `reset()` se comporta identicamente a `clear()` nesse backend por construção; achado registrado, fora do escopo de C11, não investigado). Suítes arm-jitter (582+13) + ndsemu (175) + gbaemu (216) verdes |
| [D1](trilha-d-compat/d1-gba-rtc-gpio.md) | GBA: RTC via GPIO de cartucho (Pokémon Emerald/Boktai) — trilha D nova: compat de hospedeiros | — | ✅ (repo gbaemu; protocolo verificado contra o GBATEK real via fetch direto — "GBA Cart I/O Port (GPIO)", "GBA Cart Real-Time Clock (RTC)" e "DS Real-Time Clock (RTC)" [o GBA reaproveita quase todo o protocolo serial do NDS, só troca a tabela de comandos e move o bit AM/PM de hour.bit6→bit7]. `S3511aRtc` novo [`cartridge/rtc/`]: máquina de estados do chip S-3511A só com os 3 fios SCK/SIO/CS [`updatePins`], LSB-first, byte de comando `0110 CCC D` decodificado sem depender de nenhum estado do GPIO — não sabe nada sobre Direção, isso é responsabilidade de quem chama. **v1 (decisão do próprio enunciado da task): data/hora sempre lida AO VIVO do `GbaRtcClock` no instante do comando** — sem offset, sem campos internos de ano/mês/dia persistidos; o único registrador realmente stateful é o de controle [modo 12/24h + power-off auto-clear-on-read, `CONTROL_RESET_VALUE=0x00` para nunca mostrar "battery has run dry" sem ter modelado perda de energia real]. `GbaRom` ganha uma janela de GPIO opcional [0x080000C4-C9, só interceptada quando `rtc != null` — G3 intacto para os 5 jogos de referência]: leitura de pino de saída = latch simples, leitura do pino SIO como entrada = `rtc.sioReadback()`, gate de habilitação global via bit0 do registrador de controle do GPIO [distinto do registrador de controle DO CHIP — nomenclatura cuidada no código para não confundir as duas camadas]. `GbaRtcDetector` [game code, mesmo padrão do override A2CE→SRAM]: `AXVE`/`AXPE`/`BPEE` [Ruby/Sapphire/Emerald] + `U3IJ`/`U32J`/`U33J` [Boktai]; `GbaCartridge.hasRtc()` novo. Save state: `SAVE_STATE_VERSION` 2→3, bloco do RTC [fase/bit-shift/comando/direção/buffer de parâmetro/registrador de controle — NÃO a hora] escrito só quando `cartridge.hasRtc()`, resolvido via `bus.find(GbaRom.class).rtc()` [sem mudar a assinatura do construtor de `GbaConsole`]. Armadilha do enunciado sobre ordem D1×C6 resolvida: como `GbaRom.contains()` não mudou, a tabela de páginas de C6 [`probeBucketMembers`, que só sonda os EXTREMOS do bloco] continua enxergando `GbaRom` do jeito que já enxergava antes — nenhuma mudança em `GbaBus` foi necessária, D1 funciona igual antes/depois de C6. 25 testes novos [`S3511aRtcTest` 6 — protocolo puro com relógio FAKE, BCD 59→0x59 não 89/0x3B, wrap de meio-dia em modo 12h, reset limpa o registrador de controle; `GbaRomGpioTest` 4 — regressão sem RTC, gate de habilitação [teste 2 da task], readback de saída, SIO como entrada fim-a-fim; `GbaRtcDetectorTest` 4 — jogos com/sem RTC incl. FireRed; `GbaConsoleRtcSaveStateTest` 1 — save state NO MEIO de uma transação serial, retomada com sucesso após reload [teste 4 da task]]. Suite gbaemu 231 verde [219+12 já contando testes de tasks anteriores]. arm-jitter não tocado nesta task — gate de regressão é só gbaemu [G5 não se aplica, sem mudança na lib compartilhada]) |
| [D2](trilha-d-compat/d2-pokemon-battle-glitch-interpreted.md) | gbaemu: 3 bugs visuais de batalha do FireRed, backend-independentes (fade com terço inferior preto, inimigo entrando pela direita, overlay de status ausente) — 3 hipóteses fortes (HDMA em vblank / wrap 9-bit do OAM X / OBJWIN), 1 PR por sintoma | — | ✅ 2026-07-16 — PR1: HDMA disparava nas 228 linhas do frame, não só nas 160 visíveis (`Events.hblankStartedVisibleCount` novo, só no disparo de DMA); usuário validou fade OK. Hipótese 2 (wrap OAM X) REFUTADA: usuário confirmou inimigo já entrando pela esquerda após o PR1 — era artefato do bug 1, não um bug de wrap, nenhum código mexido. PR3: sprites `mode==2` (OBJ window) eram só pulados, nunca viravam máscara — `objWindowCoverage` novo + `activeWindowMaskLine` ganhou o 3º nível de prioridade de janela (WIN0>WIN1>OBJ window>fora); teste `GbaVideoTest.objWindowModeSpriteMasksBlendWithoutBeingDrawnItself`. Suíte gbaemu 233 verde. Validação visual do usuário CONFIRMADA 2026-07-16 (fade, direção do inimigo e overlay de status todos corretos em batalha real) |
| [D3](trilha-d-compat/d3-smw-audio-crackle.md) | gbaemu: chiado/estática no áudio do SMW — task de diagnóstico, causa raiz desconhecida | — | 🟡 **fase 1 (diagnóstico) concluída 2026-07-16, sem causa encontrada** — `SmwAudioDiagnosticTest` novo (`-Daudio.diag=1`, ROM `smw.gba` na raiz do módulo, gitignored) mede título (15s) e gameplay real (60s, auto-mash de botões pra passar do título/file-select e andar+pular) tanto no mix completo quanto CADA canal isolado (`GbaAudio.setChannelMuted`, já existia como API do painel de debug — não precisou reintroduzir os toggles `DBG_DISABLE_*` do histórico). **Achados**: zero railing (0%) e zero clicks (>100 de salto) em toda a captura — não bate com nenhum padrão já corrigido (DC offset do PSG, DMA não recarregando, pitch 2 oitavas); os 4 canais PSG (CH1-4) ficaram **mudos o jogo inteiro** (título E gameplay, incl. pulos) — este port da SMW parece usar só Direct Sound A/B pra música E efeitos, então PSG está descartado como fonte; espectro (FFT, bandas bass/mid/treble/ultra) do mix não mostra energia elevada em agudos (~2%/0,5%) — sem assinatura de ruído branco/"hiss". Único sinal fora do trivial: a fração de saltos pequenos (10-40 em amplitude de 8 bits) do mix combinado (3,64% em gameplay) é MAIOR que a soma dos dois canais Direct Sound isolados (0,46%+0,63%=1,09%) — pode ser só superposição normal de duas fontes independentes (não anômalo) ou uma pista fraca de interação/aliasing entre os FIFOs A/B; não investigado a fundo (sem hipótese forte o bastante pra virar fix). **Devolvido ao usuário** conforme critério de aceite #3 da task (causa não óbvia) — precisa de caracterização melhor (quando exatamente ocorre o chiado: título, in-game, um efeito específico?) pra próxima sessão ter uma hipótese pra testar. 4 WAVs em `target/smw-audio-*.wav` pra ouvir. Suite gbaemu verde (`mvn -o test`, sem regressão) |
| [D4](trilha-d-compat/d4-metroid-audio-channel-accelerated.md) | gbaemu: canal (melodia) baixo, dessincronizado, acelerado e intermitente no Metroid Fusion — refinada 2026-07-16 (hipótese adicional: FIFO DirectSound/timer, driver m4a põe a música toda no PCM) | — | 🟡 **fase 1 concluída 2026-07-16, hipótese do enunciado REFUTADA** — instrumentação nova (`GbaAudio.directSoundTimer/fifoUnderruns/fifoSamplesRequested/maxOverflowsPerCall`, `GbaTimerController.overflowPeriodCycles`) mostrou Direct Sound A/B no mesmo timer, Hz esperado≈efetivo, **zero underruns e zero lotes de overflow >1** em 60s de gameplay real — a hipótese de FIFO/timer do enunciado não se sustenta. Achado novo: PSG CH1/CH2 tocam MUITO baixo (pico 7-12/127) contra Direct Sound (~85-88); CH3/CH4 mudos — candidato pra "melodia baixa" se for esse o canal que o usuário ouviu, mas precisa confirmação auditiva. WAVs em `target/metroid-audio-gameplay*.wav`, devolvido ao usuário (ver task). Suite gbaemu 239 verde |
| [D5](trilha-d-compat/d5-ndsemu-platinum-trava-buneary.md) | ndsemu: Platinum trava na intro (Buneary/título→menu, ARM9 em `0x020B2688`) — formalizada 2026-07-16 (estava aberta sem task); LER as pistas falsas em `ndsemu-game-compat` antes ⚠️ MODELO FORTE | — | 🟡 **fix implementado 2026-07-17 (ndsemu `e9bebfd`), aguardando validação do usuário na GUI.** Causa-raiz (RE dinâmico + PokePlat como mapa de símbolos): o título ACEITA o START (overlay 77 `TitleScreenLoop` estado 3→4) e espera o fade-out de SOM completar — o grupo 0 do módulo de fades de sequência só esvazia quando o driver de som do ARM7 dá a sequência one-shot (id 2, player 2) por TERMINADA, e ele faz poll do bit 31 de `SOUNDxCNT`; no ndsemu os canais só avançavam quando `Spu.mix` era consumido (headless: nunca; GUI: em wall-clock), então o one-shot nunca "acabava" → soft-lock eterno. Fix: `Spu.advance(bus,ciclos)` clockado pelo `tickHardware` produz num ring interno e `mix` drena (fim de canal agora corre no TEMPO EMULADO); 2 testes de regressão novos; boot ×4 OK; headless agora vai título→novo jogo (tela de touch). O GX-wait `0x020B2688` do enunciado era sintoma, não causa. **Achado colateral NOVO (sem task): Platinum NÃO BOOTA em INTERPRETED** — ARM9 preso no handshake IPCSYNC do `PXI_Init` (`0x020C640C`) desde o frame 0; só boota em ASM; pré-existente ao fix (ver pendências modelo forte) |
| [D6](trilha-d-compat/d6-gbaemu-bios-lenta.md) | gbaemu: animação da BIOS real lenta e interrompida (backend-independente) — 3 hipóteses (waitstate da região BIOS / IntrWait no caminho real / handoff prematuro) ⚠️ MODELO FORTE | — | ⬜ |
| [E1](trilha-e-manutencao/e1-javadoc-portugues.md) | Traduzir javadocs/comentários em inglês para português (violações da regra G7; ~15 arquivos main + ~15 test no censo de 2026-07-16) — 4 lotes, **1 sessão por lote**, checklist de progresso no próprio arquivo; só diff de comentário, zero código | — | ✅ FECHADA 2026-07-16 (Lote 1: `core/src/main` `decoder/`+`ir/`+`ir/opt/`, 9 arquivos; Lote 2: `codegen/`+`jit/`+`debug/`+`core/`+`memory/`+`arch/`+`coprocessor/`+`swi/`, 11 arquivos; Lote 3: `core/src/test` inteiro, 20 arquivos; Lote 4: `truffle/` main+test, 15 arquivos revisados — já 100% português, zero diff — + varredura final do grep limpa. `mvn -o test` verde 614 core + 13 truffle em todos os lotes) |
| [E2](trilha-e-manutencao/e2-armtracelistener-memory-abort-hook.md) | `ArmTraceListener#onMemoryAbort` — fecha a lacuna de observabilidade sob `runBlocks`/JIT (achado da sessão extra de debug da F3, `virtual-arm-box`) | — | ✅ (2026-08-16) — gancho novo aditivo em `ArmCore#enterMemoryAbort`, único ponto de convergência de `step()`/bloco interpretado/bloco compilado(JIT); PC exato da instrução faltosa observável nos 3 caminhos pela primeira vez (antes só `step()` tinha eventos por-instrução). 3 testes novos em `ArmCoreMemoryAbortTest` provando paridade do PC exato nos 3 caminhos. `mvn -o test` verde (core) + `mvn -o install`; G5 revalidado (gbaemu verde, ndsemu verde, armbox 40/41 — a 1 falha é a mesma pré-existente de `Armv7TortureTest`/VFP, não-regressão) |
| [F1](trilha-f-infra/f1-licenca-bsd.md) | Licença BSD 3-Clause nos 5 repos próprios (`LICENSE` + `<licenses>` no POM + README) | — | ✅ (2026-08-15) — `LICENSE` (texto SPDX BSD-3-Clause) + `<licenses>` no POM (pai, no arm-jitter) + seção `## Licença` no README, nos 5 repos; `mvn -o validate` verde nos 5; commit por repo (`arm-jitter` `4b6b08d`, `gbaemu` `016a0f5`, `ndsemu` `5e1e5eb`, `armbox` `c76afc7`, `virtual-arm-box` `606997e`), paths explícitos (`git add LICENSE pom.xml README.md`, nunca `-A` — ndsemu/gbaemu/armbox tinham arquivos soltos não relacionados, deixados de fora). Destrava **F5** |
| [F2](trilha-f-infra/f2-rename-virtual-arm-box.md) | `linuxbox` → `virtual-arm-box`: rename (diretório/artefato/pacote) + abstração `Machine` + `--machine=` | F1 | ✅ (2026-08-15) — diretório `IdeaProjects/linuxbox` → `virtual-arm-box` (`git mv` dos pacotes, histórico preservado), pacote `dev.vitorsilverio.linuxbox` → `dev.vitorsilverio.virtualarmbox` (+ `.boot`/`.device`), `pom.xml` (`artifactId`/`mainClass`), `Machine` novo (`typeByte`/`runSlice`/`core`/`runtime`) com `VersatilePbMachine implements Machine`, `Main` com `--machine=` (default `versatilepb`; nome desconhecido → `usage()`+exit 2, `IllegalArgumentException` capturada) e laço operando sobre `Machine`. `README.md`/`ROADMAP.md` novos (tabela de máquinas, escada `versatilepb`→`raspi1`→`virt64`, seção `## Armazenamento`). Documentação do arm-jitter atualizada (`README.md`, `ROADMAP.md`, `docs/RFC-SOFTMMU.md`, `tasks/README.md`, `tasks/trilha-b-arquiteturas/b4.1-mmu-softmmu.md`) — entradas históricas anotadas `virtual-arm-box (ex-`linuxbox`)` na primeira ocorrência. `mvn -o test` verde no virtual-arm-box (23 testes, incl. `VersatilePbBootTest` sem nenhuma asserção alterada — só `package`/`import`); G5 não se aplica (nenhum outro repo depende dele). **Armadilha real encontrada**: o rename de diretório falhou com "Device or resource busy" porque este próprio agente tinha o shell (Bash/PowerShell) com `cwd` dentro de `linuxbox/` — não era o IntelliJ; resolvido saindo do diretório antes do `mv`. |
| [F3](trilha-f-infra/f3-raspi1-machine.md) | `virtual-arm-box --machine=raspi1`: Raspberry Pi 1/Zero (BCM2835, ARMv6K, Device Tree) — LONGA, 3 marcos | F2 | 🟡 PARCIAL (2026-08-15, sessão 3/3 = ÚLTIMA do orçamento original) — sessão 1 deixou a infra completa (M1 bloqueado por DESEMPENHO). Sessão 2 eliminou o gargalo de desempenho (`boot.ZImageDecompressor`, descomprime o `kernel.img` no HOST) e corrigiu **2 bugs reais de CP15** (`arm-jitter`: `Cp15VmsaCoprocessor` não reconhecia `c13,c0,{0,2,3,4}`; `virtual-arm-box`: `Bcm2835Cp15Extras` só conhecia `MIDR`/`CTR` do esquema `c0`) mas ficou bloqueada no limite deliberado de BE8 (`CPSR.E=1`, task `B1.5` do `arm-jitter`, MVP só little-endian). **Sessão 3 (esta): o bloqueio de BE8 foi FECHADO pela task `B1.8`** (suporte real, sessão dedicada do `arm-jitter`) — com o `.m2` local atualizado, **M1 FECHA DE VERDADE nos dois backends** (`reachesEarlyconBannerAcceiteM1Interpreted`/`...M1Jit`, <1s cada, nenhum bug novo). **M2 NÃO fecha — bloqueio NOVO** (não BE8/CP15/desempenho): o boot entra num laço de `Oops` do próprio kernel (`Unable to handle kernel paging request`) em `unflatten_device_tree`/`fdt_next_tag`, poucas dezenas de milhares de instruções depois do banner do M1. Confirmado como divergência REAL via o oráculo QEMU 8.0.0 (mesmo kernel+dtb+initramfs bootam limpo no QEMU, muito além de `Freeing unused kernel memory`, sem Oops) — causa raiz NÃO isolada nesta sessão; achado colateral: `ArmTraceListener` sob `ArmCore#runBlocks`/`JitRuntime#execute` tem uma lacuna de cobertura que impediu localizar a instrução exata do primeiro abort. `mvn -o test` verde no virtual-arm-box (62 testes, 4 skipped = M2×2+M3×2); `VersatilePbBootTest` continua verde; nenhum arquivo do `arm-jitter` tocado nesta sessão (G5 não se aplica). **F3 segue PARCIAL** (fora do orçamento de 3 sessões, precisa de sessão extra para M2/M3) — `F10` (mesmo repo) fica bloqueada até fechar. **Sessão 2026-08-16 (continuação): causa raiz real da tempestade de IRQ ISOLADA E CORRIGIDA no `arm-jitter`** — trace instrução-a-instrução (`ArmCore#step()`) provou que `IRQ_PENDING_BASIC` do `Bcm2835Ic` era lido como `0` pelo driver `irq-bcm2835.c` mesmo com o bit certo armado no periférico; causa: `IrExecutionSupport`/`AsmRuntimeHelpers` (`readWordForLoad`/`writeWordForStore`/halfword) decompunham TODO `LDR`/`STR` não-PC sob `ArmFeature.UNALIGNED_ACCESS` (ARMv6K+) em 4 `read8`/`write8` independentes mesmo quando o endereço já estava ALINHADO — real hardware só faz isso para endereço genuinamente desalinhado; qualquer periférico MMIO cujo `read8` não reimplemente "byte N da word" (todos os deste repo) tinha o valor silenciosamente truncado ao byte baixo. Corrigido com checagem de alinhamento antes de escolher o caminho atravessado (interpretado + JIT), 3 testes de regressão novos (2 interpretado + 1 equivalência nativa), G5 revalidado (gbaemu 240 verde, ndsemu 183 verde, armbox 40/41 — a 1 falha é a mesma pré-existente de `Armv7TortureTest`/VFP, confirmada não-regressão via `git stash`). Com o fix, o boot em JIT avança MUITO além do ponto anterior (calibrate_delay, `devtmpfs: initialized`) — mas M2 **ainda não fecha**: um bloqueio NOVO aparece, Oops de instrução UNDEFINED em `vfp_enable()` (chamado por `vfp_init()`/`on_each_cpu_cond_mask`, com IRQs desligadas), matando o `init`. **Sessão de investigação do Oops em `vfp_enable` (2026-08-16): causa raiz ISOLADA E CORRIGIDA no `arm-jitter` — a hipótese anterior (VMSR/VMRS de FPEXC/FPSID) estava ERRADA.** Lendo o fonte real do kernel (`arch/arm/vfp/vfpmodule.c`): `vfp_enable()` não toca em registrador VFP nenhum — ela lê/escreve `CPACR` (`c1,c0,2`, CP15) via `MRC`/`MCR`, concedendo acesso a CP10/CP11 antes de qualquer instrução VFP rodar. `Cp15VmsaCoprocessor` nunca reivindicava `c1,c0,2` (só `c1,c0,0`/`SCTLR`) → UNDEFINED. Corrigido como armazenamento simples round-trip (mesma decisão de escopo do `c7`, sem enforcement de trap — este host decodifica VFP a partir do preset, nunca de `CPACR`), 1 teste de regressão novo, `mvn -o test` verde (1364 core+truffle) + `mvn -o install`, G5 revalidado (gbaemu/ndsemu/armbox verdes). **Confirmado ao vivo via harness diagnóstico temporário**: com o fix, `VFP support v0.3: not present` aparece limpo e o boot avança bem além — identity map, devtmpfs, hw-breakpoint, PL011 UART driver, mailbox enabled, 3 requisições `raspberrypi-firmware` respondidas, `kprobes: kprobe jump-optimization is enabled`, tudo em <4s de tempo simulado do kernel. **M2 AINDA NÃO fecha**: bloqueio NOVO e mais tardio logo depois de `kprobes:` — console para de crescer por completo (27,9 milhões de fatias/~18min reais sem nenhum byte novo, nem Oops nem panic observável). Causa raiz não isolada (suspeitas para a próxima sessão: `WFI` sem IRQ chegando, ou mailbox esperando resposta de uma tag ainda não implementada). `mvn -o test` verde no virtual-arm-box (66 testes, 4 skipped). Commits: arm-jitter `caac7af`, virtual-arm-box `c8c3e9e`. **Sessão de investigação do silêncio pós-`kprobes:` (2026-08-16): hipóteses de WFI/mailbox DESCARTADAS** (evidência direta via `onMemoryAbort`: a CPU não fica `HALTED`, ela entra num LAÇO DE ABORTOS `SECTION_TRANSLATION` silencioso, sem `printk`/`die()` chegando a rodar). **Achado real e independente CORRIGIDO** (`virtual-arm-box`, não `arm-jitter`): `Bcm2835Machine#create` nunca envolvia o barramento do `ArmCore` em `InvalidationAwareAddressSpace` — mesmo bug histórico do gbaemu (SMC/JIT sem invalidação); `kprobes: kprobe jump-optimization` é a primeira vez que este repo exercita código de guest automodificável. Corrigido, mas **NÃO fecha M2 sozinho**: o mesmo endereço-raiz de abort (`0x208d00c0`, `SECTION_TRANSLATION`) reaparece idêntico antes e depois do fix — mesma causa raiz pré-existente, ainda não isolada. `mvn -o test` verde (66 testes, 4 skipped); nenhum arquivo do arm-jitter tocado (G5 não se aplica). **Sessão de trace instrução-a-instrução (2026-08-16/17)**: 1 bug real corrigido (`InvalidationAwareAddressSpace`/`DualInvalidationAwareAddressSpace` não encaminhavam `fetch16`/`fetch32`/`translationGeneration`), causa raiz do abort storm NARROWED a `CPSR.E` oscilando para `true` (não isolada naquela sessão). **Sessão de fechamento do CPSR.E (2026-08-17): causa raiz isolada e CORRIGIDA no arm-jitter — abort storm 100% resolvido**. O kernel real executa `SETEND BE`/`SETEND LE` deliberadamente perto do laço ocioso; uma IRQ no meio herdava `CPSR.E=1` porque `AProfileExceptionModel#enterException` nunca reprogramava `E` (ARM ARM B1.8.3 exige `CPSR.E=SCTLR.EE` em toda entrada de exceção). `ExceptionEndiannessPolicy` novo (mesmo padrão aditivo de `ModeChangeListener`), `Cp15VmsaCoprocessor` implementa forçando `SCTLR.EE`; `Bcm2835Machine`/`VersatilePbMachine` registram o gancho. 3 testes novos, `mvn -o test` verde (1370 core+13 truffle) + `install`; G5 revalidado (gbaemu/ndsemu/virtual-arm-box verdes, armbox 40/41 pré-existente não-regressão). Boot agora avança até tentar montar a raiz (mailbox/USB/mmc0 ok) — **M2 ainda não fecha**: novo panic `VFS: Unable to mount root fs` porque o `FdtPatcher` não escreve `/chosen/linux,initrd-start`/`linux,initrd-end` (precisa aprender a CRIAR propriedades novas, hoje só sobrescreve existentes) — causa raiz já identificada, correção fica para a próxima sessão. **Sessão de extensão do `FdtPatcher` (2026-08-17): `linux,initrd-start`/`linux,initrd-end` implementados (`withNewProperty`, cria propriedades novas em `/chosen`), panic de VFS/root-mount RESOLVIDO — o boot chega a executar `/init` de verdade pela primeira vez.** **M2 ainda não fecha**: bloqueio novo e mais tardio — `Oops - undefined instruction` em `v6_clear_user_highpage_aliasing` mata o `init`; opcode decodificado do dump `Code:` do Oops é `MCRR p15,0,r2,r3,c6`, provável lacuna de DECODE de `MCRR`/`MRRC` no `arm-jitter` A32 (nunca exercitado por gbaemu/ndsemu/armbox). Achado colateral não-fatal: 2 ocorrências de "Division by zero in kernel" em `pl011_set_termios`, boot segue normalmente depois. `mvn -o test` verde (68 testes, 4 skipped); backend INTERPRETED não re-executado (orçamento). Ver `tasks/FILA-EXECUCAO.md` para o detalhe completo |
| [F4](trilha-f-infra/f4-arm-jitter-1.0.0-escopo.md) | arm-jitter **1.0.0**: bump de versão, `CHANGELOG.md`, política de versionamento, docs sem afirmação desatualizada | F1 | ✅ (2026-08-15) — 4 POMs em `1.0.0`, `capi/pom.xml` com `maven.deploy.skip=true`, `CHANGELOG.md` novo, `## Versionamento` no README, README/ROADMAP corrigidos (MMU/full-system agora ✅ citando `virtual-arm-box`, sem a menção ao `PREFETCH_ABORT` recursivo fechada por B4.1.5 em 2026-08-14); `mvn -o test`/`install` verdes (JBR 25); ver `tasks/FILA-EXECUCAO.md` P6 para o detalhe. ⚠️ **consumidores (gbaemu/ndsemu/armbox/virtual-arm-box) ficam quebrados até a F7** |
| [F5](trilha-f-infra/f5-maven-central-publicacao.md) | Publicar no Maven Central (`dev.vitorsilverio` verificado por DNS TXT; GPG; profile `release`) | F1, F4 | ✅ FECHADA (2026-08-15) — `dev.vitorsilverio:arm-jitter:1.0.0` e `arm-jitter-truffle:1.0.0` publicados de verdade (usuário já tinha conta+namespace verificado+chave GPG feitos; dropou o deployment quebrado de uma tentativa anterior e clicou Publish no bom, `6c7807a4-8f12-4a70-8b1e-d8fcb6ac5362`). 2 bugs reais corrigidos no caminho: (1) `central-publishing-maven-plugin` **não** respeita `maven.deploy.skip` — `capi/pom.xml` ganhou `<skipPublishing>true</skipPublishing>` explícito, senão o jar comum do capi (inútil como dependência) entrava no bundle; (2) versão `0.7.0` do plugin quebra (`UnrecognizedPropertyException: "warnings"`) contra a API atual do Central — bump pra `0.11.0`, versão movida para `<pluginManagement>` (unconditional) porque `capi/pom.xml` precisa resolvê-la mesmo fora do profile `release`. Chave GPG do usuário estava só no keyring do devkitPro MSYS2 (`~/.gnupg` = `C:\Users\user\.gnupg`) — copiada (export/import, original preservado) para o keyring padrão do Gpg4win (`%APPDATA%\gnupg`) a pedido do usuário, pra ficar visível no Kleopatra/backups; `settings.xml` aponta `gpg.executable` pro Gpg4win. **Aceite de resolução confirmado**: numa pasta temporária sem `~/.m2/repository/dev/` (renomeada, restaurada depois), `mvn dependency:resolve` contra só `dev.vitorsilverio:arm-jitter:1.0.0` bate `repo1.maven.org` (sync completou ~10min após o Publish) e resolve com sucesso, `org.ow2.asm:asm:9.7.1` vindo transitivamente — BUILD SUCCESS, zero install local envolvido. `docs/PUBLICAR.md` documenta o passo a passo + as armadilhas encontradas. Destrava **F6** e **F7** |
| [F6](trilha-f-infra/f6-github-actions-pipeline.md) | GitHub Actions: `ci.yml` nos 4 repos + `release.yml` por tag no arm-jitter | F5 | ✅ FECHADA (2026-08-15) — **achado real antes de escrever o YAML**: a nota da própria task ("gbaemu tem arquivos grandes versionados na raiz") estava desatualizada — `gba_bios.bin`/`pokefirered.gba`/`smw.gba`/`roms/` são todos gitignorados (`*.bin`/`*.gba`/`*.sav`/`*.ss` + `gba-tests/`), nunca existiram no repo remoto; e `roms/` do ndsemu (`firmware/` também gitignorado) idem. Confirmado com `git ls-files`/`git check-ignore -v`, não com suposição. 5 testes do gbaemu (`ChainCycleBudgetBenchTest`, `InterpretedThroughputBenchTest`, `ArmTestRomTest`, `ThumbTestRomTest`, `GbaTestRomSuiteTest`) não tinham guarda nenhuma e teriam **falhado** (não pulado) num checkout limpo — ganharam `assumeTrue(Files.exists(...))`, mesmo padrão já usado nos testes de diagnóstico de áudio/sprite. Validado localmente renomeando `roms/`+`gba-tests/`+os 3 arquivos de raiz (não apagando — simulação reversível do checkout do runner): 240 testes, 29 skipped, 0 falhas, `BUILD SUCCESS`. ndsemu (183 testes) e armbox (41 testes) não precisaram de nenhuma mudança de teste — nenhum arquivo de teste referencia asset externo (ndsemu confirmado do mesmo jeito, firmware/roms renomeados); `testdata/` do armbox e do arm-jitter (nenhum, biblioteca pura) já são 100% versionados. `ci.yml` (`ubuntu-latest`/Temurin 25) idêntico nos 4 repos; `release.yml` só no arm-jitter (checagem tag×POM antes de publicar). `autoPublish` do profile `release` virou `true` no `pom.xml` (o 1.0.0 já estava confirmado no Central pela F5/F7, então não há mais motivo pro clique manual). `docs/PUBLICAR.md` ganhou a seção `## 7` com os 4 segredos (`MAVEN_CENTRAL_USERNAME`/`_PASSWORD`/`GPG_PRIVATE_KEY`/`MAVEN_GPG_PASSPHRASE`) — **usuário ainda precisa cadastrá-los** em Settings → Secrets de cada repo antes de `release.yml` funcionar de verdade. Badges de CI (4 repos) + Maven Central (arm-jitter) nos READMEs. **Execução real observada** (não só o YAML): arm-jitter `31904533795` ✅, gbaemu `31904541508` ✅, ndsemu `31904584653` ✅, armbox `31904590726` ✅ — todos `conclusion: success` na primeira tentativa, sem precisar excluir o módulo `truffle` (rodou limpo no Temurin 25). **`release.yml` NÃO testado ponta a ponta** (decisão explícita da própria spec: não publicar uma versão vazia só para provar o pipeline — fica provado na primeira release real). Achado colateral: `ndsemu` e `armbox` usavam remote SSH (`git@github.com:...`) sem chave autorizada neste ambiente — trocado para HTTPS (mesmo padrão já usado por arm-jitter/gbaemu) para conseguir empurrar; usuário avisado antes do push (`AskUserQuestion`). |
| [F7](trilha-f-infra/f7-consumidores-central.md) | Consumidores passam a `arm-jitter:1.0.0` do Central e largam a declaração de `org.ow2.asm` | F5 | ✅ FECHADA (2026-08-15) — 4 POMs (`gbaemu`/`ndsemu`/`armbox`/`virtual-arm-box`) em `1.0.0` (+ `arm-jitter-truffle` no armbox); `org.ow2.asm` removido de gbaemu/ndsemu (não importado em `src/`, `grep -rn "org\.objectweb\.asm"` confirmou vazio nos 4 repos antes de editar; `mvn dependency:tree` do gbaemu confirma que chega transitivo do arm-jitter). **Aceite objetivo**: com `~/.m2/repository/dev/vitorsilverio` renomeada, `mvn test` passou nos 4 (gbaemu 240, ndsemu 183, armbox 41, virtual-arm-box 23 — todos BUILD SUCCESS, zero install local), pasta restaurada depois. Documentação corrigida nos 4 repos (`README.md`/`AGENTS.md`: fim da instrução de `mvn install` local como pré-requisito normal) + seção nova `## Desenvolvendo a lib junto com um consumidor` no `arm-jitter/README.md` (fluxo `-SNAPSHOT`, nunca commitado). **G5 completo desta onda** — commit por repo |
| [F8](trilha-f-infra/f8-github-issues-setup.md) | Fronteira issues×`tasks/`, labels, milestones e templates de issue nos 4 repos | — | ✅ fechada 2026-08-15 (seção no `tasks/README.md`, 11 labels × 4 repos, 3 milestones, templates `bug.yml`/`feature.yml`/`config.yml`) |
| [F9](trilha-f-infra/f9-github-issues-criacao.md) | Postar as 20 issues do backlog conhecido (corpos prontos em `tasks/issues/`) | F8 | ✅ fechada 2026-08-15 — 20 issues postadas (gbaemu#1-5, ndsemu#1-7, arm-jitter#1-5, armbox#1-3), manifesto preenchido, os 2 placeholders `#TBD-*` resolvidos, `Fecha:` adicionado nas 8 tasks correspondentes. `virtual-arm-box/01` fica pendente por decisão (repo sem remote) |
| [F10](trilha-f-infra/f10-disco-virtual-raw-qcow2.md) | Disco virtual no `virtual-arm-box`: `raw` + **QCOW2** (leitura e escrita, compatível com outras VMs) + PL181 MMCI/SD no `versatilepb` — LONGA, 3 PRs | F2 | ⬜ |
| [F11](trilha-f-infra/f11-raspi3-aarch64-machine.md) | `virtual-arm-box --machine=raspi3-64`: Raspberry Pi 3, AArch64 (`kernel8.img`+DTB reais do `raspberrypi/firmware`, sem GIC/PSCI/toolchain) — rota alternativa mais barata que B6.6.6 | F3 (M1/M2), B6.6.7 ✅ | 🟡 PARCIAL (2026-08-18, sessão 2 — destravada pela B6.6.7 do mesmo dia): itens 1-2 do "Inclui" (assets + `FdtPatcher.withNodeRemoved`) já vinham fechados da sessão anterior (ver histórico abaixo). **Itens 3-5 fecharam nesta sessão**: `Raspi364Machine implements Machine64` (interface NOVA — achado real: `Machine` existente é ligada a `ArmCore`/`JitRuntime` 32-bit, incompatível com `Aarch64Core`; `RunnableMachine` extraído como supertipo comum de `runSlice()`/`typeByte()`, o que o laço genérico do `Main` de fato usa — mudança aditiva, G3, `VersatilePbMachine`/`Bcm2835Machine` inalterados). Mapa físico BCM2837 (`0x3F00_0000`, DIFERENTE do `0x2000_0000` do BCM2835) montado reaproveitando a MESMA `PagedAddressSpace`/os MESMOS periféricos MMIO de 32 bits de `device/bcm2835/` (UART/IC/mailbox/CPRMAN, zero mudança), envolvida como `AddressSpace64` via `AddressSpace64.wrapping()` — achado real: nenhuma `PagedAddressSpace64` existe nem precisou existir, o adapter de wrapping (RFC-IR-64BIT §3.2) já serve para um mapa físico que cabe nos 32 bits baixos. `device/bcm2836/` novo: `Bcm2836GenericTimer` (implementa `Aarch64SystemRegisterBus` para `CNTFRQ_EL0`/`CNTPCT_EL0`/`CNTP_TVAL_EL0`/`CNTP_CTL_EL0`/`CNTP_CVAL_EL0` — achado: são registradores de SISTEMA A64, não MMIO, primeira vez que este repo implementa essa interface fora do `arm-jitter`) + `Bcm2836LocalIntc` (MMIO, `brcm,bcm2836-l1-intc`, janela física SEPARADA `0x4000_0000`, roteia a PPI do timer genérico + passthrough do `Bcm2835Ic` legado). `boot/CompositeSystemRegisterBus` novo (delega ao primeiro bus que atende — `Aarch64VmsaSystemRegisters` de B6.6.3/MMU e `Bcm2836GenericTimer` cobrem subconjuntos DISJUNTOS de `Aarch64SystemRegisterId`, `Aarch64Core#setSystemRegisterBus` só aceita um). DTB do Pi 3 real (`bcm2710-rpi-3-b.dtb`) inspecionado por `strings` (sem `dtc`): `cpu@1..3` removidos (D1, sem SMP/PSCI), `mmc@7e202000`/`mmc@7e300000` (achado NOVO: o Pi 3 tem DOIS nós MMC, sdhost legado E SDHCI real, os dois desabilitados)/`usb@7e980000` desabilitados. **Achado real D2 (decisão de escopo, resolvida sem tocar o `arm-jitter`)**: `Aarch64Core` (B6.6.4/B6.6.7) só modela EL0→EL1 transiente para abort/IRQ, não um "EL1 persistente" que a execução principal de um kernel ocuparia — esta máquina contorna forçando `exceptionState().setInEl1(true)` no boot (simula firmware entregando o kernel já em EL1 não-seguro, evita precisar de EL2) — mas isso deixa uma PENDÊNCIA EXPLÍCITA não resolvida: `enterIrq`/`enterMemoryAbort` sempre saltam para os offsets "lower EL AArch64" (`+0x400`/`+0x480`), corretos só para EL0→EL1; um kernel já "em EL1" tomando sua PRÓPRIA exceção esperaria `+0x200`/`+0x280` ("current EL, SPx") — resolver exigiria estender `Aarch64Core` com um segundo par de offsets, mudança de arquitetura fora do "Inclui". **Bloqueio REAL que impediu fechar o aceite desta sessão**: a máquina/DTB/carga foram provados byte-a-byte corretos (`PC=0`/`X0`=endereço do DTB batendo com `text_offset=0` real do `kernel8.img`), mas a PRIMEIRA instrução do kernel real (`ccmp x18, #0x0, #0xd, pl`, offset `0x0` — CONFIRMADO via `aarch64-none-elf-objdump -D -b binary` direto no binário, é o truque de polyglot EFI "MZ" que praticamente todo `kernel8.img` distribuído usa) lança `UnsupportedOperationException`: **`CCMP`/`CCMN` (Conditional Compare) nunca foram implementados em nenhuma sub-task do épico B6.3** — gap de FEATURE real (não um bug), fora do "Inclui" desta task por decisão explícita da spec ("sem mudança no arm-jitter, exceção: bug real"); precisa de sub-task própria (mesmo rigor de corpus real do B6.3.x) para o usuário priorizar. `Raspi364BootTest#smokeTestBootsWithoutException` PINA este achado como regressão (`assertThrows` no encoding exato); os 3 testes de marco textual (`reachesEarlyconBanner*`/`reachesFreeingKernelMemory*`) ficam `@Disabled` até a sub-task de CCMP/CCMN fechar. Achado menor: sem busybox `aarch64` estático real disponível (mesmo bloqueio de B6.2 aceite #2/B4.0.3 item 3), `testdata/raspi3-64/initramfs.cpio.gz` é SINTÉTICO (só o registro `TRAILER!!!`, documentado como tal) — suficiente porque os marcos de boot desta task acontecem antes do kernel tentar montá-lo. `mvn -o test` verde no virtual-arm-box (suíte completa, sem regressão); nenhum arquivo do `arm-jitter` tocado nesta sessão (G5 não se aplica, task explicitamente não pede). Ver o histórico da sessão anterior abaixo para o relato completo dos itens 1-2. **Destrava**: nada ainda — aguarda a sub-task de `CCMP`/`CCMN` no `arm-jitter` (histórico anterior, sessão 1): 🔴 **BLOQUEADA — parede arquitetural no `arm-jitter`, não em ambiente/toolchain (2026-08-18)**. Parte 1 do "Inclui" (assets) FECHOU: `testdata/raspi3-64/kernel8.img`+`bcm2710-rpi-3-b.dtb` baixados do MESMO commit já fixado por `testdata/raspi1/README.md` (`12eeaa12...f1976c`), sha256 documentados; achado de sessão: o download via `raw.githubusercontent.com` veio gzip-encoded e precisou de `gunzip` manual antes de bater o magic `"ARM\x64"` real (registrado no `testdata/raspi3-64/README.md` para não repetir o susto). DTB real confirmado por `strings`: `cpu@0..cpu@3` (quad-core), `brcm,bcm2836-armctrl-ic` (IC legado, reusável de `device/bcm2835/` sem mudança), `brcm,bcm2836-l1-intc`, `arm,armv7-timer` (timer genérico — achado NOVO não coberto pela F3), `brcm,bcm2836-smp` (SMP via mailbox, confirma "sem PSCI"). Parte 2 ("Inclui" item 2) FECHOU: `FdtPatcher.withNodeRemoved` novo (remoção REAL de nó — token `FDT_BEGIN_NODE`/subárvore inteira/`FDT_END_NODE`, distinto de `withNodeDisabled` que só sobrescreve `status`; achado de teste: uma busca ingênua pelo NOME do nó removido ainda bate em `/aliases` — `cpu1 = "/cpus/cpu@1"` é uma referência por CAMINHO em valor de STRING, não o nó em si — a checagem forte precisou ser pelo token `FDT_BEGIN_NODE` exato), 5 testes novos contra o `.dtb` real. `mvn -o test` verde no virtual-arm-box (suíte completa); nenhum arquivo do `arm-jitter` tocado (G5 não se aplica) |
| [RFC-3DS](trilha-g-3ds/RFC-N3DSEMU.md) | RFC do `n3dsemu` — decisões D1-D8 (só ARM11, HLE, `.3dsx`, Vulkan/LWJGL, vertex shader interpretado, TEV→SPIR-V) | — | ✅ escrita 2026-08-15 |
| [G1](trilha-g-3ds/g1-esqueleto-n3dsemu.md) | `n3dsemu`: repo novo, memória, loader `.3dsx`, `ARM11_MPCORE`, primeira `svc` (M1) | F7 | ✅ (2026-08-15, repo novo `n3dsemu`; `loader/Loader3dsx` com relocação completa (algoritmo transcrito do carregador de referência do Homebrew Launcher, spec do 3dbrew não documenta a semântica); mapa RFC §3 sobre `PagedAddressSpace` + `LoggingOpenBus`; `core/N3dsCp15` com `c13`/TLS desde o início; `kernel/SvcTable` intercepta toda `svc`, loga com nomes reais [tabela extraída via `objdump` no `libctru.a` real, não copiada de memória] e lança `UnsupportedSvcException`; `ARM11_MPCORE` (B5.2) + `ExclusiveMonitor` compartilhado (B5.1, RFC D1) desde já. **Achado real documentado, não é bug do arm-jitter** [decisão de compatibilidade, ver Javadoc de `SvcTable`]: o decoder ARM compartilhado interpreta o imediato de `SVC` na convenção GBA/NDS [número nos 8 bits altos]; o Horizon usa a convenção oposta [número direto no campo] — `SvcTable` relê a instrução crua da memória do guest em vez de tocar no decoder compartilhado [evitaria quebrar gbaemu/ndsemu/armbox]. `n3dsemu testdata/application.3dsx --trace-svc` chega a `0x21 svcCreateAddressArbiter`, idêntico nos 3 backends [JIT/`--interp`/`--check`] — o aceite mais importante, confirma `ARM11_MPCORE` executando ARMv6K real de forma consistente. `mvn -o test` verde (15 testes); G5 não se aplica [repo novo]. Corpus `testdata/` com 4 exemplos reais do devkitPro [r68/libctru 2.7.0], compilados localmente [armadilha de ambiente: precisa do shell MSYS2 do PRÓPRIO devkitPro, não do Git Bash — documentado em `testdata/README.md`]. Destrava G2) |
| [G2](trilha-g-3ds/g2-kernel-hle-svc.md) | Kernel Horizon em HLE: handles, memória, threads, sincronização, `svcOutputDebugString` (M2) | G1 | 🟡 PARCIAL — **PR1** (2026-08-15, commit `b6198c5`): handles + memória + `svcOutputDebugString`, ver histórico desta linha. **PR2** (2026-08-15, sessão seguinte): `kernel/Scheduler` novo (cooperativo, um único `ArmCore` reutilizado por todas as threads via `ArmCore#saveState`/`loadState` completo — não o `CpuState` de 8 campos do `SwiDispatcher`, que não cobre r4-r12/bancos privilegiados; TLS por thread alocado e trocado via `N3dsCp15#setThreadLocalStorage` a cada troca); `ThreadObject` deixou de ser um `record` (agora mutável: prioridade/estado/contexto salvo mudam em vida); `MutexObject` (recursivo, com dono), `SemaphoreObject`, `EventObject` (oneshot/sticky) — os 3 implementam `Waitable`, contrato novo (`isAvailableFor`/`acquire`) que também cobre esperar uma HANDLE DE THREAD terminar; `AddressArbiterObject`, `SessionObject`, `MemoryBlockObject`. `SvcTable` ganhou `svcCreateThread`/`ExitThread`/`SleepThread`/`Get`+`SetThreadPriority`, `svcCreateMutex`/`ReleaseMutex`, `svcCreateSemaphore`/`ReleaseSemaphore`, `svcCreateEvent`/`SignalEvent`/`ClearEvent`, `svcCreateMemoryBlock`/`Map`/`UnmapMemoryBlock`, `svcArbitrateAddress`, `svcWaitSynchronization1`/`N` (índice do objeto acordado em `r1`, `waitAll` vs `waitAny`), `svcConnectToPort` (cria `SessionObject`) e `svcSendSyncRequest` (loga o cabeçalho IPC decodificado de TLS+0x80 e lança, "não inclui" da task — serviços reais são a G3). **Achado real, implementado além da lista literal de SVCs da task**: `svcCreateAddressArbiter` (`0x21`) não estava na lista, mas `svcArbitrateAddress` (`0x22`, que ESTÁ) não tem como funcionar sem um handle de arbiter real — o próprio teste da PR1 já documentava isso como bloqueio ("para o boot real progredir além da primeira svc observada"); implementado como pré-requisito estrito do que já estava no escopo, não "por precaução". Convenções de registrador de TODAS as SVCs novas conferidas contra a montagem REAL dos wrappers (`arm-none-eabi-objdump -d libctru.a`, mesma metodologia da PR1) — achado extra: `svcWaitSynchronization`/`N`/`ArbitrateAddress` passam o timeout de 64 bits em pares de registrador não-óbvios (`r2:r3` no primeiro, `r0`+`r4` no N, `r4:r5` no arbiter), sem shuffling de pilha, dependendo do padding AAPCS natural. **Achado real reportado, NÃO corrigido (fora do escopo desta task)**: com `0x21` implementado, `n3dsemu testdata/application.3dsx` progride além da primeira `svc` e esbarra numa decisão ARQUITETURAL de OUTRO épico do arm-jitter — `FpscrRegister#setValue` (decisão nº 3 do épico B3) só suporta o modo IEEE round-to-nearest da VFP, e o `crt0`/newlib do libctru grava um FPSCR com `LEN`/`STRIDE` (aritmética vetorial VFPv2) diferente de zero antes de `main()`. **O aceite objetivo "roda até `svcExitProcess`, sai código 0" desta task NÃO é alcançável enquanto essa decisão do arm-jitter não for revisitada** (seria uma task própria do arm-jitter, fora do escopo de kernel HLE desta) — confirmado idêntico nos 3 backends (JIT/`--interp`/`--check`, `Application3dsxTest` parametrizado) e com `n3dsemu --trace-svc` real. Todo o resto do aceite fechou: `svcSendSyncRequest` loga o cabeçalho IPC antes de lançar; teste unitário por objeto de kernel (`MutexObjectTest` — recursão + release por não-dono, `SemaphoreObjectTest`, `EventObjectTest` — oneshot/sticky, `SvcTableTest` — `waitAll` vs `waitAny` com índice, timeout que expira via bloqueio real + adiantamento do relógio virtual `core.addCycles`, nunca `System.nanoTime()`). `mvn -o test` verde (84 testes; só o repo `n3dsemu` tocado, G5 não se aplica). **Sessão de continuação (2026-08-16, motivada pela investigação que achou os 2 gaps abaixo dos SVCs `0x38`/`0x3A`)**: 2 bugs reais fechados. **(1)** `svc 0x39` (`svcGetResourceLimitLimitValues`), fora da lista original da task — mesmo padrão do achado de `svcCreateAddressArbiter` da PR2: um SVC vizinho que o `__system_allocateHeaps` do crt0 usa e a spec original não previu. Sem ele, o array de saída do teto de `COMMIT` nunca era escrito (ficava com o que já estava na pilha do guest, tipicamente `0`), o tamanho de heap calculado pelo crt0 virava `0` e o `svcControlMemory(MEMOP_ALLOC)` seguinte falhava com `MISALIGNED_SIZE` — implementado espelhando `handleGetResourceLimitCurrentValues` (mesma convenção de registrador), com `ResourceLimitValues#limitValueOf` novo. **(2)** Achado arquitetural mais profundo, descoberto ao investigar por que o `ALLOC` continuava falhando mesmo com um teto plausível: `MemoryMap.LINEAR_HEAP_BASE`(`0x08000000`)/`NEW_HEAP_BASE`(`0x14000000`) deste projeto estavam com os endereços TROCADOS em relação ao 3dbrew real (`Memory_layout`: o heap "geral", mapeado por `ControlMemory` **sem** a flag `LINEAR`, fica em `0x08000000`; o heap LINEAR de verdade, via `MEMOP_ALLOC_LINEAR`, fica em `0x14000000` — o oposto) — confirmado via `WebFetch` na wiki antes de corrigir, não um palpite. Consequência prática: o segundo `svcControlMemory` do crt0 (que usa a flag `LINEAR`, endereço escolhido pelo kernel) caía no pool errado e falhava com `OUT_OF_RANGE`. Corrigido renomeando para `GENERAL_HEAP_BASE`/`LINEAR_HEAP_BASE` nos endereços certos (`MemoryManager`/`N3dsMachine`/`N3dsAddressSpace` atualizados) — e, como bug lateral necessário para o primeiro `ALLOC` (endereço explícito, sem a flag) continuar funcionando, `MemoryManager#controlMemory` passou a escolher o pool pelo ENDEREÇO quando `addr0≠0` (a flag `LINEAR` só decide quando o kernel escolhe o endereço, `addr0==0` — um `addr0` explícito já é auto-suficiente no Horizon real). Os dois heaps (16 MiB cada, antes 2 MiB/16 MiB desbalanceados) agora batem exatamente o teto de `COMMIT` que `ResourceLimitValues` relata, já que o crt0 reparte esse teto meio a meio quando não há `.smdh`/exheader. **`n3dsemu testdata/application.3dsx` não panica mais** (era `svcBreak(PANIC)` toda vez) — os dois `svcControlMemory` de heap agora sucedem nos 3 backends. **Novo limite encontrado, NÃO corrigido (fora do escopo desta continuação cirúrgica)**: o backend JIT entra num laço que chama `svcCreateAddressArbiter` (`0x21`) repetida e indefinidamente no MESMO PC — confirmado manualmente até 200 mil fatias sem sair sozinho; provavelmente precisa de sincronização/escalonador cooperativo de verdade reagindo a esse padrão (fora do "não inclui" desta task, território de G3 ou de uma G2.2). **Achado extra**: INTERPRETED/CHECK avançam MUITO mais devagar por fatia que o JIT (blocos compilados/encadeados cobrem mais instruções por fatia) — dentro do mesmo orçamento de fatias que o JIT já usa para alcançar o laço, os outros dois backends ainda não saíram do segundo `svcControlMemory`; o aceite original da G2 ("JIT e `--interp` produzem exatamente a mesma sequência de SVCs") não foi revalidado ponta a ponta por essa divergência de RITMO (não necessariamente de comportamento) — fica para a sessão que atacar o laço do arbiter também confirmar isso com um orçamento de fatias generoso o bastante para os 3 backends convergirem. `mvn -o test` verde (87 testes; só `n3dsemu` tocado, G5 não se aplica). |
| [G2.2](trilha-g-3ds/g2.2-address-arbiter-loop.md) | Investigar o laço de `svcCreateAddressArbiter` que travava o boot antes da G3 | G2 | ✅ (2026-08-18) — causa raiz achada e corrigida, fix pequeno e óbvio (permitido pela própria task): não era uma SVC retentando — trace de duas fases (`--trace-svc` + `ArmCore#step()` puro, SEM JIT/`runBlocks`, então o achado vale para JIT e INTERPRETED igualmente, não é bug do arm-jitter) mostrou o HISTÓRICO DE PC idêntico bit-a-bit antes de cada ocorrência (`_start`→`startup`→`initSystem`→`__libctru_init`→`__sync_init`→`svcCreateAddressArbiter`, `lr`/`r0` sempre congelados): o boot inteiro reiniciava do zero a cada 271.058 instruções, não uma SVC repetindo — ela só aparecia de novo por ser a primeira SVC de cada tentativa. Causa real: `srvInit` do libctru abre com `MCR p15, 0, r0, c7, c10, {5}` (`DMB`, encoding CP15 do ARMv6K — `dmb` dedicado só a partir do ARMv7, confirmado via `objdump` no `.3dsx` real, não por analogia); `N3dsCp15` só reconhecia `TPIDRURO` e devolvia `ArmException.UNDEFINED` para qualquer outro registrador (mesmo comportamento do hardware real para coprocessador ausente); como este HLE não configura vetor de exceção (RFC D2: sem MMU/LLE), o PC caía no endereço 4 (memória zerada, decodificada como `andeq r0,r0,r0`) e andava sequencialmente até tropeçar de volta em `0x00100000`, reiniciando `_start`. **Fix**: `N3dsCp15` trata `DSB`/`DMB` (`c7,c10,{4}`/`{5}`) como no-op (este HLE não reordena acessos nem mantém cache de instrução separado) — mesmo padrão de "registro/SVC vizinho não reconhecido" já visto 2x na G2, agora em CP15. Com o fix, o boot passa de `srvInit` e alcança `svcConnectToPort`/`svcSendSyncRequest` nos 3 backends — a fronteira real com a G3, não mais um reinício infinito. Teste novo `N3dsCp15Test` (regressão do DSB/DMB) + teste novo em `Application3dsxTest` (`passaDeSrvInitEAlcancaSvcSendSyncRequestNosTresBackends`, parametrizado nos 3 backends) + Javadoc de `Application3dsxTest` reescrito com o relato completo. `mvn -o test` verde (94 testes; só `n3dsemu` tocado). **Destrava G3.** |
| [G3](trilha-g-3ds/g3-servicos-srv-apt-hid-fs.md) | IPC + serviços `srv:`/`APT`/`hid`/`fs:USER`/`gsp::Gpu` mínimo (M3) | G2.2 | 🟡 PARCIAL (2026-08-18) — codec IPC (`ipc/`), `ServiceRegistry`+7 serviços (`srv:`/`APT:U`/`hid:USER`/`fs:USER`/`gsp::Gpu`/`cfg:u`/`ptm:u`) e pulso periódico de 60 Hz no `Scheduler` implementados; `svcSendSyncRequest` despacha de verdade (não lança mais). **2 achados reais corrigidos**: (1) `Loader3dsx` concatenava code/rodata/data pelo tamanho BRUTO — o carregador de referência arredonda cada segmento até 0x1000 antes do próximo começar; sem isso `srv:GetServiceHandle` sempre lia string vazia (confirmado via objdump no `read-controls.3dsx` real). (2) a thread interna de relay de interrupção do `gsp::Gpu` precisa da FILA de interrupções populada (3dbrew GSP Shared Memory), não só do evento bruto sinalizado. `mvn -o test` verde (118 testes, +19 novos). **Aceite NÃO fechado**: `read-controls.3dsx` ainda não sai sozinho via `--script`+START — trava girando `svcClearEvent`/`svcWaitSynchronization` no evento de VBlank, thread de relay (prioridade mais alta) nunca devolve controle ao loop real da aplicação; raiz não isolada (candidato a G3.2, mesmo padrão G2→G2.2). `application.3dsx` (hello-world) nunca chama `svcExitProcess` por design (sem checagem de tecla) — item do aceite original inatingível com este ROM. Commit `468bf5c`. |
| [G3.2](trilha-g-3ds/g3.2-gsp-relay-starvation.md) | Investigar a inanição do loop de `read-controls.3dsx` (thread de relay do `gsp::Gpu` nunca devolve controle ao `Scheduler`) que impede o aceite de G3 de fechar | G3 | 🟡 PARCIAL (2026-08-18) — causa raiz do sintoma ORIGINAL achada e corrigida, fix pequeno e óbvio (permitido pela própria task), mas revelou um bloqueio NOVO e mais profundo. **Achado real**: `GspGpuService#handleRegisterInterruptRelayQueue` devolvia `1` (constante antiga `FIRST_INITIALIZATION_FLAG`, nome errado) no segundo parâmetro normal da resposta IPC — confirmado via 3dbrew que esse campo é o **"GSP module thread index"**, não um booleano. O guest usa esse valor para calcular a base do próprio bloco de 0x40 bytes na fila de interrupções compartilhada (`clientBlock = sharedMemBase + threadIndex*0x40`, confirmado por disassembly do `read-controls.elf` real via `arm-none-eabi-objdump`, função `gspEventThreadMain`) — devolver `1` fazia o guest ler/escrever no bloco do CLIENTE 1 (offset `0x40`) enquanto `GspGpuService#pushInterrupt` sempre escreve no bloco do CLIENTE 0 (offset `0`, único cliente desta HLE, RFC D1): a thread de relay real nunca via nenhuma interrupção enfileirada e girava `svcClearEvent`/`svcWaitSynchronization` para sempre sem processar nada. **Corrigido** (renomeado para `GSP_MODULE_THREAD_INDEX = 0`); confirmado com `--trace-svc` real: antes do fix, `svcArbitrateAddress` (sinal de fim-de-drain que acorda quem espera) só acontecia UMA vez em todo o boot; depois do fix, acontece a CADA ciclo de VBlank (relay agora drena a fila de verdade). Teste de regressão novo `GspGpuServiceTest` (2 casos). `mvn -o test` verde. **Bloqueio NOVO encontrado, NÃO corrigido (fora do "pequeno e óbvio", território de investigação própria)**: mesmo com o relay drenando corretamente e chamando `svcArbitrateAddress` (SIGNAL) a cada quadro, a THREAD PRINCIPAL da aplicação nunca é escalonada de novo depois de criar a thread de relay — confirmado rodando até 29.377 ciclos de VBlank (`--slices=3000`, ~8min de tempo virtual) sem NENHUM SVC da main thread aparecer no trace. Ou seja, `read-controls.3dsx` continua sem sair sozinho, mas agora por um motivo DIFERENTE e mais profundo (a main thread parece nunca reentrar no `Scheduler` de jeito nenhum, não apenas nunca ser acordada por um `Waitable` específico) — candidato a **G3.3**, mesmo padrão G2→G2.2→G3.2. Ver Javadoc de `GspGpuServiceTest` para o relato da parte corrigida. |
| [G4](trilha-g-3ds/g4-vulkan-apresentacao.md) | Janela Vulkan (LWJGL 3 + GLFW) apresentando os framebuffers do `gsp` (M4) | G3.3 (ainda não criada, ver nota da G3.2) | ⬜ |
| [G5](trilha-g-3ds/g5-pica200-render.md) | PICA200: listas de comando, vertex shader interpretado, TEV→SPIR-V (M5) — LONGA, 3 PRs | G4 | ⬜ |
| [G6](trilha-g-3ds/g6-roms-comerciais.md) | **[REFINAR]** ROMs comerciais `.cia`/`.3ds` | G5 | 🧑 bloqueada no usuário (dump de `boot9.bin`) |
| [G7](trilha-g-3ds/g7-backport-vulkan-ndsemu.md) | **[REFINAR]** trazer o núcleo Vulkan para o ndsemu — condicional a "se der certo" | G5 | ⬜ |

Legenda: ⬜ pendente · 🟡 em andamento · ✅ concluída

## Matriz de validação por arquitetura

"A arquitetura X funciona?" tem resposta objetiva em
[docs/VALIDACAO-ARQUITETURAS.md](../docs/VALIDACAO-ARQUITETURAS.md) (níveis N1-N4,
comandos e status). Toda task que mude uma célula da matriz cita o arquivo no Aceite.

## Pendências que EXIGEM sessão de modelo forte (não pegar como task comum)

Registradas para não virarem tasks vagas; um agente comum NÃO deve tentá-las:

1. ~~Glitches do FireRed = granularidade de bloco do ASM~~ **REVOGADO 2026-07-16**:
   o usuário re-testou e os bugs de batalha acontecem IGUAIS nos dois backends
   (e a velocidade é igual) — a atribuição ao JIT de 2026-07-15 estava errada.
   Os bugs agora são tasks concretas com hipóteses: **D2** (3 bugs visuais de
   batalha), **D3** (SMW chiado), **D4** (Metroid melodia), **D6** (BIOS lenta).
   `INTERPRETED` segue default do gbaemu (decisão de produto mantida — mas pela
   simplicidade/fidelidade estrutural, não mais por "ASM causa glitch").
2. ~~Animação da BIOS lenta~~ → virou a task **D6** (com hipóteses; segue sendo
   sessão de modelo forte).
3. **Dispatch megamórfico remanescente do ndsemu** (`JitRuntime.execute` ~12-14% do
   perfil pós-superblocos, medição de 2026-07-11 em C1) — precisa de profiling novo
   e desenho; não há spec.
4. **Idle-loop skip** (detectar busy-wait em IO e avançar o relógio até o próximo
   evento) — potencialmente o maior ganho para jogos CPU-bound do ndsemu (teto
   ~50fps do MKDS), mas o desenho é arriscado (falso positivo = travamento/timing
   quebrado); precisa de RFC própria antes de virar task.
5. **Rodadas de spec futuras**: B4.1.x em arquivos próprios quando B4.1.1 começar;
   B6.3+ quando B6.2 fechar (escopos já fixados nos épicos).
6. **Divergência ASM×interpretador no JUS** (achada durante a re-medição da C11 fase 2,
   2026-07-16: `asmcheck` a partir de `roms/JUS.ss`, ~300 chunks, diverge em `r1` no
   `block@0x1ff8f44` — diferença de 0x38 num registrador só, resto idêntico). NÃO é
   causada pelo Fix C (`ASM_CHECK` não liga loop-superblocos; `JitRuntime.reset()` se
   comporta byte-a-byte como o `blockCache().clear()` antigo nesse backend) — pré-
   existente, versão de origem desconhecida, não investigada.
7. **Bug real achado na sessão de B6.3.1 (2026-07-25), NÃO corrigido (fora do
   "Inclui" daquela task)**: `Ir64BlockExecutor#executeAlu` (B6.1, `Ir64Op.Alu64`
   — forma imediata de `ADD`/`SUB`) resolve `Rd|SP`/`Rn|SP` checando SÓ a flag
   booleira (`dstIsStackPointer`/`src1IsStackPointer`), nunca o ÍNDICE do
   registrador (`== 31`) — o pseudocódigo real do manual (`if n == 31 then SP[]
   else X[n]`; `if d == 31 && !setflags then SP[] = result else X[d] = result`)
   e o próprio `readBaseRegister`/`writeBaseRegister` (load/store, mesmo
   arquivo) checam o índice. Confirmado com um teste descartável (não commitado)
   que `add x4, x5, #0x123` (`Rd=4`, `Rn=5`, nenhum dos dois é `31`) grava o
   resultado em `SP` em vez de `X4` — qualquer `ADD`/`SUB` (imediato) real com
   `Rd`/`Rn` != 31 está quebrado hoje. O código NOVO de B6.3.1
   (`Ir64Op.AluExtendedRegister`) implementa a checagem CORRETA (por índice),
   não copia esse padrão. Precisa de uma task de correção dedicada (fix +
   revisão dos testes de B6.1 que não pegaram isso porque coincidiam com `SP`
   default `0`).
